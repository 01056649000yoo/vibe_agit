# 학기형 칭호·단계 보상 통합 계획

> 상태: 2026-08-31 공용 시즌 어댑터·보상 정책/원장/RPC·학생 수령 UI를 운영 배포하고 관리자 `테스트`
> 학급에서 직접 수령까지 확인했다. 전체 공개용 전역 스위치, 학급별 우선 예외, 새 학급의 첫 시즌 자동 생성도
> 운영 DB에 적용했다. 1차 대상은 `기록가`와 `독서가`이며 보상은 자동 지급하지 않고 학생이 직접 받는다.

## 1. 목표와 범위

- 작가·소통·기록가·독서가 네 칭호가 **한 학기 시즌 ID와 시작·마감 시각을 공유**한다.
- 새 학기를 시작하면 네 칭호의 진행도와 단계별 보상 수령 기회만 새로 시작한다.
- 작성 글·댓글·반응·포인트 잔액·구입/장착한 공방 아이템·지난 시즌 스냅샷은 지우지 않는다.
- 기록가·독서가의 2~7단계를 달성할 때 포인트 보상을 연다. 자동 지급하지 않고 학생이 직접 받는다.
- 이미 현재 단계가 오른 학생도 이번 시즌의 도달 단계까지 소급해 직접 받을 수 있다.
- 기록가 또는 독서가 한 종목을 끝까지 달성하면 **총 5,000P**를 직접 수령해 완성 세트 하나를 고를 수 있게 한다.

## 2. 현재 구조에서 확인된 사실

- `dragon_growth_seasons`의 활성 시즌 시작 시각을 작가·소통·기록가·독서가 네 집계가 이미 함께 사용한다.
- 시즌 마감 스냅샷에도 네 칭호의 원자료와 최종 단계가 함께 보관된다.
- 새 시즌 시작은 칭호 진행도와 수호룡의 학기 성장 상태만 새로 시작하고 포인트·소품은 보존한다.
- 화면 메타데이터는 `src/modules/writing/title-status/titleTracks.js`, 단계 기준은
  `src/constants/writerLevels.js`에 모여 있다. 포인트 지급은 `src/modules/points/`의 공용 엔진 계약을 사용한다.
- 공방의 자유 구매 완성 세트는 4,000P·5,000P·6,000P이고, 글쓰기 기본 보상은 과제 약 100P,
  일기 80P, 독서록 100P다. 학급별 설정은 이 값보다 크거나 작을 수 있다.

따라서 새 시즌 표를 하나 더 만들지 않는다. 기존 시즌을 칭호 공용 시즌으로 감싸고, 보상 정책과 수령 원장만
독립 모듈로 추가한다. DB 표와 기존 RPC 이름은 호환성 때문에 급히 바꾸지 않는다.

## 3. 공용 모듈 계약

`src/modules/writing/title-status/`를 칭호 공용 모듈로 유지하고 다음 역할을 한곳에 둔다.

| 구성 | 책임 |
|---|---|
| 칭호 레지스트리 | ID, 이름, 단계 정의 참조, 진행 단위, 보상 사용 여부 |
| 시즌 어댑터 | 현재 `season_id`, 상태, 시작·마감 시각, 지난 시즌 스냅샷 |
| 보상 상태 | 단계별 `잠김 / 받을 수 있음 / 받음`, 받을 수 있는 합계 |
| 전용 API | 내 상태 조회, 선택 단계 또는 받을 수 있는 단계 모두 수령 |
| 표시 컴포넌트 | 학생 홈·나의 아지트·글쓰기 발자국이 같은 카드와 상세창 재사용 |

첫 적용에서는 `writer`와 `reader`(화면명 소통)는 `rewardEnabled: false`, `diary`와 `reading`만
`rewardEnabled: true`로 둔다. 작가·소통은 이미 수호룡 성장과 전설 세트 달성 조건이므로 포인트까지 한꺼번에
붙이지 않는다. 4주 운영 수치가 안정된 뒤 같은 레지스트리 설정으로 확대할 수 있다.

## 4. 단계별 포인트 확정안

시작 단계는 활동 없이 즉시 열리므로 0P다. 2단계부터는 초반 달성도 분명히 체감하고, 뒤 단계일수록 더 큰
성취감을 얻도록 보상 폭을 높인다.

| 단계 | 단계 보상 | 한 칭호 누적 |
|---:|---:|---:|
| 1 | 0P | 0P |
| 2 | 200P | 200P |
| 3 | 400P | 600P |
| 4 | 600P | 1,200P |
| 5 | 800P | 2,000P |
| 6 | 1,200P | 3,200P |
| 7 | 1,800P | **5,000P** |

- 기록가·독서가를 모두 7단계까지 달성하면 칭호 보상은 최대 **10,000P**다.
- 균형 참여 예시를 `과제 승인 12편 × 100P + 확인 일기 14일 × 80P + 확인 독서록 8편 × 100P +
  두 칭호 4단계 누적 2,400P + 게임·기타 300~500P`로 잡으면 약 **5,820~6,020P**다.
- 보통 수준으로 두 종목을 함께 이어 가면 첫 완성 세트를 살 수 있고, 한 종목 7단계 달성자는 그 종목의
  칭호 보상만으로도 4,000~5,000P 세트를 고를 수 있다. 두 종목을 모두 끝낸 상위 활동 학생은 10,000P의
  칭호 보상과 글 자체 보상을 받는 구조이며, 이는 높은 성취에 대한 의도적인 차등이다.
- 교사는 1차에 칭호 보상액을 바꾸지 못한다. 학급별 글 보상 편차까지 있는 상태에서 보상 설정을 하나 더 열면
  경제를 비교하기 어려우므로, 전역 고정값으로 4주 측정한 뒤 조정한다.

## 5. DB와 보안 설계

### 5-1. 시즌 정책 고정

- 활성 시즌마다 보상 정책 버전과 단계별 금액의 스냅샷을 둔다.
- 이번 시즌에 이미 성장한 학생도 같은 정책을 적용받도록 기존 활성 시즌에 1차 정책을 연결한다.
- 운영 중 전역 기본값을 바꾸더라도 이미 시작한 시즌의 보상액은 바뀌지 않는다.

### 5-2. 수령 원장

비공개 `student_title_reward_claims` 원장을 추가한다.

- 키: `season_id + class_id + student_id + track_id + level` 유일
- 기록: 정책 버전, 지급 포인트, 수령 시각, 포인트 원장 ID
- 학생·교사 브라우저의 직접 INSERT/UPDATE/DELETE는 막고 전용 RPC만 사용
- 지난 시즌 수령 기록은 삭제하지 않고 감사·통계용으로 보존

별도의 대량 소급 지급은 하지 않는다. 현재 서버가 계산한 단계 이하에서 원장에 없는 단계가 곧 `받을 수 있음`이다.
따라서 이미 기록가 4단계인 학생은 2·3·4단계가 바로 열리고, 학생이 누른 뒤에만 포인트가 지급된다.

### 5-3. 수령 RPC

전용 RPC 한 번이 다음을 모두 처리한다.

1. 로그인 사용자가 실제 활성 학생인지 확인
2. 실제 학급의 현재 `active` 또는 `closing` 시즌 확인
3. 서버에서 해당 칭호 원자료와 현재 단계를 다시 계산
4. 요청한 단계가 2~현재 단계이고 이번 시즌 미수령인지 확인
5. 공용 `point_engine_apply()` 호출
6. 단계별 수령 원장 기록 후 새 잔액·수령 목록 반환

중복 방지 키는 `title-reward:{season_id}:{track_id}:{level}`로 고정한다. 연속 클릭·네트워크 재시도·동시 요청에도
학생 한 명에게 한 시즌 한 단계가 한 번만 지급된다. 새 활동 유형 `title_reward`를 DB CHECK, 포인트 표시
레지스트리, 통계 분류에 같은 변경으로 추가한다.

승인 취소 등으로 현재 단계가 내려가기 전에 이미 받은 포인트는 회수하지 않는다. 아직 받지 않은 단계는 수령 순간의
서버 단계가 조건을 만족해야 한다. 작별 기간에는 받을 수 있지만 새 시즌을 시작하면 지난 시즌의 미수령 보상은
만료한다. 교사 시즌 마감 화면에 미수령 학생 수를 보여 최종 종료 전에 안내할 수 있게 한다.

## 6. 학생·교사 화면

### 학생

- 현재 칭호 상세의 각 단계에 `잠김 / 받기 +100P / 받음` 상태를 표시한다.
- 이미 여러 단계를 달성한 학생은 단계별 버튼과 함께 `받을 보상 모두 받기`를 제공한다.
- 모두 받기도 RPC 한 번·한 트랜잭션으로 처리하되 포인트 원장에는 단계별 기록을 남긴다.
- 수령 성공 즉시 카드의 보유 포인트와 단계 상태를 갱신한다.
- 학생 홈에 새 조회·폴링·Realtime을 추가하지 않고 기존 칭호 응답과 bootstrap에 보상 요약을 합친다.

### 교사

- 수호룡 시즌 화면의 이름은 DB 호환을 유지하면서 화면에서 `학기 성장·칭호 시즌`으로 넓힌다.
- 학생별 네 칭호 단계와 기록가·독서가 보상 `수령/미수령`을 읽기 전용으로 보여 준다.
- 시즌 마감에는 지급 합계와 미수령 학생 수를 확인하고, 새 시즌 시작 시 네 칭호가 함께 새로 시작됨을 안내한다.
- 1차에는 교사의 보상액 수정·학생 대신 수령·수령 취소 기능을 만들지 않는다.

## 7. 구현 단계와 확인 지점

### Step 1 — 모듈 경계 정리, 동작 변화 없음

- [x] 네 칭호 레지스트리와 시즌 어댑터 계약 확정
- [x] 현재 학생 홈·나의 아지트·친구 아지트·교사 수호룡 화면 호출처 수 대조
- [x] 기존 네 칭호 단계·시즌 스냅샷 회귀 검사 고정

확인 후 다음 단계로 간다.

### Step 2 — 보상 정책·수령 원장·RPC

- [x] 새 마이그레이션에 정책 스냅샷, 수령 원장, `title_reward` 활동 유형, 수령 RPC 추가
- [x] 현재 단계 이전 보상 해제, 단계별·모두 받기, 재시도·동시 요청, 다른 학생/학급 차단 검사
- [x] `npm run migrate:check` 전체 ROLLBACK 통과 후 운영 `agit-db` 적용 — 248/248, 대기 0

확인 후 다음 단계로 간다.

### Step 3 — 학생 수령 UI

- [x] 공용 칭호 상세창에 보상 상태와 단계별/모두 받기 버튼 추가
- [x] 포인트 카드와 최근 내역의 `칭호 단계 보상` 표시 및 즉시 잔액 갱신 연결
- [ ] 인증 학생 실기기에서 모바일 2×2 카드와 좁은 상세창, 느린 응답·중복 탭·오프라인 재시도 확인

확인 후 다음 단계로 간다.

### Step 4 — 교사 시즌 관리 통합

- [x] 드래곤 화면의 직접 RPC 호출을 공용 칭호·학기 시즌 API 어댑터로 이동하고 화면 명칭 확대
- [ ] 네 칭호와 보상 수령·미수령 현황을 기존 시즌 화면에 합침
- 작별 기간·종료·되돌리기·새 시즌 시작에서 보상 원장과 스냅샷 보존 확인
- 새 시즌에는 네 진행도와 보상 기회만 새로 시작하고 포인트·소품이 유지되는지 검증

확인 후 다음 단계로 간다.

### Step 5 — 관리자 시험 학급 선공개

- [x] 관리자 `테스트` 학급에만 기능 플래그를 허용하고 실제 기록가 LV.3 학생으로 단계별 직접 수령 확인
- [x] 서버 단계 재검증, 중복 클릭, 다른 학생/학급, 시즌 상태 경계를 운영 스키마 ROLLBACK 스모크로 확인
- [x] 운영 수령 원장과 포인트 원장의 건수·금액 일치 확인

사용자 확인과 전체 코드·운영 데이터 재점검을 마쳐 전역 공개로 전환한다. 실기기 좁은 화면과 시즌 마감 직전
수령은 공개 후에도 수동 인수 항목으로 계속 확인한다.

### Step 6 — 전체 공개와 경제 관측

- [x] 현재·미래 학급용 전역 ON/OFF, 학급별 우선 예외, 새 학급 첫 시즌 자동 생성 구현·운영 DB 적용
- 전체 공개 후 기술 안정성은 7일, 포인트 경제는 최소 4주 관측
- 4주마다 활동 학생의 자동 획득 중앙값, 4,000P 도달률, 칭호 보상 비중, 수령률, 획득 대비 소비,
  세트 구매 수를 비식별 집계
- 균형 참여 학생의 예상 학기 중앙값이 5,000P 미만 또는 8,000P 초과, 칭호 보상이 자동 획득의 70% 초과,
  중복 지급 1건 이상이면 공개 확대나 금액 조정을 멈춘다.
- 금액은 한 번에 10~20%만 조정하고 이미 시작한 시즌에는 소급 변경하지 않는다.

## 8. 검증 게이트

- 단위/구조: 네 칭호 레지스트리, 7단계 보상표, 호출처 동기화, 홈 추가 조회 없음
- DB: 단계 판정, 현재 시즌 범위, 중복·동시성, 잔액·원장 같은 트랜잭션, 지난 시즌 분리
- 보안: anon 차단, 학생 본인만 수령, 다른 학급·다른 학생·미달 단계 거절, 직접 테이블 쓰기 차단
- 시즌: active/closing/closed/되돌리기/새 시즌 시작과 스냅샷 보존
- 화면: PC·태블릿·휴대전화, 단계별 상태, 모두 받기, 포인트 즉시 갱신
- 명령: `npm run checklist`, 관련 전용 검사, `npm run test:architecture`, `npm run test:security`,
  `npm run lint`, `npm run build`; 마이그레이션은 `migrate:check` 뒤 별도 승인으로 적용

## 9. 이번 계획에서 하지 않는 것

- 기존 글·포인트·아이템·지난 시즌 기록 삭제
- 작가·소통 칭호의 포인트 보상 즉시 도입
- 교사별 칭호 보상액 자유 설정
- 순위·경쟁 보상, 자동 수령, 미수령 보상의 새 시즌 이월
- 포인트 상점 전체 가격의 동시 변경
