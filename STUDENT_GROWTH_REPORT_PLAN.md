# 학생 글쓰기 성장 리포트 계획

> **상태**: **보류**(2026-09-26). 같은 날 문제 분석 뒤, 먼저 **승인된 글의 처음 글 → 고친 글 형광펜 비교**(v1.9)만 적용했다.
> 리포트는 글이 더 쌓이고 아래 "보류한 까닭"이 풀리면 다시 본다. 아래 계획과 결정은 그때 출발점으로 남겨 둔다.
>
> **보류한 까닭(운영 DB 읽기 전용 집계, 2026-09-26)**: 2026년 1학기 글을 낸 학생 729명 중 1편만 낸 학생 40%, 3편 이하 74%,
> 세 달 이상 쓴 학생 18%, 최근 30일 학생 1인 제출 중앙값 1편 — 달별 그래프·능력치 변화가 성립하지 않는다. 그 밖에 12월(2학기)·1월(연간)
> 리포트가 거의 겹치고 7·12월은 성적 마감과 겹침, AI 능력치 판단은 개인정보 처리방침의 AI 이용 목적에 없음(개정 필요),
> 학급에 학년·학년도 칸이 없음, 맞춤법 별이 도구 사용을 잴 위험, 꾸준함 별이 과제 빈도를 잴 위험, 리포트용 AI 호출이 전역 3건 대기열과 겹칠 위험.
>
> **목표**: 교사가 한 해에 세 번(1학기·2학기·연간 종합) 학생별 글쓰기 성장 리포트를 만들어, 학생 화면에서
> 바로 보여 주거나 인쇄해 가정에 배부한다. 글의 기록과 **처음 글 → 고친 글**의 변화, 다섯 갈래 능력치의
> 자람을 그래프로 보여 주고, "다음엔 이렇게 해 보면 더 좋겠어요"를 담는다.
> 첫 운영은 **관리자(선생님 본인)만** 쓰고, 평가를 통과한 뒤에만 승인 교사 전체에 공개한다.

## 1. 확정한 결정 (2026-09-26)

| 결정 | 내용 |
|---|---|
| 횟수 | 공식 리포트는 한 해 3번: 1학기 · 2학기 · 연간 종합. 한 달 단위 흐름은 그래프의 **달마다 점**으로 보여 준다(매달 리포트를 만들지 않는다) |
| 능력치 | 다섯 갈래: **내용 · 구성 · 표현 · 맞춤법 · 꾸준함** |
| 보이는 방식 | **별 단계(★1~5)**. 숫자 점수는 학생·가정에 보이지 않는다 |
| 과제 평가 점수 | **넣지 않는다.** 과제마다 기준(3~5단계)이 달라 이어 그리면 성장이 아니라 기준 차이가 보이고, 성적표처럼 읽힌다. 리포트 조회는 평가 점수 열을 읽지도 않는다 |
| 가정 배부 | 가정에 배부하는 것을 기준으로 만든다 — **성장에 중점**, 다른 학생과 비교하지 않음 |
| 공개 단계 | 1차는 관리자만(`admin_only`). 선생님이 평가한 뒤 `public` 으로 연다 |
| 발행 횟수 | 학년도마다 학생 한 명에 **1학기 1회 · 2학기 1회 · 연간 종합 1회**. 같은 기간 리포트를 두 번 만들 수 없다(서버에서 막는다) |
| 여는 때 | 교사가 만들 수 있는 때를 서버가 연다: **1학기 = 7월, 2학기 = 12월, 연간 종합 = 1월**(한국 시간 1일 0시) |
| 학생 보기 | 학생은 **뷰어에서 보기만** 한다(A4 쪽 보기). 내려받기·저장·인쇄 단추가 없다 |
| 학생 보기 기간 | 공개한 날부터 **그 학년도 끝(2월 말일, KST)까지**. 새 학년이 되면 학생 화면에서 닫힌다(서버에서 판단) |
| 교사 인쇄 | 가정 배부용 인쇄·`PDF로 저장` 은 **교사만** 한다 |

## 2. 제품 원칙

- **지난번의 나와만 비교한다.** 반 순위·반 평균·다른 학생 이름을 리포트에 싣지 않는다(받은 댓글도 수만 센다).
- **잘한 점이 먼저, 다음 목표는 두세 가지.** 모든 문장은 "~해졌어요", "~해 보면 더 좋겠어요" 처럼 성장과 다음 걸음으로 쓴다.
  부족함을 단정하거나 진단하는 말("~를 못한다", "~가 약하다")은 쓰지 않는다.
- **별이 줄어든 갈래는 줄었다고 쓰지 않는다.** 처음과 같거나 낮으면 "꾸준히 이어 가고 있어요" 로 쓰고, 그래프에도 줄어든 칸을
  빨갛게 강조하지 않는다. 별은 그 기간 안의 대표 글로 재는 것이라 글 종류에 따라 흔들릴 수 있기 때문이다.
- **AI 문장은 초안일 뿐이다.** 교사가 확인·수정하고 확정하기 전에는 학생·가정에 보이지 않는다.
- **학부모가 읽을 수 있는 말로 쓴다.** 전문 용어(루브릭·성취기준 번호 등)를 쓰지 않고, 별 옆에 "별은 반 친구와의 비교가 아니라
  우리 아이의 글에서 보이는 모습입니다" 라는 설명을 둔다.
- **인쇄물에 싣는 개인정보는 최소로.** 학년·반·이름·기간·담임 이름만. 학교 이름은 교사가 켤 때만. 접속 코드·포인트·칭호는 싣지 않는다.
- AI 없이도 1단계 리포트(활동·맞춤법·꾸준함·처음→고친 글·선생님 한마디)는 완성된다.

## 3. 능력치 다섯 갈래 — 재는 방법

| 갈래 | 무엇을 보나 | 재는 방법 | 비용 |
|---|---|---|---|
| 내용 | 생각·까닭·예시가 충분하고 주제에 맞는가 | AI가 대표 글을 기준표로 판단 → ★1~5 | 리포트 만들 때만 |
| 구성 | 처음·가운데·끝, 문단 나누기, 흐름 | AI → ★1~5 | 〃 |
| 표현 | 문장이 자연스럽고 낱말이 다양하고 알맞은가 | AI → ★1~5 | 〃 |
| 맞춤법 | 글 100자당 틀린 표현 수 | 앱의 **초등 맞춤법 탐지 규칙**(`spellingDetectionRules.js`·`elementarySpellingEntries.js`)으로 기간 안 **모든 제출 글**을 센다 → 구간별 ★ | 0원 |
| 꾸준함 | 글 쓴 주의 비율, 고쳐 쓰기 횟수 | 학급 발자국의 날짜별 기록(`student_writing_daily_snapshots`) → 구간별 ★ | 0원 |

- 맞춤법 별은 "자주 틀리는 초등 맞춤법 기준" 이라고 리포트에 밝힌다(목록에 없는 오류는 세지 못한다).
- 맞춤법 기록(`class_spelling_student_daily`)은 **찾아본 기록**이지 틀린 수가 아니므로 능력치에 쓰지 않는다.
- AI 판단 원칙
  - 글이 올라올 때마다 부르지 않는다. 리포트를 만들 때 **학생마다 달에 대표 글 최대 2편**만 판단한다.
  - 한 번 판단한 글은 `(글, 기준표 버전)` 으로 저장해 다시 만들거나 연간 종합에서 **다시 부르지 않는다.**
  - 기준표에 버전을 붙인다(`growth-rubric-v1`). 기준표가 바뀌면 새 버전으로만 판단하고 옛 리포트는 옛 버전 그대로 둔다.
  - 학년군(1~2·3~4·5~6)에 맞춘 기준표 문장을 쓴다. 저학년 글을 고학년 잣대로 재지 않는다.
- 대표 글 고르기(자동 추천, 교사가 바꿀 수 있음): 그 달 제출 글 가운데 ① 교사가 승인한 글 ② 분량이 충분한 글(학년별 최소 글자)
  ③ 고쳐 쓴 글을 먼저. 일기·독서록은 활동 수에는 넣되 능력치 판단 대표 글에서는 뺀다(글 성격이 달라 별이 흔들린다).

## 4. 리포트 구성 (A4 2쪽, 인쇄·화면 공통)

1. **머리**: `○○○의 1학기 글쓰기 성장 리포트` · 기간 · 학년 반 · 담임 이름
2. **한눈에 보기**: 쓴 글 · 고쳐 쓰기 · 독서록 · 일기 · 받은 공감 (숫자 카드)
3. **달마다 이만큼 썼어요**: 달별 막대(과제 글·자유 글·독서록·일기 쌓기)
4. **글 능력치**: 거미줄 그래프 — 기간 첫 달(점선) vs 마지막 달(색칠), 다섯 갈래 ★ 표
5. **능력치가 자란 길**: 갈래별 달마다 점을 이은 선 그래프(데이터가 없는 달은 점을 찍지 않고 선을 끊는다)
6. **대표 글, 처음 → 고친 뒤**: 한 편(연간은 두 편). 바뀐 문장에 형광펜 표시 + "이렇게 좋아졌어요" 한 줄
7. **잘한 점 👍** (2~3개)
8. **다음엔 이렇게 해 보면 더 좋겠어요 🌟** (2~3개, 해 볼 수 있는 행동으로)
9. **선생님 한마디** (교사가 직접 씀, 필수)
10. **읽는 법**: 별의 뜻과 "반 친구와의 비교가 아닙니다" 안내(가정 배부용, 작은 글씨 금지 — 0.8rem 이상)

- **연간 종합**: 4번에 1학기 끝·2학기 끝을 겹쳐 그리고, 6번에 한 해 중 가장 많이 자란 글을 넣는다.
- 그래프는 라이브러리 없이 **SVG 로 직접 그린다**(번들 무게, 인쇄 선명도). 색은 디자인 토큰, 흑백 인쇄에서도 구분되게 점선·무늬를 함께 쓴다.
- 그래프마다 같은 내용을 글로도 적는다(스크린리더·흑백 인쇄 대비).

## 5. 발행 횟수와 여는 때

| 리포트 | 여는 때(KST) | 담는 기간 | 학년도 |
|---|---|---|---|
| 1학기 | 7월 1일 | 3월 1일 ~ 7월 31일 | 그해 |
| 2학기 | 12월 1일 | 8월 1일 ~ 12월 31일 | 그해 |
| 연간 종합 | 이듬해 1월 1일 | 3월 1일 ~ 12월 31일 | **지난해**(1·2월은 앞 학년도에 속한다) |

- **닫는 때(처음 값)**: 한 번 열리면 그 학년도가 끝나는 **2월 말일까지** 만들 수 있다. 방학 중에 늦게 만드는 교사를 막지 않기 위해서다.
  열리기 전에는 `기간 고르기` 에서 그 칸이 잠기고 "7월 1일부터 만들 수 있어요" 처럼 여는 날을 보여 준다.
- **담는 기간의 끝 = 만드는 날과 기간 끝 중 이른 날.** 7월 10일에 만들면 7월 10일까지만 담는다. 교사는 끝 날짜를 기간 안에서 당길 수 있다
  (학교마다 방학 날짜가 달라서).
- **한 기간에 한 번**: `(class_id, student_id, school_year, period_type)` 하나. 확정 전 초안은 몇 번이든 다시 만들 수 있고,
  확정을 풀고 고친 뒤 다시 확정해도 **같은 리포트**로 친다(새 발행이 아니다). 학생 화면에는 늘 마지막 확정판만 보인다.
- 여는 때·횟수는 화면이 아니라 **리포트를 만드는 RPC 가 서버 시각(KST)으로 판단**한다. 교사 컴퓨터 시계를 바꿔도 열리지 않는다.
- **관리자 시험 모드**: 공개 단계가 `admin_only` 인 동안에는 관리자가 여는 때와 상관없이 만들어 볼 수 있다(그렇지 않으면 9월에 시작한 시험을
  7월까지 못 한다). 시험으로 만든 리포트에는 `시험` 표시를 달고, 학생 공개는 관리자 반에서만 된다. `public` 으로 바꾸면 이 예외는 사라진다.

## 6. 화면과 흐름

### 교사 (1차: 관리자만)

- 자리: **평가·기록 → 성장 리포트** (세부 메뉴 하나 추가, 상단 메뉴 구조는 그대로). 공개 단계가 `admin_only` 인 동안
  관리자가 아닌 교사에게는 메뉴 칸 자체를 그리지 않는다.
- 흐름: `① 기간 고르기(1학기/2학기/연간 — 학년도 날짜 자동, 고칠 수 있음)` → `② 초안 만들기(반 전체, 진행 막대)` →
  `③ 학생별 검토(대표 글 바꾸기, AI 문장 고치기, 선생님 한마디)` → `④ 확정(판 고정)` → `⑤ 전달(학생 공개 / 인쇄·PDF)`
- ④ 확정하면 그 시점의 숫자·그래프·문장·대표 글 원문을 **판으로 고정**한다(문집 확정판과 같은 방식). 나중에 학생이 글을 고쳐도
  이미 나간 리포트는 바뀌지 않는다. 확정을 풀면 초안으로 돌아가고 학생 화면에서도 내려간다.
- ⑤ `반 전체 인쇄`·`PDF로 저장`(**교사만**): 학생마다 새 쪽으로 넘어가며 A4로 출력. 기존 문집 인쇄(`class-agit/anthology/print.js`)와 같은 방식,
  앱의 PDF 원칙대로 서버에서 PDF 를 만들지 않는다(`writing/export/README.md`).

### 학생 (3단계)

- 학생 홈에 `선생님이 보낸 성장 리포트` 카드와 알림(`student_notification_events`). 누르면 자기 리포트 목록(최대 3개: 1학기·2학기·연간).
- **뷰어에서 보기만**: 인쇄물과 **같은 A4 쪽**을 쪽 단위로 보여 주는 보기 화면 — 쪽 넘기기(이전·다음·쪽 번호), 확대·축소(쪽 맞춤·폭 맞춤),
  전체 화면. 태블릿에서는 좌우로 밀어 쪽을 넘긴다.
- **내려받기 없음**: 학생 화면에는 `PDF로 저장`·인쇄·내려받기 단추를 두지 않는다. 파일 주소(blob·data URL)를 만들지 않고, 쪽은 화면 안에서만 그린다.
  브라우저 인쇄(Ctrl+P)는 `@media print` 에서 리포트 내용을 숨기고 "선생님께 받은 인쇄본을 보세요" 한 줄만 나오게 한다.
  글 선택·끌기·오른쪽 클릭 저장도 뷰어 안에서 막는다.
  (화면 캡처는 어떤 웹 화면도 막을 수 없다 — 막는다고 말하지 않는다.)
- **보는 기간**: 공개한 날부터 그 학년도 끝(2월 말일 KST)까지. 기간이 지나면 학생 리포트 RPC 가 내용을 돌려주지 않고, 목록에서도 사라진다.
  화면에서 숨기는 것만으로 막지 않는다. 교사 화면에서는 기간이 지나도 계속 보인다(기록 보관).
- 화면 보기와 교사 인쇄는 **같은 A4 렌더러 하나**를 쓴다. 따로 만들면 화면과 인쇄물이 어긋난다.
- 학생 홈은 무거운 계산을 하지 않고 **확정된 판 한 건만** 읽는다(학생 홈 성능 계약).

## 7. 데이터와 권한 (초안)

| 표 | 내용 |
|---|---|
| `growth_reports` | 학급·교사·학년도·기간 종류(`semester1`·`semester2`·`annual`)·시작/끝 날짜·상태(`draft`·`confirmed`·`published`)·기준표 버전·시험 여부. `(class_id, school_year, period_type)` 하나 |
| `growth_report_students` | 리포트·학생·**판 스냅샷(JSONB: 그래프 수치·별·대표 글 발췌)**·AI 초안·교사 문장·선생님 한마디·확정 시각. `(report_id, student_id)` 하나 = 학생마다 기간당 한 번 |
| `growth_post_trait_scores` | 글·기준표 버전·내용/구성/표현 단계(1~5)·짧은 근거·모델·판단 시각. `(post_id, rubric_version)` 하나 — AI 재호출 방지 캐시 |

- 공개 단계: `system_settings.growth_report_stage` = `admin_only` | `public` (활용 안내서 AI 길잡이와 같은 방식, 서버가 소유).
- 모든 쓰기·읽기는 `SECURITY DEFINER` RPC. 교사는 **자기 반만**, 학생은 **공개된 자기 리포트만**. anon 에 열지 않는다(`check:rpc-surface`).
- `admin_only` 동안 교사용 RPC 는 `profiles.role='ADMIN'` 이 아니면 거절한다 — 화면을 숨기는 것만으로 막지 않는다.
- 판단용 AI 에는 글 본문과 학년군만 보낸다. 학생 이름·학교·반은 보내지 않는다.
- 평가 점수 열(`initial_eval`·`final_eval`)은 어떤 RPC 도 읽지 않는다 — 검사로 막는다.
- AI 호출은 `vibe-ai` 에 새 종류(`GROWTH_TRAITS`·`GROWTH_SUMMARY`)로 붙이고, 모델 이름은 단일 원본을 쓴다. 호출 수 상한은 서버에서 원자적으로 센다.

## 8. 단계

### 1단계 — AI 없는 리포트 (관리자만)
- [ ] 공개 단계 설정·관리자 확인 RPC, 메뉴 `평가·기록 → 성장 리포트`(관리자에게만)
- [ ] 기간 고르기·리포트 만들기/확정/확정 풀기, 학생별 판 스냅샷
- [ ] 여는 때(7월·12월·1월, KST)와 기간당 1회를 서버에서 판단, 잠긴 칸에 여는 날 안내, 관리자 시험 모드
- [ ] 달별 활동 막대, 맞춤법·꾸준함 ★(탐지 규칙·날짜별 기록), 한눈에 보기 카드
- [ ] 대표 글 자동 추천·교체, 처음 → 고친 뒤 비교(바뀐 문장 형광펜)
- [ ] 선생님 한마디·잘한 점·다음 목표 **직접 입력**
- [ ] A4 인쇄(학생 한 명·반 전체), 가정 배부 안내 문구
- [ ] dev-lab 시나리오(글 0편·보통·아주 많음·고쳐 쓴 글 없음), PC·폰·인쇄 미리보기 촬영

### 2단계 — AI 능력치와 문장 초안 (관리자만)
- [ ] 학년군별 기준표 v1, `GROWTH_TRAITS` 판단·캐시, 내용·구성·표현 ★
- [ ] 거미줄·선 그래프 완성
- [ ] `GROWTH_SUMMARY`: 잘한 점·다음 목표 초안(성장 문장 원칙을 지시문과 검사로), 교사 수정
- [ ] 반 전체 초안 진행 막대·실패한 학생만 다시 만들기

### 3단계 — 학생 공개·연간 종합 (관리자 반에서만)
- [ ] 학생 홈 카드·알림, 학생 리포트 목록
- [ ] 보기 전용 뷰어(쪽 넘기기·확대·전체 화면·밀어 넘기기), 내려받기·인쇄 막기, 인쇄와 같은 A4 렌더러
- [ ] 학생 보기 기간(공개 ~ 학년도 끝 2월 말일 KST)을 서버에서 판단
- [ ] 연간 종합(두 학기 겹쳐 보기, 한 해 대표 글)

### 4단계 — 평가 뒤 전체 공개
- [ ] 선생님 평가(실제 반 1학기 리포트) → 고칠 점 반영
- [ ] `growth_report_stage = public`, FEATURE_MAP·도움말(`teacherGuides.js`)·활용 안내서·학생 도움말 동시 반영

## 9. 검증

- 성장 문장 검사: AI 초안과 고정 문구에 비교·순위·단정 표현(`반에서`, `평균보다`, `못한다`, `부족하다` 등)이 없는지 검사한다.
- 권한 검사: 다른 반 교사·다른 학생·anon 이 리포트를 읽지 못하는지, `admin_only` 에서 일반 교사 RPC 가 거절되는지.
- 판 고정 검사: 확정 뒤 원글을 고쳐도 리포트가 그대로인지, 확정을 풀면 학생 화면에서 내려가는지.
- AI 캐시 검사: 같은 글·같은 기준표로 두 번 만들 때 AI 호출이 0번인지.
- 평가 점수 차단 검사: 리포트 RPC 본문에 `initial_eval`·`final_eval` 이 없는지.
- 인쇄: 흑백에서 그래프 구분, 학생마다 새 쪽, 글자 0.8rem 이상.
- 여는 때 검사: KST 경계(6월 30일 23:59 → 7월 1일 0:00, 12월 31일 → 1월 1일의 학년도 계산)를 서버 함수에 시각을 넣어 확인한다.
  `public` 에서는 관리자 예외가 사라지는지도 본다.
- 횟수 검사: 같은 기간 두 번째 만들기가 거절되는지, 확정 풀기 → 다시 확정이 새 발행으로 세지지 않는지.
- 뷰어 검사: 화면 쪽 수와 교사 인쇄 쪽 수가 같은지(같은 렌더러), 폰 폭에서 쪽 맞춤이 잘리지 않는지.
- 보기 전용 검사: 학생 화면 코드에 저장·인쇄 단추, `URL.createObjectURL`, `window.print` 가 없는지, 인쇄 미리보기에 내용이 나오지 않는지.
- 보기 기간 검사: 2월 말일 23:59 → 3월 1일 0:00(KST)에 학생 RPC 가 내용을 돌려주지 않는지, 교사 RPC 는 그대로인지.
