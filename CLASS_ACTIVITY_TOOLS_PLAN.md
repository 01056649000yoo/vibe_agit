# 학급 활동 도구함 확장 계획

> **상태**: 2026-09-01 `우리 반 스크린` 1차 구현. 활동 모달 계열은 후속
>
> **목표**: 자리·역할 배치처럼 교사가 설정하고 학급 전체가 프로젝터·대형 화면의 전용 모달에서 함께 보는
> 수업 도구를 독립 모듈로 하나씩 추가한다.

## 1. 확정 방향

- 모든 도구는 현재 선택한 아지트 학급 명부를 사용하며 학생을 따로 등록하지 않는다.
- `교사 설정 화면 → 활동 시작 → 전체 화면 수업 모달 → 완료 결과 → 지난 기록` 흐름을 공통으로 사용한다.
- 공통 화면 틀과 애니메이션은 공유하되, 도구별 설정·결과·기록은 각 모듈이 소유한다.
- 한 번에 전부 만들지 않는다. T0 공통 수업 모달의 첫 실제 적용 대상으로 T5 글감 조합기를 만들고,
  운영 확인 뒤 공통 셸을 확정해 나머지 도구에 하나씩 적용한다.
- 기존 자리·역할 도구는 안정화된 현재 형식을 유지하며, 모둠 구성처럼 성격이 같은 기능만 같은 모듈에 붙인다.

### 2026-09-01 결정 — 지속형 `우리 반 스크린`을 별도 모듈로 먼저 만든다

- 구글 클래스룸 화면에 연동하지 않고 아지트 자체 교사 도구로 제공한다.
- 잠깐 실행하고 끝나는 T0~T9 활동 모달과 달리, `우리 반 스크린`은 안내 자료와 현재 현황을 계속 띄우는
  **지속형 보드**이므로 `src/modules/tool/class-board/`가 독립 소유한다.
- 보드 셸은 위젯 발견·배치·저장·스크린 표시만 맡고 표시 기능은 `widgets/<widget-id>/` 모듈로 추가한다.
- 화면의 기본 큰 구획은 왼쪽 자료 70%·오른쪽 현황 30%로 두되, 현황은 수업 중 접고 펼칠 수 있게 한다. 접으면
  현황 갱신을 쉬고 왼쪽 자유 캔버스가 전체 폭을 사용한다. 텍스트·이미지는 좌표 기반 이동·가로세로 크기 조절·
  핀 고정을 지원하며 저장한 좌표와 핀 상태는 스크린에도 그대로 적용한다.
- 편집 중에는 텍스트·이미지 본체를 마우스로 직접 잡아 옮길 수 있고 기존 상단 이동 손잡이도 함께 유지한다.
  짧은 클릭은 선택만 하며, 핀을 꽂으면 본체와 손잡이 이동·크기 조절을 모두 잠근다.
- 스크린에서도 `화면 편집`을 켜 왼쪽 텍스트·이미지를 추가·업로드·배치할 수 있다. 편집은 임시 보드로
  진행해 명시적으로 저장하거나 취소하며, 오른쪽 실시간 현황은 움직이거나 삭제할 수 없는 읽기 전용 영역이지만
  수업 중 접고 펼칠 수 있다.
- 이미지 업로드 직후에는 실제 이미지와 현재 왼쪽 캔버스의 픽셀 비율로 프레임 크기를 한 번 자동 맞춘다. 이후에는
  교사가 가로·세로·모서리 손잡이로 비율 제한 없이 다시 조절할 수 있고, 최종 퍼센트 배치만 기존 보드에 저장한다.
- 저장한 스크린 최대 20개는 편집 화면 상단 탭으로 전환한다. 탭 목록 오른쪽의 한 줄 작업바에서
  `새 탭`·`저장`·`삭제`·`복제`·`복구`를 처리한다. `삭제`는 기존 archive 경계로 목록에서만 제외한다. 탭 전환은
  처음 받은 작업공간 응답만 사용해 추가 조회하지 않고, 예전 `보관`과 삭제 화면은 복구 목록을 열 때만 읽는다.
- 1차 수업 위젯은 수동 날씨·로컬 타이머·로컬 스톱워치·담당 학급 학생 무작위 뽑기다. 외부 날씨 API와 위치 정보는
  쓰지 않고, 학생 뽑기는 이름 최대 100명을 한 번 읽되 식별자·결과·순서를 저장하지 않는다.
- 캡처 이미지는 편집 중 `Ctrl+V`로 바로 붙여넣는다. 선택 이미지가 있으면 교체하고, 없으면 빈 이미지 칸을 먼저
  채운 뒤 필요할 때 새 이미지 칸을 만든다. 파일 선택과 같은 최적화·비공개 업로드를 거쳐 실제 비율로 자동 맞춘다.
- 첫 위젯은 텍스트·비공개 이미지·글쓰기 현황이다. 현황은 가장 최근 미션의 제출자·미제출자 이름표와 오늘의
  일기·독서록 완료 숫자를 담당 교사 화면에만 표시하며, 보드는 살아 있는 현황을 저장하지 않는다.

## 2. 공통 전체 화면 수업 모달

### 화면 계약

- 상단: 도구 이름, 학급명, 현재 단계, 전체 학생 수
- 중앙: 추첨·타이머·점수·응답처럼 학생과 함께 볼 핵심 활동판
- 보조 영역: 남은 학생, 진행률, 모둠별 상태 또는 다음 단계
- 하단: 시작, 다음, 일시정지, 되돌리기, 완료 같은 교사용 진행 버튼
- 완료 뒤: 완성 결과 보기, 다시 하기, 기록 저장
- 프로젝터용 전체 화면, 음소거·효과음, PC·태블릿·모바일 반응형

### 동작 계약

- 진행 중 실수로 닫혀 결과가 사라지지 않게 하며, 닫을 수 있는 활동은 확인 절차를 둔다.
- 학생 이름과 남은 시간은 교실 뒤에서도 읽을 수 있는 크기로 표시한다.
- `prefers-reduced-motion`과 키보드 포커스·Tab 순환·스크린리더 상태 안내를 지원한다.
- 모달을 닫거나 새로고침해도 필요한 활동은 현재 세션을 복구한다.
- 완료 결과는 DB에 저장하고 지난 기록에서 당시 결과판 모양 그대로 복원한다.
- 현재 자리·역할 추첨 모달의 포털·효과음·이름표 이동·완료 포커스는 공통 셸로 일반화한다.

## 3. 도구 식별자와 구현 위치

| 단계 | 도구 | 전체 화면 모달의 핵심 모습 | 구현 위치 |
|---|---|---|---|
| T0 | 공통 수업 모달 셸 | 학급명·학생 수·진행 상태·공통 조작부 | `src/modules/tool/shared/` |
| T1 | 모둠 자동 구성 | 뽑힌 이름표가 각 모둠 카드로 이동해 모둠 완성 | 기존 `classroom-arrangement` 확장 |
| T2 | 발표·질문 순서 추첨 | 이름표가 발표 순서표로 이동하거나 질문 학생 확정 | `presentation-order` |
| T3 | 수업 타이머 | 현재 단계·남은 시간·다음 단계 큰 화면 표시 | `class-timer` |
| T4 | 모둠 점수판 | 모둠별 점수·순위·점수 획득 효과 | `group-scoreboard` |
| T5 | 함께 만드는 글감 조합기 | 인물·장소·사건·감정·첫 문장 조합 | `writing-roulette` |
| T6 | 친구 피드백 짝짓기 | 이름표가 읽어 줄 친구에게 연결 | `peer-matching` |
| T7 | 빠른 투표·이해도 신호등 | 학생 응답을 막대그래프·신호등으로 집계 | `quick-response` |
| T8 | 활동 빙고 | 학급·모둠 빙고판과 완료 현황 | `activity-bingo` |
| T9 | 모둠 대항 미션 | 단계·힌트·점수·최종 순위 | `class-competition` |

## 4. 단계별 기능 범위

### T1 — 모둠 자동 구성

- 모둠 수 또는 모둠당 인원 선택
- 남녀 혼합, 같은 모둠 금지·권장, 최근 같은 모둠 회피
- 특정 학생 모둠 고정, 결석 학생 제외, 모둠별 인원 차이 최소화
- 로또 추첨 뒤 이름표가 실제 모둠 카드로 이동
- 완료된 모둠표와 지난 모둠 기록을 그룹별로 복원

### T2 — 발표·질문 순서 추첨

- `발표 순서`와 `질문 학생` 두 모드
- 전체·선택 학생, 추첨 인원, 이미 뽑힌 학생 제외, 결석·면제 처리
- 발표 순서표 완성, 발표 완료 체크, 오늘 뽑힌 횟수 표시
- 최근 앞 순서 회피는 선택 설정으로 제공

### T3 — 수업 타이머

- 단일 타이머와 `준비 → 활동 → 발표 → 정리` 다단계 타이머
- 단계별 이름·시간·알림음, 일시정지, 시간 추가
- 새로고침 복구가 필요한 진행 세션과 저장 가능한 수업 템플릿 분리
- 단순 실행 이력은 남발하지 않고 교사가 저장한 템플릿·결과만 보관

### T4 — 모둠 점수판

- 모둠별 점수 증감, 점수 사유, 실행 취소, 공동 순위·순위 숨김
- 수업 임시 점수와 학생 영구 포인트를 분리
- 수업 종료 뒤 교사가 명시적으로 선택할 때만 포인트 엔진을 통해 보상 전환

### T5 — 함께 만드는 글감 조합기 (첫 구현 도구)

- 이야기 글 모드에서 주인공·장소·사건·물건·감정을 항목별 룰렛으로 하나씩 추첨한다.
- 확정된 항목은 카드에 고정하고 원하는 항목만 다시 뽑을 수 있게 한다.
- 교사는 기본 글감과 직접 입력한 글감을 섞고, 사용할 항목·속도·효과음·글쓰기 시간을 정한다.
- 전체 화면 모달은 `항목 추첨 → 최종 글감 카드 → 글쓰기 타이머 → 결과 저장` 순서로 진행한다.
- 첫 버전은 이야기 글 모드, 5개 항목, 항목별 다시 뽑기, 최종 카드, 타이머, 지난 기록까지 구현한다.
- 후속 확장에서 첫 문장·필수 단어, 생활 글 모드, `선생님 과제에 담기`, 학생 자유 글의
  `오늘의 글감으로 쓰기`, 같은 글감으로 쓴 우리 반 글 모아보기를 연결한다.

### T6 — 친구 피드백 짝짓기

- 친구 피드백은 자기 자신 제외, 최근 친구 회피, 상호 교환·순환 방식을 지원한다.
- 배정 결과에서 친구 아지트의 대상 글로 바로 이동한다.

### T7 — 학생 참여 응답

- 빠른 투표와 `이해 / 조금 어려움 / 도움 필요` 신호등을 한 모듈의 두 모드로 제공한다.
- 익명 집계와 이름 확인을 교사가 선택한다.
- 학생 홈에서 상시 조회·연결하지 않고 활동 화면을 연 동안만 응답한다.
- 학생 1명당 응답 RPC 1회, 학급 직접 범위, 입력·응답 수 상한을 둔다.

### T8~T9 — 활동·경쟁 확장

- 활동 빙고로 어휘·독서·교과 활동의 가벼운 진행판을 먼저 만든다.
- 이후 기존 앱에서 보관한 경쟁 활동 데이터를 참고해 모둠 대항 미션을 설계한다.
- 경쟁 활동은 단계 잠금, 제한 시간, 힌트, 점수 차감, 중간·최종 순위를 지원한다.
- 결과물을 글쓰기 소재로 가져올 수 있게 하되 연구소 코드를 이식하지 않는다.

## 5. 데이터·성능·보안 원칙

- 도구 설정·진행 세션·완료 기록은 학급 `class_id`로 직접 범위를 제한한다.
- 목록은 안정 정렬과 상한을 두고, 학생 홈 bootstrap에 도구별 조회를 추가하지 않는다.
- 투표처럼 즉시성이 필요한 기능도 활동 상세 화면을 연 동안만 연결하고 종료 즉시 해제한다.
- 결과 변경은 도구 전용 RPC 한 번으로 끝내며 권한은 실제 교사·학생·학급 연결로 검증한다.
- 포인트 전환은 `src/modules/points/` 계약과 `point_engine_apply()`를 사용하고 안정적인 `event_key`를 둔다.
- 학생 이름·응답·점수를 브라우저 로그나 공개 저장소에 남기지 않는다.
- 기능은 학급별 기본 OFF 모듈로 추가하고, OFF 시 기존 데이터는 삭제하지 않는다.

## 6. 도구 하나의 완료 기준

1. 범위와 필수·권장 조건 확정
2. 모듈 매니페스트·성능표·데이터 계약 작성
3. 교사 설정 화면 구현
4. 공통 전체 화면 수업 모달 연결
5. 완료 결과와 지난 기록 결과판 구현
6. 도움말 추가
7. 회귀·권한·변이 검사와 프로덕션 빌드 통과
8. PC·프로젝터·태블릿·모바일 실기 확인
9. 운영 배포와 배포 후 HTTP 검증
10. 실제 수업 확인 뒤 다음 도구 착수

## 7. 다음 작업

`우리 반 스크린` 자유 배치판을 운영 교사 계정·프로젝터에서 먼저 확인한다. 텍스트·이미지 이동·크기 조절·핀,
미션 이름표와 일기·독서록 현황의 가독성, 보드 복제·보관 흐름을 확인한 뒤 추가 위젯과 배치 프리셋을 정한다.
지속형 보드 확인과 별개로 활동 모달 계열의 첫 구현은 **T0 공통 수업 모달 기본 셸 + T5 함께 만드는 글감
조합기 첫 버전** 순서를 유지한다. 다음 활동 도구 후보는 T1 모둠 자동 구성이다.
