# 끄적끄적 아지트 마스터 로드맵

> **사용법**: 작업 세션을 시작할 때마다 이 파일을 먼저 읽는다.
> "현재 위치"를 확인하고, 해당 스테이지의 체크리스트에서 이어서 작업한다.
> 항목을 완료하면 `[x]`로 갱신하고, 결정이 바뀌면 "결정 기록"에 한 줄 남긴다.
> 이관(Stage 1)의 상세 절차·검증 사실은 [INTEGRATION_PLAN.md](INTEGRATION_PLAN.md) 참조.
> **작업 내역은 [WORKLOG.md](WORKLOG.md)에 기록**(모델 공통 규칙은 [AGENTS.md](AGENTS.md)). 여러 모델이 번갈아 작업하므로 시작 전 두 파일을 읽고, 끝나면 남긴다.

---

## 🎯 비전: 4대 방향 (2026-07-16 확정)

1. **완전 이관 우선** — 끄적끄적 아지트를 앱+Supabase DB까지 맥미니 호스팅으로 먼저 이관한다
2. **연구소 통합** — 이관 후 아지트 연구소를 로그인(SSO)까지 통합하고, 학생이 연구소
   **하위 활동들의 결과물을 글쓰기에서 불러올 수 있는 구조**를 만든다
3. **기존 기능 모듈화** — 글쓰기를 제외한 기능들을 파트별로 분류·모듈화해서,
   기능이 추가되어도 다른 기능에 문제가 없는 구조로 리팩토링한다
4. **코어 셸 불변** — 핵심 글쓰기 기능은 그대로 두고, 글쓰기의 추가 기능도 모듈로만
   붙인다. 코어 셸은 유지하고 기능 모듈만 추가하는 구조로 간다

## 🎯 제품 집중 3대 기둥 (2026-07-24 확정)

앱을 이 **3가지에만 집중**한 도구로 고도화한다. 이 셋에 기여하지 않는 기능은 **삭제 또는 기본 OFF** 후보다.
이것이 "무엇을 남기고 무엇을 지울지"의 판정 기준(Stage 3c)이다.

1. **교사의 글쓰기 지도** — 미션 제시, AI·교사 피드백, 승인, 평가·기록 도우미
2. **학생의 자율적 글쓰기 + 과제 제출** — 선생님 장르 과제 + 학생이 언제든 쓰는 독서록(1차)·일기(2차, 2026-08-06)
3. **포인트를 활용한 재미 동기부여** — 포인트 시스템 + 드래곤/게임 보상 (**확장 중**: 드래곤 다이나믹화 +
   포인트로 즐기는 신규 엔터테인먼트 활동 다양화, Stage 4d)

> 판정 질문: "이 기능이 위 3가지 중 하나를 직접 돕는가?" — 아니면 정리 대상.
> 방법은 즉시 삭제가 아니라 **모듈화 → 기본 OFF → 선생님 확정 → 삭제** (되돌릴 수 있게, 대원칙 3).


---

## 🧭 현재 위치

> **2026-08-29 운영·보안 보완 완료**: 자비스·샘링크 취약 의존성을 0건으로 갱신·재배포하고,
> 8001·8080을 loopback으로 제한했으며 8444는 제거했다. 자비스 보안 헤더·동적 렌더링, 샘링크 HSTS,
> secret 권한 600·Docker 빌드 제외와 자비스 로컬 git 기준점도 반영했다. 외장 SSD 신규 백업은 파일 단위
> rclone crypt로 전환했다. 운영 이미지 CVE 전수 검사는 끝났고, 남은 핵심은 Tailscale SSH 공개키 전환,
> 내부 디스크 위험 수용, Supabase 공식 묶음 정상화다. 다음 자동 일정은 **2026-08-30 05:00 이후
> 백업 2일차 대조**다.
> 상세 증거는 [보안 감사 보고서](docs/SECURITY_AUDIT_20260829.md)와 [WORKLOG.md](WORKLOG.md) 최상단에 있다.
> Supabase v0.8.0+Kong 격리 복원 예행연습은 8월 29일 PASS했고, 8월 30일 04:00 백업·05:00 판정이
> 모두 PASS일 때만 05:30 자동 반영하도록 예약했다. 실패하면 운영은 변경하지 않는다.

### 3개 앱 통합 백업 대시보드와 7일 모니터링 (2026-08-28 시작)

- [x] 실행 원장 아래 아지트·샘링크·자비스 앱별 결과 3행을 저장하는 비공개 원장 추가
- [x] 기존 관리자 백업 RPC와 서비스 요약 RPC 한 번에 앱별 결과·주의 개수 포함
- [x] 관리자 첫 화면에 `앱 백업 3/3` 요약과 백업 탭 바로가기·주의 배지 추가
- [x] 백업 탭을 앱 카드 3개 → 공용 사본 → 실제 복구 → 최근 실행 순서로 정리
- [x] 매일 통합 백업과 월간 복구 리허설이 DB·파일/설정을 앱별로 판정하도록 운영 스크립트 연결
- [x] 5분 건강검진이 3행 누락·앱 실패·백업/복구 지연을 `backup_failed`로 경고하도록 확장
- [x] 새 7개 산출물 실행은 앱 결과 3행·필수 사본 완성 전까지 `RUNNING`, 완료 뒤에만 `PASS/FAIL` 확정
- [x] 운영 DB 마이그레이션·앱 배포·served bundle·5분 건강검진 확인
- [x] 03:30·04:00·매월 1일 04:40·5분 간격 LaunchAgent 실제 로드와 첫 실행 전 기준 상태 확인
- [x] 일별 원장·세 사본·보조 덤프·복구·경고·공개 응답 판정기와 매일 05:00 LaunchAgent 설치
- [x] 2026-08-28 실제 DB 덤프 목차에서 대시보드 원본 표의 `TABLE DATA` 포함 확인 — 04:50 최신
      트래픽은 다음 날 04:00 백업, 호스트 트래픽 차감 기준 파일은 재생성 가능한 임시 상태로 제외
- [x] 2026-08-29 첫 자동 실행: 부모 `PASS`, 앱 3/3, 산출물·세 사본 7/7/7, 보조 덤프·서비스·경고 정상,
      운영 관리자 RPC의 화면용 요약과 일치
- [ ] **일요일 1차 점검(2026-08-30 05:00 이후)**: 토·일 자동 판정 2개와 관리자 원장을 대조하고 보완 여부 결정
- [ ] 일요일 판정이 실패면 원인별로 운영 설정/스크립트/DB·화면 보완, 성공이면 설정을 유지하고 7일 관측 계속
- [ ] 관리자 실계정의 좁은 화면 눈검사(현재 연결 가능한 브라우저 없음)
- [ ] 첫 자동 3/3 성공을 기준으로 7일 연속 모니터링 완료
- **점검표**: [docs/BACKUP_MONITORING_20260829.md](docs/BACKUP_MONITORING_20260829.md)

### 3개 앱 운영·서버·외부 노출 보안 감사 (2026-08-29)

- [x] 16개 컨테이너·호스트 자원·통합 DB·백업/복구·서비스 응답 대조
- [x] TCP/UDP 전체 리스너, Docker 바인딩, macOS 방화벽 허용, Cloudflare Tunnel ingress 대조
- [x] 비인가 Supabase REST·관리 후보 경로 401/400 차단 확인
- [x] 아지트 전체 보안 하네스와 세 앱 운영 의존성 감사
- [x] env·백업 파일 권한, Docker 이미지 내부 env 파일, 컨테이너 격리 옵션 점검
- [x] **P0** 자비스 Next 15.5.24·ws 8.21.3·postcss 8.5.26·sharp 0.35.4 패치, 타입·Docker 빌드·배포
- [x] **P0** 샘링크 Next 16.3.3·nanoid 3.3.18·postcss 8.5.26·sharp 0.35.4 패치·빌드·배포
- [ ] **P0** SSH 공개키 확인 후 password/keyboard-interactive 비활성화, 공유기 TCP 22 전달 여부 확인
- [x] **P0** 샘링크·옛 자비스 secret env 권한 600과 두 저장소 `.dockerignore` 비밀/백업 패턴 보강
- [x] 8001·8080 loopback 제한, 소비자 없는 8444 TCP/UDP 제거 후 Tunnel·공개 서비스 회귀 확인
- [x] 자비스 보안 헤더와 샘링크/API HSTS, 아지트 API 호스트 Caddy HSTS 공개 응답 반영
- [x] 외장 SSD 신규 사본을 `agitssdcrypt:`로 파일명·내용 암호화하고 7/7 cryptcheck·실제 복구 리허설 통과
- [~] 내부 FileVault는 무인 재부팅·Docker 자동 복구를 우선해 보류하고 물리 접근 제한으로 보완;
      맥 밖 rclone 복구키 사본 확인과 기존 외장 평문 사본 정리는 대기
- [x] 자비스 비밀 제외 로컬 Git 기준점 생성 — 사용자 결정에 따라 GitHub 원격·push 없이 통합 백업으로 복구
- [~] 컨테이너 격리 강화 — 샘링크 비root/read-only/cap drop/NNP 완료, 아지트·연구소·classroom-tools 대기
- [x] 샘링크 보안 보완·classroom-tools loopback 제한·아지트 누적 완료 커밋을 각 `origin/main`에 push하고
      샘링크·classroom-tools GitHub Actions 자동 배포 성공 확인
- [x] Docker 소켓 없는 Trivy 임시 tar 방식으로 운영 고유 이미지 15개 CVE 점검·갱신 우선순위 기록
- [x] Docker 32GB 내부 디스크 100%로 Kong 건강검사가 실패한 원인을 확인하고, 실행 이미지·볼륨·빌드 캐시는
      보존한 채 미사용 이미지 15.18GB 정리. 14GB 여유·Kong healthy·전체 16개 컨테이너 실행 확인 (2026-08-30)
- [ ] 관리자 상단 건강 요약과 `운영 → 서버 상태`의 새로고침 상태를 한 원본으로 연결. 폴링 없이 서버 상태
      새로고침·관리자 홈 복귀·창 포커스 때 해제된 컨테이너 경고가 두 화면에서 함께 사라지도록 보완 (2026-08-31 진단)
- [x] Supabase self-hosted 월간 릴리스 선택·검증·롤백 정책 수립 —
      [정책 문서](docs/SUPABASE_RELEASE_POLICY.md) · [실행 런북](docs/UPDATE_RUNBOOK.md)
- [x] Realtime `/realtime/v1/api/openapi`·`/tenants`를 공식 Kong `request-termination` 규칙으로 403 차단;
      통합 연구소 제출 확인 WebSocket 101, Kong healthy, 최근 오류 0 확인
- [x] Supabase `self-hosted/v0.8.0`+Kong staging 생성·이미지 선다운로드·최신 백업 격리 복원 —
      새 PG17·Auth·REST·Realtime·Storage·Edge가 모두 healthy, API 200·관리 경로 403·WebSocket 101 확인
- [ ] **2026-08-30 05:30 Supabase 묶음 정상화 예약** — 04:00 백업과 05:00 3/3 판정이 모두 PASS일 때만
      고정 staging을 반영하고, 실패 시 자동 중단·기동/스모크 실패 시 이전 설정·이미지로 자동 롤백
- [ ] 정상화 성공 뒤 `.supabase-version=self-hosted/v0.8.0` 기준점과 7일 관찰; Envoy 전환은 별도 작업
- [~] Stream Deck 28198은 두 LAN 주소·Docker에서 연결됨 — 공유기 TCP 전달 여부는 관리자 화면 확인 필요
- [ ] 관리자 실계정으로 백업 대시보드와 모바일 좁은 화면 직접 확인
- [x] 관리자 `운영 → 서비스 관리`에 12항목 분기 점검표·완료일+3개월 일정·이미지별 CVE 원장과 추이 추가;
      Docker 소켓 없는 tar 검사기와 30일 경과 판정 LaunchAgent 준비
- [x] **첫 CVE 기준 검사 완료** (2026-08-30, 이미지 16개·긴급 23) — 분기 자동화 활성
- [ ] 관리자 화면에서 **12개 항목 판정·완료** → 완료 시각 + 3개월이 다음 점검일
      근거: [docs/SERVICE_REVIEW_20260830.md](docs/SERVICE_REVIEW_20260830.md)
- **이미지 검사 결과**: [docs/DOCKER_CVE_SCAN_20260829.md](docs/DOCKER_CVE_SCAN_20260829.md)
- **보고서**: [docs/SECURITY_AUDIT_20260829.md](docs/SECURITY_AUDIT_20260829.md)

### 자비스 개인 대시보드 1차 개편 (2026-08-29 분석 → 오픈클로 관점으로 재수립)

> 자비스는 사람이 입력하는 웹앱이 아니라 **오픈클로 에이전트가 쓴 것을 확인·정정하는 관제탑**이다.
> 이 관점으로 우선순위를 다시 잡은 계획이 [고도화 계획](docs/JARVIS_UPGRADE_PLAN_20260829.md)이며,
> 아래 J1~J5는 그 계획의 단계다.

- [x] 현재 화면·운영 경로·데이터 규모·동기화·인증 경계 분석
- [x] 오픈클로 게이트웨이·스킬·마크다운 원본과 자비스 사이의 실제 자료 흐름 확인
- [x] **J1** 3003 구 서버 정리·재검증 주소 3004 하나로 축소 — 세 경로 같은 빌드 확인
- [x] **J1** GitHub 미사용 확정 — 개인 자료는 추적하지 않고 비밀 없는 코드·운영 스크립트만 로컬 Git으로
      선별 관리하며 기존 통합 백업을 원격 복구 사본으로 사용
- [x] **J2** unlock/lock/첨부PIN 액션에 로그인 검사 추가·잠금 쿠키 `secureCookie()` 통일
- [x] **J2** 잠금 쿠키 HMAC 서명 토큰화·8시간 만료와 PIN 실패 제한(10분 5회 → 30초 대기)
- [x] **J2** 무인 검사 게이트 — 타입·실제 사고 기반 불변식 6개·Next 빌드와 배포 번들 검증
- [ ] **J3** `app.sync_events` 기록 추가, 홈을 오늘/이번 주/확인 필요/에이전트 활동으로 개편
- [ ] **J3** 완료 업무 기본 접기·프로젝트 마지막 갱신/노후 표시·첨부파일 내비게이션 추가
- [ ] **J4** 저장 계열 스킬 9개를 save/lookup/cleanup 3개로 통합, DB 읽기 전용 조회 경로 추가
- [x] **J5 웹 경량화** — 4초 새로고침을 창 복귀+45초로 완화하고 주요 목록 최신 500행 상한·
      백업/경고/최근 자료 한 줄 상태 표시 완료
- [ ] **J5 Google Tasks** 1분 전체 풀링 완화
- [x] **J6 토큰 절감** — `tools.deny` 10개 적용, 세션 헤더 76.2KB→43.4KB(43%), Vault 읽기 금지·DB 우선 조회 규칙
- [x] **구글 동기화 성능** — 내용 지문 캐시로 전체 동기화 3분 24초 → 8.8초, API 326회 → 8회
- [x] **J7-a 산출물·공유** — `app.documents`/`document_shares`, 공개 `/s/<토큰>`, 민감 자료 4겹 차단
- [x] **J7-b 양식 저장소** — `app.templates`, `{{빈칸}}`/`{{본문}}` 계약은 오픈클로 참고용으로 유지.
      수동 *자료 고르기 → 양식 씌우기* 작업대는 숨기고 오픈클로가 문서를 직접 만드는 흐름으로 전환
- [x] **양식은 오픈클로로 직접 작성** — `scripts/make_template.py` + `jarvis-template` 스킬.
      기본 양식은 넣지 않는다(문구는 사용자 것을 그대로 씀)
- [~] 공유 탭 PC·모바일 실계정 눈검사 — 빌드·운영 번들·모바일 CSS는 통과, 연결 브라우저 0개로 직접 클릭만 남음
- [x] **오픈클로 자동 업데이트** — 안정판만 추종, 백업·스모크·자동 롤백, 매일 05:50 확인
      (`com.agit.openclaw-update`). 현재 `2026.7.1-2`로 이미 최신
- [x] **J8 PDF** — `scripts/read_pdf.py`(로컬 추출, 모델 호출 없음) + `jarvis-pdf` 스킬.
      지도→좁히기→뽑기 순서로 8쪽 공문 기준 4.7배 절약. 옛 `clawpdf-text-extract` 는 껐다
- [x] **J8 슬래시 명령** — `/today`·`/week`·`/task`·`/find`.
      `disable-model-invocation` 이라 평소 문맥에 안 실린다. `/week`는 채팅 표만 보여주며 공유 자료나
      링크를 자동 생성하지 않는다
- [ ] 텔레그램에서 `/today` 실제 실행 확인
- [ ] 정기 작업(cron)은 필요할 때 CLI 로 등록 — 사용자 판단
- [ ] 표준 지시(standing orders) 승격은 위 단축 명령이 익숙해진 뒤
- [ ] **J7-c 표로 정리** — 기간·조건 질의 고정
- [ ] **J7-d 사진 → 표 초안** — 확인 후 확정 흐름
- [ ] 샘링크 `short_links`로 짧은 공유 주소 발급 (`short_code` 자리 준비됨)
- [x] **J9 자료 수합 앱** — 투표·자유 의견은 제외하고, 암호가 있는 `/c/<토큰>` 입력 페이지와
      `공유 → 자료 수합`의 양식 수정·저장·열기/마감·링크 복사·응답표를 구현해 운영 배포
- [x] **J9 Google Sheet 생성 경로** — 오픈클로 `jarvis-collection` 스킬과 `make_collection.py`가
      양식 열에 맞는 새 Sheet를 소유자 전용 비공개로 만들고, 응답은 DB 우선 저장 뒤 추가만 하는 작업기 준비
- [x] **J9 상시 Sheet 반영** — 사용자 승인 범위(명시적으로 만든 수합의 제목·제출 시각·입력값, 양식별
      새 소유자 비공개 Sheet)로 30초 LaunchAgent 로드; 연속 실행 종료 코드 0·전송 대기 0 확인
- [x] **J9 선택지별 Sheet 탭** — 필수 단일선택 항목을 탭 구분으로 지정해 입력 링크·관리 카드는 하나로
      유지하고 학년·부서 등 선택지별 탭 생성; 카드 탭 수와 응답별 반영 탭 표시, 운영 `08` 적용·배포
- [x] **J10 Google Sheet 직접 입력 공유 문서** — 자료 수합 입력 양식과 분리해 실제 Spreadsheet를 만들고
      `공유` 대시보드에 등록·열기·링크 복사·대시보드 제거(Drive 원본 보존)를 구현. 첫 문서 `학습 준비물
      수합`을 1~6학년 탭과 공통 5열로 생성해 운영 `09` 적용·배포
- [x] **J10 명시적 링크 권한 자동화** — 기본 private를 유지하고 `link_viewer`·`link_editor`를 명시한
      생성/변경 명령만 Drive `drive.file` 최소 권한으로 한 파일에 적용·재검증. `/jarvis_google_sheet` 명령 준비
- [~] **J10 첫 링크 편집 적용** — 사용자는 현재 `학습 준비물 수합` 문서의 링크 편집 공개와 이후 명시
      요구 시 자동 적용을 승인. 코드·스킬은 완료했으며 브라우저 Google `drive.file` 최초 승인만 대기
- [x] **J11 공유 탭 단순화** — `/share`를 `오픈클로 참고 양식 / 오픈클로가 만든 공유문서 / 공유 링크
      모음` 세 아코디언으로 축소. 문서 목록만 기본 펼침, 수동 제작·자료 수합 관리 UI는 숨기고 코드는 보존
- [x] **J12 작업 기준 정본화** — 자비스 루트에 모델 공통 `AGENTS.md`, Claude 연결 문서, 자체
      `ROADMAP.md`·`WORKLOG.md`를 두고 기존 로컬 커밋 7개의 작업 이력을 수합. 이후 모든 작업은
      `한 문장 요청 → 오픈클로 처리 → 대시보드 확인·정정`과 업무 절감 여부를 우선 기준으로 기록
- [x] **2026-08-30 중단 복구** — 미추적 상태 줄 컴포넌트를 로컬 Git에 포함하고 목록 상한의
      오름차순 오류를 바로잡아 검사·Docker 재배포·운영 스모크 완료 (`7c64626`)
- [~] **J9 자연어 자동 라우팅** — `수합 시트`가 일반 xlsx로 처리되는 원인은 스킬의 모델 자동 호출 OFF;
      외부 DB·Google Sheet 생성을 모델이 판단하는 범위 확대이므로 별도 사용자 승인 전까지 명시 명령만 사용
- [ ] 첫 실제 자료 수합을 만든 뒤 공유 탭 PC·모바일 눈검사, Sheet 열·권한·첫 응답 반영 확인
- **고도화 계획(정본)**: [docs/JARVIS_UPGRADE_PLAN_20260829.md](docs/JARVIS_UPGRADE_PLAN_20260829.md)
- **최초 점검 근거**: [docs/JARVIS_DASHBOARD_REVIEW_20260829.md](docs/JARVIS_DASHBOARD_REVIEW_20260829.md)

### 네이버 검색 수집 기반 보완 (2026-08-28)

- [x] 네이버 소유확인 태그·Yeti 접근·noindex 부재 확인
- [x] SPA 폴백 HTML을 반환하던 `/robots.txt`를 실제 일반 텍스트 수집 규칙으로 교체
- [x] 공개 메인 한 페이지만 담은 `/sitemap.xml` 추가
- [x] canonical·OG URL·이미지를 `www` 없는 운영 apex로 통일
- [x] 자바스크립트 실행 전에도 읽히는 서비스 정체성·핵심 기능·공개 안내 정적 본문 추가
- [x] 검색 파일·대표 주소·정적 본문을 한꺼번에 지키는 회귀 검사 추가, 의도적 실패 확인
- [x] 운영 이미지 반영 및 Docker Desktop 29.7.2 업데이트·재시작 후 검색 파일·메인 응답 확인
- **다음 작업**: 네이버 서치어드바이저에서 robots 재수집 → 사이트맵 제출 → 메인 URL
  수집 요청을 실행하고, 최소 1일 뒤 수집·색인 리포트를 다시 확인한다.

### 샘링크 스택 이전 후 단축링크 저장 복구 (2026-08-28)

- [x] 새 스택에서 조회·속도 제한 RPC는 200이지만 `POST /short_links`만 404인 런타임 로그 확인
- [x] INSERT/DELETE 트리거가 부르는 `increment_created_short_links(integer)`·
      `increment_deleted_short_links(integer)` 두 함수가 `samlink` 스키마에서 누락된 원인 확정
- [x] 샘링크 원본 저장소에 조건부 복구 마이그레이션 추가, 기존 `public` 환경은 자동 건너뜀
- [x] `service_role`만 실행·`search_path=samlink`·재적용 안전성·생성/삭제 트리거를 전체 ROLLBACK 검증
- [x] 운영 `agit-db` 적용 후 실제 API 저장 200 → 리다이렉트 307 → 소유 쿠키 삭제 200,
      진단 링크·고아 기기 연결 0건 확인
- [x] 통합 DB 백업·실복구 검증 후 옛 `supabase` 컨테이너 14개 제거, 예전 API 주소를 통합 Kong으로 전환
- [x] 선택 관리 컨테이너 4개와 미사용 pooler 제거, compose profile로 재생성 방지
- [x] 이관 뒤 옛 PG15 백엔드 컨테이너가 0개이고 자비스·샘링크가 통합 Kong/DB만 쓰는지 재확인
- [x] 주말 작업 창에 오늘 백업 수동 복구 3/3을 먼저 확인하고 소비자 0인 PG15 이미지·옛 소형 볼륨 정리
- [x] 실패 중인 빈 `literacy` 잔재 백업 LaunchAgent 중지, plist는 날짜가 붙은 비활성 파일로 보존
- [x] 이관 보존 덤프의 7개 스키마 목차 확인 뒤 옛 PG15 bind 데이터 297MB 제거, 기본 Compose 재생성 방지
- [ ] `supabase_default`는 현재 Jarvis Caddy·frontend·classroom-tools 별칭 연결이므로 유지 — 필요하면
      세 컨테이너를 전용 호환 네트워크로 옮긴 뒤 이름까지 정리
- **다음 작업**: 로컬 스택 정리는 완료. 8월 30일 백업 2일차와 9월 4일까지 7일 관측을 계속한다.

### 주간 맞춤법 검수를 관리자 대시보드에서 (2026-08-28 시작)

> **왜** — 지금은 맥미니에서 손으로 스크립트를 돌리거나 LaunchAgent 로 자동으로 도는 두 길뿐이다.
> 자동으로 돌리자니 학생 유래 표현을 외부로 보내는 일을 사람이 안 보고 넘기게 되고, 손으로 돌리자니
> 맥미니에 붙어야 한다. **관리자가 화면에서 쌓인 양을 보고 직접 누르는 방식**이면 둘 다 풀린다.

**정한 것 (A안, 2026-08-28)**

- 수집 결과를 따로 저장하지 않는다. 관리자 화면이 **지난 검수 이후 쌓인 원자료 수만** 읽어 보여 주고,
  관리자가 `AI 검수 돌리기` 를 누르면 그때 거르기·AI·저장을 한 번에 한다.
- 그래서 **스키마를 바꾸지 않는다.** `spelling_weekly_review_items` 는 AI 결과 칸이 모두 `NOT NULL` 이라
  "수집만 한 후보" 를 담을 수 없는데(`20261179` 52~57줄), A안은 그럴 일이 없다.
- **LaunchAgent 는 필요 없어진다.** macOS 로그인·재부팅 의존이 사라진다. plist 와 Node 스크립트는
  되돌림 경로로 남긴다.
- 브라우저는 `start/finish_spelling_weekly_review_v1` 을 직접 못 부른다(`service_role` 에만 열려 있다,
  `20261179` 520~525줄). 그래서 **엣지 함수 하나가 반드시 필요하다.**

**단계**

- [x] 1. 거르는 계산을 두 경로가 함께 쓰는 원본으로 분리 —
      `supabase/functions/spelling-weekly-review/reviewCore.js`. sha256 은 플랫폼마다 달라 부르는 쪽이
      넣어 준다(Deno 의 Web Crypto 는 비동기라 안에서 쓰면 계산 전체가 비동기가 된다).
      Node 스크립트는 이 원본을 가져다 쓰고 되돌림 경로로 남는다. 검사 538개 통과.
- [x] 2. 엣지 함수 `spelling-weekly-review` — 관리자 확인 → `service_role` 로 start → 거르기 → OpenAI
      → finish. OpenAI 는 `vibe-ai` 를 거치지 않고 직접 부른다(지금 스크립트도 그렇다. 구조화 출력에
      `max_tokens: 5000` 이 필요해 `vibe-ai` 의 1000 토큰 상한에 맞지 않는다).
- [x] 3. 관리자 `맞춤법 승격` 화면에 쌓인 양·버튼·진행 상태 추가

- [x] 배포 완료(2026-08-28, `a217284`). 마이그레이션 적용, 엣지 함수 기동 확인(인증 없이 401).
      맥미니가 자기 공개 도메인으로 못 나가서(헤어핀 NAT) 카탈로그는 호스트 경유 내부 주소로 받는다 —
      `SPELLING_CATALOG_ORIGIN` 을 `secrets.agit.env` 에 더했다(git 밖 변경, WORKLOG 참조).
- [x] 4. 실행 시간 — 재 보니 엣지 함수 작업자 제한이 **60초**였고(`volumes/functions/main/index.ts`),
      후보 146건은 12개씩 13번이라 넘겼다. 30초짜리 덩어리로 끊고 배치마다 캐시에 적립해 이어 돌게
      바꿨다. 화면이 스스로 이어 부르고 진행 막대를 보여 준다.
- [x] 5. AI 에 보내기 전에 원자료를 골라낸다 — 학생 검색·AI 검사 목록을 보고 빼거나 직접 등록한다.
      뺀 것은 `spelling_common_reviews` 에 `rejected` 로 남아 다시 안 올라오고, 되돌릴 수 있다.
      게시된 후보는 `published_locked` 로 보호한다.
- [x] 6. 검수 기준을 `틀렸는가` 에서 **`여러 학급이 되풀이해 틀릴 규칙인가`** 로 바꿨다.
      첫 회차 146건이 `반영 권장` 83 으로 몰리고 그 안에 규칙과 한 아이의 오타가 섞인 것을 보고 고쳤다.
      지시문을 원본 하나로 모으고 `REVIEW_VERSION` 을 `weekly-v2` 로 올렸다.
- [x] 7. 끝난 회차를 **다시 검수**할 수 있게 했다(`admin_restart_spelling_weekly_review_v1`).
      관리자가 이미 게시하거나 뺀 결정은 그대로 남는다.

**지금 상태 (2026-08-28 저녁)**

- 첫 회차가 **옛 기준(v1)으로** 끝나 있다 — `반영 권장` 83 · `주의 검토` 5 · `제외 권장` 58.
  판정 캐시 146건이 모두 `weekly-v1` 이고 회차는 07:34 에 끝났는데, 새 기준 배포는 07:44 다.
- **새 기준(v2)은 배포됐지만 아직 안 돌았다.** `다시 검수하기` → `AI 검수 돌리기` 를 눌러야
  새 기준 결과를 볼 수 있다(AI 를 다시 부르므로 비용이 한 번 더 든다).

- **다음 작업**: 새 기준으로 다시 검수해 `반영 권장` 이 얼마나 줄었는지 본다. 그래도 애매하면
  지시문을 더 조인다 — 이제 몇 번이든 다시 돌릴 수 있다.
- **남은 확인**: 관리자 화면을 실계정에서 눈으로 본 적이 없다(로그인이 필요해 이 세션에서는 못 했다).
  진행 막대·목록·단추가 좁은 칸에서 어떻게 보이는지 한 번 봐야 한다.


### 연구소 `질문에 답 나누기` 활동 (2026-08-28 계획 확정)

> 만들 곳은 대부분 별도 저장소(`writing-helper`)다. 이 저장소가 맡는 것은 참고함 연결뿐이다.

**왜** — **질문에 답을 써 보는 활동이 없다.** 지금 질문에 답을 쓰는 곳은 `글 개요 짜기` 하나뿐인데,
거기서는 답이 개요 칸을 채우는 재료로만 쓰이고 **답을 서로 보거나 나누는 단계가 없다**. 결과 화면에서
글쓰기로 가는 버튼도 없다(마지막이 "이 개요를 보면서 글을 완성해봐요" 문장 한 줄뿐이다).

**자리** — `좋은 질문 고르기`의 **뒤가 아니라 옆**이다. 둘 다 `질문 만들기`를 먹고 사는 형제 활동이다.
`좋은 질문 고르기`를 꼭 먼저 해야 하는 것이 아니다. 했다면 그 결과를 가져다 쓸 수 있을 뿐이다.

```
질문 만들기 ─┬─→ 좋은 질문 고르기 ─┐
             │                      ├─→ 질문에 답 나누기 → 글쓰기
             └──────────────────────┘
```

**정한 것 (2026-08-28)**

- **질문 출처는 두 가지**, 방 만들 때 교사가 고른다.
  ① `질문 만들기` 방에서 학생 질문을 받아 **교사가 체크로 정리** — `좋은 질문 고르기`와 똑같은 방식
  ② 이미 한 `좋은 질문 고르기` 방의 **득표순 결과** 가져오기(표 못 받은 질문은 안 딸려 온다)
- 방을 둘로 쪼개지 않고 **활동 하나**로 만든다 — `한줄모아`처럼 `쓰기 단계 → 공유 보드` 한 몸이다.
- 학생은 교사가 고른 질문 **전부**에 답한다(하나만 골라 답하기 아님). 같은 질문에 답이 모여야 비교가 된다.
- `크게 보기`는 **질문 하나 + 여러 아이 답**을 펼친다. 사람별이 아니라 질문별이다.
- 글쓰기 참고함에는 **내 답만** 담는다. 친구 답 베끼기 문제를 만들지 않는다.

**단계**

- [ ] 1. 활동 뼈대와 학생 답쓰기 — `question_answer_share` 정의, 교사 설정에 질문 출처 두 가지
- [ ] 2. 공유 보드와 크게 보기 — `한줄모아` 보드와 `question-board-fullscreen.tsx`를 질문+답용으로 넓힌다
- [ ] 3. 아지트 참고함 연결 — 결과 종류 `question_answers` 추가(마이그레이션 1개 + 활동↔결과 짝 5곳)
- [ ] 4. 결과 화면에서 아지트 과제로 가는 버튼 — 지금 없는 것이므로 `개요 짜기` 결과에도 같이 단다
- [ ] 5. 답이 많을 때 읽히게 하기 — **모양은 아직 정하지 않았다**(아래 참조). 1~4 단계만으로 활동은
      성립하므로, 실제로 한 반 답을 화면에 띄워 본 뒤에 정한다.

**5단계의 모양 — 정하지 않음 (2026-08-28)**

- 처음에는 `AI 로 비슷한 답끼리 묶기` 로 잡았으나 실효성이 의심스럽다. 한 질문에 답이 10~25개뿐이라
  묶기가 값어치를 갖는 규모가 아니고, 초등학생의 짧은 답에서는 AI 가 생각의 차이가 아니라 말투의 차이를
  잡기 쉽다. 무엇보다 **수업에서 이야기를 여는 것은 비슷한 답이 아니라 다른 답이다** — 비슷한 것끼리
  모으면 `다들 비슷하네` 로 끝난다. 틀린 묶음은 교실에서 즉시 들통나 되돌릴 시간도 없다.
- 후보 ① **AI 가 묶지 말고 고르게 한다** — `서로 가장 다른 답 3~4개`. 출력이 번호 몇 개뿐이라 잘리거나
  형식이 깨질 일이 없고, 교사가 눈으로 즉시 검증하며 틀려도 다른 걸 고르면 그만이다. `크게 보기` 로
  서로 다른 답 3장을 나란히 띄우는 것이 25장을 넘기는 것보다 수업에 맞는다.
- 후보 ② **똑같은 답 접기 (AI 없이)** — 공백·문장부호만 털어 완전 일치를 접고 `3명이 같은 답` 배지를 단다.
  조사 처리도 외부 호출도 비용도 실패도 없다. 화면이 줄어드는 실제 효과는 대부분 여기서 나온다.
- 후보 ③ **학생이 스스로 고르기** — 답을 낸 뒤 `나와 비슷한 생각` 을 고르게 한다. 그 자체가 활동이 된다.
- **판단 시점**: 1~4 단계를 쓴 뒤. 지금은 아이들 답이 몇 줄짜리로 나올지조차 모른다.

**AI 를 쓰기로 한다면 — 막는 것은 없다 (2026-08-28 확인)**

- 배관은 이미 있다. 연구소 `callAgitAi` → 아지트 `vibe-ai`(`LAB_GENERAL` 이미 허용) → OpenAI `gpt-4o-mini`.
- 학생 글을 OpenAI 로 보내는 것은 **이미 허용·고지된 일이다**. 개인정보처리방침 제5조 처리위탁 표에
  수탁자 `OpenAI, L.L.C.` 와 업무 내용(글쓰기 피드백·평어 초안·맞춤법 자료 초안·댓글 표현 확인)이 적혀 있다.
  같은 조항의 `교사가 실행하고 교사가 확인하는 보조 기능` 원칙과도 맞는다 — 교사가 누를 때 한 번 돌고
  묶음은 제안이며 교사가 고친다.
- **맞춤법 주간 검수의 미승인 항목과 혼동하지 않는다.** 그쪽은 개인정보 판단이 아니라 *사람 없이 매주 도는
  배치를 처음 켜는 운영 승인*이다(주 200건·비용 발생·LaunchAgent 등록). 답 묶기는 교사가 자기 반 것을
  그 자리에서 한 번 돌리는 것이라 성격이 다르다.
- 붙일 때 **개인정보처리방침 위탁 표에 업무 한 줄을 보탠다**(`PrivacyPolicy.jsx` 제5조). 업무 내용이
  기능 단위로 적혀 있어 그대로 두면 고지와 실제가 어긋난다.
- **AI 에게 답을 다시 쓰게 하지 않는다.** 응답 상한이 1000 토큰이라(`vibe-ai/index.ts:470`)
  답을 되돌려받는 방식은 잘린다 — 연구소가 AI 맞춤법 자동 교정을 지운 이유가 정확히 그 잘림이었다(08-27).
  답마다 번호를 매겨 보내고 `{번호 → 묶음}` 과 묶음 이름만 받으면 답 75개라도 400 토큰 안쪽이다.
- 1000 토큰 상한은 `tests/vibeAiEdgeFunction.test.mjs` 가 못 박고 있다(`일반 AI는 1000토큰이다`).
  "출력이 기니 상한을 늘리자" 는 길은 그 검사부터 고쳐야 하므로, 번호만 받는 방식이 우회가 아니라 정공법이다.
- 묶음은 **제안**이고 교사가 고칠 수 있어야 한다. 아이 답을 기계가 잘못 묶으면 수업에서 바로 티가 난다.
- 안 쓴다면 대안은 **학생이 스스로 고르기**다(답을 낸 뒤 `나와 비슷한 생각` 고르기). 외부 전송이 없고
  그 자체가 활동이 된다. 낱말로 자동 분류하는 길은 한국어 조사 처리가 필요한데 저장소에 형태소 분석 자산이
  없어(맞춤법 카탈로그는 낱말 목록이지 분석기가 아니다) 값어치가 없다.

**베낄 자리** — 교사 설정의 질문 출처는 **서버 쪽이 이미 다 있다**. `getQuestionGeneratorSourceRooms`
(질문 만들기 방 + 그 방 질문들)와 `getQuestionVotingSourceRooms`(고르기 방 + 득표순, 표 0인 질문 제외)를
그대로 부르면 된다. 화면도 전자는 `QuestionVotingSetup`, 후자는 `OutlineBuilderSetup`에 이미 있으니
두 개를 나란히 놓는 일이다. 여러 칸 답쓰기는 `hanja_writing`의 `contents`, 보드·좋아요는 `one_line_share`,
크게 보기는 `components/question-board-fullscreen.tsx`(질문 수에 따른 글자 크기 조절까지 이미 있다).
아지트 쪽 마이그레이션은 `20261105`(한줄모아 붙일 때)를 그대로 따른다.

**미리 아는 걸림돌** — 활동 종류와 결과 종류의 짝이 SQL `CASE` 4곳 + JS 5곳, **아홉 군데**에 흩어져 있다
(`labReferenceApi.js`, `LabReferenceSource.jsx`, `LabResultsTool.jsx`, `MissionLabSourcesModal.jsx`,
`LabActivitiesPage.jsx`). 3단계에서 한 곳을 놓치면 목록엔 보이는데 참고함엔 안 뜨는 식으로 조용히 어긋난다.
붙이면서 원본을 하나로 모은다. 연구소 쪽도 활동별 분기가 2,200줄짜리 두 파일에 박혀 있어 얇은 껍데기가
아니다(`docs/activity-platform.md`의 목표와 실제가 다르다).

- **다음 작업**: 1단계부터 들어간다. 교사 설정 화면(질문 출처 두 가지 고르기)이 첫 조각이다.

### 쓰지 않는 RPC 정리와 재발 방지 (2026-08-26~27)

- [x] 학생이 자기 댓글을 스스로 승인하던 `record_comment_ai_review` 제거 — 운영 DB 롤백 트랜잭션에서 재현 확인
- [x] 새 판으로 옮긴 뒤 남아 있던 구형 RPC 정리 (클라이언트 공개 219개 → 211개)
- [x] 연구소를 `get_student_spelling_entries_v2` 로 옮겨 배포한 뒤 `_v1` 제거
- [x] `tests/migrationVersionCleanup.test.mjs` — 마이그레이션 파일만 보고 옛 판 처리를 강제(도커 없이 어디서나)
- [x] `npm run check:rpc-surface` — 운영 DB·이 저장소·다른 앱을 함께 봐 이름이 다른 교체까지 확인
- [x] `ops/rpc-surface-allowlist.json` — 남기려면 이유와 언제 지울 수 있는지를 적어야 통과
- [x] 두 검사를 `test:all`·푸시 전 훅·`test:security` 에 연결하고 AGENTS.md 절대 규칙에 기록
- **다음 작업**: 허용 목록 6개 중 `get_spelling_learning_workspace_v2`
  (`_v3` 가 스스로 계산하게 바꾼 뒤), `get_reading_marathon_snapshot`(DB 함수 셋을 `_v2` 로 옮긴 뒤),
  `record_system_daily_metric_v1`(지표 스크립트 되돌림 경로 정리 뒤)을 순서대로 지운다.

### 우리반 글 전시관·학급 문집 (2026-08-26 준비 → 2026-08-30 방향 확정)

- [x] 교사 상단 `글쓰기 연구소` 바로 다음에 내부 탭 `우리반 아지트 (beta)` 추가
- [x] 방향 확정 전에는 선택한 학급의 공간을 준비 중이라는 상단 배너만 표시
- [x] 준비 단계에서는 DB 조회·저장·학생 화면 변경이 전혀 없도록 빈 셸로 구성
- [x] 화면을 지연 로딩하고 개발 전용 UI 프리뷰와 메뉴 순서·무조회 회귀 검사 추가
- [x] 범위를 `2.5D 우리반 글 전시관 + 학급 문집 제작·PDF 내보내기`로 확정
- [x] 교사·학급별 여러 전시와 전시별 외부 읽기 전용 공유 링크를 계획에 추가
- [x] 관리자 내부 → 지정 학급 시범 → 모든 교사 기본 OFF 공개의 단계적 공개 계약 확정
- [x] 상세 범위·권한·구현 단계·첫 공개 합격선을 [CLASS_AGIT_PLAN.md](CLASS_AGIT_PLAN.md)에 분리
- [x] 교사 상단 메뉴를 글쓰기 → 학급 관리 → 확장 기능 → 설정 흐름으로 재정렬하고 구역 경계·BETA 배지 적용
- [x] 준비 중 화면에 여러 전시·2.5D 전시관·학급 문집·외부 읽기 공유의 업데이트 개요 표시
- [ ] 인증 교사 PC·모바일 실화면에서 상단 메뉴 폭과 준비 화면 배치 확인
- [ ] Phase 0 — `class-agit` 매니페스트·관리자 공개 단계·학급별 ON/OFF·학생 조건부 카드 계약 구현
- **다음 작업**: 상세 계획 Phase 0의 관리자 내부 셸과 실패 회귀 검사부터 구현한다. 일반 교사·학생에게는
  아직 공개하지 않는다.

### 학생 댓글 등록 확인·AI 전역 3건 대기열 (2026-08-26)

- [x] 댓글 입력의 첫 Enter·버튼은 저장하지 않고 `내가 쓴 댓글을 한 번 읽어 보세요` 확인 단계로 이동
- [x] `고쳐 쓰기 / 확인하고 등록` 공용 편집기를 공통 적용하고 두 번째 Enter로도 최종 등록
- [x] DB 슬롯 행 3개로 전역 최대 3건만 외부 AI를 부르고, 2분 리스·2회 시도·15초 후 한 번 재시도를 서버에서 관리
- [x] 반복·짧은 감탄·무작위 문자는 로컬 규칙으로 먼저 걸러 외부 AI 호출·토큰을 절약
- [x] AI 승인 댓글의 포인트 지급을 종료하고, 승인된 친구 글 댓글은 독자 칭호 성장으로만 인정
- [x] 과거 댓글 보상 9,026건·45,130P와 학생 잔액은 유지하고 포인트 내역에 `이전 기록`으로 표시
- [x] 댓글 승인 RPC·공용 포인트 엔진·구형 보상 함수·활성 화면 유형에서 신규 댓글 포인트 경로 제거
- [x] 과거 댓글 보상 9,026건의 사유에서 내부 PostID를 일괄 제거하고 `친구 댓글 보상 · 이전 기록`으로 통일
- [x] 관리자 `서비스 현황`에 대기·처리 중(3건 상한)·교사 확인 필요·오늘 완료·최장 대기 지표 추가
- [x] 교사 댓글 도움말에 학생 재확인·순차 검사·2회 실패 시 교사 정리·독자 칭호 규칙 안내
- [x] 전역 3건 제한 변이 검사, 실제 DB 전체 ROLLBACK, 전체 523/523·아키텍처 120/120·보안 묶음·린트·프로덕션 빌드 통과
- [x] 운영 `agit-db`에 `20261180` 적용, 마이그레이션 222/222·대기 0건 확인
- [x] 운영 `agit-db`에 댓글 포인트 종료 `20261181` 적용, 마이그레이션 223/223 및 과거 원장 보존 확인
- [x] 운영 `agit-db`에 과거 댓글 사유 정리 `20261182` 적용, 마이그레이션 224/224 및 잔액·글 연결 보존 확인
- [x] GitHub Actions `32970658489` 배포에서 앱 재시작·`vibe-ai` Edge 함수 동기화·운영 검증 통과
- [ ] 인증 학생·교사·관리자 실화면에서 댓글·대기열 흐름 확인
- [ ] 한 학급 동시 댓글 시나리오에서 실제 도커 메모리 최고치·대기 시간·AI 실패율 측정
- **다음 작업**: 시험 학급에서 댓글 입력→확인→대기→승인/막힘 흐름과 포인트 무변경·독자 칭호·
  관리자 지표를 대조한다.

### 관리자 첫 화면 운영 요약·교사 명단 가시성 (2026-08-26)

- [x] 첫 화면 맨 위에 한국 시간 기준 오늘 접속 교사·학생과 실제 제출 완료 글 수를 3개 카드로 표시
- [x] `현황 > 사용량` 상단에 있던 가입 교사·활동 교사·학급·학생·최근 글 지표를 관리자 첫 화면으로 이동
- [x] 조치 대상·이용 현황·시스템 상태를 세 구역으로 모으고 각 항목에서 실제 처리 화면으로 바로 이동
- [x] 선생님 첫 탭을 `전체 명단`으로 명확히 하고 25명 단위 현재 범위·전체 수·페이지 선택 제공
- [x] 장기 미접속 명단을 68vh 고정 영역과 고정 머리글 안에서 전체 스크롤 가능하게 정리
- [x] 관리자 첫 화면·탭·배지에서 `정리 후보/정리 대상`을 숨기고 장기 미접속만 운영 확인 대상으로 유지
- [x] 장기 미접속 기준을 선택형 30/60/90/180일에서 **90일(3개월) 고정**으로 바꾸고, 학급·학생 보유 여부와
      무관하게 모든 가입 교사의 마지막 접속일로 판정
- [x] 마지막 로그인 기준 `90~364일 장기 미접속`과 `365일 이상 휴면계정`을 한 목록의 필터·행 배지로 구분
- [x] 휴면은 조회 시 계산하는 논리 분류로만 두고 데이터 이동·자동 삭제 없이 로그인 시 자동 해제되도록 확정
- [x] 운영 DB 합계 대조에서 가입·승인 교사 343명, 90~364일 장기 미접속 34명, 365일 이상 휴면계정
      0명 확인 — 개인 식별 정보 미출력
- [x] 이용약관·개인정보처리방침에 같은 분류 기준과 비이관·비삭제·로그인 자동 해제를 반영하고
      2026-08-29 개정, 2026-09-05 시행으로 명시
- [x] 어느 관리자 탭·긴 목록에서도 보이는 우측 하단 `관리자 홈` 고정 컴포넌트 추가 — 첫 화면 이동과
      검색·페이지·승인대기 분류 초기화·맨 위 이동을 한 동작으로 처리
- [x] 모바일 안전영역·키보드 초점·동작 줄이기·모달 아래 계층을 반영하고 고정 위치 변이 검사 통과
- [x] 전용·전체 회귀 515/515, 린트 오류 0건, 프로덕션 빌드와 기본 필터 변이 검사 통과
- [x] 2026-08-29 운영 재배포, Docker 내부 전체 회귀 581/581·HTTP 200·서빙 번들의 휴면 분류·
      개정 시행일·자동 삭제 금지·고정 관리자 홈 버튼 확인
- [ ] 2026-09-05 시행 전 앱 공지사항에 약관·개인정보처리방침 변경 안내 게시
- [ ] 인증된 관리자 실화면에서 첫 요약 카드와 343명 페이지 이동, 장기 미접속 34명·휴면 0명의
      필터·배지·스크롤·검색 확인
- **다음 작업**: 시행 전 개정 공지를 게시하고, 브라우저가 연결되면 고정 홈 버튼을 포함한 관리자 화면과
  약관 모달을 좁은 화면까지 눈으로 대조한다.

### 교사 활용 안내서와 탭 도움말 통합 (2026-08-26)

- [x] 기존 교사 탭 상세 도움말 20개를 그대로 사용하는 공용 렌더러와 화면 이동 레지스트리 구성
- [x] `처음 시작하기`부터 `학기 마무리와 자료 관리`까지 목적 중심 큰 흐름 8개·28단계 작성
- [x] 활용 안내서 단계에서 같은 탭 도움말을 펼쳐 보고, 탭 도움말에서도 관련 전체 흐름을 열도록 양방향 연결
- [x] 안내서에서 설정 하위 메뉴·학급운영도구·놀이터 모듈을 포함한 실제 화면으로 바로 이동
- [x] 전체 화면·반응형·키보드 초점 가두기·Esc 닫기·초점 복귀와 지연 로딩 적용
- [x] 도움말 참조 누락·잘못된 하위 화면·상세 내용 복사를 막는 전용 검사와 유지보수 문서 추가
- [x] 전용 14/14·전체 회귀·아키텍처·린트 오류 0건·프로덕션 빌드 통과, 잘못된 참조 변이 검사 확인
- [ ] 인증된 교사 실화면에서 PC·모바일의 안내서 열기→상세 펼치기→해당 화면 이동 최종 확인
- **다음 작업**: 브라우저 세션이 가능한 환경에서 대표 흐름 3개와 모바일 헤더·스크롤·초점 이동을 확인한다.

### 모든 학급 맞춤법 데이터 주간 AI 검수 (2026-08-26)

- [x] 학생 AI 검사·학생 검색·교사가 승인한 학급 자료를 지난 완료 시점 이후분만 주 1회 모으는 서버 원장 추가
- [x] 기본 500개와 현재 공통 자료의 1,254개 별칭을 로컬 코드로 정확 비교해 일치 후보는 AI 호출 전에 제외
- [x] 새 후보에는 유사 자료 최대 3개만 붙이고 요청당 12개·주간 최대 200개 구조화 출력으로 AI 검수
- [x] 같은 표현·교정의 이전 AI 결과를 재사용하는 캐시와 `반영 권장 / 주의 검토 / 제외 권장` 관리자 화면 추가
- [x] 관리자 선택 뒤에만 모든 학급 공통 자료로 게시하고 원장·캐시에 학생 글·학생·학급 식별자를 저장하지 않음
- [x] 교사 `맞춤법 배움 데이터` 도움말에 주 1회 수집·중복 제거·AI 검수·관리자 정선·공통 반영과 학급별 추가 흐름 안내
- [x] 운영 `agit-db`에 `20261179` 적용 후 221/221·대기 0, 적용 전후 권한·게시 ROLLBACK 스모크 통과
- [x] 화요일 05:10 LaunchAgent 정의와 설치 파일 준비(`week_start`는 그 주 월요일로 기록한다)
- [ ] 학생 유래의 짧은 표현·교정을 외부 OpenAI로 보내는 첫 실행과 LaunchAgent 활성화에 대한 명시 승인
- [ ] 첫 실행 결과 수·기존 자료 제외 수·AI 새 검수/캐시 수와 인증된 관리자 실화면 확인

> **현재 상태(2026-08-28 확인)**: 아직 **한 번도 실행되지 않았다.** `spelling_weekly_review_runs` 0건,
> `launchctl`에 등록 없음, plist는 저장소 `ops/launchd/`에만 있고 `~/Library/LaunchAgents/`에는 없다.
> 그래서 관리자 `맞춤법 승격 > 이번 주 검수 결과`는 **항상 비어 있고** 공통 자료도 0건이다.
> 다만 **원자료는 계속 쌓이고 있고 새지 않는다** — 수집 기준이 고정 7일이 아니라 마지막 성공 시점
> (`max(finished_at)`)이라 첫 실행은 지금까지 쌓인 전부를 대상으로 한다
> (AI 검사 77건·학생 검색 77건·학급 자료 4건, 2026-08-28 기준).

- **다음 작업**(승인 후 이 순서로):
  1. `npm run spelling:weekly:check` — 외부 AI 호출 없이 준비 상태만 확인한다.
  2. `npm run spelling:weekly:run` — 첫 실행. **실제 OpenAI 호출과 비용이 발생한다.**
  3. 관리자 `맞춤법 승격`에서 후보 수와 `반영 권장 / 주의 검토 / 제외 권장` 분류를 실화면으로 확인한다.
  4. 성공을 확인한 뒤에만 `cp ops/launchd/com.agit.weekly-spelling-review.plist ~/Library/LaunchAgents/`
     후 `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.agit.weekly-spelling-review.plist`
     로 등록한다. `RunAtLoad`가 `false`이므로 등록해도 다음 화요일 05:10부터 돈다.

### 맞춤법 공통 자료 관리 화면 정리 (2026-08-26)

- [x] 후보 두 종류와 공통 자료를 모두 세로 나열하던 화면을 `검토할 후보 / 전체 공통 자료` 두 주 화면으로 분리
- [x] 상단에 검토 대기·전체 적용 중·적용 중지 현황을 모으고 후보 출처와 공통 자료 상태는 하위 필터로 정리
- [x] 학급·횟수 입력 때마다 조회하지 않고 `기준 적용`을 눌렀을 때만 갱신하도록 변경
- [x] 목록 높이를 화면의 68%·최대 620px로 제한하고 데스크톱 행·태블릿 2열·모바일 1열 반응형 배치 적용
- [x] 검토를 열면 편집 영역으로 이동하고 주 화면·후보 출처를 바꿀 때 오래된 검토 내용을 닫도록 보강
- [x] 전용 19/19·린트 오류 0건·프로덕션 빌드 통과, 목록 높이 제거 시 신규 검사 실패·원복 후 통과 확인
- [x] 커밋 `b06c3a1` 운영 배포 후 앱 컨테이너 running·내부 HTTP 200·Caddy 실도메인 경로 200 확인
- [ ] 인증된 관리자 실화면에서 후보·공통 탭, 620px 이하 버튼 배치와 긴 표현 말줄임 확인
- **다음 작업**: 후보가 있는 관리자 실계정에서 AI/검색 전환→내용 검토→검토 닫기와 공통 자료 상태 필터를 확인한다.

### AI 검사·학생 검색 기반 맞춤법 공통 자료 승격 (2026-08-26)

- [x] AI 맞춤법 검사 익명 집계와 학생 직접 검색 집계를 한 관리자 검토 화면에서 후보로 제공
- [x] 관리자가 바른 표현·설명·예문을 확인해 게시하면 재배포 없이 모든 학급에 적용하는 공통 자료 계층 추가
- [x] 공통 자료 수정·전체 적용 중지·재적용과 후보 보류를 삭제 없는 가역적 흐름으로 구현
- [x] 교사는 공통 자료를 읽기 전용으로 보고 기존처럼 자기 학급 전용 자료를 추가하도록 역할 분리
- [x] 학생 글쓰기 밑줄·한 줄 입력·맞춤법 수첩에 공통+학급 자료를 최대 100개로 합쳐 적용하고 공통 자료 우선 중복 제거
- [x] 후보·결정 원장에 학생·학급·글 원문을 저장하지 않고 관리자·담당 교사·학생 본인 범위를 RPC에서 재검증
- [x] 전용 45/45·아키텍처 120/120·보안 전체·마이그레이션 ROLLBACK·맞춤법 정확도/성능·린트·빌드 통과
- [x] 운영 `agit-db` 마이그레이션 적용 후 220/220·대기 0과 역할별 ROLLBACK 스모크 확인
- [x] 커밋 `3b6b2d8` 운영 배포 후 앱 컨테이너 running·내부 HTTP 200·Caddy 실도메인 경로 200 확인
- [ ] 관리자·교사·학생 인증 실화면 확인
- **다음 작업**: 운영 적용 뒤 관리자에서 실제 AI/검색 후보를 검토하고, 공통 자료 1개를 시험 게시해 교사 읽기 전용
  표시와 학생 글쓰기 밑줄 반영을 확인한 후 필요하면 중지한다.

### 오늘 작업 전체 코드 감사 (2026-08-26)

- [x] 기준점 `3f6fec9` 이후 23개 커밋·62개 파일의 호출처·미사용 파일·고아 CSS·디버그 잔여·비밀 노출 점검
- [x] 급식 학교 검색 선택 해제 오류와 학급 전환 뒤 늦은 작업공간 응답 덮어쓰기 방지
- [x] 댓글 `더 보기` 중 검색·탭·학급 전환 시 이전 조건의 다음 페이지 혼입 방지
- [x] 회귀 493/493·아키텍처 120/120·보안 전체·운영 SQL 스모크·린트 오류 0건·프로덕션 빌드 통과
- [x] 운영 마이그레이션 219/219·대기 0, 건강검진 마지막 종료 코드 0·현재 열린 서버 알림 0건 확인
- [x] 선생님 과제·학생 댓글·학급운영도구·급식 도구 도움말에 오늘 변경된 실제 사용 흐름 통합
- [ ] 인증된 교사 실계정에서 급식·댓글·선택 미션 카드와 확대 화면 최종 브라우저 확인
- **다음 작업**: `현황 범위`에서 특정 미션을 선택한 뒤 24명 상태 카드가 보이는지 확인하고, 기본 전체 화면도
  카드로 바꿀지는 여러 미션 합계 표현 방식을 정한 뒤 별도 결정한다.

### 학생 댓글 관리 수동 새로고침 (2026-08-26)

- [x] 현재 처리 탭·기간·검색어를 유지한 채 댓글 목록을 다시 불러오는 공용 새로고침 버튼 추가
- [x] 수동 갱신 중 기존 목록을 유지하고 버튼에 `갱신 중...` 상태 표시
- [x] 검색 자동 조회와 수동 갱신이 겹칠 때 예약 요청을 취소하고 늦은 이전 응답을 무시하도록 보강
- [x] `더 보기` 응답 대기 중 검색·탭·학급이 바뀌면 이전 조건의 다음 페이지를 새 목록에 섞지 않도록 보강
- [x] 데스크톱 검색창 옆 배치와 420px 이하 세로 배치 회귀 검사 추가
- [ ] 교사 실계정에서 검색 입력 직후 새로고침·연속 필터 전환·모바일 버튼 배치 확인
- **다음 작업**: 댓글이 있는 교사 실계정에서 현재 조건 유지와 갱신 중 목록 유지가 자연스러운지 확인한다.

### 학급운영도구 `얘들아, 밥 먹자!` (2026-08-26 운영 배포 완료)

- [x] 교사 가입·프로필 학교 검색을 브라우저 직접 나이스 호출에서 서버 함수 호출로 교체하고 교육청·학교 코드를 함께 저장
- [x] 가입 학교 자동 연동, 현재 학급만 변경, 교사 기본 학교 변경·복귀 흐름 구현
- [x] 날짜별 급식 메뉴·알레르기 표시와 학생 정보가 빠진 교실용 전체화면 급식판 구현
- [x] 현재 학급 학생 최대 100명 자동 연결, 필요한 학생만 최대 300자의 선택 비고를 입력·삭제하는 담당 교사 RPC 구현
- [x] 학생별 구조화된 알레르기·건강 항목과 처리 확인 절차를 제거하고 비고에 민감한 건강정보 입력 금지 안내 추가
- [x] 나이스 키 서버 전용 환경변수, 6시간 공개 급식 캐시, 요청 상한·시간 제한·오래된 캐시 후퇴·운영 함수 허용 목록 적용
- [x] 개인정보처리방침·교사 도움말·성능 매니페스트·정적 보안/전용 회귀·배포 동기화 검사 갱신
- [x] 전용 11/11·아키텍처 116/116·보안 정적 57/57·배포 5/5·Vite 프로덕션 빌드 통과
- [x] 변경된 비고 계약으로 맥미니 실제 `agit-db`에서 `20261175` 전체 마이그레이션·권한 스모크 ROLLBACK 재검증
- [x] 운영 `agit-db`에 `20261175` 적용, Edge 함수 동기화·무인증 401·시크릿 주입 확인
- [x] GitHub Actions `32924400525` 배포 후 앱·Edge 컨테이너 실행, 앱 로컬·실도메인 HTTP 200 확인
- [x] 기존 교사의 학교명이 나이스 검색 결과와 정확히 하나만 일치하면 도구 최초 진입 시 기본 학교 코드를 자동 연결하고, 중복·불일치 때는 추측하지 않고 학교 설정 검색어로 이어지는 보완 흐름 구현
- [x] `우리 반 비고` 명단을 기본 접힘 상태에서 접근 가능한 버튼으로 펼치고 다시 접을 수 있게 구현
- [x] 학생 비고 입력 모달을 440px 전용 폭·3줄 입력·공용 닫기 버튼의 간결한 구성으로 축소
- [x] 학생 행의 `비고 입력` 표시를 흰 배경과 모바일에서도 보이는 연녹색 버튼형 표시로 개선
- [x] 전체화면 급식판 우측 상단의 임시 문자 닫기 버튼을 공용 `ModalCloseButton`의 어두운 배경용 SVG 버튼으로 교체
- [x] 전체화면 급식판의 음식명과 알레르기 안내 글꼴·행간·색 대비를 키워 교실 프로젝터 가독성 개선
- [x] 전체화면 급식 메뉴 카드의 최대 폭·최소 높이와 제목·음식명·알레르기 글꼴을 함께 키워 화면 활용도 개선
- [x] 학교 검색 선택 해제 시 `null`을 안전하게 처리하고 학급 전환 뒤 이전 작업공간 응답을 폐기하며 학교 설정 닫기도 공용 버튼으로 통일
- [x] 전체화면 급식판을 3열 고정으로 정리하고 화면 높이에 맞춰 글자·여백을 조절해 한 화면에 담기게 수정
- [x] 전체화면 급식판에 글자 크기 4단계·2열/3열 선택과 브라우저 자동 저장 추가
- [x] 전체화면 급식 카드 최대 폭을 1720px로 넓히고 그리드 높이를 채우며, 2열·큰 글자 넘침도 첫 줄부터 안전하게 스크롤되도록 보완
- [x] 학교를 못 찾거나 연결한 학교가 풀리던 결함 4건 수정(자동 연결 재시도·검색 100건 조회·정확 일치 우선·요청 순번 가드 공유)
- [x] 배포 뒤 실제 나이스로 확인 — `중앙초등학교` 88건 중 정확 일치가 `pSize=20`에서 0건, `100`에서 6건
- [ ] 교사 실계정에서 자동 연결 실패 뒤 `자동 연결 다시 시도` 버튼과, 자동 연결 중 `다시 불러오기` 시 학교 유지 확인
- [ ] 교사 실계정에서 학교 검색→급식→학생 비고 저장·삭제→전체화면 이름·비고 미노출 확인
- [ ] 교사 실계정 프로젝터에서 음식 9개 이상인 날의 전체화면 급식판 확인
- **다음 작업**: 기존 학교명만 저장된 교사 실계정에서 최초 자동 연결과 비고 접기·펼치기를 포함해 학교 검색·급식 조회·선택 비고·전체화면 분리 흐름을 확인한다.

### 관리자 서버 현재 상태·트래픽 측정 정직성 (2026-08-25)

- [x] 재시작 뒤 맥 현재값과 대시보드의 도커 VM 오늘 최악값이 어긋나는 원인 확인
- [x] 맥 본체 현재 메모리·스왑과 도커 VM 현재값·오늘 최악값을 분리하는 DB/RPC·수집기 작성
- [x] 디스크·컨테이너도 5분 현재값으로 갱신하고 관리자 상단에 맥 메모리 카드 추가
- [x] 트래픽 막대가 달력 하루가 아닌 직전 04:50 이후 구간임을 측정 시작·종료 시각으로 표시
- [x] 컨테이너 카운터 초기화 구간의 일부 누락 경고와 `NULL` 기록을 0B로 꾸미던 화면 오류 수정
- [x] DB 적용 전에도 기존 기록 RPC로 후퇴해 launchd 수집이 끊기지 않도록 호환
- [x] 전체 473/473·보안 묶음·린트 오류 0건·빌드·실제 DB ROLLBACK 스모크 통과
- [x] 운영 `agit-db`에 `20261173_current_resource_and_traffic_windows.sql` 적용
- [x] 건강검진을 다시 실행해 맥·도커 현재값 기록, `20261174`로 기존 8/24·8/25 트래픽 측정 시각만 보강
- [x] GitHub Actions 배포 후 새 컨테이너 running·앱 직접 200·Caddy 실도메인 경유 200 확인
- [x] 누적 스왑량만으로 장애를 열던 기준을 도커 여유·PSI·직전 5분 스왑 아웃·스왑 포화도 조합으로 교체
- [x] 맥 본체와 도커 VM 메모리 압박 경고를 분리하고 관리자 카드·상단 색이 같은 열린 경고를 사용하도록 통일
- [x] 새 기준 회귀 검사 실패·복구, 전체 486/486·린트 오류 0건·프로덕션 빌드 통과
- [x] launchd가 운영 저장소의 새 건강검진 스크립트를 직접 참조함을 확인하고 5분 간격 3회 이상 기준선·증분 판정, 마지막 종료 코드 0 확인
- [x] 8/25~27 저측정 원인 대조 — Docker/앱 컨테이너 재생성으로 재시작 전 누적 카운터가 유실됐고,
      8/26·27은 `traffic_complete=false`, 구형 8/25는 완전성 미기록(`NULL`) 확인
- [ ] 불완전(`false`)·확인 불가(`NULL`) 트래픽 행을 최근 평균에서 제외하고 그래프에서 회색으로 구분
- [ ] 하루 한 번 차감 대신 5분 건강검진에서 증분을 일일 누계에 적립해 컨테이너 재시작 유실을 최대 5분으로 축소
- [ ] 관리자 실계정에서 상단·서버 상태·트래픽 표 확인
- **다음 작업**: 불완전 행이 경향 평균을 왜곡하지 않게 표시·평균을 먼저 보완하고, 5분 증분 누계로
  전환한 뒤 관리자 실계정에서 트래픽 표와 잔여 스왑·실제 압박 경고를 함께 확인한다.

### 학급 운영현황 핵심 지표 정확성 (2026-08-25)

- [x] 모호한 `고쳐쓰기`를 기간별 `다시쓰기 요청 / 수정 제출` 두 지표로 교체
- [x] 과거 원장에서 AI·교사 입력을 확정 분리할 수 없는 `받은 피드백`을 정확한 `피드백 반영`으로 변경
- [x] `작성 완료 글`을 현재 제출 상태가 아니라 최초 제출 시각·제출 이벤트 이력 기준으로 집계
- [x] `접속 학생`을 학생 로그인 기록과 인증 서버 최근 로그인 시각 중 최신값으로 보완하고 과거 한계 안내
- [x] 학생 수와 모든 기간 이벤트를 현재 활성·미삭제 학급 학생으로 제한
- [x] 운영 `agit-db`에 `20261172_class_operations_metric_contract.sql` 적용 후 214/214·SQL 스모크 통과
- [x] 운영 현황 기간 전환과 최근 활동 기간·갈래·더 보기에 최신 요청 순번 가드를 두고 늦은 응답 회귀 검사 추가
- [x] 학생 아지트에서 학생 전환 시 학습 성취의 직전 학생 응답을 폐기하고 전용 회귀 검사 추가
- [x] `77dcbf7` 운영 배포와 GitHub Actions·공개 HTTPS 응답 확인
- [ ] 교사 실계정에서 7일·30일·전체 전환과 되돌려주기 전후 작성 완료 글 숫자 유지 확인
- **다음 작업**: 제출 글 하나를 되돌려준 전후의 운영현황 숫자와 두 학습 주기·피드백 반영 카드 배치,
  학급 A/B 전환을 교사 실계정 화면에서 확인한다.

### 연구소 질문 만들기·고르기 개선 (2026-08-27)

> 커밋은 별도 저장소(`writing-helper`) `89d87d6` ~ `ef8fd02`. 상세는 WORKLOG 참조.

- [x] 정한 질문 수를 채워야 제출되게 하고, 목표와 최소를 따로 정하게 함(목표 3·최소 2 가능)
- [x] 질문 수 상한을 상수 하나로 모으고 5개까지 열기
- [x] 질문 칠판을 교실 화면 가득 띄우는 `크게 보기` 추가
- [x] 읽으면서 `☆ 담기` 로 고르기 후보를 미리 담고, 고르기 활동에 이미 골라진 채로 올라오게 함
- [x] 응답 잘림으로 실패하던 AI 맞춤법 자동 교정 제거(교사 직접 수정은 유지)
- [ ] **교사 실계정에서 화면 확인** — 이 세션은 로그인 미들웨어 때문에 화면을 하나도 못 봤다
  - [ ] 목표 5·최소 3 방에서 3개만 쓰고 제출되는지, 2개면 막히는지
  - [ ] 목표를 낮추면 그보다 큰 최소가 회색으로 막히는지
  - [ ] 칠판 `크게 보기` 가 프로젝터에서 읽히는지
  - [ ] 칠판·전체화면·학생별 보기·질문만 모아보기 네 곳의 담기와, 고르기 활동 반영

### 글쓰기 연구소 도움말 최신 아지트 연동 반영 (2026-08-25)

- [x] 공용 도움말 원본을 최신 개요 자동 저장·순서 변경·완성 전 전체 확인 흐름으로 갱신
- [x] 개요의 과제별 고정·승인 전 교체·승인 후 잠금·연구소 수정본 자동 반영을 현재 동작과 일치시킴
- [x] 질문 만들기 원문과 학생이 직접 고른 좋은 질문의 아지트 참고함 연결 범위를 정확히 구분
- [x] 질문 실시간 전체 보기와 연구소 자료 연결 기본 ON 설정 안내 추가
- [x] 연구소 전체 검사 76/76·ESLint 오류 0건·Next.js 프로덕션 빌드 통과 후 별도 저장소 main 푸시
- [ ] 교사·학생 실계정에서 대시보드 도움말과 활동 만들기 도움말의 모바일·데스크톱 표시 확인
- **다음 작업**: 연구소 실서비스에서 두 도움말 진입점의 문구 줄바꿈과 활동별 접힘 내용을 확인한다.

### 글쓰기 발자국 학습 횟수·맞춤법 데이터 정확성 (2026-08-25)

- [x] 학생별 `다듬기`를 실제 `다시쓰기 요청 / 수정 제출` 학년도 횟수로 교체
- [x] 기본 표·전체화면 카드·학생 상세가 같은 두 필드를 표시하도록 통일
- [x] 운영에 존재하던 최근 30일 맞춤법 통계가 누락된 `detail` 부모 때문에 응답에서 사라지는 원인 확인
- [x] `detail.spelling_labels` 응답 복구와 두 학습 횟수 증가 SQL 스모크 추가
- [x] 운영 `agit-db`에 `20261171_footprint_learning_counts_and_spelling_labels.sql` 적용 후 213/213·스모크 통과
- [x] 2026-08-31 코드·운영 원자료 재감사 — 대표 학급의 완료 글 303편과 학생 합계가 RPC와 일치하고,
      학급/학생 범위 불일치·현재 상한 초과·빈 글·알 수 없는 자율 글 유형은 0건 확인
- [x] **P0 완료 시각 바로잡기**: 과제의 달력·월별·활동일·최근 글은 `created_at`이 아니라 `approved_at`을 사용.
      운영에서 1,342편의 날짜, 17개 학급 223편의 월, 학생 101명의 활동일 수가 달라지는 영향 확인
- [x] **P0 맞춤법 카드 경로 통일**: 운영 24개 학급·422회 검색이 응답의 `detail.spelling_labels`에 있으나
      카드가 한 단계 다른 경로를 읽어 화면에는 0개로 보이는 계약 오류 수정 및 화면 경로 회귀 검사 추가
- [x] **P1 친구 교류 정의 통일**: 자기 글 댓글 265건·반응 485건은 제외하고, 남김/받음 모두 실제
      댓글·반응 이벤트 수로 맞춰 비대칭 집계를 없앤다
- [x] **P1 학급 빠른 전환 최신 응답 가드**: 느린 이전 학급 RPC가 새 학급 화면을 덮지 못하게 한다
- [x] **P2 선택 지표 반영**: 학급 활동일, 과제/독서록/일기 학급 합계, 학생별 교사 피드백 횟수,
      활동 포인트/교사 조정 포인트를 같은 RPC와 기본·전체화면·학생 상세에 반영
- [ ] **P2 후속 후보**: 월별 참여 학생은 응답에 이미 있으나 전용 표시는 다음 화면 정리 때 검토.
      평가 향상도·장르 비교는 현재 표본 부족으로 보류
- [ ] 교사 실계정에서 학생별 두 횟수와 맞춤법 발자국 막대의 실제 화면 확인
- **다음 작업**: `20261209` 운영 적용·앱 배포 후 기본·전체화면·학생 상세와 학급 A/B 빠른 전환을
  교사 실계정으로 확인한다.

### 과제 글 되돌려주기 알림 일관성 (2026-08-25)

- [x] 일반 되돌려주기와 강제 회수 뒤 다시 주기를 같은 `writing.rewrite_requested` 알림으로 통일
- [x] 실제 다시쓰기 상태로 바뀌는 전환은 이전 회수 여부와 관계없이 알리고 같은 상태 재저장은 중복 차단
- [x] 회수→다시 주기→학생 상단 폴링 전체 SQL 스모크와 실제 `agit-db` ROLLBACK 검증 통과
- [x] 운영 `agit-db`에 `20261169_assignment_return_always_notifies.sql` 적용 후 211/211·스모크 통과
- [x] 다시쓰기 요청·학생 재제출 RPC가 오래된 `recalled_at/recalled_by`를 지우도록 통일
- [x] 강제 회수→다시쓰기→재제출→다시쓰기 반복 주기의 알림 2건·상단 폴링 SQL 스모크 통과
- [x] 운영 `agit-db`에 `20261170_assignment_rewrite_clears_recall.sql` 적용 후 212/212·모순 상태 0건 확인
- [ ] 교사·학생 실계정 두 창에서 강제 회수→다시쓰기→재제출→다시쓰기 상단 알림 반복 확인
- **다음 작업**: 교사·학생 두 창을 연 채 전체 반복 경로에서 각 반려가 약 12초 안에 상단 알림을 띄우는지 확인한다.

### 교사 미션 카드 크기 조절 (2026-08-25)

- [x] 선생님 과제 우측 상단의 `한 줄 3·4·5·6`과 별도 밀도 설정을 `미션 카드 작게·보통·크게`로 통합
- [x] `과제 만들기·관리` 전체 너비에서 작게 4열·보통 3열·크게 2열로 카드 너비와 정보량 조절
- [x] 작게는 여백·제목·태그·버튼 문구를 압축하고 크게는 여백·제목을 확대
- [x] 크기 조절기를 상단 교사 메뉴에서 빼고 선생님 과제 도움말 바로 옆으로 이동
- [x] 보관함 도움말 옆에도 같은 조절기를 두고 선생님 과제와 선택값·4/3/2열 기준 공유
- [x] 모바일은 선택값과 무관하게 1열을 유지하고 제출 현황에는 조절기를 표시하지 않음
- [x] 새 크기 선택을 브라우저에 기억하고 기존 압축·5열 이상 설정은 `작게`, 나머지는 `보통`으로 안전 이관
- [x] 공통 `card-layout` 모듈에 크기 선택지·열 수·조절기·사용 계약을 모으고 글쓰기 미션은 저장 키/과거 이관만 소유
- [x] 전용 검사 통과뿐 아니라 조절기를 제거하면 실패하는 변이 검사와 운영 Docker 재배포 완료
- [ ] 교사 실계정에서 과제·보관함 세 크기 전환, 새로고침 뒤 공유 유지, 좁은 화면과 긴 제목·많은 태그 확인
- **다음 작업**: 실제 과제가 여러 개인 학급에서 과제와 보관함의 세 크기를 바꿔 수업 중 정보 밀도를 확인한다.

### 교사 과제 실시간 제출 전광판 (2026-08-25)

- [x] `과제 만들기·관리`와 `실시간 제출 현황`을 독립 내부 탭으로 분리하고 마지막 선택 기억
- [x] 작성 중 전광판으로 바꿔도 폼 상태를 유지하고 미션 만들기·카드 크기 조절은 관리 화면에만 표시
- [x] 전광판 상단의 `확인할 글`·`활성 과제`·`학생` 요약 카드를 제거해 실제 확인 영역을 먼저 표시
- [x] 전광판을 전체 너비로 넓혀 좁은 최근 제출 영역과 넓은 학생별 상태표를 데스크톱 0.68:1.62·960px 이하 1단으로 배치
- [x] 확인 대기 수를 제출 현황 탭 배지로 표시하고 탭 키보드 이동 지원
- [x] 학생 이름별 활성 글 과제의 승인·확인 대기·다시쓰기·미제출 수를 색상 표로 표시
- [x] 최근 학생 제출·재제출 8건을 과제별로 묶고 `첫 제출`·`다시 제출`·`3회 제출` 등 차수를 표시하며 행에서 해당 글 상세 바로 열기
- [x] `제출 기록 모아보기`를 누를 때만 활성 과제의 최신 제출·재제출 기록을 최대 100건 단일 RPC로 조회
- [x] 모아보기에서도 과제별 제목은 한 번만 표시하고 학생 행에서 정확한 `post_id` 글 상세로 연결
- [x] 전광판 12초 폴링은 기존 8건 상한을 유지하고 100건 기록 조회는 자동 실행·폴링하지 않도록 분리
- [x] 활성 과제×학생 원본 행렬 대신 학생당 네 상태 합계만 최대 100명 반환해 12초 응답 크기를 제한하고 의견 모으기는 승인 흐름에서 제외
- [x] 과제 카드의 기존 승인 수 기반 `완료`를 실제 제출 수 기반 `제출/전체`로 바로잡기
- [x] 첫 과제 개요에 최초 스냅샷을 포함해 교사 첫 화면 요청 2회 예산 유지
- [x] 제출 현황 내부 탭을 열 때만 즉시 확인 후 12초 경량 폴링, 요청 겹침 방지·화면 복귀 즉시 갱신·오류 후퇴 적용
- [x] 승인·회수·다시쓰기·교사 회수 직후 추가 읽기 없이 학생별 상태표 즉시 반영
- [x] `현황 범위`에서 전체 활성 글 과제 또는 미션 하나를 선택하고, 선택 미션은 학생마다 현재 상태 하나로 표시
- [x] 선택 미션의 승인·확인 대기·다시쓰기·미제출 숫자를 눌러 학생 명단 필터링
- [x] 12초 스냅샷에서 새 제출과 상태 변화가 감지되면 해당 행을 2.2초 강조하고 스크린리더에도 갱신 안내
- [x] 전체·선택 미션을 v2 RPC 한 요청으로 전환해 범위별 이중 폴링과 미션별 N+1 방지
- [x] 선택 미션의 세로 상태표를 색상 카드로 바꾸고 기본 화면은 4열×최대 6행으로 24명을 한눈에 표시
- [x] `실시간 크게 보기` 공통 모달에서 같은 현황을 6열×4행으로 확대하고 상태 필터·12초 갱신·변화 강조 공유
- [x] 확대 모달 때문에 별도 RPC·폴링을 만들지 않고 모바일에서는 2열 카드로 반응형 전환
- [x] 담당 학급/ADMIN 권한·활성 과제 100개·학생 100명·최근 제출 8건·글 본문 미반환 DB 계약과 인덱스 작성
- [x] 실제 `agit-db` 스키마에서 신규 SQL·권한 스모크 전체 ROLLBACK 통과
- [x] 운영 `agit-db`에 `20261167_teacher_assignment_submission_board.sql` 적용
- [x] 실제 `agit-db` 스키마에서 `20261168_teacher_assignment_submission_history.sql` 권한·100건 상한 스모크 전체 ROLLBACK 통과
- [x] 운영 `agit-db`에 `20261168_teacher_assignment_submission_history.sql` 적용
- [x] 실제 `agit-db` 스키마에서 `20261176_teacher_submission_student_status_board.sql` 학생별 상태·제출 차수 스모크 전체 ROLLBACK 통과
- [x] 운영 `agit-db`에 `20261176_teacher_submission_student_status_board.sql` 적용
- [x] 실제 `agit-db` 스키마에서 `20261177_teacher_submission_board_mission_scope.sql` 범위·권한·응답 상한 스모크 전체 ROLLBACK 통과
- [x] 운영 `agit-db`에 `20261177_teacher_submission_board_mission_scope.sql` 적용
- [ ] 교사 실계정에서 전체↔미션 전환·상태 필터·변경 강조·1/2/3회 제출·긴 이름·모바일 배치 확인
- **다음 작업**: 교사·학생 실계정 두 창으로 미션을 선택한 뒤 최초 제출→다시쓰기→재제출→승인을 실행해 12초 안의 행 강조와 상태 필터 숫자를 확인한다.

### 교사 수업용 학생 작품 전체 화면 감상 (2026-08-25)

- [x] 최초글·최종글 비교 카드 각각을 눌러 전체 화면 감상 모달 열기
- [x] 현재 최종글만 보는 화면에서도 같은 감상 모달 열기
- [x] 교사 대시보드 `학생 글 모아보기`의 학생별 처음글·마지막글에도 같은 확대보기 적용
- [x] 학생 이름·글 제목·초안/수정본 구분을 전체 화면에서도 유지
- [x] 본문 글자 크기를 20~48px 여섯 단계로 조절하고 현재 크기 표시
- [x] 일반 글과 사진이 있는 구조화 보고서를 기존 렌더러로 함께 지원
- [x] ESC 닫기·키보드 초점 순환·원래 글 카드 초점 복귀·모바일 안전 영역 적용
- [x] 전용 회귀 검사와 프로덕션 빌드·린트 통과
- [ ] 교사 실계정과 교실 프로젝터에서 짧은 글·긴 글·보고서의 가독성 확인
- **다음 작업**: 교실 화면에서 28px 기본값과 40·48px 확대 시 한 줄 길이·스크롤 감각을 확인한다.

### 학생 어휘의 탑 다시 볼 낱말 모아보기 (2026-08-25)

- [x] 층별 `다시 볼 낱말` 수 옆에 0개일 때 비활성화되는 `확인` 버튼 추가
- [x] 선택한 한 층의 기존 카드함 RPC 응답에서 실제 `needs_review` 상태만 모아 표시
- [x] `자주 헷갈려요`·`다시 볼 낱말` 묶음을 처음부터 펼치고 뜻·낱말 양방향 가리기 유지
- [x] 공부 뒤 기존 12문항 개인 적응 연습으로 바로 이어지는 버튼 추가
- [x] 다른 층 연습 진행 중·잠긴 층은 목록 열람을 유지하면서 잘못된 연습 시작 차단
- [x] 추가 DB·목록 RPC·폴링 없이 기존 층별 최대 100건 카드함 조회 재사용
- [ ] 학생 실계정에서 여러 층의 0개·1개 이상 표시, 모바일 모아보기와 재연습 진입 확인
- **다음 작업**: 배포 뒤 다시 볼 낱말이 있는 학생 계정으로 `확인`→뜻/낱말 가리기→연습 시작 흐름을 확인한다.

### 과제 글 연구소 개요 고정·교사 확인 (2026-08-25)

- [x] 학생·과제마다 연구소 개요 하나를 고정하고 최초 선택·다른 개요 교체 이력을 남기는 DB/RPC 작성
- [x] 개요 선택·교체 전에 과제명과 개요 전체를 보여 주는 확인 대화상자 추가
- [x] 학생 참고함 재진입·화면 복귀 시 고정 개요 최신본을 최대 20건 제한 조회 안에서 우선 표시
- [x] 최종 승인 전에는 다른 개요로 교체하고, 승인 뒤에는 선택만 잠그되 같은 개요 최신 내용은 계속 조회
- [x] 교사 글 상세 기존 RPC 한 번에 학생 고정 개요·첫 제출/승인 뒤 수정 여부 포함
- [x] 교사 글 상세의 바깥 칸을 `연구소 참고자료`로 일반화하고 현재 활동명 `글 개요짜기`는 카드 안에 표시
- [x] 제출·되돌려주기·재제출과 독립된 학생+과제 포인터로 흐름 유지
- [x] 연구소 완성 개요 추가 저장도 같은 결과 ID의 `portable_results` 최신본을 갱신하도록 보완
- [x] 아지트 정적·아키텍처·보안 정적·Vite 빌드와 실제 `agit-db` 롤백 스모크 통과
- [x] 연구소 단위 검사·린트·Next 프로덕션 빌드 통과
- [x] 아지트·연구소 커밋을 `main`에 푸시하고 두 자동 배포 성공
- [x] Tailscale SSH로 운영 `agit-db`에 선행 `20261164`와 개요 고정 `20261165` 적용, 207/207·스모크 통과
- [ ] 학생·교사 실계정으로 최초 고정→교체→개요 추가 수정→제출→되돌려주기→재제출→승인 흐름 확인
- **다음 작업**: 학생/교사 실계정으로 최초 고정·다른 개요 교체·연구소 추가 수정과 제출·되돌려주기·
  재제출·승인 뒤 최신본 표시까지 전체 흐름을 확인한다.

### 교사·관리자 대시보드 지표 정확도 (2026-08-24)

- [x] 학생 코드 로그인·저장 세션 복구가 `last_login`을 기록하도록 수정
- [x] 교사 학급 운영에서 `접속 학생`과 `글쓰기 활동 학생`을 분리
- [x] 오늘·최근 7일을 한국 달력 날짜로 통일하고 임시저장 글을 제출 글에서 제외
- [x] 교사 수정 시각이 학생 활동으로 잡히는 집계와 독서록 학급 조인 보정
- [x] 관리자 계정을 선생님 수에서 제외하고 활성 학급·학생 기준으로 사용량 집계
- [x] 승인된 선생님·제출된 글 라벨과 서버 미기록 상태 표시 정리
- [x] 정적 회귀·아키텍처·보안 정적·린트·Vite 프로덕션 번들 통과
- [x] Tailscale SSH로 맥미니 `agit-db`에서 `20261162`·`20261163` 실제 스키마 롤백 스모크 통과
- [x] 승인받아 `20261162`·`20261163` 운영 적용 후 205/205·대기 0건과 적용 후 스모크 확인
- [ ] 적용 뒤 학생 실계정 접속과 교사·관리자 대시보드 숫자 대조
- **다음 작업**: 학생이 다시 접속한 뒤 교사·관리자 대시보드의 접속 숫자를 실계정으로 대조한다.

### 학생 개요짜기 교사 항목 빼기 동작 수정 (2026-08-24)

- [x] `빼기` 뒤 같은 질문이 `+ 쓸래요`로 즉시 다시 나타나던 상태 전이 원인 확인
- [x] 새 활동은 교사가 제시한 개요 항목을 모두 펼쳐 시작하고 학생이 불필요한 항목을 실제 화면에서 숨기도록 수정
- [x] 뺀 항목은 접힌 복구 목록에서 다시 넣을 수 있고, 실수로 복구할 때 작성 중 답변 유지
- [x] 뺀 항목을 개요 완료 제출·남긴 항목·작성 완료 집계에서 모두 제외
- [x] 과거 완성본 수정 시 저장 결과에 없던 교사 항목을 뺀 상태로 복원하고 `선생님 틀 그대로` 방식 유지
- [x] 연구소 `main` 푸시·통합 이미지 빌드·컨테이너 교체·내부/Caddy TLS HTTP 200 운영 배포
- [ ] 운영 배포 후 학생 실계정으로 빼기·다시 넣기·완료 후 다시 고치기 클릭 확인
- **다음 작업**: 브라우저 연결이 가능한 환경에서 학생 실계정 클릭 흐름을 확인한다.

### 속담·사자성어 공통 학습 데이터 토대 (2026-08-23 시작)

- [x] 옛 속담 85개와 사자성어 3파일 100개를 공통 항목 185개·원본 문제 변형 385개로 손실 없이 병합
- [x] 낱말 답만 있던 속담 41개의 완성 표현 초안을 복원하고 원본 불일치 1건을 자동 검수 신호로 격리
- [x] 항목·문제·묶음을 분리한 검수용 카탈로그와 재생성·누락·조기 승격 자동 검사 추가
- [x] 기존 `learning_item_progress`와 같은 키를 쓰는 공통 콘텐츠 DB 토대·직접 권한 차단·승격 제약 작성 및 롤백 검증
- [x] 185개 모두 5·6학년 교육과정 기준으로 분류하고 속담은 직접 연결, 사자성어는 확장 자료로 구분
- [x] 모든 항목을 5·6학년 제공 대상으로 두고 쉬운 속담·사자성어 각 10개만 3·4학년 미리 만나기로 분류
- [x] 속담·사자성어 각 20개의 `core-v1` 시범팩 선정, 시범 40개 내용 난이도 초안과 DB 분리 열·승격 제약 적용
- [x] 3·4학년 미리 만나기 20개의 표현·뜻·한자·교실 예문과 쉬운 4지선다 뜻 고르기 편집 검수
- [x] 원본 표현 2건을 표준형·문장 부호에 맞게 정리하고 편집 검수 자료의 학생 공개·DB 시드 차단 유지
- [x] 나머지 속담 75개·사자성어 90개의 뜻·예문·내용 난이도·5·6학년용 4지선다 편집 초안 작성
- [x] 원본 초성 불일치 속담과 빠진 앞부분, 어색한 사자성어 뜻·예문·조사를 초안에서 교정
- [x] 185개 전부 내용 난이도 1~4·뜻 고르기를 갖추되 20개 검수/165개 초안 상태를 자동 검사로 분리
- [ ] 3·4학년 편집 검수 20개 교사 최종 확인
- [ ] 나머지 시범 20개부터 165개 편집 초안의 표현·뜻·한자·예문·난이도·오답 직접 검수
- [ ] 시범 40개 교사 확인 후 검수 완료 팩 DB 승격
- [ ] 오늘의 말 미션 설정·학급별 일일 스냅샷·학습 카드·확인 문제·멱등 포인트 지급 구현
- [ ] 시범 운영 뒤 교사 확인이 끝난 묶음을 순차 공개하고 별도 속담·사자성어 학습 콘텐츠로 확장
- **다음 작업**: 3·4학년 편집 검수 20개를 교사가 최종 확인하고, 이어 나머지 시범 20개의 완성된 초안을
  항목별로 직접 검수한다. 185개 전체를 한 번에 DB에 올리지는 않는다.

### 학급 활동 도구함 확장 (2026-08-23 계획 확정)

- [x] 자리·역할 배치와 같은 전체 화면 수업 모달 기반 확장 계획을 `CLASS_ACTIVITY_TOOLS_PLAN.md`에 저장
- [x] 첫 글쓰기 밀착 도구를 `함께 만드는 글감 조합기`로 확정하고 첫 버전 범위 저장
- [ ] T0: 학급명·학생 수·진행 상태·교사 조작부를 가진 공통 수업 모달 셸과 계약 검사
- [ ] T5: 함께 만드는 글감 조합기 — 이야기 글 5개 항목, 부분 재추첨, 최종 카드, 타이머, 지난 기록
- [ ] T1: 모둠 자동 구성 — 조건 설정, 이름표 이동, 모둠표·지난 기록
- [ ] T2: 발표·질문 순서 추첨 — 순서표·완료 체크·중복 제외
- [ ] T3: 수업 타이머 — 단일·다단계·새로고침 복구·템플릿
- [ ] T4: 모둠 점수판 — 임시 점수·실행 취소·선택적 포인트 전환
- [ ] T6: 친구 피드백 짝짓기
- [ ] T7: 빠른 투표와 이해도 신호등 학생 응답
- [ ] T8~T9: 활동 빙고를 거쳐 모둠 대항 미션으로 확장
- **다음 작업**: T0 기본 셸과 T5 첫 버전을 함께 구현해 운영 확인하고, 공통 셸 확정 뒤 T1에 착수한다.

### 서바이벌 자리·역할 배치 아지트 통합 (2026-08-23)

- [x] 자리 배치와 역할 나누기의 격자·추첨·조건·기록 포맷을 교사 `수업 도구` 모듈로 이관
- [x] 자리 배치는 학생 수와 활성 좌석 수가 정확히 같을 때만 화면과 엔진 양쪽에서 실행 허용
- [x] 정확 일치 차단 운영 배포: `f353a65` (Deploy `32627426215`)
- [x] 자동 맞춤·드래그·격자 축소의 좌석 수를 화면과 일치시키고 누락된 로또 드럼 애니메이션 복구
- [x] 자리·역할 배치 화면·도움말·파일 오류 안내에서 옛 서비스명 제거
- [x] 학생 배치 구분을 화면에서 남/여로 통일하고 기존 A/B 저장값·백업 호환 유지
- [x] 설정·자료 이전을 남녀 요약과 자리·역할 2열 카드 중심의 한 화면 구조로 재배치
- [x] 로또 드럼을 자리판 중앙에 표시하고 자리 추첨의 보통 속도를 한 명 약 1초로 완화
- [x] 남녀 표기·설정 화면·추첨 위치와 속도 개선 운영 배포: `3851449` (Deploy `32629311856`)
- [x] 자리 배치 시작 시 로또·전체 자리판을 함께 보여 주는 전용 추첨 모달 추가
- [x] 이름 확정 뒤 이름표가 목표 좌석으로 이동하고 도착 순간 좌석이 채워지는 반복 효과 적용
- [x] 로또 추첨 모달·이름표 좌석 이동 효과 운영 배포: `83572e9` (Deploy `32629710058`)
- [x] 자리 이웃을 빈칸을 건너뛰지 않는 상·하·좌·우로 통일하고 대각선은 제외
- [x] 고정 자리·이웃 금지는 반드시 지키며 충돌하면 추첨을 중단하고 설정 확인 안내
- [x] 남녀 혼합·가까이 배치·최근 같은 자리·최근 같은 이웃을 독립 권장 조건으로 분리
- [x] 기존 최근 중복 회피 저장값을 같은 자리·같은 이웃 두 옵션으로 자동 호환
- [x] 상하좌우 이웃·필수/권장 조건 운영 배포: `504ea3b` (Deploy `32631267345`)
- [x] 칠판 옆에 아지트 학급·빠른 입력 학생 수와 좌석 일치 여부를 실시간 표시
- [x] 칠판 옆 실시간 학생 수 카운터 운영 배포: `cdb814f` (Deploy `32632099161`)
- [x] 역할 나누기 시작 시 로또·전체 역할표를 함께 보여 주는 전용 추첨 모달 추가
- [x] 뽑힌 이름표가 실제 역할 칸으로 이동하고 도착 순간 해당 역할 칸이 채워지는 반복 효과 적용
- [x] 역할 추첨 모달·이름표 역할 칸 이동 효과 운영 배포: `548c9f9` (Deploy `32632435050`)
- [x] 지난 자리 기록을 당시 행·열·빈칸을 유지한 자리판으로, 역할 기록을 역할별 그룹 카드로 복원
- [x] 지난 자리·역할 기록 결과판 복원 운영 배포: `a9d10cf` (Deploy `32632826875`)
- [x] 자리·역할 결과를 전자칠판 전체 화면으로 보고 두 사람을 맞바꾸기
- [x] 랜덤 원본과 최신 교사 수정본을 함께 보존하고 지난 기록에서 좌우 비교
- [x] 맞바꾼 학생의 남녀 그룹은 함께 이동하고, 교사 편집은 조건 점수를 계산·차단하지 않고 교사 편집 기록으로 저장
- [x] 최근 중복 회피 범위를 같은 자리·같은 이웃 3회, 같은 역할 4회로 한 원본에서 통일
- [x] 우측 상단 사용법에 자리·역할·결과 수정 흐름을 안내하고 결과판 위에 두 학생 맞바꾸기 표시
- [x] 자리·역할이 두 벌로 갖고 있던 교사 편집 흐름을 `useEditableResult`·`ResultEditBar` 한 벌로 통합(2026-08-24, 커밋 대기)
- [x] 아지트 학급 명부를 사용하고 설정·결과·기존 백업 원본을 통합 DB의 권한 검증 RPC로 저장
- [x] 기존 서바이벌 주소를 아지트 가입·로그인 안내와 브라우저별 JSON 백업 전용 화면으로 전환
- [x] 기존 주소 첫 접속·새로고침 때 이전·가입·백업·종료일을 알리는 접근성 모달 자동 표시
- [x] 나머지 서바이벌 자료는 삭제하지 않고 원본 백업으로 보관해 향후 경쟁형 활동 설계에 사용
- [x] 운영 배포: 아지트 `fb46a21`(Deploy `32626666124`), 서바이벌 `4abd4b9`(Deploy `32626878779`)
- [x] 2026-08-29 종료 시점 재확인: `classroom-tools`는 9월 20일까지 안내·백업을 제공하고
      **9월 21일 00:00 KST 이후** 301 전환 확인과 함께 종료
- [ ] **2026-09-20까지** 전환 안내·백업 제공: 이후 기존 컨테이너·배포 워크플로를 내리고 `survival.`을 아지트 수업 도구로 301 연결

### 구현 기능 실기 확인 완료·로드맵 정리 (2026-08-23)

- [x] 현재 구현·배포된 기능의 교사·학생 실계정, PC·태블릿·모바일, 인쇄·내보내기 실기 확인 완료
- [x] 과거 단계에 남아 있던 인수 확인 항목을 실제 완료 상태와 맞추고, 후속 개편으로 대체된 항목을 정리
- [x] 실기 확인과 실제 운영 전환이 한 항목에 섞인 연구소 `helper.` 전환 계획을 분리
- **앞으로 할 일**: 미구현 기능, 운영 정책 결정, 정기 데이터 검토, 연구소 `helper.` 경로 전환처럼 실제 작업이 남은 항목만 아래에 유지

### 교사 독서록 확인 창 자동 닫기 (2026-08-23)

- [x] 독서록 `확인 완료`·`보완 요청` 저장 성공 뒤 상세 창 자동 닫기
- [x] 처리 결과를 닫힌 창 대신 목록 위 안내로 유지
- [x] 저장 실패 시 상세 창과 입력 내용 유지
- [x] 자동 닫힘 회귀·변이 검사, 전체 회귀·린트·프로덕션 빌드 통과
- [x] 운영 배포 (`b38192a`, Deploy `32587657971`)
- [x] 교사 실계정에서 확인 완료·보완 요청 뒤 상세 창 자동 닫힘 확인

### 선생님 과제 도움말 핵심 기능 탭 구성 (2026-08-23)

- [x] 변경 이력형 `최근 업데이트` 문구와 구역 제거
- [x] `과제 만들기 / 연구소 연결 / 제출·피드백 / 승인·평가` 4개 핵심 기능 탭 구성
- [x] 실제 과제 운영 순서와 주의사항을 각 탭에 맞게 재분류
- [x] 탭 접근성 계약·내용 누락 회귀 검사, 전체 회귀·린트·프로덕션 빌드 통과
- [x] 운영 배포 (`ed0b848`, Deploy `32587417772`)
- [x] 교사 실계정에서 4개 탭 전환·모바일 배치 확인

### 선생님 과제 도움말 최신화 (2026-08-23)

- [x] 실제 과제 만들기·글 종류 카탈로그·제출 검토 기능과 기존 도움말 대조
- [x] 도움말에 `최근 업데이트` 구역 추가
- [x] 글 종류 11종·프리셋·전용 틀·편지지·연구소 연결·질문 잠금·검토·평가 흐름 정리
- [x] 누락 재발 방지 회귀 검사, 전체 회귀·린트·프로덕션 빌드 통과
- [x] 운영 배포 (`f435ef5`, Deploy `32586959419`)
- [x] 후속 4탭 도움말 개편 기준으로 스크롤·강조 표시 확인

### 편지지 반복 도트 배경 정리 (2026-08-23)

- [x] 어버이날·친구 사랑·헤어지는 친구 작성본의 전체 반복 도트 제거
- [x] 친구 사랑 빈 편지지의 반복 도트와 헤어지는 친구 빈 편지지의 원형 색종이 장식 제거
- [x] 선택 화면 설명·모양 원본을 그라데이션·말풍선·리본·꽃·별 중심으로 갱신
- [x] Chrome A4 6쪽·PNG 시각 검증 — 본문·15줄·장식, 잘림·겹침 이상 없음
- [x] 운영 배포 (`76c94d8`, Deploy `32586465838`)

### 작성 편지 7종 밑줄 제거·출력 통일 (2026-08-23)

- [x] 기본·스승의 날·나라사랑 작성 편지에만 남아 있던 가로줄 배경 제거
- [x] 작성 편지 7종은 줄 없음, 빈 편지지 7종은 손글씨용 15줄 유지하는 회귀 검사 추가
- [x] Chrome A4 7쪽·PNG 시각 검증 — 문단·줄바꿈·장식, 잘림·겹침 이상 없음
- [x] 운영 배포 (`979fb77`, Deploy `32585987212`)

### 편지지 받는 사람 밑줄 길이 정리 (2026-08-23)

- [x] 7종 공용 받는 사람 밑줄을 92mm에서 68mm로 축소
- [x] 92mm가 다시 들어오면 실패하는 회귀 검사 추가
- [x] Chrome A4 7쪽·PNG 시각 검증 — 이름 칸·`에게` 간격, 잘림·겹침 이상 없음
- [x] 운영 배포 (`ba68b9f`, Deploy `32585500955`)

### 나라사랑 편지지 KR 표기 제거·재디자인 (2026-08-23)

- [x] PDF에서 `KR`로 풀리는 국기 이모지를 인쇄가 안정적인 꽃 표식으로 교체
- [x] 강한 항공우편 사선 테두리를 차분한 이중 테두리·태극색 모서리로 재구성
- [x] 교사 선택 화면의 설명·모양 원본을 새 디자인에 맞게 갱신
- [x] Chrome A4 2쪽·PNG 시각 검증 — KR 표기·잘림·겹침 없음
- [x] 운영 배포 (`8b5cbc4`, Deploy `32585198352`)

### 스승의 날 편지지 세로줄 제거·재디자인 (2026-08-23)

- [x] 빈 편지지와 작성 편지의 공책식 빨간 세로줄 제거
- [x] 칠판 머리글·은은한 초록 가로줄·아래쪽 연필 장식 중심으로 재구성
- [x] 교사 선택 화면의 설명·모양 원본을 새 디자인에 맞게 갱신
- [x] Chrome A4 2쪽·PNG 시각 검증 — 세로줄·잘림·겹침 없음
- [x] 운영 배포 (`f17757d`, Deploy `32584812657`)

### 편지지 위아래 여백 균등화 (2026-08-23)

- [x] A4 공용 여백과 편지지 안쪽 여백을 합쳐 위·아래 각각 20mm로 통일
- [x] 빈 편지지 7종을 Chrome 인쇄 엔진으로 A4 7쪽 재생성
- [x] PNG 시각 검증 — 제목·15줄·날짜·쓴 사람·장식 잘림 없음
- [x] 운영 배포 (`2037e02`, Deploy `32584470643`)

### 빈 편지지 인쇄 문구 정리 (2026-08-23)

- [x] 교사 선택용 편지지 이름과 빈 편지지 PDF 인쇄 문구를 원본 목록에서 분리
- [x] 7종 상단을 편지지 종류명이 아닌 짧은 감성 문구로 교체
- [x] 학생이 작성한 편지는 실제 편지 제목을 계속 출력
- [x] 편지 회귀·전체 회귀·린트·프로덕션 빌드 통과
- [x] 후속 공용 좌표 정리 때 Chrome A4 7쪽 재생성·PNG 시각 검증 완료
- [x] 운영 배포 (`2d21632`, Deploy `32583783343`)

### 글 종류 선택 화면 한눈에 보기 (2026-08-23)

- [x] 전용 틀 4종은 `양식이 준비된 글`, 나머지 7종은 `기본 글쓰기`로 작성 방식에 따라 분리
- [x] PC는 두 구역 모두 4열로 고정하고 모바일은 2열 유지
- [x] 카드에서 글 이름을 가장 크게 두고 전용 배지는 구역 제목으로 통합, 분류·설명은 보조 문구로 축소
- [x] 1차 단일 그리드 운영 배포 완료 (`ba6b4da`, Deploy `32582455972`)
- [x] 작성 방식별 4열 개편 운영 배포 (`0d8a250`, Deploy `32582938277`)
- [x] 교사 실계정 PC·태블릿에서 미션 만들기 전체 종류와 화면 밀도 확인

### 독서마라톤 개인전·단체전·완주 메달 (2026-08-20)

- [x] 경기 방식을 `개인전 / 우리 반 전체전 / 교사 지정 모둠전`으로 분리하고 시작 뒤 명단·방식 고정
- [x] 단체전에서도 학생별 책·쪽수·거리 참여도를 교사가 확인하고, 모둠별 현재·지난 결과 함께 표시
- [x] 단체전 메달을 `팀 완주 + 교사 설정 개인 최소 요건(책/쪽수/없음)`으로 지급
- [x] 개인전 원형 골드·단체전 각진 블루 메달을 구분하고 학생 이름 옆 대표 메달 + 나의 아지트 메달함 연결
- [x] 독서록·일기 교사 반응을 `확인 / 보완 요청` 두 가지로 단순화하고, 사유·한마디는 둘 다 선택 입력으로 통일
- [x] 자율 글은 제출 횟수를 막지 않고 교사가 `확인`한 글에만 하루 보상 상한 안에서 포인트 지급
- [x] 기존 지급 포인트는 유지하고, 같은 글 재확인·재시도에는 안정적인 이벤트 키로 중복 지급 차단
- [x] 학생이 확인·보완 요청된 글을 수정하면 다시 검토 대기로 돌리고 미확인 알림과 마라톤 집계 갱신
- [x] 확인 완료된 독서록만 집계하고, 보완 요청 전환 시 아직 보관 전인 거리·완주·메달을 함께 재계산
- [x] 미확인·쪽수 미확인 독서록은 저장을 막지 않고 마라톤 집계만 보류하도록 NULL 처리 회귀 수정
- [x] 참가자·팀 누계를 쓰기 시점에 갱신하고 학생 상세는 상위 3명+본인만 반환, 홈 추가 호출·폴링·Realtime 없음
- [x] `20261135~20261138` 운영 DB 적용 및 운영 앱 배포 — 로컬 앱·Caddy TLS 프록시 HTTP 200 확인
- [x] `20261139_self_writing_teacher_approval_rewards.sql` 운영 적용 및 앱 재배포 — DB 181/181, 로컬·Caddy TLS HTTP 200
- [x] 독서록·일기 검토 화면을 같은 압축형 통계·대기 카드·일괄 확인 UI로 통일 — 첫 20편, PC 4열·넓은 화면 5열
- [x] 독서록·일기 학생별 책장을 같은 4열 카드·펼침 UI로 통일하고 전체 기록은 대기 카드와 같은 밀도에 유형별 색상 적용
- [x] 모둠전은 학생 배정을 초안으로 저장한 뒤 배정 누락·중복 없이 확인하고 시작하며, 첫 독서 실적 전에는 배정을 복구할 수 있도록 개선
- [x] 모둠 추가·삭제 시 균등 자동 배정, 인원 균형을 유지하는 랜덤 배정, 색상별 모둠 카드의 학생 개별 이동 UI 적용
- [x] 세 배정 버튼을 한 줄로 정리하고 학생들과 함께 보는 대형 배정 모달, 학생 카드의 우리 모둠 합계·내 기여 구분 적용
- [x] 전체 기록의 미확인·보완 요청은 노란 대기색, 확인 완료는 독서록 파랑·일기 보라로 상태 변화 즉시 구분
- [x] 독서록·일기 도움말을 교사 확인 보상·보완 요청·확인 글 잠금·학생별 책장 내보내기 기준으로 최신화하고,
      독서마라톤 쪽수 자동 확인 실패 시 거리 반영 대기와 교사 보정 절차, 확인 일기의 책장 유지·공개 범위까지 안내
- [x] 학생 독서록의 일반 도움말은 확인·완료조건·포인트 중심으로 정리하고, 독서마라톤 운영 내용은 오른쪽의
      별도 `독서록 이벤트 안내` 아이콘으로 분리
- [x] 독서록·일기 일괄 확인을 공용 RPC 1회·한 트랜잭션으로 처리하고 포인트·학생 알림까지 함께 반영
- [x] `20261140_self_writing_bulk_review_workspace.sql` 운영 적용 및 앱 재배포 — DB 182/182, 로컬·Caddy TLS HTTP 200
- [x] 독서록·일기 보완 요청을 학생 홈 `다시 쓸 글`에 합산하고 최근 요청 글의 해당 편집기로 바로 연결
- [x] 독서록·일기 교사 요약을 `미확인 / 보완 중 / 확인 완료 / 전체`로 분리하고, 보완 요청이 확인 완료 수와
      완료 목록으로 넘어가는 상태 분류 오류 수정 (`20261205`, 2026-08-31 운영 PostgreSQL 17 전체 ROLLBACK 검증 완료)
- [x] `20261205_split_self_writing_review_statuses.sql` 운영 적용 및 앱 배포 — DB 247/247·대기 0,
      GitHub Actions `33353472446`, 로컬 앱·공개 Caddy TLS HTTP 200
- [ ] 교사 실계정에서 독서록·일기의 네 상태 수·목록과 수정 재제출 뒤 `미확인` 복귀 확인
- [x] `20261141_self_writing_revisions_in_student_todo.sql` 운영 적용 및 앱 재배포 — DB 183/183, 로컬·Caddy TLS HTTP 200
- [x] 활동 알림에서 연 독서록·일기는 닫으면 학생 홈, 아지트 책장에서 연 글은 닫으면 아지트 책장으로 복귀
- [x] 확인 완료 독서록·일기는 학생 화면과 DB에서 수정·삭제를 잠그고, 보완 요청으로 바꾸면 다시 수정 허용
- [x] 학생 일기는 오늘을 기본 날짜로 두되 확인 전에는 오늘을 포함한 지난 날짜로 바꿀 수 있고, 확인 완료 뒤에는 날짜도 잠금
- [x] 일기 날짜 선택·맞춤법 배움 데이터 도움말 운영 배포(`bfaa0b7`) — DB 186/186, 로컬 앱·Caddy TLS HTTP 200
- [x] 독서록 전체 기록에서 학급의 확인 완료 글을 Excel·PDF·Google 문서로 단일 RPC 내보내기
- [x] `20261142_lock_checked_self_writing_and_export.sql` 운영 적용 및 앱 재배포 — DB 184/184, 로컬·Caddy TLS HTTP 200
- [x] `20261143_reading_marathon_team_assignment_flow.sql` 운영 적용 및 앱 재배포 — DB 185/185, 로컬·Caddy TLS HTTP 200
- [x] 독서마라톤을 `초안 저장 → 시작 → 완주/중간 종료` 수명주기로 단순화하고 진행 중 `사용함 / 안 함`
      스위치 제거. 기존 `paused` 캠페인은 기록 손실 없이 운영 상태로 복구하고 재발 방지
      (`20261153_reading_marathon_lifecycle_only.sql`, DB 195/195·로컬 앱/Caddy TLS HTTP 200)
- [x] 교사·학생 실계정으로 3개 경기, 확인/보완 알림, 팀 최소 요건, 이름 옆 메달과 아지트 메달함 확인

### 코드 안정화·대량 사용자 경로 효율화 (2026-08-20)

- [x] 연구소 좋은 질문 모달의 무한 로딩·이전 질문 잔류·요청 순서 뒤집힘 방지 및 회귀 검사 추가
- [x] 전체 테스트 명령(`test:all`)과 Docker 배포 게이트 연결 — 현재 278/278
- [x] 학생 홈의 활성 미션 500개 선조회 제거, 다시 쓰기 바로가기를 인덱스 기반 전용 RPC 1회로 축소
- [x] 활동 보고서 이력을 학생 ID로 저장하고 동명이인 오복원·부분 실패 성공 안내·지연 저장 제거
- [x] 보관 글·회의 아이디어·평가 미션·공지 첫 목록을 필요한 필드와 최대 20~100개 상한으로 제한
- [x] `20261135_student_home_latest_returned_assignment.sql` 운영 적용 및 앱 배포
- [x] 맞춤법 구버전 V1 학생 실행 권한을 닫고 안전 기준 밖 누적 표현 정리·관리자 후보 재필터 적용
- [x] 일기 날짜 변경 뒤 남는 이전 날짜 임시본을 로컬과 단일 서버 RPC에서 함께 정리
- [x] `20261145~20261146` 운영 DB 적용 및 앱 배포 — DB 188/188, 앱 HTTP 200, V1 학생 권한 차단 확인
- [x] 일일 백업의 필수 8개·3중 사본 실패를 종료코드와 상태 파일에 반영하고, 평문 파일 권한 600·엄격 복구 리허설 적용
- [x] 관리자 대시보드에서 일일 백업·월간 실복구 상태와 최근 20건을 확인하고 26시간/40일 미갱신 경고 표시 — DB 189/189·운영 배포 완료
- [x] 관리자 교사 목록을 검색·상태 필터·10명 상한의 서버 페이지 RPC로 전환 — 운영 DB `20261151` 적용,
      일반 교사 차단·이메일 검색·페이지 상한 롤백 스모크 통과
- [x] 서비스 점검(5분)·일일 지표(04:50)를 macOS launchd에 설치하고 첫 실행 종료코드 0 확인
- [x] 장애·복구 외부 메일과 GitHub Actions uptime 알림을 제거하고 5분 점검은 관리자 상태 기록 전용으로 유지
- [x] Caddy 점검 페이지 운영 복사·문법 검증·reload — 실제 `agit-app` 중지 시 점검 본문·보안 헤더,
      재시작 뒤 앱 HTTP 200 복구 확인

### 맥미니 전체 장애 점검 안내 이중화 (2026–27 겨울방학 예정)

- [ ] 맥미니 전원·호스트 Caddy·가정 인터넷이 모두 불능이어도 **평소 서비스 주소**에서 점검 화면이
      자동으로 나오도록 외부 장애 안내 계층을 설계한다.
- [ ] 우선안은 `Cloudflare Worker → 맥미니 Caddy 원본`으로 둔다. 앱과 정적 파일의 실제 호스팅은
      계속 Caddy가 담당하고, Worker는 원본 연결 오류·`502/503/504` 때만 정적 점검 화면을 반환한다.
- [ ] 적용 전 가비아 DNS 전체 백업, 원본 전용 호스트의 재귀 호출 방지·접근 보호, 무료 한도 대비 실제 요청량,
      `api.`·`helper.`·Google OAuth 영향 범위를 확인하고 시험용 하위 도메인에서 장애·복구 리허설을 한다.
- [ ] 겨울방학 적용 전까지는 현재 Caddy 내부 점검 화면만 유지한다. 따라서 맥미니 또는 인터넷 전체 장애 때는
      점검 화면 대신 브라우저 연결 오류가 보이는 현재 한계를 운영상 수용한다.

### 개요 짜기 두 방식 · 득표 질문 연결 (2026-08-20)

- [x] 개요에 불러오는 질문을 후보 전체가 아니라 **득표순 결과**로 바꿈(투표 전이면 후보 유지, `친구들 N표` 표시)
- [x] 교사가 `학생이 고쳐 쓰기`(기본)와 `선생님 틀 그대로` 두 방식 중 선택(`studentEditable`)
- [x] 고정 틀에서 항목이 바로 열리고, 처음 적을 때 답 항목이 만들어지도록 수정
- [x] 연구소 결과에 활동을 가려 주는 한 줄(`hint`) 추가 — 한줄모아는 핵심 낱말로 구분
- [x] 동그란 번호 배지를 공용 조각(`badge-circle`)으로 모으고 개요 번호 정렬 오류 수정 — 재발 방지 검사 추가
- [x] 학생 대시보드 `과제 글쓰기` 카드의 도움말 버튼 제거(불필요)
- [x] 교사 실계정으로 두 방식과 득표 질문 불러오기 확인

### AI 맞춤법 검사 — 글 한 편에 한 번 (2026-08-19)

- [x] 외부 맞춤법 검사기 대신 기존 AI 경로 사용 결정(부산대 무료 API는 서비스 종료·기관 구독 전환)
- [x] 서버가 횟수를 쥐도록 `spell_check_used_at`·`spell_check_result` 추가 및 **서버 소유 열 가드에 포함**(`20261129`)
- [x] 본문을 클라이언트에서 받지 않고 Edge Function이 DB에서 읽은 뒤 원자적으로 선점 후 호출
- [x] 학급 교사 기준 분당 30건 속도 제한 범위 신설(`20261130`)
- [x] 결과는 제안 목록으로만 표시(자동 수정 금지)하고 글 행에 저장해 재요청 방지
- [x] 교사 학급 스위치로 등록(기본 꺼짐, `surface: 'editor'`)
- [x] 자리 정리 — 에디터 아래가 아니라 **글쓰기 참고함 안의 갈래**(`📚 참고 자료 / 🔍 맞춤법`)로 이동
- [x] 제출한 글에만 열리도록 제한하고, 누르기 전 `정말 다 썼나요?`를 화면 안에서 확인(서버도 같은 조건 검사)
- [x] `다시 쓰기` 요청을 받은 글에서만 열리도록 조건 좁힘(화면·서버 동일)
- [x] 참고함 바로 아래 안내 줄로 진입점 이동(쓸 수 있을 때만 표시)
- [x] 교사(글쓰기 창 관리·선생님 과제)·학생(대시보드 과제 글쓰기 카드) 도움말에 규칙 안내
- [x] 학생 실계정으로 버튼·재요청 차단 동작 확인
- [x] AI가 찾은 `틀린 표현 → 바른 표현`을 누적 집계로 저장(`spelling_ai_findings`, 학급·학생 식별자 없음)
- [x] 관리자 `맞춤법 승격 검토` 화면 — 학급 수·횟수 기준으로 후보를 추리고 묶음으로 확정/보류(`20261132`)
- [x] 확정한 표현을 카탈로그에 붙여 넣을 코드 조각으로 내보내기(사람이 예문·문맥 확인 후 배포)
- [ ] 첫 월간 검토 실행(데이터가 쌓인 뒤)

### 개인정보 처리 위탁 현행화 (2026-08-19)

- [x] AI 피드백이 보내던 학생 이름 제거 — 글 ID·제목·본문만 전송(낱개·일괄 두 경로)
- [x] 처리방침 제5조에서 Supabase 행 삭제 및 자체 호스팅 명시
- [x] OpenAI 행 추가 — 전송 정보(내용만·식별정보 제외)·보유 기간(모델 학습 미사용, 남용 점검 최대 30일) 기재
- [ ] 암호화 백업(Google Drive, rclone crypt)을 위탁 표에 넣을지 결정

### 맞춤법 배움 데이터 화면 정리 · 검색 말뭉치 (2026-08-19)

- [x] `자주 찾아본 표현`이 늘 `미분류`로 보이던 원인 수정 — 학생 화면이 없는 필드(`label`)를 읽던 것과, 찾은 항목의 표현을 저장하지 않던 것
- [x] 교사 화면이 내부 키(`common:…`)를 실제 표현으로 되돌려 보여 줌(이미 쌓인 옛 기록 포함)
- [x] 화면을 `우리 반 배움 현황 / 항목 만들기 / 등록 데이터` 세 갈래로 나누고, 현황을 첫 화면으로 배치
- [x] 자료 없는 표현에 `자료 만들기` 버튼을 붙여 검색 기록 → 자료 등록 동선을 이음
- [x] 항목 만들기 폼을 `표현 적기 → 내용 확인 → 등록` 3단계 상자로 정리
- [x] 학급·학생과 끊어진 누적 말뭉치 `spelling_search_corpus` 신설(40자 이하 표현만, `20261128`)
- [x] 도움말·화면에 `학급 등록 → 학기말 검토 → 기본 자료 승격` 흐름 안내 추가
- [x] 검색을 기존 자료·공식 사전·문장·추천 후보로 분류하고, 문장 원문은 저장하지 않으며 2명 이상 또는 3회 이상 반복된 짧은 미등록 표현만 교사에게 추천(`20261144`)
- [x] 학생 검색 기록은 수첩을 닫을 때 최대 20건을 한 번만 보내고, 교사 후보는 화면을 열 때 단일 RPC로 최대 30건만 조회
- [x] `20261144_spelling_search_candidate_filtering.sql` 운영 적용 및 앱 배포 — DB 186/186, 로컬 앱·Caddy TLS HTTP 200
- [x] 맞춤법 배움 데이터 도움말에 최근 30일·후보 기준·문장 원문 보호·반영 시점과 통계 의미를 최신화하고 운영 배포(`bfaa0b7`)
- [x] 교사 실계정으로 세 탭과 검색 목록·`자료 만들기` 버튼 확인
- [x] 학기말 승격 실무: 말뭉치·학급 자료를 검토해 기본 자료로 옮기는 관리자 흐름(`20261132`)
- [ ] 첫 월간 승격 검토 실행(운영 데이터가 더 쌓인 뒤)

### 질문 만들기 한 화면 동선 · 관점 질문 카드 (2026-08-19 저녁)

- [x] 안내·카테고리·카드·쓰기로 넘기던 네 화면을 한 화면(`question_build`)으로 합쳐 화면 이동을 없앰
- [x] 담기와 제출을 분리(마지막 질문 저장 시 자동 제출하던 것 제거)하고, 담은 질문을 같은 화면에서 수정·삭제
- [x] 제출 뒤 결과 화면 맨 위에 `✏️ 질문 고치기`를 두고 결과 화면 폭도 1200px로 통일
- [x] 학생 화면 폭 계약 통일(`max-w-[1200px] px-4 py-6`) — 빈 사이드칼럼 때문에 좁아 보이던 문제 제거
- [x] AI 질문 카드를 완성된 질문이 아니라 **관점을 여는 문장**(`~ 질문을 만들어 보세요`)으로 생성
- [x] 질문 카드는 입력창에 그대로 채우지 않고 힌트로 표시(물음표로 끝나는 완성 질문일 때만 채움)
- [x] 활동 만들기 화면에서 `＋ 질문 카드 만들기` 모달로 즉시 카드 생성·선택(AI 5장 생성 포함)
- [x] 교사 실계정으로 카드 즉시 만들기와 AI 카드 문장 결 확인
- [x] 학생 태블릿 가로에서 한 화면 동선과 제출 뒤 수정 확인

### 질문 만들기 전체 제출 실시간 모아보기 (2026-08-24)

- [x] 제출 전에도 열 수 있는 `전체 질문 실시간 보기` 모달과 제출 완료·작성 중·미접속·질문 수 표시
- [x] 최종 제출 질문만 자동 반영하고 새 제출을 5초 강조하며 학생별/질문만 보기와 카드 토글·교사 수정 유지
- [x] 학생 상시 WebSocket 없이 교사 모달이 열린 동안 방별 Realtime 채널 1개만 사용
- [x] 질문 원문 없는 최소 제출 신호, 방별 1초 디바운스, 중복 조회 방지, 30초 보완·장애 시 5초 폴백
- [x] 실제 승인 역할과 방 소유 교사만 이벤트를 읽는 RLS·Realtime publication·100명 결과 조회 인덱스 작성
- [x] 연구소 회귀 58/58·Next.js 프로덕션 빌드와 맥미니 PostgreSQL 롤백·전체 보안 검사 통과
- [x] 모달을 92vh·최대 7xl로 확대하고 학생 이름 선택 시 상단 칠판에 해당 학생의 최종 질문을 함께 표시
- [x] 학생별/질문만 보기 양쪽 이름 선택, 선택 카드 강조·칠판 자동 이동·공용 번호 배지·접근성 회귀 추가
- [x] 확대·칠판 변경 연구소 커밋/푸시(`~/writing-helper` `2697330`)
- [ ] 사용자 승인 후 `20261164_question_generator_live_results.sql` 운영 적용
- [ ] 적용 뒤 교사·학생 실계정으로 자동 반영·채널 해제 확인
- **⚠️ 적용 전까지 실시간이 동작하지 않는다**(2026-08-24 점검). 운영 `agit-db`의
  `writing_helper.activity_events` 는 권한 목록이 비어 있어(`relacl` NULL) service_role 조차 INSERT 를
  못 하고, `supabase_realtime` publication 에도 없다. 권한이 없어 실패하는 방향이라 정보 노출·제출
  유실은 없고 5초/30초 보완 폴링이 결과를 회수하지만, 교사 화면은 실시간이 아니라 폴링으로 돈다.
- **다음 작업**: 운영 적용 뒤 권한·publication·RLS 정책 세 가지를 다시 확인하고, 소규모 실계정 흐름을
  먼저 본다. 600개 동시 요청을 운영 서비스에 직접 보내지 않고, 필요하면 격리된 시험 방·계정으로
  별도 부하 시험을 실행한다.

### 연구소 결과 불러오기 기본 켜짐 (2026-08-19)

- [x] 코드·DB 기본값을 켜짐으로 바꾸고, 손댄 적 없는 학급 118개의 설정을 함께 이동(`20261127`)
- [x] 스위치를 끄면 글쓰기 창 버튼뿐 아니라 **참고함의 연구소 자료**도 함께 닫히도록 연결
- [x] 글쓰기 도구 줄의 중복 버튼 제거 — 진입점을 참고함 하나로 모으고 `본문에 넣기`도 그쪽으로 이동(`surface: 'reference'`)
- [x] 참고함 버튼 오른쪽에 용도 설명 한 줄 추가
- [x] 연구소 도움말 신설 — 활동 4가지의 용도·사용 시점·학생 동선·결과 연결을 `features/activities/guide.ts` 한 곳에 두고 대시보드 탭과 활동 만들기 창이 함께 읽음
- [x] 교사 실계정으로 도움말 두 화면(대시보드 탭·활동 만들기 창) 확인
- [x] 교사 실계정에서 끄고 켜며 학생 글쓰기 창과 참고함이 함께 닫히는지 확인

### 글쓰기연구소 질문 만들기 세 방식 정리 (2026-08-19)

- [x] 질문 작성 방식(`mode`)을 활동 설정에 저장하고, 시작 조건 판정을 `question-generator/config.ts` 한 곳으로 모음
- [x] 방식별 시작 조건 분리 — 직접 만들기(준비물 없음)·질문 카드 활용(묶음 1개 이상)·선생님 추천 질문(질문 1개 이상)
- [x] 옛 방(`mode` 없음)은 질문 카드 활용으로 읽어 기존 활동이 그대로 열리게 함
- [x] 학생 화면 단계를 방식이 정하게 함 — 직접 1단계·추천 질문 2단계·카드 3단계
- [x] 질문 만들기 네 화면을 아지트 글쓰기와 같은 1200px 폭으로 통일하고, 쓰기 화면을 좌우 2단(미션·힌트 / 넓은 입력창)으로 개편
- [x] 교사 활동 내용판에 방식 표시 추가 및 카드 미리보기가 늘 비어 있던 문제(`cards` → `prompts`) 수정
- [x] 역할(탐정 모드·상담사 모드) 제거 — 학생 1단계를 **질문의 큰 카테고리 6개**(상상·반전 / 마음·가치 / 감각·관찰 / 이유·해결 / 연결·비유 / 관점·시간) 고르기로 교체
- [x] 카테고리 분류 기준을 `question-generator/areas.ts` 한 곳으로 모으고(교사 설정·활동 만들기 화면의 사본 제거) 활동 설정에서 `roles` 제거
- [x] 교사 실계정으로 세 방식 활동을 각각 만들고, 학생 태블릿 가로에서 단계 수와 입력창 폭 확인

### 학생 홈의 연구소 새 활동 알림 · (beta) 표시 (2026-08-19)

- [x] 학생·교사 대시보드의 이름을 `글쓰기 연구소 (beta)`로 변경
- [x] 교사 상단 메뉴는 안정화된 연구소 진입점임을 반영해 `(beta)`를 제거하고 `글쓰기 연구소`로 표시
      (2026-08-30, 학생 홈 카드의 Beta 표시는 유지)
- [x] 홈 부트스트랩 RPC에 `home.has_new_lab_activity` 추가 — 우리 반에 열려 있는 활동 중 내가 시작하지 않은 것이 있으면 참
- [x] 모듈이 `studentDashboard.newFlagKey`만 선언하면 홈 카드에 NEW가 붙는 계약으로 구현(홈은 카드마다 따로 조회하지 않음)
- [x] 학생 실계정에서 새 활동에만 NEW가 뜨고 참여 뒤 사라지는지 확인

### 글쓰기연구소 4대 활동 데이터 파이프라인 연계

- [x] '좋은 질문 고르기' 득표 질문들을 아지트 교사 글 미션 생성(`MissionForm`)에서 바로 불러와 미션 질문으로 활용할 수 있도록 모달 및 RPC 연계 (`get_teacher_question_voting_ranking_v1`)
- [x] 학생이 '한줄모아'에서 작성한 핵심 낱말 문장을 아지트 글쓰기 시 참고함(`LabReferenceSource`, `LabResultsTool`)에서 열어볼 수 있도록 DB 테이블 제약조건, RPC, 클라이언트 렌더링 연동 (`one_line`)
- [x] `writing-helper` 회귀 테스트(`tests/unified-roster.test.mjs`) 갱신 및 38/38 테스트 통과 확인
- [x] 교사 실계정으로 미션 생성 시 좋은 질문 불러오기 및 학생 글쓰기 참고함 한줄모아 확인

### 비로그인 첫 화면 한눈 보기

- [x] 대표 이미지의 수호룡·제목·설명 구도를 살리고 `쓰고, 읽고, 키우며 함께 자라는 우리 반 아지트` 핵심 문장과 학생·교사 로그인을 첫 화면 중심에 배치
- [x] 장황한 기능 카드 4개를 `쓰고 다듬는 글 / 읽고 채우는 책장 / 활동으로 키우는 수호룡` 세 경험 키워드로 압축
- [x] 키워드를 누르면 세 영역의 실제 경험을 탭으로 넘겨보는 상세 모달 제공
- [x] 하단 소개와 상세 모달을 현재 기능 기준으로 최신화 — 과제·자율 글·피드백·연구소, 독서록·책장·독서마라톤·친구 아지트, 포인트·칭호·수호룡·놀이터
- [x] 글쓰기 소개를 교사의 과정 설계·지도, 학생의 다양한 글 형식, 글쓰기 연구소 4대 생각 활동과 글쓰기 연결 흐름으로 보강
- [x] 로그인 앱 소개 최신화 운영 배포 — GitHub Actions·`agit-app`·Caddy HTTPS 200 및 운영 번들 문구 확인
- [x] 연구소 소개의 한자 활용 문장을 제거해 현재 4대 활동으로 교정·운영 배포 — 로그인 번들 부재 검사 포함
- [x] 학생 코드 로그인을 브라우저 기록의 내부 단계로 등록해 화면·브라우저·기기 뒤로가기 시 연구소가 아닌 아지트 첫 화면으로 복귀
- [x] 학생 로그인 뒤로가기 수정과 `아지트에서는 이렇게 활동해요` 제목을 운영 배포 — GitHub Actions·`agit-app`·Caddy HTTPS 200 및 운영 번들 반영 확인
- [x] 기능 소개 제목을 친근한 `아지트에서는 이렇게 활동해요`로 정리
- [x] 모달의 바깥 클릭·ESC 닫기, 배경 스크롤 잠금, 키보드 초점 순환·원위치 복귀와 모바일 구성을 적용
- [x] 학교 도입 안내·개인정보 처리방침·이용약관을 하단 한 줄 링크로 정리
- [x] 세 경험 카드를 둥근 단일 패널의 가로 3분할 선택 바로 바꾸고 `수식어 / 글·책장·수호룡` 두 줄 리듬과 화살표로 균형 통일
- [x] 대표 이미지·핵심 문장·로그인·기능 바·하단 링크 높이와 여백을 다시 줄여 세로 목록이나 별도 스크롤 없이 한 화면 구성을 유지
- [x] 하단 3분할의 절약된 높이는 유지하되 과도하게 잘린 대표 이미지는 이전의 넉넉한 1723:600(모바일 1723:560) 비율로 복원
- [x] 대표 이미지와 내용이 겹치던 상단 연필 아이콘·서비스명·보조 문구를 제거해 대표 이미지부터 바로 보이도록 정리
- [x] 확보된 높이를 세 가지 성장 영역에 재배분해 제목·아이콘·핵심 명사·버튼 높이와 하단 링크 가독성을 확대
- [x] `아지트에서 만나는 세 가지 성장`과 `눌러서 자세히 보기`를 더 키우고 모바일에서는 두 줄로 분리해 가독성 확보
- [x] 하단 세 안내 링크를 문구 길이에 맞춘 3개 영역으로 전체 폭에 배분하고 PC·모바일 글자 크기·자간·간격 최적화
- [x] 하단이 넓어 보이지 않도록 개인정보 처리방침·이용약관을 첫 화면에서 빼고 `학교·학급 도입 안내` 링크 하나로 축약
- [x] 단일 하단 링크 이름을 공개 페이지 제목과 같은 `학습지원소프트웨어 선정기준 안내`로 통일
- [x] 세 번째 성장 이름을 수호룡 성장과 아지트 꾸미기를 함께 드러내는 `키우고 꾸미는 드래곤 아지트`로 변경
- [x] 세 번째 성장 카드의 `드래곤 아지트`를 PC·모바일 모두 한 줄로 표시하도록 글자 크기·자간을 반응형으로 조정
- [x] 운영 PC·모바일에서 첫 화면 밀도와 기능 모달의 터치·키보드 동작 확인

### 공개 학습지원소프트웨어 선정기준 안내

- [x] 비로그인 로그인 화면 하단에 학교 도입 검토·학습지원소프트웨어 선정기준 안내 링크 제공
- [x] 제품 개요와 개인정보보호 9개 세부 기준의 충족 여부·처리방침 증빙을 공개 페이지로 정리
- [x] 학교 상황에 맞게 사용할 학교운영위원회 안건 상정 HWPX 원본 다운로드 제공
- [x] 공개 경로·기준 수·첨부 원본 해시를 Docker 빌드 게이트의 회귀 검사에 포함
- [x] 운영 PC·모바일에서 공개 페이지 표 스크롤과 HWPX 실제 다운로드·한글 문서 열기 확인

### 교사 학생 아지트 열람

- [x] `학급 운영 → 학생 아지트`에서 학생별 칭호·포인트·성장 상태·실제 꾸미기 상태·최근 완성 글 책장을 읽기 전용으로 확인
- [x] `학생 관리 → 학생 명단 → 아지트 보기`에서 같은 화면으로 이동하며 해당 학생을 바로 선택
- [x] 기존 교사 권한 확인 RPC와 학급 직접 범위 조회를 재사용하고 목록 상한(학생 100명·완성 글 60편), 지연 로딩, 학급 캐시 키 적용
- [x] 교사 화면은 수호룡 방을 최대 260px로 제한하고 성장·활동 요약을 옆에 배치해 책장까지 한눈에 보이는 밀도 높은 구성으로 정리
- [x] 책등을 누르면 선택한 글 한 편의 본문·독서록 책 정보·보고서 구조·처음글 비교를 교사 읽기 전용 상세창으로 지연 조회
- [x] 교사 실계정 PC·태블릿에서 실제 학생 데이터와 명단 바로가기 확인

### 학생 홈 알림 구조 정리

- [x] `내 글 소식`을 친구 반응과 친구·선생님 댓글 전용 창으로 분리하고 할 일 카드의 중복 소식 행 제거
- [x] `내 글 소식`의 새 소식을 빨간 점 대신 버튼 전체 강조·`새 소식` 문자 배지·접근성 이름으로 표시
- [x] 새 항목이 있는 친구 반응·댓글 개별 탭을 모두 확인하면 자동 읽음 처리하고 창을 닫을 때 새 소식 목록 정리
- [x] `지금 할 일`을 시작 전 과제·작성 중 과제·다시 쓸 글 세 개수만 표시하도록 bootstrap 집계 분리
- [x] 학생 홈 상단을 `지금 할 일 / 활동 알림` 고정 2분할로 구성하고 빈 상태도 같은 자리에 표시
- [x] 할 일 세 개수를 가로 3칸으로 압축하고 두 패널 본문 높이를 78px로 맞춰 알림 중심의 짧은 배너로 정리
- [x] 다시쓰기 요청·글 승인+포인트 지급·승인 취소+포인트 회수·교사 포인트 조정을 단일 이벤트 원장과 표시 레지스트리로 모듈화
- [x] 로그인·새로고침 즉시 확인, 홈 복귀 60초 stale 재검증, 4~6분 공용 분산 동기화와 상세 최대 50건 계약 구현
- [x] 새 SQL을 실제 `agit-db` 스키마에서 스모크 포함 전체 ROLLBACK 검증
- [x] 운영 DB에 `20261023_student_activity_notifications.sql` 적용하고 적용 후 롤백 스모크·124/124 상태 확인
- [x] 앱 변경을 PR #2로 `main`에 병합하고 운영 번들에서 알림 패널·빈 상태 문구 확인
- [x] 독서록·일기 같은 자율 글 `writing_reward` 포인트도 활동 알림의 “포인트 내역이 생겼어요”로 연결
- [x] 학생·교사 실계정으로 네 이벤트 유형, 읽음 유지, 글/편집기 이동과 PC·모바일 2분할 확인
- [x] 과제 글 반려·승인 두 유형만 학생 앱 최상단에 8초간 표시하는 전역 배너 구현
- [x] 전역 배너를 상단 가로 전체 폭으로 넓히고 배너 어디를 눌러도 즉시 닫히도록 보완
- [x] Realtime 대신 보이는 탭 12초 경량 폴링, 최초 0~12초 분산, 요청 겹침 방지, 숨은 탭 중단·복귀 즉시 확인, 오류 후퇴 적용
- [x] 실제 학생·학급·현재 과제 글 상태를 다시 확인하고 커서 뒤 최대 10건의 최소 신호만 반환하는 전용 RPC·부분 인덱스 작성
- [x] 운영 `agit-db`에 `20261166_priority_writing_notification_poll.sql` 롤백 검증 후 적용(208/208)
- [ ] 학생 실계정에서 반려·승인 배너가 12초 이내 표시되는지 확인
- [ ] 격리 시험 학급에서 1,000명·12초 분산 폴링 p95·오류율·DB 연결·CPU 측정

### 장르별 글 반응 모듈

- [x] 공용 반응 레지스트리에 기본·보고서·회의 프로필을 두고 모든 글쓰기 장르가 매니페스트에서 프로필을 선언
- [x] 학생 글·친구 아지트·내 글 소식·교사 반응 모아보기가 같은 반응 이름·아이콘 해석기를 사용하도록 통일
- [x] 학생 반응 저장은 장르의 `input_template`에 허용된 유형만 서버 RPC가 검증하고 직접 테이블 쓰기는 차단
- [x] 교사 반응 모아보기는 창을 열 때만 학급·미션 범위 전용 RPC 1회로 최대 100편을 조회
- [x] 운영 DB에 `20261024_writing_reaction_profiles.sql` 적용하고 GitHub Actions 운영 배포·컨테이너 반영 확인
- [x] 학생·교사 실계정으로 일반 글·보고서·회의 글의 반응 선택, 내 글 소식 표시, 교사 반응 모아보기 확인

### 친구 아지트 우리 반 새 글 탐색

- [x] 최신 글 1차 분류를 `전체 새 글 / 선생님 과제 / 자율 글`로 나누고 전체 공개 글을 기본 화면으로 제공
- [x] 시·보고서·회의 등 새 미션 장르는 선생님 과제 안에 자동 포함하고 기존 과제별 세부 필터 유지
- [x] 독서록·일기와 향후 자율 글 유형은 각 모듈 매니페스트의 `communityFeed` 선언으로 세부 필터 자동 등록
- [x] 글·작성자·미션·반응을 학급 직접 범위 단일 RPC로 읽고 공개 시각+글 ID 커서와 최대 50편 상한 적용
- [x] 기본 진입을 공개 글 RPC 1회로 줄이고 과제 목록·친구 명단은 해당 탭 첫 진입에 지연 조회, 동일 피드 요청 중복 방지
- [x] 공개 일기만 포함하고 비공개 일기·처음 글 비공개 원문·익명/타 학급 접근을 서버에서 차단
- [x] 운영 DB에 `20261025_class_public_writing_feed.sql` 적용하고 GitHub Actions 배포·운영 번들·HTTP 200 확인
- [x] 학생 실계정 PC·모바일에서 전체/과제/자율 글, 독서록/일기 필터와 다음 페이지·상세 열기 확인

### 운영 호스팅·자동 배포 단일화

- [x] 앱 운영 경로를 맥미니 Docker + 호스트 Caddy로 단일화하고 `vercel.json` 삭제
- [x] 보안 헤더·외부 origin 허용 기준을 `Caddyfile.container` 한 곳으로 모으고 Vercel CSP 검증 제거
- [x] `main` 푸시 → GitHub Actions → self-hosted 러너 → Docker 빌드·컨테이너 교체·HTTP 200 확인 계약을 자동 검사
- [x] 저장소 밖 GitHub–Vercel 연동을 해제해 PR에 붙는 Vercel 배포·Preview Comments 검사 제거
  (2026-08-21 Vercel 프로젝트 `agit`의 Git 연결 `null` 확인)
- [x] PR #2를 `main`에 병합하고 GitHub Actions `Deploy` 성공과 운영 컨테이너 반영 확인

### Stage 2 연구소 통합 착수 점검

- [x] 아지트·연구소 Git 상태, 실제 운영 컨테이너·Caddy·GitHub Actions, 구·신 DB 스키마와 보존 대상 자료를 교차 점검
- [x] 보존 대상 두 교사의 방·세션·학급·질문 카드 지문이 구·신 DB에서 같아 현재 추가 복사가 필요 없음을 확인
- [x] 새 DB의 연구소 학급 2개·학생 27/29명 매핑과 과거 세션 252건 중 확정 연결 234건·미연결 18건 확인
- [x] 연구소 미커밋 작업을 안전 브랜치에 기준 커밋하고 5종 전용 정리 브랜치에서 검증·의미 단위 커밋 완료
- [x] 누락 한자 반응 마이그레이션을 구·신 DB 양쪽에서 전체 ROLLBACK 검증
- [x] 정리 브랜치를 `main`에 병합하고 구 DB 마이그레이션 적용·GitHub Actions Docker 배포·공개 로그인 200 확인
- [x] 연구소 Docker 빌드 문맥에서 `.env*`를 제외하고 공개 빌드 인자와 서비스 키 런타임 env를 분리한 뒤 배포 검증 강화
- [x] 연구소의 개인·공용 OpenAI 키 설정과 관련 설명을 제거하고 구 연구소 세션·승인 교사 매핑·공용 속도 제한을 확인하는 아지트 `vibe-ai` 내부 브리지로 AI 기능 일원화
- [x] 연구소 교사용 화면을 아지트 공통 색상·간격·카드·버튼 계약과 단일 상단 셸로 정렬하고 다섯 활동 안내를 현재 구조에 맞게 정리
- [x] 연구소 교사 셸의 1280px 제한을 없애 아지트와 같은 전체 폭·반응형 여백으로 맞추고, 공통 헤더·메뉴·제목·버튼 글자를 아지트 글자 계단과 굵기로 통일
- [x] 연구소 상단 `학급·활동 / 질문 카드 / 도움말` 탭을 PC에서 16px·52px 높이·넓은 간격으로 보강하고 모바일 압축 규칙은 유지
- [x] 연구소의 별도 서비스 관리자 화면·권한 환경변수를 제거하고 통합 DB 현황과 기존 AI 교사 매핑 활성 상태를 아지트 관리자 모드의 ADMIN 전용 RPC·탭으로 통합
- [x] 연구소 로그인 우측·대시보드 브랜드·로그인 하단의 사용자용 배포 번호 표시 제거
- [x] 연구소 상단의 한자 단어집 메뉴와 직접·인쇄 페이지를 제거하되 통합 DB의 카드 8개와 과거 활동 결과 호환은 보존
- [x] 연구소 교사 실계정 PC·태블릿에서 공통 셸과 학급→활동→결과 동선 확인
- [x] 새 통합 DB를 바라보는 `/lab` 병행 컨테이너를 별도 3001 포트에 기동하고 basePath·자산·인증 콜백·QR/단축링크 경로 계약과 기존 서비스 병행 상태 검증
- [x] 교사 상단 업무 메뉴를 `글쓰기 / 글쓰기 연구소 / 학급 운영` 순서로 두고 연구소를 같은 탭의 `/lab/dashboard`로 연결
- [x] 교사 실계정으로 통합 `/lab`의 기존 QR·단축링크 확인
- [ ] 호스트 Caddyfile을 영구 반영한 뒤 짧은 점검 시간에 `helper.` 경로 전환
      (기존 `helper.` 컨테이너와 경로는 전환 확인 전까지 롤백용으로 유지)
- [x] 통합 `/lab`에서만 같은 이름의 루트 경로 쿠키를 사용하고 실제 DB 승인 교사를 확인하는 SSO를 구현·운영 배포
- [x] 교사 실계정 PC·태블릿에서 기존 로그인 세션 자동 이관과 메뉴 클릭 후 재로그인 없는 `/lab` 진입 확인
- [x] 2026-08-29 운영 재확인: 최근 7일 학생 세션 갱신 183건·최근 30일 새 방 29개로
      `writing-helper-lab-app`은 현재 사용 중인 `/lab` 본 서비스이며 종료 대상이 아님
- [x] `student_sessions.agit_student_id`와 실제 `students.auth_id` 연결 기반 자동 입장·본인 결과 RPC를 구현하고 과거 미연결 세션은 별도 보류
- [x] 통합 대상 5종 매니페스트에 스키마 버전·표준 결과 종류 계약을 필수 등록
- [x] 활동 완료 시 매니페스트가 표준 결과 본문을 만들고 공용 저장 RPC·본인 조회 RPC·학생 글쓰기 불러오기 도구로 연결
- [x] 개요짜기 `직접 추가하기`에서 같은 반의 교사 정리·확정 `좋은 질문 고르기` 후보만 지연 조회해 개요 항목으로 사용
- [x] 연구소의 다섯 학생 글 입력란을 아지트 글쓰기와 같은 1200px 폭·공유 맞춤법 물결 밑줄·학급 승인 자료에 연결
- [x] 밑줄 교정 문구와 상시 버튼에서 아지트 500개 설명·예문·공식 출처를 검색하는 지연 로딩 맞춤법 수첩 제공
- [x] 통합 교사 결과 목록·상세에서 개인 QR을 숨기고 학생·교사 개요 완성 화면을 입력 화면과 같은 1200px로 통일
- [x] 통합 다섯 학생 활동의 완료 화면에서 아지트 글쓰기 대시보드로 돌아가는 공통 복귀 동선 제공
- [x] 통합 다섯 학생 활동의 시작·입력·다시쓰기·완료 전체 화면에 홈·글쓰기 연구소 목록 공통 복귀 메뉴 제공
- [x] 학생 글쓰기 입력창에 열고 접을 수 있는 공통 참고함 셸을 두고 선생님 안내·핵심질문·학생 답변, 가로·세로 배치 안내와 긴 글 우측 sticky 연결
- [x] 기존 연구소 결과 모달을 공통 참고함의 지연 출처 어댑터로 연결하고, 연결 미션 결과 우선·다른 내 결과 선택 흐름 제공
- [x] 학생·교사 실계정 PC·태블릿에서 개요 입력→완성 폭·아지트 복귀, 참고함 우측/상단 전환, 결과 보기 QR 제거, 대표 오류 밑줄→맞춤법 검색과 모바일 정렬 확인

**Stage 0~1 완료(컷오버), Stage 1.5(속도)·Stage 3(모듈화)는 대부분 완료.** 상세 이력은
[WORKLOG.md](WORKLOG.md), 시스템이 어떻게 생겼는지는 [ARCHITECTURE.md](ARCHITECTURE.md),
성능·보안 기준과 실측은 [PERFORMANCE_HARNESS.md](PERFORMANCE_HARNESS.md)·
[SECURITY_HARNESS.md](SECURITY_HARNESS.md)를 본다. 이 섹션은 **지금 뭘 하는 중이고 다음이 뭔지**만 담는다
— "어떻게 완료했는지"는 여기 적지 않는다(WORKLOG 담당).

### 집에서 이어서 할 운영 반영 — 장르형 글쓰기 PDF

- [x] 일반 글 PDF를 유지하고 장르 매니페스트의 `pdfExport` 렌더러로 전용 양식을 선택하는 구조 구현
- [x] 시 쓰기를 제목·지은이·과제명·연 단위 시구가 정렬된 A4 양식으로 구현하고 과거 평문 시 호환
- [x] 별도 학생 편집기 장르의 전용 PDF 누락·12pt 미만 스타일을 막는 아키텍처 검사와 추가 지침 작성
- [x] 시 과제 내보내기에서는 보고서 양식 선택을 숨기고 시 전용 PDF로 바로 연결
- [x] 교사 실계정에서 실제 학생 시 여러 편을 PDF로 저장하고 프린터 여백·한글 글꼴 확인

### 집에서 이어서 할 운영 반영 — 보고하는 글쓰기

- [x] `report` 장르 미션·교사용 기본 칸 설계·학생용 칸 추가/삭제/위아래 이동 UI 구현
- [x] 학생 기기 사진 최적화(긴 변 720px·최대 256KB)와 비공개 Storage/RLS 마이그레이션 작성
- [x] 맥미니 `agit-db` 실제 스키마에서 `20261018_report_writing_images.sql`+스모크를 전부 `ROLLBACK` 검증
- [x] 운영 DB에 `20261018_report_writing_images.sql` 적용 후 보고서 앱 배포
- [x] 저장 용량 축소 배포 전 운영 DB에 `20261019_report_image_storage_optimization.sql` 적용
- [x] 일반 글·보고하는 글 전용 A4 PDF 양식과 교사 내보내기 UI 구현
- [x] 맥미니 `agit-db` 실제 스키마에서 `20261020_writing_pdf_export.sql`+스모크를 전부 `ROLLBACK` 검증
- [x] 운영 DB에 `20261020_writing_pdf_export.sql` 적용 후 PDF 내보내기 앱 배포
- [x] macOS Storage bind mount의 xattr 업로드 오류를 `agit-storage-data` named volume 전환으로 해결하고 백업·복구 검사 보완
- [x] 사진 포함 초안 upsert의 기존 글 ID 검증 오류를 수정하고 왼쪽 4:3 고정 사진 칸·PDF 양식 구현 및 운영 DB `ROLLBACK` 검증
- [x] 운영 DB에 `20261021_report_image_upsert_validation.sql` 적용 후 앱을 `main`에 푸시
- [x] 비공개 사진 서명 URL의 CSP 차단 원인을 수정하고 보고서 PDF 사진 칸을 68×51mm로 확대
- [x] CSP·PDF 사진 확대 커밋을 `main`에 푸시하고 운영 CSP 헤더·배포 번들 반영 확인
- [x] 학생 보고서 한 칸을 `사진 + 사진에 대한 관찰 결과` 입력 하나로 단순화하고 기존 사진 설명 자동 호환
- [x] 보고서 단순 입력 커밋을 `main`에 푸시하고 운영 HTTP 200·배포 번들 반영 확인
- [x] 보고서 PDF를 왼쪽 56mm·5:4 사진, 오른쪽 위 교사 질문, 오른쪽 아래 보고서 내용으로 재배치
- [x] 보고서 PDF 재배치 커밋을 `main`에 푸시하고 GitHub Actions 운영 배포 성공 확인
- [x] 보고서 PDF를 상단 전체 폭 교사 질문, 하단 왼쪽 사진·오른쪽 본문 구조로 재정리
- [x] 질문 전체 폭·사진 옆 본문 커밋을 `main`에 푸시하고 GitHub Actions 운영 배포 성공 확인
- [x] 보고서 PDF를 `질문 포함 지도형`과 `질문 없는 완성본`으로 선택하고 질문 한 줄형·사진/글 비율 재설계
- [x] 보고서 사진을 Excel의 내용 뒤 사진 열과 Google Docs의 각 글 본문 뒤에 실제 이미지로 포함
- [x] Office 사진 내보내기 커밋을 `main`에 푸시하고 GitHub Actions 배포 `31479865159` 성공 확인
- [x] 운영 Google Cloud의 Drive API 활성화와 교사 실계정 WebP 보고서의 Excel·Google Docs 이미지/임시 파일 삭제 확인
- [x] 학생 실계정의 기존 사진 표시와 교사 PDF 지도형 52mm·완성본 58mm 사진의 실제 가독성 확인
- [x] 앱 배포 후 교사 과제 생성 → 학생 칸 이동·사진 추가/교체/삭제·제출 → 교사/친구/내 서재 열람을 실계정으로 확인
- [x] 학생 태블릿에서 큰 사진 2~3장을 넣고 타이핑 지연·사진 회전·업로드 실패 복구 확인
- [x] 교사 실기기에서 일반 장문·사진 3장 보고서를 PDF로 저장하고 2페이지 넘김과 실제 인쇄 확인

**주의**: 보고서 앱만 먼저 배포하면 `report-images` 버킷이 없어 사진 추가가 실패한다. 마이그레이션 적용 후 앱을
배포하며, 사진 원본은 DB JSONB가 아니라 비공개 Storage에 저장한다.

### 집에서 이어서 할 운영 반영 — 맞춤법 배움 데이터

- [x] 맥미니에서 `git pull` 후 `npm run migrate:status`로 `20261017_spelling_learning_module.sql` 적용 상태 확인 (118/118·대기 0)
- [x] `npm run migrate:check` 실행 — 이미 적용되어 롤백 대상은 없었고 적용 기록·체크섬·실제 테이블/RPC를 직접 확인
- [x] 운영 `agit-db` 적용 상태 확인 (`supabase-db`가 아니라 `agit-db`)
- [x] `supabase/functions/vibe-ai/index.ts`를 운영 `vibe-ai` Edge 함수에 배포하고 기존 환경변수·허용 Origin 유지 확인
- [x] 교사 계정으로 `수업 도구 → 맞춤법 배움 데이터`에서 AI 초안 생성·수정·승인 확인
- [x] 학생 계정으로 승인 항목의 밑줄·수첩 검색을 확인하고, 수첩을 닫은 뒤 교사 화면과 `학급 발자국` 집계 확인
- [ ] `npm run test:security`와 운영 핵심 스모크 실행 후 결과·배포 정보를 `WORKLOG.md`에 기록

**주의**: DB 마이그레이션과 Edge 함수 중 하나만 반영된 중간 상태에서는 새 화면이 정상 동작하지 않는다. 두 작업을
같은 운영 반영 묶음으로 처리하되, DB는 반드시 `migrate:check` 통과 후 적용한다.

### 집에서 이어서 할 운영 반영 — 일괄 AI 피드백 전역 제어

- [ ] OpenAI 프로젝트 Limits 화면에서 운영 `gpt-4o-mini`의 실제 RPM·TPM과 공유 한도 모델 확인
- [ ] DB에 일괄 피드백 작업 대기열과 상태(`queued`·`processing`·`completed`·`failed`)를 두고 교사·학급 권한 RLS 설계
- [ ] Edge 함수가 대기열을 교사별 순환 방식으로 가져가 한 교사의 긴 작업이 다른 교사를 막지 않게 구현
- [ ] 조직 전체 동시 OpenAI 요청을 최대 3건, 전체 시작 속도를 30 RPM, 토큰 안전선을 150,000 TPM으로 제한
- [ ] OpenAI 응답의 `x-ratelimit-remaining-*`가 20% 이하이면 자동 감속하고, 429는 `Retry-After` 우선·지수 백오프와 무작위 지연으로 최대 3회 재시도
- [ ] 학생 글은 요청당 2건을 유지하되 입력 예상량이 큰 경우 1건으로 자동 축소하고, 완료한 묶음은 즉시 저장
- [ ] 작업 재실행 시 완료 건을 건너뛰는 안정적인 작업 키를 두고 실패 건만 이어서 처리
- [ ] 교사 2~3명, 10명 동시 실행 스모크에서 공정한 순환·부분 실패 안내·DB 연결 수·OpenAI 429 여부 확인
- [ ] `npm run test:security`와 관련 부하 검사를 통과한 뒤 Edge 함수·DB를 함께 운영 반영하고 결과를 `WORKLOG.md`에 기록

**현재 적용됨**: 브라우저에서는 교사당 동시 요청 1건, 요청 시작 2초 간격(최대 30 RPM), 학생 글 2건 묶음,
묶음 완료 즉시 저장과 부분 실패 안내까지 적용했다. 브라우저 제한은 여러 교사를 합친 조직 전체 한도를 보장하지
못하므로 위 전역 제어가 완료되기 전에는 운영 안전장치의 최종 완료로 표시하지 않는다.

**다음 세션에서 정할 것**: 나의 아지트 디자인 3차 vs Stage 2 연구소 운영 전환 vs
Stage 3c 잔여 정리, 순서 미정. 서바이벌 자리·역할 통합은 완료했고 2026-09-20까지 안내·백업 제공 후 종료 일정만 남았다.

### 밀린 작업 (보류·후속, 급하지 않음)

- [ ] **UI 디자인 정리 후속**: 학생 홈·나의 아지트·친구 아지트 → 교사 글쓰기 관리·학급 운영 → 관리자·레거시
  순서로 화면 단위 적용. 글쓰기 코어 로직은 수정하지 않는다. 학생 홈은 1차 완료, 나의 아지트/친구 아지트 남음.
- [ ] **일기 후속(필요할 때)**: 교사 학급 운영 대시보드의 `일기 확인` 카드, 친구 아지트 `최신 글`의 일기 탭,
  교사 학생별 일기 내보내기.
- [ ] **`읽는 중 / 다 읽음` 위치 재검토**(보류, 2026-08-04 사용자 결정으로 지금은 그대로 둠): 효과가 전부
  내 서재에만 나타나 글 쓰는 화면에서는 눌러도 변화가 없어 학생에게 의미 없어 보인다는 지적.
- [ ] **교사 대시보드 고도화 D. 라벨 통일**: 설정 화면의 `AI쫑알이 생성`·`생기부 도움자료`가 옛 이름 그대로
  (문구 변경만, 소요 적음).
- [ ] **교사 대시보드 고도화 B. 국어 평가·평어 동선 통합 2~3차**: 제출·평가 완료·미평가·미제출 수와
  `평가 입력/결과 보기`를 학생 평가 한 화면에 통합, 학생별 결과 목록(이름·평가 근거 유무·생성 문장·복사 버튼).
- [ ] **나의 아지트/친구 아지트 정보 계층 정리**: 나의 아지트는 홈 성장 상태판과 같은 시각 언어로 칭호·드래곤·
  내 서재·발자국 탭 흐름 정리. 친구 아지트는 이름·칭호 탐색·최신 글·친구 책장의 탐색 흐름과 카드 밀도 정리.
- [ ] **Postgres 에러 130건 로그 확인** (오래전부터 대기): Logs → Postgres. timeout/connection이면 이관으로
  해소, RPC exception이면 코드 수정.
- [x] **독서마라톤 순위표 배경 동기화 재조회 방지**: 펼쳐 읽은 상세는 같은 캠페인의 4~6분 홈 동기화가 와도
  유지하고, 캠페인이 바뀌었을 때만 상세를 다시 읽도록 캠페인 ID 기준으로 고정했다.

### 구현 기능 실기기 확인 완료

전체 클릭 순서와 기대 결과는 [MANUAL_ACCEPTANCE_CHECKLIST.md](MANUAL_ACCEPTANCE_CHECKLIST.md) 한 곳에서
관리한다. 2026-08-23 현재 구현·배포된 기능의 학생·교사 동선과 PC·태블릿·모바일·인쇄 인수 확인을
완료했다. 이후 새 기능을 구현할 때만 해당 검사표와 이 로드맵에 새 확인 항목을 추가한다.

**측정 예약**: AI 댓글 오탐률은 2026-08-20 전후 데이터가 쌓이면 확인한다. 측정 전에는 프롬프트를 바꾸지 않는다.

### 🎯 이번 방학 사용자 우선순위 (2026-07-24 확정)
선생님이 이번 방학에 할 4가지 — 대부분 기존 스테이지에 매핑되고, 포인트 고도화만 신규:
1. **학생 자율 글쓰기**(1차 독서록 — 교사 과제 없이) → **Stage 4c** (기존)
2. **포인트·드래곤 시스템 관리/활용 효율화** → **Stage 4d 신설** (아래)
3. **연구소 ↔ 현재 글쓰기 툴 통합** → **Stage 2**(SSO+데이터연동) + **Stage 4b**(결과물 불러오기 UI) (기존)
4. **기능 추가해도 안 느려지는 구조** → **Stage 1.5**(속도) + **Stage 3**(모듈화·지연로딩)로 상시 확보 (기존)

**+ 3개 앱 통합 (2026-07-25 확정)**: 연구소=연동(SSO+데이터), 서바이벌=아지트 모듈 흡수 → Stage 2로 구체화.

---

## 전체 순서와 원칙

```
Stage 0    이관 전 대청소              (방학 초, 반나절)
Stage 1    맥미니 완전 이관 (앱+DB)    (방학 전반, 7~10일)      ← 비전 1
Stage 1.5  대시보드 속도 개선 패키지    (이관 안정화 직후, 2~3일)
Stage 2    연구소 통합: SSO + 결과물 연동 기반  (3~5일)          ← 비전 2
Stage 3    기존 기능 파트별 모듈화      (모듈당 0.5~1일, 점진)    ← 비전 3
Stage 4    글쓰기 확장 + 기능 고도화     (모듈당 2~4일)           ← 비전 4
           ├ 4a 확장 슬롯 / 4b 연구소 결과물 불러오기(우선순위3)
           └ 4c 자유 글쓰기(우선순위1) / 4d 포인트·드래곤 고도화(우선순위2)
Stage 5    이후 개선 과제              (순서 무관, 여유 시)
```

**대원칙**
1. 이관(Stage 1)이 끝나고 안정화되기 전에는 구조를 건드리지 않는다 — 문제 원인 분리
2. 모든 신규 기능은 통합 DB를 전제로 한다 — Stage 2 이전에 기능 개발 금지
3. 기능 "삭제"보다 "모듈화 + 기본 OFF"를 우선한다 — 되돌릴 수 있게
4. **코어 셸(글쓰기 파이프라인)은 Stage 3~4에서도 수정하지 않는다** — 확장 포인트로만 연결
5. 연구소 코드를 아지트로 이식하지 않는다 — 데이터(RPC) 연동만
6. 커밋은 작게, 모듈 하나 옮길 때마다 빌드·동작 검증

---

## Stage 0 — 이관 전 대청소 (예상 반나절)

코드를 깨끗한 상태로 이삿짐 싸기. 동작 변경 없음.

- [x] 루트 잡동사니 삭제: `check_*.js`(14), `inspect_*.js`(4), `test_*.js`(6), `debug_missions.js`, `deep_inspect.js`, `create_table.js`, `run_migration_dummy.js`, `output.txt`, `help.txt`, `performance_report.html`, `temp_mission_form_backup.jsx`, `playwright-report/`, `test-results/`, 빈 파일 `.claude/worktrees/blissful-galileo`
- [x] `security-test-plan.js`, `security-test-student.js` → `scripts/`로 이동 (이관 후 RLS 검증에 재사용)
- [x] `.gitignore`에 `playwright-report/`, `test-results/`, `.DS_Store` 추가
- [x] 미사용 의존성 제거: `openai`, `react-router-dom` → 빌드 확인
- [x] 린트 `no-unused-vars` 정리 (실측 132건 → 0건) → 빌드 확인
- [x] vite 프로덕션 빌드에서 console 제거 (`esbuild: { drop: ['console'] }`) — 학생 정보 콘솔 노출 차단
- **하지 말 것**: `exhaustive-deps` 경고 수정, 1000줄+ 파일 리팩토링을 이관 작업에 섞지 않는다(Stage 3에서 진행).

## Stage 1 — 맥미니 완전 이관: 앱 + Supabase DB (예상 7~10일) 【비전 1】

**상세 절차는 [INTEGRATION_PLAN.md](INTEGRATION_PLAN.md) Phase 0~5.** 여기는 요약 체크포인트만.

- [x] Phase 0 완료 (2026-07-24): 학교망 테스트 ✓ / Cloud 백업 ✓(`~/backups/agit-cloud-20260724/`) / 빈 접미사 스택 제거 ✓ / 도메인 계획 ✓ / 연구소 데이터 결정 ✓
- [x] Phase 2: 아지트 Docker화 + Caddy 라우팅(`/`, `/lab`) + writing-helper `basePath` + 테스트 서브도메인
- [x] Phase 1: DB 이관 — **클라우드 전체 덤프 기준** (마이그레이션 파일 드리프트 주의) / auth append 이관 /
      익명 로그인 활성화 + **IP당 속도제한 상향** / Edge Functions / 연구소 UUID 수동 매핑 / RLS 검증
- [x] Phase 4: 실기기 리허설 → 점검 공지 → 최종 동기화 → DNS 전환 → 안정화 모니터링 → 맥미니 운영 단일화
- [x] Phase 5: 암호화 백업 자동화(rclone→Drive) + 복구 리허설 완료(**2026-07-30** — 상세는
      **[backup.md](backup.md)**). 이후 백업 상태·서비스 점검은 관리자 대시보드와 macOS launchd에서 운영한다.
      백업은 `agitcrypt:` 로 암호화해 올리고, **매월 1일 04:40 자동 복구 리허설**이 임시 DB 에 실제로
      복원해 검증한다(`~/backups/auto/rehearsal-status.txt` 한 줄로 확인).
- [x] 이관 후 수업 규모 실사용에서 로그인·로딩 정상 확인(기존 장애 2종 해소)
- [x] 도커 VM 디스크(31.3GB) 여유 감시: 배포마다 쌓이는 빌드 캐시가 채우면 Kong 건강검진 실패와 Storage
      업로드 실패로 번진다. 2026-08-16에 16.01GB를 회수하고(100%→52%), 배포 워크플로 마지막에 빌드 캐시를
      6GB 상한으로 유지하는 `Trim docker build cache` 단계를 추가해 재발을 자동으로 막는다.

**완료 기준**: 본 도메인이 맥미니를 가리키고, 앱 호스팅과 자동 배포가 Docker·Caddy·self-hosted 러너만 사용한다.

## Stage 1.5 — 대시보드 속도 개선 패키지 (이관 안정화 직후, 예상 2~3일)

원인 진단(2026-07-17): 느림의 정체 = **직렬 왕복 세대 수 × DB 응답 지연**의 곱.
이관이 지연을 줄이고(코드 무변경), 이 스테이지가 세대 수를 줄인다(교사 6→2, 학생 4→2 수준).

### 운영 체감·정량 기준
- [x] 학교 태블릿·교사 PC의 실제 수업 사용에서 로그인과 첫 화면 로딩 이상 없음 확인(2026-08-23)
- **운영 목표**: 교사 대시보드 < 2초, 학생 대시보드 < 1.5초(학교망 기준). 정량 재측정이 필요할 때
  [PERFORMANCE_HARNESS.md](PERFORMANCE_HARNESS.md)의 동일 조건 측정표에 기록한다.

### 서빙 최적화 (맥미니 정적 호스팅 기준 — Stage 1 Phase 2와 함께 적용)
- [x] Caddy 압축 활성화 (zstd/gzip) — 2026-07-24 실응답 `content-encoding: zstd` 검증
- [x] 해시된 정적 자산에 `Cache-Control: immutable` 헤더 (재방문 시 재다운로드 제거)
      — 2026-07-24 Vite 해시 정규식 수정·운영 배포·실응답 검증 완료
- [x] HTTP/2 확인 — 2026-07-24 실제 도메인 경유 앱·API 응답 검증

### 교사 대시보드 (왕복 6세대 → 2세대)
- [x] **교사 부트스트랩 RPC** (2026-08-09 코드 추적으로 완료 확인) — 이 항목은 미완료로 남아있었지만
  실제로는 `get_teacher_app_bootstrap_v1`(`20261007_teacher_app_bootstrap.sql`)가 이미 프로필·교사정보·
  학급 목록·공지·`frequent_tags`·`default_rubric`·`mission_default_settings`를 1회로 묶고
  (`useAuthStore.fetchProfile`), `useTeacherDashboard`/`useMissionManager`도 `teacherBootstrap`이 있으면
  개별 조회를 건너뛰도록 이미 배선돼 있었다. 기본 진입 화면(과제 목록) 코드 경로를 끝까지 따라가 보니
  **로그인 bootstrap 1회 + `get_teacher_mission_overview_v1` 1회, 총 2회**로 이미 목표를 달성한 상태다.
  "왕복 11회" 메모는 이 RPC가 배선되기 전 수치가 그대로 남아있던 것으로 보인다. 실측(브라우저 네트워크
  탭)으로 최종 확인은 아직 안 했다 — 다음에 브라우저 있는 세션에서 확인.
- [ ] 로그인 화면에서 TeacherDashboard·MissionManager 청크 프리로드 (`import()` 미리 호출 — 비밀번호 입력하는 동안 다운로드)

### 학생 대시보드 (왕복 4세대 → 2세대)
- [x] 아래 "1,000명 동시 사용 점검"의 `get_student_home_bootstrap_v1` P0 작업으로 대체·완료됨(2026-08-08).
- [ ] 학생 코드 입력 화면에서 StudentDashboard 청크 프리로드 (아직 미실시)

### 1,000명 동시 사용 점검 (2026-08-08 코드·운영 통계 재진단)

현재 맥미니(M4 10코어·16GB), PostgREST 풀 25, PostgreSQL 연결 한도 100이다. 500 웹소켓 실측은 통과했지만
Realtime `max_concurrent_users=600`이라 **1,000명이 학생 화면의 구독을 모두 열면 400명은 실시간 연결을 보장할 수 없다.**
DB 크기보다 학생 홈의 중복 조회·짧은 폴링·첫 방문 정적 파일 전송이 먼저 병목이 될 가능성이 높다.

- [x] **P0 학생 홈 부트스트랩 RPC**: 로그인 직후 조건에 따라 약 18~21회 발생하는 요청을 코어 상태 1회 +
      열린 선택 모듈 조회만 남긴다. 포인트·펫·학급 모듈/편집 설정·할 일·반려 수·일기/독서록 오늘 상태·새 소식 수를
      `get_student_home_bootstrap_v1()` 한 응답으로 묶고, 모듈의 상세 데이터는 실제로 열 때만 전용 RPC로 읽는다.
- [x] **P0 lightweight 조회 오류 제거**: 학생 홈에서는 해당 훅 자체를 제거하고 bootstrap의 학급 설정을 재사용해
      설정 1행조차 별도 조회하지 않는다. 전체 활동 집계는 실제 아지트 화면을 열 때만 기존 전체 훅이 담당한다.
- [x] **P0 짧은 폴링 통합**: 모듈 설정 10초(1,000명=100회/초), 편집 설정 30초(33회/초), 학생 인증 120초
      (8회/초)를 각 기능 타이머로 따로 돌리지 않는다. 로그인/화면 복귀/교사 변경 응답을 공용 갱신기로 합치고,
      수업 중 상시 폴링은 최소 3~5분 + 무작위 지연으로 낮춘다. 2026-08-25 이후 과제 글 반려·승인 두 신호만
      12초 코어 폴링 예외이며, 전체 목록·설정·인증 폴링은 다시 늘리지 않는다.
- [x] **P0 Realtime 1,000명 정책 결정**: 학생 상시 Realtime을 모두 제거했다. 승인·회수·포인트·다시쓰기 결과는
      단일 이벤트 원장에 기록하고 공용 홈 bootstrap이 최신 미확인 1건과 개수만 읽는다. 로그인·새로고침·홈 복귀와
      4~6분 분산 동기화로 수렴하며, 실제 목록은 학생이 활동 알림 창을 열 때만 최대 50건을 읽는다.
      2026-08-25부터 과제 글 반려·승인만 12초 최소 신호 폴링으로 먼저 알리고 나머지 흐름은 유지한다.
- [x] **P0 접힌 카드 지연 조회**: 독서마라톤은 접힌 홈 카드에서도 전체 순위·최근 기록까지 읽는다. 홈에는 목표·내 거리만
      주는 compact RPC를 쓰고, 펼칠 때 전체 순위를 조회한다. 일기/독서록 상태도 부트스트랩 결과를 재사용한다.
- [x] **P1 글 제출 원자화**: 현재 과제 제출은 기존 글 조회→upsert→장르별 후처리/보상의 2~3요청이다.
      `submit_assignment_post_v1()` 같은 기능 전용 RPC가 글 저장·원본 보존·미션 버전 확인·후속 보상을 한 트랜잭션으로 끝낸다.
- [x] **P1 교사 N+1 제거**: 보관함은 보관 미션 수만큼 제출 수 count를 따로 호출한다(최대 500+2회).
      학급/페이지 단위 grouped RPC 1회로 바꾸고 미션 목록·제출 집계에도 공통 페이지 계약을 적용한다.
- [x] **P1 중복 인덱스 정리(운영 적용 완료)**: 운영 DB에 동일 키 인덱스 14묶음이 있다. 실제 실행 계획과 제약 인덱스를 확인한 뒤
      새 마이그레이션으로 중복만 제거해 글 저장·댓글·포인트 기록의 쓰기 비용과 백업 크기를 줄인다.
- [x] **P1 첫 방문 대표 이미지 감소**: 투명도가 없는 랜딩 PNG를 같은 1723×913 크기의 JPEG로 바꿔
      1.7MB→276KB로 줄였다. CDN 분리는 실제 첫 접속 부하 측정에서 정적 전송이 병목일 때만 진행한다.
- [x] **부하 시험 완료 (2026-08-09, 학생 홈 읽기 경로)**: 1,000명·10분 기준 전부 통과(`pass: true`).
      실측 수치와 아직 안 한 것(혼합 비율·쓰기 경로 확장)은 `PERFORMANCE_HARNESS.md`의
      "측정 기록" 표에 남긴다 — 앞으로 재시험할 때마다 같은 표에 한 줄씩 쌓아 비교한다.

### 적용 후 현재 부하 실측 (2026-08-08)

- 학생 복구 로그인은 `get_student_by_auth`+홈 bootstrap **2회**, 새 코드 로그인은 익명 인증·코드 바인딩·토큰 갱신·
  bootstrap **4회**다. bootstrap은 1.7KB, 예열 뒤 약 0.4ms/회였다.
- 학생은 글쓰기 진입 2회, 변경이 있을 때만 2분마다 DB 초안 1회, 최종 제출 RPC 1회+홈 갱신 1회다.
  제출 RPC 5.4ms, 승인 RPC 6.7ms로 DB 처리 자체는 가볍다.
- 교사 콜드 로그인 첫 과제 화면은 이 메모를 쓴 시점엔 약 **11회**였는데, **2026-08-09 코드 추적으로
  재확인한 결과 이미 로그인 bootstrap 1회 + 과제 개요 1회, 총 2회로 줄어 있었다**(위 "교사 대시보드"
  항목 참고 — `get_teacher_app_bootstrap_v1` 배선 완료). 이 메모가 그 작업 전 수치인 채로 남아있었던
  것으로 보인다. "승인 후 재조회 5회" 메모도 마찬가지로 stale했다 — 다시 확인해보니
  `handleApprovePost`·`handleRecovery`·일괄 승인·일괄 회수 전부 이미 서버 응답으로 `posts`/
  `submissionCounts`를 직접 갱신하고 있어 재조회가 없었다. 유일하게 남아있던 "다시 쓰기 요청"
  (♻️, 단건·일괄)만 처리 후 `fetchPostsForMission()`으로 전체를 다시 불러오고 있어서 2026-08-09에
  같은 방식(로컬 갱신)으로 맞췄다. 이제 승인·회수·다시 쓰기 요청 세 동작 전부 재조회 없음.
- 학생 홈 Realtime 소켓은 0개다. 교사 업무 결과는 DB 트랜잭션 안에서 학생별 단일 알림 이벤트로 기록되고,
  공용 홈 bootstrap·화면 복귀·4~6분 동기화에서 읽는다. 과제 글 반려·승인만 전역 배너용 12초 최소 신호
  폴링으로 앞당기며 포인트·일반 활동 알림은 기존 주기를 유지한다.
- 다음 구현 순서 항목(제출글 페이지 상한·교사 bootstrap·재조회 제거·1,000명 실부하)은 2026-08-09 확인·
  실행으로 전부 완료. 남은 건 위 "아직 안 한 것"(혼합 비율·쓰기 경로 포함 확장 시험)뿐, 급하지 않음.

**완료 기준**: 목표 시간 달성 + 측정 기록을 이 파일 결정 기록에 남김.

## Stage 2 — 3개 앱 통합: 연구소 연동 + 서바이벌 흡수 (예상 5~8일) 【비전 2】

**통합 전략 (2026-07-25 앱 분석으로 확정)** — 세 앱 성격이 근본적으로 달라 방식을 나눈다:

| 앱 | 스택/인증/데이터 | 통합 방식 | 이유 |
|---|---|---|---|
| **연구소** | Next.js / 이메일·비번 / 자체 DB(구 스택) | **별도 앱 유지 + SSO·데이터 연동** (2a~2d) | 프레임워크 다름·자체 인증/DB → 흡수는 재작성이라 비효율·위험. 코드 이식 X |
| **서바이벌** | React+Vite+TS+Tailwind / 인증 없음 / IndexedDB·localStorage | **자리·역할만 아지트 모듈로 흡수** (2e) | 자체 명단 중복을 없애고 나머지는 원본 보관 후 미래 활동에서 선별 활용 |

> 핵심: "무조건 다 합치기/링크"가 아니라 **앱마다 최적 방식** — 연구소는 연동, 서바이벌은 흡수.

### 2a. 연구소를 통합 스택 + 구글 인증으로 (SSO 선행 기반) 【신규】
현재 연구소는 **구 스택(Kong 8000) + 이메일/비번 인증**, 아지트는 **새 스택 + 구글 OAuth**.
SSO가 되려면 둘을 한 스택·한 인증으로 통일해야 한다 (JWT 시크릿은 이미 공유 = 기반 있음).
- [~] 연구소 앱을 **새 통합 스택(Kong 8100)** 바라보는 `/lab` 병행 컨테이너 기동·공개 200 검증 완료. 기존 `helper.`는 롤백용으로 유지하며, 2026-08-13 지문 비교에서는 보존 대상
      두 교사 자료가 같았으므로 전체 덮어쓰기는 하지 않고, 컷오버 직전 같은 범위의 지문을 다시 비교해 달라진 행만 선별 동기화
- [x] 교사 인증 통일: 통합 `/lab`은 아지트의 구글 OAuth 루트 쿠키와 실제 승인 교사 판정을 사용한다. 구 `helper.`의
      이메일/비밀번호 인증은 롤백용으로 유지한다. 기존 연구소 교사 계정 ↔ 아지트 계정 UUID 매핑은 유승현·최원진 완료.
- [ ] dev 검증 후 운영 전환, **롤백 경로 확보**(연구소 env를 구 스택으로 되돌리기)

### 2b. 로그인 통합 (SSO)
- [x] 세션 저장 방식 정렬: 아지트 브라우저와 연구소 SSR이 `sb-agit-auth-token` 루트 쿠키를 공유한다.
- [x] 교사: 연구소 첫 진입 시 실제 승인 상태를 확인하고 `teacher_profiles`를 멱등 준비한다.
- [x] 아지트 교사 상단 `글쓰기 연구소` → 같은 탭의 `/lab/dashboard` 진입 UI 연결.

### 2c. 학생 JWT 자동 입장 (QR 입장 방식 개편)
현재 연구소는 QR/링크로 방에 들어가 **번호+이름을 직접 입력**하는 방식.
이를 **아지트 JWT로 인증된 학생이면 명단 자동 매칭 → 바로 활동 참여**로 바꾼다.
이 매핑이 2c(학생별 결과물 불러오기)의 선행 조건이다.

- [~] 학생 신원 매핑: `writing_helper.class_students.agit_student_id`·`classes.agit_class_id`의 확정 27명 연결을
      과거 호환에 사용하고, 신규 활동은 아지트 학급·학생 ID를 직접 저장한다. 미확정 학생은 수동 확인 대기.
- [x] `student_sessions.agit_student_id` nullable FK와 방별 중복 방지·학생별 조회 인덱스를 추가했다. 매핑 학급의
      과거 252건 중 이름·번호가 확정 명단과 모두 일치한 234건만 backfill했고, 나머지 18건은 추정하지 않았다.
- [x] 연구소 방 입장 흐름 수정: JWT 메타데이터 값을 신뢰하지 않고 실제 DB가 확인한
      `students.auth_id=auth.uid()` 연결과 학급 매핑이 맞으면 번호·이름 입력을 생략해 세션 자동 생성/재접속
- [x] 구 `helper.`의 QR+번호·이름 수동 입장은 롤백용으로 유지하고, 통합 `/lab`은 아지트 학생 로그인을 요구한다.
- [x] 아지트 학급 ↔ 연구소 room 직접 연결: 교사가 아지트 학급을 선택하면 명단 복사 없이 `agit_class_id`로
      활동을 만들고, 학급 상세는 소유권 1회 확인 뒤 현재 학생과 활동을 병렬 조회한다.
- [x] 학생 홈에 매니페스트 기반 `글쓰기 연구소` 메뉴를 두고, 메뉴를 열 때만 `get_my_lab_activities_v1` 한 번으로
      우리 반 활성·미만료 활동을 최근 20개씩 읽어 시작·이어하기·결과 보기로 `/lab/room/{roomId}`에 연결한다.
- [x] 통합 `/lab` 교사 활동 화면에서는 입장용 QR·단축주소 생성을 중단하고 학생 대시보드 자동 표시를 안내한다.
      구 `helper.`의 입장 QR과 학생 개인 결과 공유 QR은 각각 롤백·결과 공유 용도로 유지한다.

### 2d. 활동 결과물 연동 기반 (데이터 계층)
현재 신규 운영 활동 4종의 결과물을 글쓰기에서 쓸 수 있도록 RPC 계층을 만든다.
과거 `한자 활용 문장` 결과는 기존 기록을 깨지 않기 위한 읽기 호환 대상으로만 유지하며 현재 기능으로 소개하지 않는다.
(활동별 저장 위치 확인 완료 — 2026-07-17 조사)

| 활동 | 결과물 위치 | 글쓰기에서의 용도 |
|---|---|---|
| 개요짜기 | 신규=`student_sessions.answers`, 과거 호환=`outline_queue.result` | **학생이 만든 처음·가운데·끝 개요** → 학생 에디터로 불러오기 |
| 질문만들기 | 세션 제출물 `selections[].remixedQuestion` | 학생은 본인이 만든 질문을 에디터로, 교사는 학급 질문을 미션 가이드로 가져오기 |
| 좋은 질문 고르기 | 세션 제출물의 학생 선택 질문 ID + 방의 질문 원문 | 학생이 고른 질문 → 에디터의 생각거리로 삽입 |
| 한줄쓰기 | `one_line_entries.content` | 학생 한 줄 → 에디터 삽입 / 학급 모음 참고 |
| 한자 활용 문장 (과거 호환) | `student_sessions.submission.contents` | 과거 학생 기록 조회·불러오기만 유지 |

- [x] 표준 결과물 인터페이스: `(스키마버전, 활동유형, 활동제목, 완료일, 본문조각[], 메타)` 형태 통일.
      활동 매니페스트가 완료 시 표준 결과를 만들고 공용 RPC는 그 값만 읽어, 신규 활동마다 조회 RPC 분기를 추가하지 않는다.
- [x] 신규 운영은 `outline_builder`·`question_generator`·`question_voting`·`one_line_share` 4종으로 고정하고,
      `hanja_writing`은 과거 방·결과 읽기 호환용으로만 허용 목록에 남겼다. 각 매니페스트에는 `schemaVersion: 1`과 결과 종류를 등록했다. `word_game`·
      과학·도덕 별도 활동의 실행 코드는 제거했지만 기존 DB 테이블과 자료는 삭제하지 않았다.
- [x] 학생용 RPC: `get_my_lab_results_v1()` — `public.auth_student_id()`와 세션의 `agit_student_id`가 같은 본인 결과만,
      안정 정렬·커서·최대 50건 상한 적용
- [x] 교사용 질문 결과 조회 RPC: `get_teacher_question_voting_ranking_v1` — 좋은 질문 고르기 결과를
      미션 생성용으로 조회하고 득표순으로 선택
- [x] 미션↔연구소 활동(room) 매핑 테이블 — 과제별 개요·고른 질문 활동을 하나씩 연결하고 학생 본인 결과를 우선 조회
- [x] 별도 `학생 글쓰기 지도` 탭 계획은 교사 상단 **글쓰기 연구소**(`/lab`) 통합 메뉴와 미션 생성의
      `연구소 좋은 질문 불러오기` 흐름으로 대체. 연구소 활동 운영은 `/lab`, 과제 연결은 선생님 과제에서 담당한다.
- [x] `설정 → 글쓰기 창 관리`에는 연구소 운영 기능을 넣지 않고, **연구소 결과물 불러오기 ON/OFF와 학생 미리보기만**
      추가한다. OFF여도 연구소 활동과 결과 데이터는 지우지 않고 학생 글쓰기 창의 불러오기 진입만 숨긴다.
- [x] 검증: 확정 연결된 과거 결과 206건을 보강하고 실제 학생 인증 문맥에서 본인 결과 RPC 조회 확인

### 2e. 서바이벌 — 전체 흡수가 아니라 **쓸모 있는 것만 골라 흡수** (2026-08-17 재조사로 방향 수정)

> ⚠️ **2026-07-25 분석의 전제 세 가지가 틀렸다.** 실제 코드를 다시 읽고 바로잡는다.
>
> | | 옛 분석 | **실제 (2026-08-17 확인)** |
> |---|---|---|
> | 언어 | "같은 React" | **TypeScript** (아지트는 JS 전용, tsconfig 없음) |
> | 스타일 | 언급 없음 | **Tailwind** (아지트는 일반 CSS) |
> | 저장 | "localStorage뿐, 정리할 게 없음" | **IndexedDB(Dexie)** — `classes`·`students`·`history` 3개 표 |
> | 규모 | 23파일 1MB | 18파일 **10,416줄** (한 화면 최대 1,478줄) |
>
> 특히 **자체 학급·학생 명단을 들고 있다**는 점이 핵심이다. 그대로 흡수하면 명단이 두 벌이 된다.
> "정리할 게 없어 흡수가 가장 효율적"이라는 근거가 사라졌으므로 **13종 전체 이식 계획을 폐기**하고,
> 아지트에 실제로 보탬이 되는 것만 두 갈래로 골라 가져온다. 예상 기간 5~8일도 다시 잡아야 한다.

#### 2e-1. 나머지 자료 → 원본 백업 후 미래 경쟁형 활동의 재료로 보관
문제팩과 활동 기록은 지금 어휘의 탑이나 아지트 게임으로 바로 옮기지 않는다. 기존 앱의 전체 JSON을
통합 DB에 원본 그대로 보관하고, 향후 경쟁형 활동을 실제로 설계할 때 콘텐츠만 검수해 가져온다.

| 팩 | 문항 | 원본 | 향후 활용 참고 |
|---|---:|---|---|
| **속담 기본팩** | **85** | `src/data/proverbs.json` | `quiz`(초성 빈칸)→`clozeInput`, `answer`→`word`, `meaning`→`definition` |
| 사자성어 | 100 | `idioms.json`·`idiom_initials.json`·`idiom_meaning_quiz.json` | 3파일이 같은 100개의 형태별 분산 → 병합 필요. `초성` 형태 1종 추가 |

- [x] 기존 페이지에서 IndexedDB 학급·학생·기록과 허용된 localStorage 활동 자료를 한 JSON으로 내려받기
- [x] 아지트가 파일 지문·버전·요약·원본 payload를 최대 5MB 범위에서 통합 DB에 보관
- [x] 선택한 기존 학급의 자리·역할 설정만 현재 아지트 학급 학생과 고유 이름으로 안전하게 연결
- [~] 속담 85개·사자성어 100개를 공통 카탈로그로 병합하고 교육과정·제공 학년 분리, 시범 40개 선정,
      3·4학년 미리 만나기 20개 편집 검수까지 완료. 교사 확인·나머지 20개 검수·일일 미션 연결은 현재
      위치의 계획에서 계속 진행

#### 2e-2. 자리배치·역할배정 → 교사 `수업 도구` (완료)
아지트에 **슬롯이 이미 있다** — `TeachingToolsHub`와 매니페스트 `part: 'tool'`(선택할 때만 지연 로딩).
지금 등록된 도구가 `쌤링크` 하나뿐이라 허브가 비어 있다. 이 둘은 **점수·포인트가 없어**
보상 규칙을 건드리지 않고, 필요한 것은 학급·학생 명단뿐인데 아지트가 이미 갖고 있다.

- [x] 자리배치와 역할배정을 한 모듈로 옮기고 자리 모양·고정석·상하좌우 이웃 금지·남녀 혼합·최근 자리/이웃 회피 유지
- [x] 서바이벌의 IndexedDB 명단 대신 아지트 학급·학생 명부 사용(빠른 임시 입력은 저장하지 않음)
- [x] 아지트 저장소 기준에 맞춰 JS+일반 CSS로 구현하고 선택할 때만 지연 로딩
- [x] 설정·최대 50개 결과 이력을 교사 담당 학급 권한 RPC로 저장하고 직접 테이블 접근 차단
- [x] 1,000명 성능 매니페스트, 권한 스모크, 엔진 단위 검사와 실제 브라우저 애니메이션 검증

#### 2e-3. 기존 서바이벌 앱 전환·종료
협동스피드퀴즈·토너먼트·학급미션·아이디엄배틀 등은 **아지트의 3대 기둥에 직접 기여하지 않고**
이식 비용만 크다. 기존 주소는 3개월 동안 이전 안내와 브라우저별 백업만 제공한 뒤 종료한다.

- [x] 모든 기존 경로를 아지트 이동 안내·가입 필요·종료일·백업 버튼이 있는 한 화면으로 전환
- [x] 여러 기기·브라우저마다 따로 백업해야 한다는 안내와 아지트 수업 도구 바로가기 제공
- [ ] Docker 8080과 중복되고 연구소 3001번과 충돌하는 구 `com.jarvis.survival` LaunchAgent 영구 비활성화
- [ ] **2026-09-20까지** 안내·백업을 제공한 뒤 기존 앱 컨테이너와 자동 배포를 중지하고 `survival.`은 아지트 도구 URL로 301 리다이렉트
- [ ] 향후 게임 점수를 포인트로 연동할 때는 **보상 계산을 반드시 서버가 미션·설정에서 읽는다**
      (2026-08-17 결정 기록 참조 — 클라이언트가 보낸 값을 신뢰하면 그날 막은 취약점이 재현된다)

**완료 기준**: 한 번 로그인으로 연구소까지 사용 / 연구소 결과물을 아지트에서 조회 /
서바이벌의 **자리·역할 배치가 수업 도구로 동작**하고 기존 자료의 원본 백업 경로를 제공 /
2026-09-20까지 안내·백업 제공 후 기존 서바이벌 앱 종료와 301 전환.
(2026-08-23 수정: 문제팩 즉시 이식보다 전체 원본을 보존하고 미래 경쟁형 활동을 만들 때 선별 사용한다.)

## Stage 3 — 기존 기능 파트별 모듈화 (모듈당 0.5~1일, 점진) 【비전 3】

> **하나로 본 계획 (2026-08-07 정리 — 리팩토링 전 계획 + 진행 현황 + 오늘 분석 통합)**
> - **원래 계획(리팩토링 전)**: 기존 기능(과제·글쓰기·게임·커뮤니티)을 3대 기둥 기준으로 재평가해 모듈
>   시스템 위로 옮기고, 기여 안 하는 기능은 걷어낸다. 새 기능은 코어를 안 건드리고 모듈 하나만
>   추가하면 되게 만드는 게 목표다.
> - **지금까지 한 것**: 모듈 레지스트리·아지트 놀이터 도입(3a, 2026-07-26~08-06 완료) + 드래곤·어휘의
>   탑·아지트온클래스·친구 아지트·회의 안건 만들기 5개 이전(3b, 표 완료). 드래곤과 어휘의 탑 모두
>   교사·학생 표준 진입 계약으로 완전 이전됐다(어휘의 탑 2026-08-07 완료).
> - **통합 전 분석 결과**: ①모듈화가 덜 끝났던 어휘의 탑 어댑터와 README 8개는 2026-08-07 해결했다.
>   ②애초에 모듈화 대상이 아니었던 과제 파이프라인·교사 확인 화면이 계속 커지는 문제는 남아 있다.
>   다음 주 통합이 정확히 이 두 곳을 건드린다(2d는 학생 에디터에 얹힘) → 아래 3c 3·4번으로 관리한다.
> - **다음 결정**: 3c 남은 일 vs 나의 아지트 디자인 3차 vs Stage 2 착수, 순서를 다음 세션에서 정한다.

### 3a. 기반 공사 (모듈 시스템)
- [x] 모듈 레지스트리 도입 (2026-07-26, `32f1f53`) — `src/modules/` 구조, 매니페스트 규격·검증·파트 분류
- [x] 교사 `학급 운영 / 학급 발자국` 카드 확장 레지스트리 도입 (2026-08-04, `e80cff8`) —
      모듈 manifest의 카드 기여분을 공통 호스트가 기본·전체화면·확대 모달에 반영하고 계약 오류를 개발 중 검증
- [x] 학급별 `enabled_modules` 컬럼 추가 + 읽기/저장 훅 (NULL=기존 동작 보존). 개별 플래그 흡수는 모듈 이전 시 순차 진행
- [x] 교사용 모듈 on/off UI — 놀이터 상단 공통 패널에서 시작(2026-07-26) 후 드래곤·어휘의 탑 각 관리 카드에 통합(2026-07-27). 학급별 토글→`enabled_modules` 저장
- [x] **아지트 놀이터 도입** (2026-07-26, `cfceee9`, 후속 `549e784`) — 포인트 콘텐츠를 놀이터 하나로 묶어 메뉴 고정.
      드래곤·어휘의탑이 그 안에서 열림. 신규 포인트 활동은 놀이터에 등록만 하면 됨(Stage 4d와 직결).
      OFF 실시간 반영·조회 전 fail-closed 보강(`b9e95cf`, 2026-07-26 운영 배포 완료).
      - [x] 학생 놀이터를 학생 홈과 같은 카드 체계의 **내 포인트 지갑 + 포인트 모으기 + 포인트 쓰기** 허브로 개편.
        잔액은 홈 bootstrap 값을 재사용하고 최근 내역은 학생이 펼칠 때만 본인 범위 최대 20건을 조회하며,
        신규 콘텐츠는 `playground.economy`·표시 문구만 선언해 자동 분류한다. (2026-08-13)
- [~] 메뉴·대시보드가 레지스트리에서 렌더되도록 전환 — 드래곤·어휘의 탑은
      `useEnabledModules`와 각 놀이터 관리 카드의 ON/OFF로 통일(`8c481a5`, 2026-07-27 카드 통합), 친구 아지트는 고정 모듈로 등록(`b1acef5`).
      아지트온클래스·아이디어마켓의 학생/교사 진입점도 레지스트리로 전환(`a17efd3`).
      - [x] 포인트·놀이 신규 모듈은 `teacherEntry` 관리 카드와 `studentEntry` 놀이터 진입점을 매니페스트에서 자동 렌더링.
        각 화면은 열 때만 지연 로딩하고 독립 오류 경계를 사용. 기존 드래곤·어휘탑 관리는 운영 DB 호환 어댑터로 격리 (2026-07-29)
      - [x] 드래곤·어휘탑 교사 화면을 각 manifest의 `teacherEntry`로 연결하고 게임별로 필터링해 지연 로딩 (2026-07-29).
        운영 DB 로직은 회귀 방지를 위해 공유 호환 어댑터에 보존
      - [x] 학생 화면 모듈 설정은 Realtime·포커스와 함께 보이는 탭에서 10초 재확인해 운영 Realtime 누락에도 최종 상태로 수렴 (2026-07-29)
      - [x] 교사 포인트·놀이를 왼쪽 모듈 메뉴와 오른쪽 전체 현황·선택 모듈 관리로 전환하고 학생 놀이터 미리보기 제공 (2026-07-29)
      - [x] 선택 콘텐츠의 학생 미리보기를 기본 접힘으로 바꾸고 공통 헤더·좌측 메뉴·우측 본문 여백을 축소했다.
        작가 수호룡의 시즌·성장 현황과 어휘의 탑 운영 요약·설정을 정보 손실 없이 낮은 패널로 재배치해 일반 PC에서
        우측 본문을 크게 내리지 않고 핵심 정보를 확인하도록 정리했다. (2026-08-16)
      - [x] 교사 설정의 학급 관리·서비스 설정을 한 화면으로 통합하고 사용 안내 메뉴 제거, `part: tool` 기반 수업 도구 런처 추가 (2026-07-29)
      - [x] 첫 수업 도구로 쌤링크를 manifest 등록하고 `샘링크.kr` 서비스 화면 임베드·새 창 대체 경로 연결 (2026-07-29)
      - [x] 수업 도구를 PC 좌측 세로 메뉴·우측 즉시 실행 구조로 전환하고 모바일 가로 메뉴 대응 (2026-07-29)
      - [x] 쌤링크 전용 임베드 모드로 외부 앱형 프레임과 사업자 푸터를 걷어내고 통합형 본문 화면 제공 (2026-07-29)
      - [x] 아지트 CSP `frame-src`에 쌤링크 운영 origin(`https://xn--9y2br3k43n.kr`)만 복구하고 Google 로그인과
        쌤링크 외 출처가 추가되지 않도록 운영 Caddy 헤더 회귀 검사 추가 (`2026-08-13`)
      - [x] 포인트·놀이 탭의 중복 상단 제목을 제거해 다른 상위 탭과 콘텐츠 시작선 통일 (2026-07-29)
      - [x] 설정을 학급 관리·AI 피드백·평어 도우미·글쓰기 창 관리의 좌측 메뉴 허브로 재구성 (2026-07-29)
      - [x] 학급 관리를 1~5개용 칩 전환·컴팩트 패널로 축소하고 학급 간 글 공유 확장 슬롯 마련 (2026-07-29)
      - [x] 교사 관리 본문의 모듈 이전: **드래곤 완료** — 전용 시즌·학급 성장 대시보드로 이전(2026-08-06).
        **어휘의 탑 완료** — 전용 `TeacherManager`·`StudentEntry`로 이전하고 레거시 관리자 제거(2026-08-07)

### 3b. 파트별 모듈 이전 (하나씩, 각각 검증 후 커밋)
3대 기둥 대조 결과 — 유지/정리 후보를 함께 표기 (최종 삭제 여부는 3c에서 선생님 확정):
| 파트 | 모듈 | 3기둥 대조 | 이전 |
|---|---|---|---|
| 게임·동기부여 | 드래곤 기르기 | ✅ 유지 (기둥3) | [x] 2026-07-26 (`48dd818`, 청크 분리) |
| 게임·동기부여 | 어휘의 탑 | ✅ 유지 (기둥3, 포인트 보상) | [x] 2026-07-26 (`73aeda9`, ON/OFF 일원화 `8c481a5`). 1412줄 분할은 미실시 |
| 학급 커뮤니티 | 아지트온클래스 | 🗑️ 삭제 완료 (2026-08-09) | [x] 2026-07-26 모듈 분리, 2026-07-27 `available:false` 격리 → 2026-08-09 재활성화 계획 없어 완전 삭제(코드·DB) |
| 학급 커뮤니티 | 친구 아지트 | ✅ 고정 유지 (사용자 확정) | [x] 2026-07-26 (`a04f55e`, 고정 `b1acef5`, 학생별 캐시 `a0e27ab`, 안건 후보 `9503d35`, 조회 루프 `4ca5862`) |
| 글쓰기 확장 | 회의 안건 만들기(구 아이디어마켓) | ✅ 유지 (기둥1·2, `meeting` 장르 미션·미션 생성으로 활성화) | [x] 2026-07-26 (`a17efd3`, 미션 관리 `8047133`, 장르 `c894b41`, 학생 작성 통합 `77ab8c5`) |

**모듈 이전 수용 기준 (전 모듈 공통)**
1. 코어 셸과의 접점은 레지스트리 등록 + 포인트 API 호출뿐
2. 독립 lazy chunk로 분리 (빌드 결과에서 확인)
3. 선택 모듈은 플래그 OFF 시 메뉴에 안 보이고 코드도 로드되지 않음. 고정·미션 활성형 모듈은 토글에 노출하지 않음
4. 이전 전후 동작 동일 (기능 개선은 이전과 같은 커밋에서 하지 않는다)

### 3c. 남은 정리 — 미사용 기능 정리 + 통합 전 우선순위 (2026-08-07 하나로 합침)
3대 기둥에 기여하지 않는 기능을 걷어내는 원래 계획(방학 목표)과, Stage 2 착수 전 코드 분석에서 새로
나온 항목을 하나의 우선순위로 합쳤다. **모듈화(3b) → 기본 OFF → 확정 → 삭제** 순서는 유지한다.

**완료**
- [x] 삭제/유지 최종 확정 (2026-07-26) — 아지트온클래스는 UI 완전 숨김 보관, 아이디어마켓은 글쓰기
  입력미션으로 유지 (`a17efd3`). 아지트온클래스에 공지 등 교사 지도용 요소가 섞여 있으면 그 부분만 코어로 흡수할 것.

**남은 것 (우선순위 순 — 1·2가 Stage 2 선행조건)**
1. [x] **모듈 계약 마무리** (2026-08-07): 어휘의 탑을 표준 계약
   (`manifest.js`+`TeacherManager`+`StudentEntry`)으로 전환하고 1,266줄 레거시 관리자를 제거했다.
   나머지 8개 모듈(드래곤 제외)에 README를 추가했다. 학생 대시보드의 전용 진입·설정 조회도 제거했다.
2. [x] **QA 전용 장치를 핫패스에서 분리** (2026-08-07): 일반 학생 `get_my_title_status()`는
   `student_title_test_overrides`를 조회하거나 반환하지 않는다. QA 고정값은 권한을 검증하는 교사 수호룡
   대시보드에서만 확인한다. 일반 친구 목록의 시험 필드도 제거했다(마이그레이션 `20260924`~`25`).
3. [~] **교사용 "글 하나 확인" 공용 부품**: 학생 쪽엔 `MyPostEngagementPanel`(유형 무관 제출·의견·댓글
   통일)이 있는데 교사 쪽엔 대응 부품이 없다. `ArchiveManager`(711줄)·`PostDetailViewer`(802줄)·
   `SubmissionStatusModal`(578줄) 등 40개 파일·11,032줄짜리 `components/teacher/`가 각자 따로 진화 중.
   - [x] 2026-08-08 데이터 기반 1차: 글·학생·과제·반응·댓글을 `get_teacher_post_detail_v1` 한 번으로 읽고,
     제출 목록에서 반응·댓글 본문을 제거했다. 공용 시각 컴포넌트 추출은 다음 교사 글 화면 수정 때 진행한다.
4. [x] **과제 승인 로직을 RPC로** (2026-08-08, 운영 적용 완료): `20261005`에서 글 상태·포인트·로그를
   한 트랜잭션으로 묶었다. 서버가 글에서 학생·학급·과제·보상을 다시 계산하고 중복 승인/회수를 차단한다.
   개별·일괄 승인과 회수, 여러 학생 포인트 지급도 부분 성공이 없도록 RPC 한 번으로 처리한다. 이어 공용
   `point_engine_apply()`+기능별 RPC 구조를 만들고 교사 포인트 첫 화면 2회→1회, 회의 결정 최대 3회→1회로 줄였다.
5. [ ] 대형 파일 분할: 현재 1,000줄+인 `ActivityReport`, `IdeaMarketManager`, `useMissionManager`,
   `StudentWriting`, 학생·교사 독서록 화면 6개를 기능 경계대로 분할한다. 파일이 크다는 이유만으로 동작 중인 코드를
   먼저 건드리지는 않고, 해당 기능을 수정할 때 공용 부품/API 경계부터 뺀다.
   - 교사 독서록(`TeacherReadingLogManager`, 957줄)은 그중 약 160줄이 인라인 `<style>` 블록이다(2026-08-10 확인).
     손댈 때 `teacherReadingLog.css`로 먼저 빼면 코드 자체는 800줄 아래로 떨어진다.
6. [x] **삭제 확정 기능: 아지트온클래스 완전 삭제** (2026-08-09) — 굳이 되살릴 계획이 없어 "숨김 보관"에서
   완전 삭제로 재결정. 모듈 폴더 3개 파일·전용 훅(`useClassAgitClass.js`)·registry 등록·
   `DashboardMenu`/`StudentDashboard`의 진입점을 제거했다. DB는 `20261016_remove_agit_on_class.sql`로
   `agit_honor_roll` 테이블과 `classes.agit_settings` 컬럼을 삭제하고 `get_student_home_bootstrap_v1()`에서
   해당 필드를 뺐다(다른 스키마의 무관한 `writing_helper.classes.agit_class_id`는 그대로 둠). ROLLBACK
   검증 후 운영 적용(117/117), 아키텍처·보안 테스트 포함 전체 자동 테스트·빌드 통과.
7. [x] 죽은 코드 청소: 미사용 컴포넌트·훅·유틸·의존성, 도달 불가 분기 제거
   - [x] 아지트온클래스(`available:false`라 마운트 자체가 안 됨)의 `postgres_changes` 구독 코드 제거
     (2026-08-07) — 이후 2026-08-09에 모듈 자체가 완전 삭제되며 이 항목도 자연히 해소.
   - [x] 안 쓰는 이미지 정리: 이전 랜딩 PNG와 `public/classroom_bg.png` 합계 약 2.7MB(코드에서 참조 없음)
     제거. `public/agit_hideout_bg.png`(800KB)는 아지트온클래스 완전 삭제(6번)와 함께 제거.
   - [x] **2026-08-08 감사 1차 정리**: `FeaturesModal`과 연결이 끊긴 친구 발자국·포인트 카드/요약 4개
     664줄, 호출되지 않는 함수·훅, 무효 lint 의존성 2개, Git에 추적된 Supabase/TestSprite 임시 파일 9개를 제거했다.
     옛 보안 스크립트 2개는 실행할 때 운영 URL·공개 키를 받게 바꿨고 불필요한 공개 export를 내부 구현으로 좁혔다.
   - [x] **DB 2차 후보 분석·파일 작성**: 운영 DB에서 참조 함수를 전수 확인해 현재 칭호·교사 통계가 쓰는
     `writing_activity_events`와 기록 함수는 유지한다. 친구 카드 전용 RPC 2개, 구형 본인 RPC·스냅샷 갱신 함수,
     일일 스냅샷 5,620건과 설정표만 제거하는 `20261009`를 새로 만들고 전체 ROLLBACK 검증했다.
     2026-08-08 사용자 승인 후 운영 적용하고 적용 후 ROLLBACK 스모크도 통과했다.
8. [x] 린트 잔여 정리: `exhaustive-deps` 경고 등 (Stage 0에서 미룬 것) — 2026-08-07 재확인 결과
   `npx eslint . --no-cache` 0경고·0오류로 **이미 해결됨**. 계획에 미완료로 남아있던 걸 정정.
9. [x] 정리 전후 빌드·핵심 흐름 자동 검증: ESLint, 프로덕션 빌드, 전체 자동 테스트 81개,
   마이그레이션 ROLLBACK 스모크, `git diff --check` 통과. 실계정 E2E는 별도 인수 검사표에서 진행한다.
10. [ ] **가벼움 2차 점검 (2026-08-07)**: 폴링·코드 스플리팅·이미지 포맷은 이미 잘 되어 있음을
    재확인(아래 "굳이 손 안 대도 되는 것" 참고). 새로 찾은 두 가지:
    - [x] **랜딩페이지 히어로 이미지 최적화** (2026-08-08) — 화면은 기존 수호룡 이미지를 유지하되
      1.7MB PNG 대신 약 282KB JPEG를 실제로 사용한다. 미사용 PNG 파일 삭제만 7번 청소 항목에 남겼다.
    - [x] **죽은 Vercel 분석 SDK·호스팅 설정 제거** (2026-08-08~12) — `@vercel/analytics`·
      `@vercel/speed-insights`의 App 마운트와 패키지 의존성을 제거하고, 맥미니 단일 운영 확정 뒤
      루트 `vercel.json`과 이중 CSP 검증도 삭제했다.
11. [x] **Realtime 구독 최소화 완료 (2026-08-08)**: 학생 과제 목록·글쓰기·알림, 어휘의 탑,
    아지트온클래스의 `postgres_changes`를 모두 제거했다. 현재 `src`의 `.channel()`·`postgres_changes`는 0건이다.
    학생은 4~6분 분산 홈 동기화와 화면 복귀, 교사는 진입·화면 복귀·사용자 동작 응답으로 갱신한다.
    과제 글 반려·승인 두 신호만 2026-08-25 결정의 12초 코어 폴링 예외를 사용한다.
    새 기능도 기본적으로 Realtime을 쓰지 않으며 예외는 성능 하네스에 연결·이벤트 예산을 먼저 기록한다.

**완료 기준**: 메뉴·코드·DB에서 3대 기둥 외 기능이 사라지거나 기본 OFF, 9개 모듈 계약이 일관, 빌드 크기 감소 확인.

**굳이 손 안 대도 되는 것**(과도한 리팩터 방지): 드래곤 모듈(9파일 2,233줄·마이그레이션 13개)은 기능
규모 대비 적정, 마이그레이션 109개·ESLint 0경고는 정상 범위, 자율 글쓰기(독서록·일기) 모듈화는 이미 잘돼 있음.

**인프라(git 밖, `~/agit-supabase/docker-compose.yml`)**: 아지트가 이 맥미니의 우선 서비스이고 나머지
앱(자비스·연구소·샘링크·서바이벌)은 Stage 2에서 아지트로 흡수/연동될 예정이라, `agit-realtime`에
`mem_reservation 512m / mem_limit 1g / cpu_shares 2048`(기본 1024의 2배)을 걸어 다른 앱에 밀리지
않게 했다(2026-08-07, 무중단 적용). 통합이 진행되면서 다른 컨테이너들도 정리되면 이 우선순위 폭을
다시 검토한다.

## Stage 4 — 글쓰기 확장 모듈 (모듈당 2~4일) 【비전 4】

코어 글쓰기 파이프라인은 수정하지 않고, **확장 포인트(슬롯)**를 통해서만 연결한다.

### 4a. 글쓰기 확장 포인트 정의 (1회성 기반 공사)
- [x] 에디터 확장 지점을 도구 매니페스트와 `surface` 계약으로 정의하고 참고 자료·맞춤법·연구소 결과 도구를
      지연 연결. 등록 도구가 없으면 기존 편집 흐름에 영향을 주지 않는다.

### 4b. 생각 정리 모듈 (연구소 결과물 불러오기 UI)
- [x] **학생**: `연구소 결과 불러오기` 지연 도구에서 내 개요·질문·고른 질문·한 줄·한자 문장을 선택해
      일반 글 본문에 삽입한다. 구조화 장르는 데이터 불일치를 막기 위해 복사만 제공한다.
- [x] **교사 글쓰기 창 관리**: `연구소 결과 불러오기`의 학급별 ON/OFF와 학생 미리보기만 제공.
      현재 `writing_editor_settings.enabled_tools`와 글쓰기 도구 매니페스트 계약을 사용하고 연구소 운영 설정은 넣지 않는다.
- [x] **교사**: 미션 생성 폼의 `연구소 좋은 질문 불러오기`에서 `get_teacher_question_voting_ranking_v1`로
      좋은 질문 고르기 결과를 불러와 `guide_questions`에 선택 삽입
- [ ] 미션에 연구소 활동이 연결된 경우 "연구소에서 생각 정리하기 🧪" 바로가기 (→ `/lab` 자동 입장 → 복귀)

### 4c. 학생 자율 글쓰기·글쓰기 발자국·친구 정보창 【방학 우선순위 1】

**확정 범위**: 학생 자율 글쓰기는 **독서록 하나로 시작**한다. 일기·자유글·자율 시쓰기는 넣지 않고,
선생님 과제는 기존 자유글·시·회의 안건 등 장르 미션으로 계속 확장한다.

#### 4c-5. 이웃 아지트 【선택 학급 제한 공개 DB 반영 — 학급 선택 전 2026-08-31】

`학급 관리`에 탭을 추가하지 않고 교사·학생에게 각각 독립된 **이웃 아지트** 메뉴를 제공한다. 한 교사가 만든
공간에 초대로 최대 네 학급이 참여하며, 학생 흐름은 `글 읽기 → 글 쓰기 → 댓글 → 간직하기`로 제한한다.
상세 권한·데이터·RPC·이번 주 구현 순서는 [`NEIGHBOR_AGIT_PLAN.md`](NEIGHBOR_AGIT_PLAN.md)를 정본으로 사용한다.

- [x] 현재 학급 관리에 잘못 표시된 미사용 `invite_code`·코드 복사 UI 제거 (2026-07-29). 학생별 로그인 코드는 학생 명단에서만 유지
- [x] 등록형 독립 모듈의 교사 메뉴를 `학급운영도구` 오른쪽 `이웃 아지트(제작 중)`으로 이동하고 Beta 배지·설정의 중복 진입점 제거 (2026-08-31)
- [x] 준비 중 화면에 교사 연결·글 확인·학생 이웃 피드·학급별 사용 관리의 업데이트 개요 표시 (2026-08-30)
- [x] 두 학급 연결안을 호스트가 여는 최대 네 학급 초대 공간으로 확장하고, 학급당 활성 공간 1개 MVP 결정 (2026-08-30)
- [x] `internal` 관리자 선공개 → 사용자 명시 승인 → 모든 승인 교사 `public_beta` 공개와 학생 기본 OFF 계약 확정 (2026-08-30)
- [x] 공개 단계·공간당 최대 4학급·학급당 활성 공간 1개를 모듈 정책 한 곳에 고정하고, `internal`의
      일반 교사 준비 화면·학생 진입점 부재와 최종 학생 노출 조건 회귀 검사 추가 (2026-08-30)
- [x] Step 1 전용 표 9개·복합 학급/학생 외래키·RLS·직접 권한 회수·호스트/최대 학급 제약을
      `20261199`로 작성하고 실제 운영 스키마 롤백 검증 완료. 운영 DB에는 미적용 상태 유지 (2026-08-30)
- [x] Step 2 공간 생성·호스트 관리, 24시간 일회성 SHA-256 해시 초대키, 10분 5회/30초 실패 제한,
      게스트 신청·승인·퇴장·종료·호스트 이전 RPC와 실제 관리자·교사 역할 흐름 롤백 검증 완료.
      운영 DB와 화면에는 미적용 상태 유지 (2026-08-30)
- [x] Step 3 기존 `student_posts` 원본 공유 신청·본인 회수, 원학급 교사 공개/반려/회수, 참여 교사
      긴급 숨김과 원학급 교사 복원 RPC 구현. 원글 회수 시 공개 중단, 공개 뒤 수정 시 재검토 대기 전환,
      기존 글·댓글·반응 RLS 불변과 실제 교사·학생 역할 흐름을 롤백 검증 (2026-08-30, 운영 미적용)
- [x] Step 4 학생 홈 기존 bootstrap에 접근 가능·공간·새 글 수만 병합하고, 학급 모듈 ON과 서버 접근 신호를
      모두 통과할 때만 `이웃 아지트` 카드·직접 경로를 허용. 교사 학급별 학생 스위치 RPC와 연결 (2026-08-30, 운영 미적용)
- [x] Step 5 한 학생·한 글·한 줄 댓글, 공감 1종, 다른 학생 글 간직하기 참조와 학생 전체/교사 학급별 집계를
      전용 RPC로 연결. 원글 회수·공간 종료 뒤 본문 차단과 활동 원장 보존을 역할 스모크로 확인 (2026-08-30, 운영 미적용)
- [x] Step 6 관리자 `운영 → 기능 공개` 탭에 현황·2~4학급 내부 시험 공간·교사/학생 읽기 전용 미리보기·
      여섯 항목 인수 점검·긴급 중지·2단계 전체 교사 공개를 구현. 실제 ADMIN만 접근하고 공개 전 점검과
      정확한 확인 문구를 서버에서도 강제하며 변경 감사 이력을 남김. 1440/900/390px 모의 화면과 전체
      롤백 검증 완료 (2026-08-30, Step 6 시점 미적용·Step 7에서 운영 적용)
- [x] Step 7 오늘자 백업·복구 PASS 뒤 운영 DB 241/241 적용 및 앱을 `internal`로 배포. 실제 관리자 RPC
      정상, 실제 일반 교사 관리자 RPC 차단, 실제 학생 홈 미노출, 익명 401·직접 표 권한 없음, 앱/Caddy 200과
      오류 0건 확인. 관리자 실화면 인수와 내부 시험 공간 생성만 사용자 확인 대기 (2026-08-30)
- [x] 관리자 내부 시험용 로그인 불가 합성 교사 2명·학생 0명 학급 2개를 만들고 `20261200` 비공개 등록부로
      후보와 공간 생성 RPC를 제한. 운영 학급은 시험 선택에서 제외하고 DB를 242/242로 적용 (2026-08-30)
- [x] 실제 학급 최대 8개를 관리자가 고르는 `limited_beta` 단계를 추가하고 최소 2학급 전환·선택 학급 전용
      교사/학생 RPC·bootstrap 게이트·선택 해제 시 모듈과 학생 공개 동시 OFF를 구현하고 운영 DB 243/243 적용
      (2026-08-31, 앱 배포·학급 선택 전)
- [x] 학급 관리 안의 옛 `학급 간 글 공유` 예정 카드를 제거하고, 등록형 `neighbor-agit` 모듈의 독립 설정 진입점으로 분리 (2026-08-10, 운영 기능은 미구현)
- [x] 교사 흐름: 이웃 아지트 생성 → 호스트 권한 → 혼동 문자 제외·만료·일회성 초대 키 전달 → 게스트 학급 선택 → 호스트 최종 승인 (2026-08-31, 운영 반영 전)
- [x] 교사 화면은 이웃 초대·연결 학급·학생 공개·학생 글 승인/돌려보내기·글/댓글 숨김만 제공 (2026-08-31, 운영 반영 전)
- [x] 학생 화면은 하나의 글 피드와 기존 제출 글 공개 신청·회수, 한 줄 댓글, 공감 1종, `간직하기`만 제공
      (2026-08-31, 운영 반영 전)
- [x] 기존 글쓰기 편집기·임시 저장·맞춤법 기능을 복제하지 않는 `writingBridge` 계약 추가. Step 4에서는
      승인된 기존 글 읽기 피드만 연결했으며 새 글 쓰기 진입은 관리자 내부 화면 뒤 별도 연결 (2026-08-30)
- [x] 글 원본은 기존 글 테이블에 유지하고 전용 공유 연결 테이블에서 승인·공개·숨김 상태만 관리하여 복사본을 만들지 않음 (2026-08-30)
- [x] 같은 DB 안에 이웃 아지트 전용 공간·참여 학급·초대·공유 글 연결·댓글·반응·간직하기 테이블을 두며 별도 물리 DB는 만들지 않음 (2026-08-30, 운영 미적용)
- [x] 기존 학급 댓글·반응의 학급 범위 RLS는 넓히지 않고, 이웃 전용 댓글·반응·간직하기 쓰기/조회와
      학급별 교사 집계를 Step 5 전용 RPC로 연결 (2026-08-30, 운영 미적용)
- [ ] `내 아지트`에 `이웃 글 보관함`을 연결하고 원작자/교사가 공개를 중단하면 본문·상대 댓글은 숨기되 내 활동 기록은 보존
- [x] 학생 홈 bootstrap에 새 글 수만 추가하고 실제 목록은 이웃 아지트를 열 때 20편·절대 상한 50의 전용
      커서 RPC, 전문은 글 선택 시 별도 RPC로 지연 조회. 홈 추가 요청·폴링·Realtime 없음 (2026-08-30)
- [x] 참여 학급 권한, 호스트/게스트 관리 경계, 공간별 학생 공개 필명, 초대 시도 제한, 다른 공간·교사 OFF·
      숨김·퇴장·글 ID 추측 접근 차단을 정적 검사와 실제 역할 롤백 스모크로 검증 (2026-08-30)
- [ ] 이번 주 Step 0~7을 따라 데이터/RPC→초대→글 검토→학생 피드→상호작용→관리자 내부 화면 순서로 구현
      (Step 0~6 구현·Step 7 internal 배포 완료, 관리자 실화면 인수 남음)
- [x] 운영에는 `internal`로만 배포하고 실제 일반 교사 관리자 RPC·학생 홈 노출이 차단된 상태 확인 (2026-08-30)
- [ ] 관리자 실계정으로 `운영 → 기능 공개`의 내부 시험 공간과 PC·태블릿·모바일 표시 인수
- [x] 사용자 승인에 따라 전체 공개 대신 관리자가 고른 학급만 실제 화면을 쓰는 `limited_beta`로 방향 변경.
      선택 학급도 학생 스위치는 기본 OFF이며 교사가 별도로 켜야 함 (2026-08-31, 운영 DB 반영·학급 선택 전)
- [x] 제한 공개로 개발·점검한 뒤 관리자 화면의 스위치로 정상 공개하고 필요하면 다시 제한 공개로 되돌리는
      공개 범위 전환을 추가. 정상 공개 전 여섯 항목과 확인, 서버 감사 원장은 유지 (2026-08-31)

**초기 버전 제외**: 외부 공개, 대댓글, 여러 반응, 포인트·순위, 학생별 상대 학급 선택, 사진·첨부,
한 학급의 여러 활성 공간, 다섯 학급 이상 대형 공간.

**완료 기준**: 승인된 참여 학급과 교사가 ON한 학생에게만 메뉴가 열리고, 학생은 기존 글쓰기 경험으로 글을
제출하며 교사 승인된 글만 단일 피드에 공개된다. 댓글·공감·간직하기는 이웃 전용 권한을 통과하고 기존
학급 글 데이터와 RLS는 바뀌지 않는다. 전체 교사 공개는 관리자 내부 인수와 사용자 승인 뒤에만 실행한다.

#### 4c-1. 자율 독서록 최소 완성 흐름
- [x] `student_posts`에 출처(`assignment/self`)·자율 유형(`reading_log`)·공개 범위(`private/class`) 추가.
      학급별 시스템 미션을 만들지 않고 `mission_id IS NULL` 자율 글로 저장해 과제 통계와 분리 (`20260729_self_directed_reading_logs.sql`)
- [x] 기존 과제 글 2,773건을 `assignment/class`로 무손실 보존하고, 자율 글의 학급은 학생 학급에서 자동 연결
- [x] 학생 대시보드·모바일 하단 메뉴에 `나의 독서록` 진입점 추가, 여러 독서록 목록·작성·수정·삭제 제공
- [x] 모바일 하단 6개 메뉴의 목적지·활성 표시·방문 기록을 단일 규칙으로 통합하고, 메뉴 간 이동 뒤 기기
      뒤로가기는 메인 대시보드로, 실제 하위 글 화면은 정해진 부모 목록으로 복귀하도록 보완
- [x] 기존 과제 글쓰기의 제목·본문 입력부를 공통 `WritingEditorFields`로 분리해 독서록에서도 재사용
- [x] **기본 친구 공개**, 학생이 원할 때 비공개로 전환. 2026-07-28 사용자 결정 반영 및 같은 반 RLS 검증
- [x] 학생 글·댓글·반응의 작성자 위조 방지, 글/상호작용 identity 변경 차단, 자율 글 댓글 학급 연결 기반 보강
- [x] 학생 실기기 스모크: 독서록 작성→기본 공개 확인→비공개 전환→수정→삭제

#### 4c-1a. API 도서 정보·나의 책장 (2026-07-27 구현·운영 기반 적용)
- [x] 책 정보(`book_catalog`)·학생별 책장(`student_library_items`)·독서록 연결(`reading_log_entries`)을
      글쓰기 코어와 분리한 모듈 테이블로 추가. 기존 독서록은 수동 등록 책으로 자동 연결
- [x] `upsert_my_reading_log` RPC 한 번으로 책·책장·독서록을 같은 트랜잭션에서 저장하고 학생 JWT 본인 여부를 재검증
- [x] 카카오 책 검색 Edge Function `book-search`: REST API 키 서버 보관, 학생 인증·허용 Origin·검색 길이·호출 간격 제한,
      제목·저자·출판사·ISBN·표지만 반환(책 소개는 학생 글에 삽입하지 않음)
- [x] 검색 결과에서 표지 책을 선택하거나 API 장애·미등록 도서는 직접 입력할 수 있는 UI 구현
- [x] Kakao 표지를 `R120x174.q85` 웹용 썸네일로 고정하고 정확한 CDN origin만 CSP에 허용
- [x] 독서록 목록을 `전체 / 읽는 중 / 다 읽음` 표지형 나의 책장으로 전환하고 책 한 권당 독서록 한 편을 바로 표시
- [x] 운영 DB 마이그레이션·기존 독서록 무손실 백필, Edge Function 배치·런타임 시크릿 반영,
      실제 학생 JWT 역할의 저장/비공개/공개 RLS 롤백 스모크 완료
- [x] 학생 실기기에서 카카오 검색→책 선택→읽는 중/완독→한 책 한 편→공개/비공개 전환 확인

#### 4c-2. 친구 아지트 공개 책장
- [x] 친구 정보창의 `공개 서재` 슬롯에서 과제·독서록을 나무 책장과 클릭 가능한 책등으로 탐색.
      전체/과제/독서록 탭과 긴 제목 전체 보기, 모바일 좌우 스와이프 제공 (`2026-07-30`)
- [x] 공개 독서록 상세에 기존 친구 글 반응·댓글을 그대로 연결(별도 상호작용 테이블을 만들지 않음)
- [x] 고쳐쓰기 글은 `처음 글 / 최종 글` 비교를 학생이 허용한 범위(`show_original`)에서 표시
- [x] 공개 취소 즉시 친구 목록·상세·댓글/반응 직접 조회에서 숨겨지는지 RLS 검증
- [x] 학생 실기기 스모크: 친구 선택→과제/독서록 필터→상세 반응·댓글→공개 취소→처음/최종 글 허용 범위 확인

#### 4c-3. 글쓰기 발자국(가볍고 자주 보는 통계)
- [x] 업데이트 이후 이벤트만 기록: 작성·저장·공개·고쳐쓰기·피드백 반영·댓글·반응을 append-only 원천 이벤트로 남김
- [x] 학생용 핵심 수치는 단순하게 유지: 쓴 글 수, 고쳐쓴 횟수, 받은/남긴 댓글, 받은 반응, 꾸준히 쓴 날
- [x] 평가 향상도는 1차 일상 통계에서 제외해 적은 평가 횟수를 과대 해석하지 않음. 후속 추가 시에도 보조 지표로만 표시
- [x] ~~매일 00:10 일별 스냅샷을 계산하고 친구 아지트는 **스냅샷 1건만 조회**~~ → **폐기(2026-07-30)**.
      실측(집계 0.75ms)으로 미리 계산할 이유가 없고, 재료 이벤트가 7건뿐이라 스냅샷을 믿으면 전교생이 0으로 보였다.
      내 발자국·친구 발자국 모두 실시간 집계로 통일했고, **00:10 LaunchAgent 는 껐다**
      (`site.agit.writing-footprint-snapshot.plist.disabled`, 표·함수는 되돌릴 수 있게 유지)
- [x] 댓글 통계는 도입 이후 자료를 사람/글 기준 `DISTINCT`로 계산하고 댓글 비승인·삭제, 반응 취소를 즉시 반영(실시간 집계)
- [ ] `댓글을 많이 달아준 친구`는 최소 표본·동률 처리와 긍정적 표현을 적용하고 공개 범위는 교사가 학급 단위로 조절
- [x] 학생 실기기 스모크: 새 글·고쳐쓰기·댓글·반응→본인 화면·친구 아지트 카드에 **바로** 반영되는지 확인

#### 4c-4. 친구 아지트를 확장 가능한 RPG 정보창으로 전환
- [x] 커뮤니티 진입을 `최신 글 / 친구 아지트` 두 탭으로 분리하고 글 공유를 기본값으로 설정.
      글 나눔에는 본인 글도 `내 글`로 포함하고, 방문 목록은 친구 이름·작가·소통·기록가·독서가 칭호를 전면에 표시
      (`2026-07-30`, 네 칭호 확장 2026-08-31)
- [x] `FriendProfileShell`은 레이아웃·학생 식별·권한만 담당하고 카드를 슬롯/매니페스트로 등록.
      대표 드래곤·공개 글 책장·둘이 나눈 기록만 활성화하고, 전체 글쓰기 통계·포인트 활동은 친구 화면에서 제외
- [x] 활성 카드마다 독립 lazy 청크·로더/캐시·오류 경계를 두어 한 카드 실패가 친구 아지트 전체를 막지 않게 함
- [x] 친구 목록 최초 조회는 학급 단위 RPC 한 번으로 이름·칭호를 집계하고, 상세 진입 때 선택한 학생의 공개 책장·관계 기록만 지연 조회
- [x] 포인트 잔액·총액뿐 아니라 활동 횟수도 친구 화면에서 제외. 기존 요약 RPC·카드는 삭제하지 않고 연결만 끊어 되돌릴 수 있게 보존
- [x] 드래곤 업그레이드·신규 게임을 셸 수정 없이 새 프로필 카드 매니페스트로 붙일 확장 지점 마련
- [x] 묶음 실기기 스모크: 이름·칭호 목록→정보창→드래곤·책장·둘이 나눈 기록 독립 로딩→공개 글 상세→닫기/재방문

#### 4c-4a. 나의 아지트·내 서재
- [x] 나의 아지트 서재에서 본인의 과제·독서록·자유글을 직접 여는 읽기 전용 상세 화면 연결.
      비공개 글·보관된 과제도 본인은 계속 읽고, 처음글/마지막글·피드백·독서록 교사 확인을 함께 표시 (`2026-07-30`)
- [x] 상세 조회를 글·독서록 확인 기록·과제 각각의 `class_id`와 본인 `student_id`로 직접 제한하고,
      `dataCache`/`classKey` 30초 캐시·상세 화면 lazy 청크·상세→서재→대시보드 뒤로가기 적용
- [x] 활동 알림에서 바로 연 글 상세는 실제 복귀 경로에 맞춰 공용 `뒤로 가기` 버튼을 표시하고 홈으로 복귀하며,
      아지트 책장에서 직접 연 상세만 `← 내 서재`를 표시 (`2026-08-20`)
- [x] 작가 칭호·성장률을 서재 최근 60편 합계가 아니라 대시보드의 전체 승인 글 통계와 통일
- [x] `작가 축 + 독자 축` 합의를 실제 화면에 반영: 기존 작가 칭호를 보존하고 독자 7단계와 다음 목표를 별도 줄로 표시.
      독자 점수는 친구의 서로 다른 글마다 반응/댓글 기본 1점 + 댓글 20자당 1점(글당 보너스 최대 3점)으로 계산 (`2026-07-30`)
- [x] 운영 DB에 `20260809_reader_title_indexes.sql` 적용 완료: `get_my_reader_title()` + 인덱스 3개.
      실데이터 대조까지 확인(`{score: 204, post_count: 173}`). 2026-07-31 재확인 (`2026-07-30`)
- [x] 작가 축을 `0 → 글 1편 → 390 → 910 → 1,820 → 3,250 → 5,460 → 10,920 → 15,600 → 26,000` 10단계로 적용.
      최초 글자 수 문턱보다 30% 상향했으며, 첫 단계는 승인 글 수, 이후는 누적 글자 수로 판정하고
      나의 아지트·글쓰기 발자국의 목표 단위와 진행률을 통일 (`2026-07-30`)
- [x] 상시 노출하던 긴 칭호 두 줄을 작가/독자 훈장형 뱃지 2개로 축소하고, 현재 단계에 맞는
      **작가 10종·독자 7종 고유 이미지**와 LV를 표시. 뱃지를 누르면 성장 원리와 단계별 고유 뱃지,
      현재/달성/잠김 상태를 보는 설명판 제공 (`2026-07-30`)
- [x] 작가 칭호 집계 기준을 나의 아지트와 발자국에서 하나로 통일: 과제글은 미션별 가장 최근 한 편, 자율글은 각 글 한 편.
      클라이언트는 `collectWriterPosts()`, SQL은 `level_posts` CTE로 같은 규칙을 쓰고 발자국은 `completed_posts`를 넘긴다.
      (재작성이 쌓이면 같은 학생이 화면마다 다른 칭호를 보던 문제) (`2026-07-30`)
- [x] 자율 일기·독서록을 작가 성장에서 분리하고 `작가 / 소통 / 기록가 / 독서가` 네 칭호로 개편.
      기록가는 교사 확인 일기 날짜, 독서가는 확인 독서록 수와 서로 다른 책 수를 함께 센다. 이번 시즌
      처음부터 새 기준으로 소급 재계산해 작가 단계가 내려갈 수 있지만 포인트·글·보유 아이템은 유지한다
      (`20261202`~`20261203`, 2026-08-31 운영 DB 245/245 적용·역할 스모크 완료)
- [x] 학생 홈과 같은 학급 친구 아지트의 목록·상세에도 네 칭호를 표시한다. 학생 홈은 기존 bootstrap 칭호 응답을
      그대로 쓰고 친구 명단은 기존 RPC 한 번에 기록가·독서가 원자료를 합쳐 추가 조회를 만들지 않는다
      (`20261204`, 2026-08-31 운영 DB 246/246 적용·역할 스모크 완료). 학생 홈은 포인트를 별도 칸으로
      분리하고 네 칭호를 화면 크기와 관계없이 2×2로 배치해 이름과 단계를 읽기 쉽게 유지한다. 레벨 우측
      상하·좌우 중앙 정렬도 운영 배포 완료 (`8b2690f`, GitHub Actions `33354155420`).
- [x] 네 칭호가 기존 학기 시즌 ID·시작·마감·스냅샷을 공유하고 기록가·독서가 단계 보상을 직접 수령하는
      통합 계획 수립 — [TITLE_SEASON_REWARD_PLAN.md](TITLE_SEASON_REWARD_PLAN.md) (2026-08-31)
- [x] 1차는 기록가·독서가 2~7단계에 `200/400/600/800/1,200/1,800P`를 적용해 종목별 누적 5,000P로
      구성하고, 시즌별 정책 스냅샷·수령 원장·공용 포인트 엔진 기반 멱등 RPC 구현. 이미 오른 단계도
      자동 지급 없이 직접 수령 (`20261206`, ROLLBACK 검증 후 2026-08-31 운영 DB 적용 완료)
- [x] 학생 공용 칭호 상세의 단계별/모두 받기와 즉시 포인트 갱신 연결 — 제한 공개 등록부는 기본 OFF
- [x] 실제 일기·독서록을 만들지 않는 비공개 기록가·독서가 테스트 단계 덮어쓰기를 공용 칭호 모듈과
      보상 서버 검증에 함께 연결. 관리자 `테스트` 학급 학생의 기록가는 LV.3으로 설정 (`2026-08-31`)
- [x] 현재·미래 학급을 한 번에 여는 전역 공개 스위치와 학급별 우선 예외, 새 학급의 첫 공용 시즌 자동 생성을
      추가. 기본 OFF·관리자 전용 변경·즉시 전역 중단이 가능하며 운영 DB 250/250 적용 뒤 전체 공개 ON
      (`20261208`, `567c036`, GitHub Actions `33385237100`)
- [ ] 교사 시즌 화면에 지급·미수령 현황을 연결하고, 전체 공개 뒤 7일 기술 안정성·4주 포인트 경제를 관측
- [ ] 새 칭호 실기기 인수: 학생·교사 2×2 카드, 같은 날 일기 중복 제외, 같은 책 독서록의 편수/책 수 분리,
      이번 시즌 작가 단계 재계산과 시즌 마감 스냅샷 확인 (`MANUAL_ACCEPTANCE_CHECKLIST.md`)
- [x] 운영 DB에 `20260730_align_writer_level_totals.sql` 적용 완료 — 발자국 `total_chars`·`completed_posts`가
      나의 아지트와 같은 `level_posts` 규칙을 쓴다. 2026-07-31 재확인 (`2026-07-30`)
- [x] 칭호 뱃지 17종을 512px PNG(5.72MB) → 256px WebP(258KB, -95%)로 최적화하고 참조 없는 뱃지 2종 제거.
      재생성은 `npm run badges:optimize` (새 의존성 없이 설치된 Edge의 Chromium WebP 인코더 사용) (`2026-07-30`)
- [x] 학생 실기기 스모크: 두 칭호·다음 목표, 과제·독서록·비공개 글·보관된 과제 상세와 단계별 뒤로가기 확인
      WebP 단독 제공이라 교실 태블릿·크롬북에서 뱃지 표시도 함께 확인

#### 4c-5. 교사 활용·후속 연결
- [x] 교사 대시보드 `글쓰기 관리 → 학생 독서록`에 학급 요약·미확인/확인 필터·학생/제목/책 검색과
      선택 시 큰 본문 상세, `확인했어요`·교사 한마디 저장을 제공. 검토 기록은 학생 글과 분리하고 학생은 자기 결과만 조회
- [x] 비공개 안내를 `친구에게 비공개 · 선생님은 확인 가능`으로 명확히 하고, 학생 나의 책장에 교사 확인·한마디 표시
- [x] 교사·학생 실기기 스모크: 글쓰기 관리→학생 독서록→비공개 글 상세→확인/한마디→학생 책장 표시
- [x] 교사는 독립 상단 `학급 발자국` 탭에서 현재 학년도 학급 요약·달력·월별 글 수/평균 길이·포인트 흐름과
      학생별 완료 글/글자/활동일/교류/최근 글을 확인. 원시 내용은 반환하지 않고 학급 직접 필터·상한 집계 RPC만 사용 (`2026-07-30`)
- [ ] 자율 독서록의 AI 피드백·포인트 정책은 실제 사용량을 본 뒤 결정(1차에는 자동 보상 없음)
- [ ] 독서록 외 자율 장르는 사용 효과가 확인된 뒤 같은 `self_writing_type` 모듈로 추가
- [ ] 끄적끄적 아지트 헬퍼는 위 구조가 안정된 **마지막 단계**에 데이터/RPC로만 연결하고 코드는 이식하지 않음

### 4d. 포인트 동기부여 고도화 + 엔터테인먼트 확장 【방학 우선순위 2, 신규】
기둥3을 **확장**한다: 포인트로 즐기는 활동을 다양화하고 드래곤을 더 다이나믹하게. 새 활동은 3a 모듈 시스템 위에
독립 모듈로 추가(지연 로딩·학급별 on/off → 앱 성능 유지). ⚠️ 포인트 적립/사용 로직은 코어 셸이라 정합성 검증 필수.

> **2026-08-07 포인트 경제 선행 결정**: 어휘의 탑을 고도화하기 전에 공방 가격과 한 학기 포인트 총량을
> 먼저 맞춘다. 새 콘텐츠마다 보상 상한을 더하지 않고 모든 게임이 일간·주간 공용 보상 상한을 나눠 써
> 콘텐츠가 늘어도 인플레이션이 생기지 않게 한다. 운영 기준선·가격 등급·어휘 탐험안은
> [POINT_ECONOMY_PLAN.md](POINT_ECONOMY_PLAN.md)에 보존했다. 꾸준히 참여한 학생의 기준은 주 1,000P,
> 모든 학습 게임의 공용 상한은 주 250P 초기안으로 잡았다. 2026-08-22 실제 일기·독서록 일일 제출 운영을
> 반영해 공방은 일반 유료 37종·전체 46,800P로 조정하고, 전설 5종은 작가 10·독자 7 달성 선물로 분리했다.
> 같은 날 포인트가 많아도 살 것이 없지 않도록 작가 단계 제한 없는 자유 구매 세트 3종·15개를
> 아이템당 800/1,000/1,200P로 추가해, 운영 적용 전 카탈로그 기준 유료 52종·전체 61,800P로 확장했다.
> **2026-08-31 재조정 방향**: 실제 운영 편차를 반영해 보통 참여 학생은 한 학기 첫 자유 구매 완성 세트
> 한 개 이상을 목표로 둔다. 기록가·독서가는 각 종목 7단계 누적 5,000P, 두 종목 최대 10,000P로
> 동기부여를 강화하고, 관리자 시험 학급 4주 측정 전에는 기존 활동 보상·상품 가격을 함께 바꾸지 않는다.

**(A) 포인트 시스템 기반 정비 (먼저)**
- [x] 학생 포인트 허브: 보유 포인트·최근 내역과 획득/사용 콘텐츠를 한 화면에 통합하고 학생 홈의 포인트 카드도
      놀이터로 직접 연결. 콘텐츠가 없어도 지갑은 유지하며 내역은 열 때만 제한 조회 (`2026-08-13`)
- [x] 현행 포인트 흐름 매핑: 글 승인/회수·회의 참여/결정·어휘의 탑·드래곤 먹이·배경 구매·신규 환영·교사 조정으로 분류하고,
      `point_logs.activity_type`으로 기계 판독 가능하게 정규화 (`20260802_friend_point_activity_summary.sql`)
- [~] **포인트 경제 기준안 작성** (2026-08-07): 운영 비식별 통계로 현재 발행·소비·잔액과 공방 총가격을
      측정하고, 주간 총예산·게임 공용 상한·상품 등급별 목표 주 수·월간 경보 지표를 문서화했다.
      주 1,000P 초기 예산을 기준으로 입문 300P·일반 700P·희귀 1,500P·영웅 3,000P와 작가 단계별 구매 제한을
      코드·DB 마이그레이션에 반영했다. 다음은 운영 적용 뒤 4주 단위로 발행·소비를 측정하는 것.
- [ ] 교사용 포인트 관리 도구: 학급 일괄 지급/차감·내역 조회·자동지급 규칙 설정 UI
- [~] 게임/활동 보상의 서버측 검증 강화 — 어휘의 탑은 도전·문제·보상·일 80P/주 250P 공용 상한을
      서버로 이전해 운영 적용 완료. 이후 게임도 `game_point_grants`를 공유
- [ ] 포인트 상점·인벤토리 등 공통 기반(신규 활동들이 공유할 재화·아이템 계층) 설계

**(B) 드래곤 기르기 다이나믹화**
- [x] 작가 칭호 10단계와 수호룡 성장을 1:1로 연결하고, 4종 각각의 10단계 WebP 이미지 40개로 교체.
      학생 홈·나의 아지트·드래곤 방·친구 아지트·교사 화면이 공용 표현 모듈을 사용
- [x] 처음 만날 때 4종 중 하나를 선택하고 작가 3단계부터 한 번만 다시 선택. 전용 RPC가 학생 본인·단계·횟수를 서버에서 강제
- [x] 독자 칭호 1~7단계를 드래곤 외형 위의 빛·입자·궤도·오로라 효과로 결합해 4종×작가 10×독자 효과 7 조합을 표현
- [x] 배경상점 확장에도 독자 효과가 묻히지 않도록 보호 무대·이중 외곽광·단계 인장과 단계별 별/룬/왕관/오로라/수호 문장을
      공용화. 새 배경은 `readerTone(light/dark/vivid)` 대비 계약만 선언 (`dragon/README.md`)
- [x] 배경상점을 **아지트 공방**으로 개편하고 프레임·받침대·왼쪽 소품·오른쪽 소품·문패 5개 고정 슬롯을 적용.
      기존 구매 배경은 저장 ID를 유지한 모서리 프레임으로 전환하고, 서버 검증 구매·장착 RPC와 공용 `DragonHideoutScene`으로 나의 방과
      친구 아지트에 같은 장착 모습을 표시 (`20260829_dragon_decor_workshop.sql`)
- [x] 공방 유료 상품을 27종에서 40종으로 확장하고 슬롯별 8종, 입문 8·일반 12·희귀 10·영웅 7·전설 3종으로 배치.
      신규 소품 WebP 9개와 프레임·받침대 CSS, 등급 배지, 143,600P 가격표를 운영 적용·배포 완료 (`de449e4`)
- [x] 좌우 소품을 작은 CSS 도형에서 드래곤과 같은 3D 판타지 재질의 투명 WebP 8종으로 교체. 왼쪽은 기록·마법,
      오른쪽은 교감·관측·보물·휴식 역할로 구분하고 기존 구매 ID·가격·소유권은 그대로 보존 (`20260831_dragon_prop_asset_upgrade.sql`)
- [x] 문패를 단순 색상 4종에서 재질·실루엣·테두리 효과가 다른 3D 수집형 문패 8종으로 확장. 기존 4종 소유권을
      보존하고 수정·룬·별자리·용암·전설 문패에 이름을 가리지 않는 단계별 효과 적용 (`20260901_dragon_nameplate_collection.sql`)
- [x] 실제 글 통계를 조작하지 않는 관리용 작가 칭호 테스트 오버라이드 경로 추가. 학생은 직접 읽기/수정할 수 없고,
      2026-08-07부터 일반 내 칭호·친구 아지트에서는 제외해 교사 수호룡 대시보드에만 반영
      (`20260825_student_title_test_overrides.sql`, 분리 `20260924`~`25`)
- [x] 같은 테스트 학급 7명에게 실제 독자 점수를 보존한 독자 효과 1~7단계를 배정하고 내/친구 칭호·수호룡 표현에 공용 적용
      (`20260828_reader_title_test_overrides.sql`)
- [x] 먹이 포인트 차감·미접속 퇴화를 제거하고 무료 `교감하기`와 꾸미기 구매로 역할을 분리. 기존 이름·배경·보유 아이템은 보존
- [x] 교감 성공 시 수호룡 점프·좌우 반응, 황금빛 후광·하트/별, 완료 버튼·답변·누적 횟수를 표시 (`f1a2b49`)
- [x] 작가 칭호가 오르면 이전 모습→새 모습·독자 효과·빛/입자 전환을 보여주는 성장 축하창을 한 번 표시.
      서버 확인 기록으로 기기 간 반복을 막고 실제/테스트 칭호 확인 키를 분리 (`20260827_dragon_growth_celebration.sql`)
- [x] 교사 `작가 수호룡 관리 → 단계 미리보기`에서 4종×작가 10단계×독자 효과 7단계를 학생 기록 변경 없이
      조합하고 이전→선택 단계 성장 축하 연출을 재생하는 읽기 전용 도구 제공
- [x] 교사 `작가 수호룡 관리 → 공방 미리보기`에서 학생 상점의 실제 5개 슬롯·상품·가격·등급·해금 단계를
      확인하고 상품 조합을 학생 기록 변경 없이 장면에 적용하는 읽기 전용 도구 제공
- [x] 기존 상품 ID·가격·소유권을 유지한 채 유료 40종을 5개 완성 세트 25종과 슬롯별 시그니처 단품 15종으로
      정리하고, 학생·교사 공방에서 세트 전체 미리보기와 상품별 구매·장착을 분리 — 운영 배포 완료(`1464e2e`)
- [x] 프레임의 큰 모서리 색면을 얇은 재질 레일·작은 금속 장식·보석 리벳 구조로 고급화하고 실제 장면과
      학생 공방 상품 미리보기를 하나의 회귀 검사로 함께 보호 — 운영 배포 완료(`c46f422`)
- [x] 문패 9종의 이미지별 글자 안전 폭·세로 중심을 공용 계약으로 정의하고, 이름 길이에 따라 문패 크기와
      글자 크기를 자동 조절해 폭풍·전설 문패와 소형 카드에서도 이름이 장식 밖으로 나가지 않게 보정 —
      운영 배포 완료(`6d35cbe`)
- [x] 별자리·폭풍·전설 문패는 이미지 정중앙이 아닌 실제 글자판 중심 좌표에 제목·수호룡 이름 묶음을 배치해
      위쪽 치우침을 보정하고 학생 아지트·공방 미리보기에 같은 세로 정렬 적용 — 운영 배포 완료(`1794be3`)
- [x] 일반 유료 장식을 등급별 300/700/1,500/3,000P와 작가 1/3/5/7단계로 조정하고, 새 황금 받침대를 포함한
      전설 5종을 작가 10·독자 7 달성 시 한 번에 개봉·장착하는 `전설의 황금 성소`로 구성. 기존 구매 차액은
      중복 방지 키로 환급하는 `20261154` 운영 적용·배포 완료 (`6944c00`, DB 196/196, Actions `32502340786`)
- [x] 학생 공방 상품 카드를 왼쪽 설명·구매 버튼, 오른쪽 미리보기로 통일하고 좁은 2열 카드에서도 등급·
      `시그니처` 배지가 잘리지 않도록 줄바꿈과 전체 표시 적용 — 운영 배포 완료 (`6f9a340`, Actions `32503299277`)
- [x] 작가 단계와 무관하게 바로 살 수 있는 자유 구매 완성 세트 3종·15개를 추가하고 아이템당
      800/1,000/1,200P로 구성. 신규 투명 WebP 9개와 프레임·받침대 테마, 학생·교사 `자유 구매` 표시,
      52종·61,800P 회귀 검사를 반영. `20261155` 운영 적용·배포 완료 (`a1dad11`, DB 197/197,
      Actions `32505647804`, 앱이 먼저 나가고 DB가 늦어 2026-08-22에 맞춤)
- [ ] 공방 후속 꾸미기 요소 (시즌 전시품·슬롯별 상품 추가 등) ← 5개 슬롯 수는 늘리지 않고 카탈로그만 확장
- [x] 성장·퇴화 정책 재결정: 자주 접속하지 못해도 성장 하락 없음. 성장은 승인 글 누적, 포인트는 꾸미기 전용
- [x] 학기형 수호룡 시즌: `성장 중 → 작별 편지 → 시즌 보관 → 새 학기 알부터 시작`, 과거 글·포인트·소품는 보존하고
      종·칭호 확인 키만 초기화 (`20260921_dragon_semester_farewell.sql`, 2026-08-07 운영 적용·ROLLBACK 스모크 완료).
      `시즌 종료`를 잘못 누른 경우 되돌리기도 추가 — 새 학기를 아직 시작하지 않았을 때만 가능하며
      `작별 편지 기간`이 아니라 **`학기 성장 중`(종료 이전) 상태로 완전히 되돌아간다** — 학생이 쓴 편지는
      지우지 않고 재오픈 시 이어서 보인다 (`20260922`, `20260923_dragon_season_finalize_full_revert.sql`).
      교사·학생 실계정 화면 동선까지 확인 완료(2026-08-23).

**(C) 어휘의 탑 리뉴얼**
- [x] 뜻·문장·구별의 방, 5·10층 복습 보스, 층별 능력 선택, 틀려도 줄지 않는 학습 경험치 구현·운영 배포
- [x] localStorage 도전 횟수와 클라이언트 지정 보상을 서버 기록·서버 계산으로 교체하고 임의 보상/층수 RPC 차단
- [x] 3~6학년 낱말 1,573개 서버 검증 기준표와 게임 공용 하루 80P·주 250P 장부 운영 적용
- [x] 교사 관리에서 의미가 없어진 최고 층 랭킹·지난 시즌 UI와 관련 조회를 제거. 현재 10층 탐험 구조,
      출제 학년·하루 횟수·층별 시간·보상 상한과 서버 계산식을 보여주는 설정 화면으로 교체하고,
      설정 저장과 오늘 기회 초기화를 분리(기존 랭킹·시즌 데이터는 보존)
- [x] 운영 학생 인증 문맥의 시작→정답→보상→재지급 방지 ROLLBACK 스모크와 실제 학생 기기의
      오답 설명→5층 보스 복습→10층 완료 화면 동선 확인
- [~] **2차 문항 계약 확정**: 뜻 선택→문맥 선택→낱말 직접 입력→빈칸·잘못 쓴 말 고치기로 난이도를 올리고,
      품사·뜻 번호·난이도·오답 보기·허용 정답·설명·검토 상태·문항 버전을 계약에 포함한다. 자유 문장 쓰기는
      자동 공식 채점에서 제외하고, 구별·새 예문 문제는 사람이 확인한 문항만 사용한다. 2026-08-16 관리자 검수
      저장 계약에는 품사·뜻 번호·난이도·다섯 문항·허용 정답·검수 상태·항목/덱 버전·잠금을 반영했고,
      최종 학생 V2 출제·채점 어댑터 계약은 전체 문항 검수 뒤 확정한다.
- [x] 기존 1,573개 자동 진단: `npm run vocab:audit`가 정적 1,586행과 현 운영 생성 규칙의 1,573개를 함께
      보존하고, 중복 13그룹·뜻 답 노출 신호 12개·예문 표기 신호 4개·품사 보강 1,573개를 보고한다.
      학년별 10개 덱(총 40개, 덱당 38~40개) 배정과 전체 1,573개의 다섯 문항 검수 후보를 재현 가능하게 생성했다.
      3학년 첫 덱은 Codex 항목별 직접 편집 검수, 나머지 39개 덱은 자동 구조 검사·품사 보강·쓰임 구별 후보를 적용했다.
      자동 신호와 첫 덱 수정 항목을 합친 우선 확인 46개, 나머지 표본 1,527개로 나눠 전체 확인·잠금까지 마쳤다.
- [x] 기존 낱말 정보와 분리된 검토 문항 저장 구조 추가. 2026-08-16 ADMIN 전용 덱/항목 표와 조회·초기화·
      항목 수정·교사 확인·잠금 RPC, 원문 diff 관리자 UI를 운영 적용했다. 적용 뒤 실제 스키마에서 관리자 흐름과
      교사·학생 차단 ROLLBACK 스모크를 통과했다. 잠긴 문항의 학생 V2 출제 연결은 전체 검수 뒤 가장 마지막에 한다.
- [x] 3학년 첫 덱 40개 Codex 1차 직접 검토 완료: 품사·내부 뜻 번호·허용 정답 40개, 자동 문항 160개를 확인하고
      구별 문항 40개를 작성했다. 뜻 14개·예문 9개, 항목 기준 18/40개(45.0%)를 수정했다. 검수 UI는 일괄 입력·
      원문 diff·정답 변형·구별 문항·검수 상태/버전 잠금을 구현하고 운영 DB 저장·잠금을 완료했다.
- [x] 3~6학년 필수 어휘 전체 1,573개를 학년별 10개 덱으로 검수한다. 40개 덱의 품사·뜻 번호·뜻/빈칸·쓰임 구별
      후보 생성과 운영 검수 DB 일괄 반영을 완료했다. 2026-08-16 요청자 승인으로 남은 검수 신호 항목까지 포함해
      40개 덱 전체를 `교사 확인`으로 전환했으며 1,573개 문항의 DB 완결성 검사를 통과했다. 덱 잠금과 학생 V2
      출제 연결을 분리한 채 40개 덱 전체 잠금을 완료했다.
- [~] **게임 V2 수정은 문항 검토 완료 뒤 가장 마지막에 진행**: V1은 서버의 운영 비상 롤백 수단으로만 유지하고,
      V2 학생 화면을 `10개 덱 지도 → 층 선택 → 12문항 개인 연습 → 덱별 결과`로 전환했다.
      잠긴 덱에서 서버가 문항을 발급·채점하고 완료/최고 정답률을 저장한다. 각 덱 최초 12/12에는 교사가 정한
      포인트(기본 100P)를 한 번 지급하고 같은 덱 반복 성공은 0P다.
      2026-08-16 요청자 결정으로 2학기 운영 학급 114개를 전부 V2·모듈 ON으로 전환했다. 낱말별 상태와 다음
      연습의 약점·새 낱말·예정 복습 우선 출제를 연결했다. 덱 카드에는 전체·한 번 이상 학습·처음 볼 낱말·연습 중·
      다시 볼 낱말·완전히 익힘 수와 완료 횟수·최고 정답률을 표시하고, 최초 완벽 보상의 도전 중/완료 상태를
      카드에서 바로 구분한다. 교사 화면의 V1 출제 선택은 제거하고 학년별 잠긴 현재 덱 10개를 자동 기본자료로
      연결하며, 새 학급 DB 기본값도 V2로 변경했다. 학생 덱 목록은 정상과 입구를 잇는 지그재그 탐험 지도로
      재구성하고 100% 달성 층·현재 층·학습한 층을 경로에서 구분한다. 주관식·연습 중 재출제·공식 도전은 아래 설계로 남긴다.
- [x] 어휘의 탑과 학생 게임 전체 화면을 모바일 실제 가시 높이(`100dvh`)의 독립 세로 스크롤 영역으로 바꿔
      10개 덱 지도·긴 결과 화면의 하단이 가려지는 문제를 수정하고 회귀 검사를 추가했다.
- [~] 전체 학급 운영에서 단계별 정답률·직접 입력 성공률·평균 시간·찍기 신호·중도 종료·보충 수련 후 재도전 성공률·
      이의 문항을 확인하고 필요하면 학급별 V1 롤백
- [ ] 이후 후보: 학급 공동 목표, 학기 대비 성장, 배지·칭호(시험 운영 데이터 확인 뒤 결정)

**(D) 공통 학습 카드 엔진 + 학기형 어휘의 탑 V2 (설계 방향 저장, 구현 보류)**

> **2026-08-13 합의한 방향**: 어휘·맞춤법을 별도 퀴즈로 계속 늘리지 않고, 학생별로 `무엇을 익혔고 무엇을
> 더 연습해야 하는지`를 남기는 공통 학습 엔진을 먼저 만든다. 실제 학생은 매일 접속하지 않고 몇 주 만에 다시
> 들어올 수 있으므로 연속 접속·주간 초기화·미접속 감점은 사용하지 않는다. 아래 내용은 구현 확정안이 아니라
> 향후 콘텐츠 확장까지 고려한 설계 기준이며, 화면·점수·문항 계약을 더 검토한 뒤 개발한다.

- [x] **공통 항목 계약**(2026-08-17 완료): 엔진이 항목 상태·전이 규칙·복습 간격·묶음 진도와 구간 포인트를
      소유하고, 콘텐츠는 `content_type + collection_key + item_key`와 문제 생성·채점만 갖는다.
      `learning_item_progress`·`learning_collection_progress`와 `learning_engine_*` 함수로 분리했고 어휘의 탑을
      첫 콘텐츠로 얹었다(진도 48행 이관, 읽기·쓰기 모두 엔진). **`collection_key`는 엔진이 해석하지 않는
      불투명한 문자열**이라 콘텐츠마다 묶음 크기를 스스로 정한다(어휘는 `g3:d1`). 상태 전이 540개 조합이
      어휘 원본과 일치하는 것을 스모크로 고정했다. 학기 점수(SP)는 아직 엔진에 없다.
- [x] **개인 지식 카드함**: 어휘 V2 내부에 `새 낱말 / 연습 중 / 복습 필요 / 익힘` 상태를 먼저 구현하고
      덱 지도에 익힘·복습·새 낱말 수를 표시했다. 2026-08-17에 층별 카드함 화면을 붙여 낱말마다 시도·정답·오답
      횟수, 힌트 없이 성공한 문제 형태 수, 다음 복습 시점과 남은 익힘 조건을 보여 준다. 분류는
      `자주 헷갈려요 / 다시 볼 낱말 / 복습할 때가 됐어요 / 연습 중 / 거의 익혔어요 / 완전히 익힘` 여섯이다.
      아직 만나지 않은 낱말은 직접 입력형의 정답 감추기를 지키려고 목록에서 빼고 개수만 안내한다. 원래 계획했던
      `아직 확인하지 않음 / 아는 것 같음 / 연습 중 / 익힘 / 자주 헷갈림 / 복습할 때`를
      구분한다. 한 번 맞힌 결과나 아직 출제하지 않은 항목을 곧바로 `익힘/모름`으로 단정하지 않고, 서로 다른
      문제 형태의 반복 성공·오답·힌트·재복습 근거와 다음 복습 시점을 학생이 항목별로 확인하게 한다.
- [~] **개인 적응 덱**: 학년 필수 어휘 전체를 10개 층 덱에 38~40개씩 고정하고, 1차로 학생이 고른 덱의
      잠긴 문항 중 중복 없는 12개를 서버가 발급하고 덱별 결과를 저장한다. 낱말별 최근 오답·서로 다른 정답 유형·
      연속 성공·다음 복습 시점을 기록하고 한 연습의 목표 슬롯을 약점 5·복습 4·새 낱말 3으로 두되 후보가
      부족하면 새 낱말과 연습 중 낱말로 안전하게 채운다. 3일 뒤 한 유형 성공, 14일 뒤 익힘 낱말을 다시 확인하며
      실제 운영 분포에 따라 비율과 간격을 조정한다.
      틀린 항목은 정답을 외워 누르지 않도록 3~5개 뒤 다른 형태로 다시 내고, 해결되지 않으면 다음 로그인 덱에
      우선 포함한다. 2026-08-17에 같은 연습 안 보충 수련까지 구현했다. 3문항 이상 지난 오답 중 아직 다시 내지
      않은 가장 오래된 낱말을 방금 틀린 유형과 다른 선택형으로 한 번 더 내며, 입력형 오답도 선택형으로 낮춰 묻는다.
      어휘는 뜻/문맥/역방향, 맞춤법은 오류 찾기/고치기/새 문맥 적용을 사용한다.
- [~] **단계형 문제 난이도**: 처음 만난 낱말은 뜻·문맥 선택형으로 시작하되 성공한 학생은 낱말 직접 입력,
      힌트 없는 빈칸, 잘못 쓴 말 고치기로 올라간다. 한 번의 선택 정답으로 익힘 처리하지 않고 서로 다른 형태의
      성공을 근거로 삼는다. 공식 도전의 주관식은 서버가 정규화 가능한 낱말·짧은 빈칸만 사용한다.
      2026-08-17에 직접 입력형 2종(`definitionInput` 뜻→낱말, `clozeInput` 문장 빈칸)을 개인 연습에 연결했다.
      `한 유형 성공(familiar)`과 `익힘(mastered)` 낱말만 입력형으로 올리고, 처음 만난 낱말·연습 중·복습 필요는
      선택형을 유지한다. 입력형 오답은 기존 규칙대로 `복습 필요`로 내려가 다음에 다시 선택형부터 만나며 감점은 없다.
      채점은 서버가 검수된 `acceptedAnswers`와 대조하고 공백·문장부호·대소문자 차이만 무시한다.
      `잘못 쓴 말 고치기`는 아직 문항이 없어 후속이다.
- [ ] **드문 로그인용 복습 정책**: 고정 `1·3·7일`만 쓰지 않고 `다음 로그인 → 2회 플레이 뒤 → 4회 플레이 뒤`
      같은 접속 순서와 실제 경과 시간을 함께 사용한다. 오래 쉬고 돌아온 첫 덱은 약점·밀린 복습을 우선하되 한 번에
      몰아내지 않고, 미접속 감점·연속 기록 소멸·기간이 지난 보상 소멸은 두지 않는다.
- [~] **학생 게임 UI**: `어휘의 탑 지도 → 층별 개인 연습 → 결과 정리`와 덱별 최고 정답률 기록을 먼저 구현했다.
      지도 카드에는 `처음 볼 낱말 / 연습 중 / 다시 볼 낱말 / 완전히 익힘` 수, 한 번 이상 학습한 수,
      12문항 완료 횟수·최고 정답률과 포인트 목표 도전 중/완료를 표시한다. 결과에는 약점 우선 출제 이유를
      표시한다. 10개 층은 정상→입구의 지그재그 탐험 경로로 배치하고 최고 정답률 100% 층을 금색 정복 상태로,
      현재 연습 층을 움직이는 위치 표식으로 보여준다. 첫 진입 위치는 진행 중인 층 또는 아직 정복하지 않은
      가장 낮은 층이다. 2026-08-24부터 **1층만 기본으로 열고 아래층 덱마스터를 빠짐없이 통과해야 다음 한 층이
      열리도록** 지도와 두 시작 RPC를 같은 서버 계산으로 잠갔다. 규칙 전에 시작한 상위 층 판만 이어서 허용한다. 후속으로
      `덱 마스터/정상의 어휘 마스터 도전 → 낱말 카드함`을 연결한다. 내부 공통 학습 엔진·말 카드 리그
      용어는 학생에게 노출하지 않는다. 게임 중에는
      남은 카드·다시 볼 카드·숙련 확인 횟수·획득 가능한 포인트만 보여주고, 첫 성공은
      `확인 1/2`, 다른 형태까지 성공했을 때 `익힘 + 포인트`로 이유를 명확히 표시한다. 기존 학생 대시보드의
      테두리·둥근 카드·색상 토큰을 유지해 별도 게임 사이트처럼 보이지 않게 한다.
- [~] **상태 변화형 포인트**: 입장·클릭·한 문제 정답에는 지급하지 않는다. 2026-08-17에 지급 기준을
      최초 12/12 완벽 연습에서 층별 익힘 진도로 옮겼다. 층 낱말의 익힘이 25·50·75·100%를 넘을 때
      층 총액(교사 설정, 기본 100P)을 20·20·30·30%로 나눠 구간별 고정 이벤트 키로 지급하며, 한 번 받은
      구간은 이후 연습 결과와 무관하게 유지된다. 후속으로 `아는 것 같음→익힘`,
      게임 시작 전부터 약점이던 카드의 회복, 실제 예정 복습 성공처럼 서버가 검증한 상태 변화에만 전용 RPC와
      안정적인 `event_key`로 지급한다. 모든 학습 콘텐츠는 기존 게임 공용 포인트 바구니를 공유하고 콘텐츠 추가를
      이유로 총발행 상한을 늘리지 않는다.
- [~] **2단계 공식 도전** (2026-08-17 덱마스터 구현, 어휘 마스터 관문은 남음). 엔진·휘장·어댑터·화면을
      **콘텐츠 중립**으로 만들어 속담·맞춤법이 어댑터만 새로 쓰면 되게 했다.
      각 층 덱의 모든 낱말을 확인하고 숙달 조건(익힘 80%)을 채운 학생만 `덱마스터`에 도전한다.
      **초기안의 `12문제·3분`은 바꿨다** — 같은 항목의 "빠른 시간에 점수를 주지 않는다", 학기 점수의
      "같은 SP는 시간으로 가르지 않는다"와 모순이고 타자 느린 학생에게만 불리했다. 대신
      **문항당 45초**(찾아볼 여유만 막음)로 두고 난이도를 **직접 입력 비중**으로 옮겼다:
      12문항 = 선택형 7 + 직접 입력 5, 합격 = 전체 10/12 **그리고** 직접 입력 3/5.
      2문항까지 틀려도 통과한다(만점 요구는 실수 한 번에 좌절시키고, 8/12는 대충 해도 통과한다).
      출제는 덱을 문항 수만큼 구간으로 나눠 하나씩 뽑아 고르게 분산한다(개인 연습의 약점 우선과 다르게 —
      시험은 공정한 표본이어야 한다). 문항 수·합격선·시간·자격 비율은 모두 학급 설정이다.
      2026-08-18에 이 여섯 값을 **교사 대시보드에서 직접 정하도록** 화면을 붙였다(어휘의 탑 교사 화면
      `🏆 덱마스터 도전 조건`). 값끼리 물린 DB 제약은 저장 직전 화면에서 맞물리게 잘라 막는다.
      자격에는 비율 외에 **그 층 낱말을 모두 한 번씩 만나야 한다**는 조건이 함께 걸려 있다.
      - [x] **어휘 마스터(정상) 관문**(2026-08-18): 10개 덱마스터를 채우면 지도 꼭대기의 관문이 **열리고**,
            그 시험을 통과해야 휘장을 받는다. 같은 날 **1·2·3단계 순차 관문**으로 나눠,
            정상에 오른 뒤에도 할 일이 두 번 더 남게 했다. 난이도 축은 **직접입력 비중 하나**다
            (8 → 14 → 20개, 3단계는 고를 보기가 없다). 문항 수·합격선·시간은 세 단계가 같다 —
            시간을 줄이면 타자 느린 학생에게만 불리하고, 합격선을 올리면 실수 한 번에 좌절한다.
            통과할 때마다 휘장에 별이 하나씩 늘고(⭐ → ⭐⭐ → ⭐⭐⭐), 받은 별은 내려가지 않는다.
            2026-08-24에는 영구 휘장·실제 최고 통과 기록으로 빠진 앞 단계를 복구하고, 통과한 이전 단계는
            다시 도전할 수 있게 열었다. 다음 단계 건너뛰기와 오답 보충 제한은 그대로 유지한다.
            출제는 그 학년 10개 층 전체에서 고르게 나온다. 자격은 **현재 학년의 10개 층**으로 좁혀서 본다 —
            엔진의 정상 판정은 콘텐츠 중립이라 학년을 모르고, 그대로 두면 3학년 덱마스터로 4학년 정상에 오른다.
      - [x] **도전 버튼 상시 노출**(2026-08-18): 자격이 없어도 버튼을 보여 주고, 누르면 시험 대신
            무엇이 모자란지가 열린다. 목표가 보여야 그쪽으로 간다.
      - [x] **재도전 제한**(2026-08-18): 시험에서 틀린 낱말을 **연습에서 틀린 것과 똑같이** 내리고
            (`learning_engine_record_answer_v1` 재사용), 그 낱말이 전부 다시 익힘이 될 때까지 같은
            도전을 열지 않는다. 맞힌 낱말은 반영하지 않는다(시험이 익힘을 만들지 않는다).
            시간 대기는 쓰지 않았다. 가장 최근에 끝낸 도전 하나만 보고, 중도 종료는 보충 대상을 남기지 않는다.
- [ ] **학기형 비동기 경쟁**: 보유 포인트와 별도로 소비해도 줄지 않는 학기 점수(SP)를 두고 주간 만료 없이
      덱 마스터와 어휘 마스터의 한 번의 완전한 도전 최고 기록을 보존한다. 공식 도전은 같은 학년/난이도에 비교 가능한
      검토 문항 묶음, 개인 수련은 학생 약점 덱을 사용하며 공식 오답은 개인 카드함으로 보낸다. 학생 순위 공개의
      초기 기본값은 `내 주변형`으로 두고 교사가
      학급별로 `개인 성장형(순위 숨김) / 내 주변형 / 제한 경쟁형(상위 3명+내 주변) / 모둠 협력형` 중 선택한다.
      전체 개인 순위는 교사 화면에서만 확인하며, 같은 SP는 시간으로 가르지 않고 공동 순위로 처리한다.
- [ ] **학기 보존·다음 학기 연결**: 개인 숙련 카드는 학기가 바뀌어도 유지하고 학기 SP·공식 관문 결과만 시즌
      스냅샷으로 보관한 뒤 새 학기를 시작한다. 수호룡 학기 시즌과 학기 ID/기간 계약을 공유할 수 있는지 검토하되
      두 기능의 저장 상태를 직접 결합하지 않는다.
- [x] **데이터 준비와 공정성 검증**: 어휘 1,573개 전체를 학년별 10개 덱에 넣고 기존 문항 검토·교사 확인·
      잠금을 마쳤다. 잠긴 덱과 실행 문항 스냅샷을 연결해 학기 중 출제 근거를 고정할 수 있다. 맞춤법 500개 중 문장형
      100개·직접 선택 가능한 사전형 353개와 문맥에 따라 둘 다 맞는 47개를 다른 출제 계약으로 분리한다. 학생 검색
      기록은 오답으로 간주하지 않고 게임에서 실제로 틀린 항목 또는 학생이 명시적으로 연습 카드에 넣은 ID만 사용한다.
- [ ] **1,000명 운영 계약**: 홈에서 항목 전체를 읽지 않고 게임/카드함을 열 때 학급·학생 직접 범위와 페이지 상한으로
      조회한다. 학생 상시 Realtime·짧은 폴링은 두지 않으며, 한 번의 답안은 상태 변경·포인트·SP를 하나의 서버
      트랜잭션으로 끝낸다. 구현 전 스키마 크기·시도 기록 보존 기간·학기 리그 부하 예산을 확정한다.

**(E) 신규 포인트 엔터테인먼트 활동 (어휘의 탑 안정화 뒤 독립 모듈로 추가)**
- [ ] 활동 아이디어 확정 ← 🔶 선생님 결정 (예: 미니게임, 학급 공동 목표·보상, 아바타·방 꾸미기, 랭킹 이벤트 등)
- [ ] 우선순위 1~2개부터 모듈로 구현 (각각 독립 lazy chunk, 포인트 소비 API로만 코어와 연결)
- [ ] 과몰입·사행성 주의 — 교육용 수위 가이드 (뽑기류는 확정 지급·상한 등 안전장치)

**전제**: 신규 엔터테인먼트는 "포인트로 글쓰기 동기부여"라는 기둥3 목적에 종속 — 글쓰기와 무관한 순수 게임화는 지양.

### 4e. 장르별 입력 틀 【1차 완료】
기존 자유 글쓰기의 제목·본문 입력을 기본값으로 보존하고, 교사가 필요할 때만 장르 특성에 맞는 입력 틀을 선택한다.
장르별 교사 폼·학생 편집기·검증은 레지스트리에 등록해 코어 글쓰기 파이프라인을 교체하지 않고 확장한다.
- [x] 미션 생성 첫 단계에서 **자유 글쓰기 / 장르 글쓰기** 선택 (`c894b41`)
- [x] 자유 글쓰기는 기존 장르 하위 종류와 기존 입력창·저장·제출 흐름 유지
- [x] `input_template`·`template_config`·`structured_content` 확장 컬럼 마이그레이션 추가
- [x] **시 쓰기**: 제목·쓴이·연별 입력칸, 최소 연 수·연별 최소 행·완료 포인트(10P 단위) 교사 설정, 제출 검증, 자동 임시저장
- [x] 아이디어마켓의 사용자 명칭을 **회의 안건 만들기/회의 안건 미션**으로 변경하고 장르 레지스트리에 등록
- [x] 시 교사 폼·학생 편집기와 회의 화면을 독립 lazy chunk로 유지
- [x] 회의 안건을 일반 미션 목록에 통합하고 `전체/자유/시/회의` 필터·제안 건수·카드 직접 검토 제공 (`e1e3af8`)
- [x] 회의 생성·수정 후 통합 목록으로 복귀하고 저장한 카드를 강조; 별도 `안건 만들기/안건 관리` 이중 탭 제거
- [x] 회의 작성·내 글 보기는 공통 `StudentWriting`, 친구 제안은 친구 아지트로 분리하고 제출 포인트는 장르 후처리로 유지 (`77ab8c5`)
- [x] 모든 학생 미션 카드에 해당 미션의 `친구 글 보기`를 연결하고 회의 생성 폼을 일반 미션 폼과 같은 전체 폭으로 통일 (`fd77777`)
- [x] 교사 `학생 제안 보기`는 이름·제목 선택 카드와 교실 공유용 대형 본문 모달로 구성 (`609c340`)
- [x] 친구 아지트의 기존 글 탐색에 회의 제안을 포함하고 후보 카드·`마음에 들어요` 선택 수 UI로 구분 (`9503d35`, 운영 배포 대기)
- [x] 자유·시·회의 안건 장르 폼에 공통 평가 루브릭 설정을 연결하고, 장르 매니페스트 기준으로 `평가하기/리포트` 진입 제공 (`2ce1778`)
- [x] 2022 개정 국어과 `3~4학년군 / 5~6학년군` 쓰기·시 표현 성취기준을 루브릭에 연결
- [x] 미션별 평가 결과·교사 의견·학생 글을 성취기준과 함께 국어 평어용 글쓰기 평가 덧붙임 문장 작성·XLSX 내보내기에 연결
- [x] 운영 DB에 장르 입력 틀 마이그레이션 적용 (2026-07-26, 기존 미션 236개 `freeform` 보존·NULL 0)
- [x] 회의 제안·반응 작성자 조인이 학생 글을 숨기지 않도록 운영 `students` 학급 조회 RLS를 `auth_user_class_id()` 기준으로 수정
- [x] 교사·학생 JWT/RLS 조건에서 자유·시·회의 생성→임시저장→제출→회의 보상→교사 평가를 트랜잭션 롤백 방식으로 통합 검증 (2026-07-27, 운영 잔존 데이터 0건)
- [x] 교사 생성→학생 작성·임시저장→제출→교사 확인 실기기 스모크 테스트
- [x] 글 종류 목록을 `mission-types/genreCatalog.js` 한 곳으로 모으고 미션 만들기를 한 화면으로 통합.
      전용 틀 3종(시·관찰 조사 보고서·학급 회의 안건)은 그 화면으로 보내고, 자유 종류 7개는 안내·질문·분량
      프리셋으로 채운다. 일기·독후감은 자율 글쓰기와 겹쳐 목록에서 제외 (2026-08-22)
- [x] 편지 쓰기 전용 틀 추가 (2026-08-22) — 받는 사람·첫인사·하고 싶은 말·끝인사 네 칸과 계기교육용
      편지지 7종 PDF, 손으로 옮겨 쓰는 빈 편지지 인쇄. DB 변경 없이 `structured_content`·`template_config` 사용
- [x] 빈 편지지 7종의 `받는 사람 / 본문 15줄 / 날짜 / 쓴 사람` 위치를 공통 좌표로 통일하고, 계기별 전용
      장식을 빈 양식에 따로 적용. Chrome A4 PDF 7쪽 렌더·PNG 재검사 완료 (2026-08-23)
- [ ] 편지 주고받기(2단계): 배달 상태 전용 테이블·편지함·교사 확인 후 배달·짝 지정. 학생 간 사적 통로가
      되지 않도록 교사 확인을 기본으로 두고, 아무도 못 받는 학생이 없게 교사 짝 지정을 기본 방식으로 한다
- [ ] 수업 효과를 확인한 장르부터 레지스트리에 교사 폼·학생 편집기·검증기를 추가 — 다음 후보는 극본(대본).
      나머지 종류는 프리셋으로 충분해 전용 틀을 만들지 않는다

## Stage 5 — 이후 개선 과제 (순서 무관, 여유 시)

- [x] Realtime 구독 감축 (2026-07-30 대부분 완료): 공지 채널→조회 ✅, FriendsHideout 3중 구독 제거 + 복귀 시 갱신 ✅,
      학급 아지트 4중 구독 제거 ✅, 모듈 설정 구독 제거 ✅.
      2026-08-18 확인: **소스 전체에 Realtime 구독이 0건이다**(`postgres_changes`·`.channel(` 모두).
      한때 남겨 두었던 핵심 흐름(제출→확인·피드백→승인) 구독까지 모두 재호출 기반 갱신으로 바뀌었다.
      웹소켓 500 실측에서 접속 500·채널 1,000 전부 성공(2026-07-31)
- [x] Realtime 잔여: `writing_missions` 구독은 `MissionList`·`useMissionSubmit`에서 제거되어, 쓰기/목록은
      재연결 구독 대신 재호출 기반 갱신(포커스/복귀 기준)으로 전환했다.
- [ ] 회의 안건 결정 보상의 글자 수 조건 제거: 현재 `min_chars + bonus_threshold`를 넘어야 결정 보상이 지급된다.
      의도한 정책은 제출 보상과 별개로 최종 결정 시 글자 수와 무관하게 `bonus_reward`를 지급하는 것이므로,
      결정 로직의 조건과 회의 폼의 `보너스 추가 글자수` 입력을 함께 정리한다(최소 글자 수 목표는 유지).
- [x] **2026-07-31 아이디어마켓 잔여 위험 제거**: 반려 상태 안건을 `결정됨` 으로 바꾸면 CHECK 제약에 걸려
      교사 화면에 DB 오류가 떴다. `IdeaMarketManager.jsx` 가 `is_submitted: true`·`is_returned: false` 를
      함께 보내도록 고쳤다. 운영 DB 에서 롤백 시험으로 전/후 대조 확인 (`2026-07-31`)
- [x] 🗓️ **2026-07-31 묶음 후속** — 드래곤 성장·퇴화 개편, 어휘의 탑 localStorage 도전 제한과
      클라이언트 보상의 서버 기록·공용 상한 전환, 운영 적용·실계정 확인 완료(Stage 4d-C)
- [x] **관리자 대시보드 상단 재정리** (2026-08-25 완료 — 메모리·스왑은 본체 측정 뒤로 미룸)
      **지금 상태**: 2단 탭(그룹 4 · 하위 14)은 이미 있다. 문제는 **늘 보이는 상단 카드 6개가 전부
      `사람 수`** 라는 것이다 — 신규 승인 대기 · 승인된 선생님 · 등록 학생수 · 장기 미접속 · 정리 대상 ·
      새 의견 제보. **서비스가 살아 있는지 알려주는 값이 하나도 없다.** 디스크 여유·컨테이너 상태·
      메모리·스왑은 `운영 > 서버 상태` 탭 안에 숨어 있어, 문제가 나도 그 탭을 열어야만 안다.
      - **상단에 남길 것(늘 노출)** — "지금 손대야 하나"에 답하는 값만.
        ① 컨테이너 정상/전체 ② 디스크 여유 ③ **오늘 조치 필요 건수**(스왑·메모리·CPU 경고를 하나로 합침)
        ④ 승인 대기 ⑤ 새 의견 제보. **①~③은 지금 상단에 없다.**
      - **탭으로 내릴 것** — **가입 교사 수 · 학급 수 · 승인된 선생님 수 · 등록 학생 수**.
        매일 볼 필요 없는 **규모 지표**라 `현황 > 사용량` 으로 옮긴다(2026-08-25 사용자 확인).
        늘어나는 숫자는 좋은 소식이지 **조치가 필요한 신호가 아니다.**
      - **⚠️ 대시보드는 도커 VM 안만 잰다**(2026-08-24 확인). 맥 본체가 스왑 6.86GB 를 쓰고 있어도
        화면은 `스왑 3MB · 지켜보기` 로 보인다. **상단에 올리기 전에 본체 값도 재도록 고쳐야 한다** —
        아니면 "정상"이라고 크게 띄워 놓고 실제로는 굶고 있는 상태가 된다.
      - 뱃지는 이미 `pending`·`dormant`·`cleanup`·`feedback` 에 붙어 있다. 상단 카드와 **같은 수를
        보게** 맞춘다(지금은 각자 센다).
      - 상단은 **줄 하나**로 둔다. 카드 6개가 큰 칸으로 자리를 먹던 것을 오늘 연구소 실시간 보기에서
        고친 것과 같은 방식으로 정리한다.
- [ ] **좁은 화면(폰·태블릿) 뒤틀림 정리** (2026-08-25 실측, 나중에)
      화면이 작아지면 배치가 어긋나고 오른쪽이 잘린다. `viewport` 설정과 `vw` 글자 크기(56곳, 전부
      `clamp` 라 바닥 있음)는 문제가 없다. 원인은 셋이다.
      - **고정 폭이 화면을 넘는다** — `width: NNNpx` 169곳, 고정 px 격자 열 87곳, `min-width` 17곳.
        `width: min(340px, 100%)` · `minmax(0, 1fr)` 로 바꾼다(2026-08-25 공방 세트 목록에서 쓴 방법).
      - **`body { overflow-x: hidden }`(`App.css:27`)이 그 넘침을 감춘다** — 사이바는 안 보이지만
        오른쪽이 **잘려 나간다**. ⚠️ 이것을 **먼저 걷으면 안 된다.** 넘침을 다 고친 **뒤** 없앤다.
        지금 걷으면 잘려 있던 것이 사이바로 드러나 더 어지러워 보인다.
      - **중단점이 41종이라 부품마다 다른 폭에서 무너진다** — 예: 640(17곳)·760(13)·720(12)·680(9)·
        560(9)·520(9). 3~4개로 줄이면 부품들이 같은 폭에서 함께 바뀐다.
      화면 단위로 한 곳씩 고치고 실제 기기에서 확인한다. `transform: scale` 로 통째로 줄이는 방법은
      **쓰지 않는다** — 글자와 터치 영역이 함께 작아져 지금까지 고쳐 온 "글씨가 작다"가 되살아난다.
- [ ] Supabase 이미지·Caddy 정기 업데이트 루틴

---

## 🔶 미결정 / 확인 대기

| 항목 | 상태 |
|---|---|
| Postgres 에러 130건의 메시지 확인 (Logs → Postgres) | 대기 — timeout/connection이면 이관으로 해소, RPC exception이면 코드 수정 |
| `reward_for_vocab_tower` 서버 검증 여부 | ✅ 구버전 임의 금액 RPC 차단·신규 서버 계산 운영 적용 완료 |
| 연구소 기존 데이터: 유지(UUID 매핑) vs 리셋 | ✅ 결정(2026-07-24) — 유승현·최원진만 매핑, 나머지 삭제 |
| 기존 기능 중 삭제/기본OFF 확정 | ✅ 아지트온클래스=완전 삭제(2026-08-09), 아이디어마켓=입력미션 유지 (2026-07-27) |
| 자유 글쓰기 포인트 정책 | Stage 4c에서 결정 |
| 신규 포인트 엔터테인먼트 활동 종류 (뽑기·미니게임·꾸미기 등) | 🔶 Stage 4d에서 선생님 결정 |
| 드래곤 다이나믹화·이벤트 범위 | 🔶 Stage 4d에서 선생님 결정 |

## 📝 결정 기록

- 2026-08-31: **학급 발자국의 과제 활동일은 승인 시각, 독서록·일기는 완료본 생성 시각을 사용한다.**
  친구 교류는 자기 글을 제외한 실제 댓글·반응 이벤트 수로 양쪽 기준을 맞춘다. 포인트는 활동 획득과
  교사 조정을 분리하고 시작 보너스도 서버 응답에서 별도 보존해, 교사 조정이 학습 활동처럼 보이지 않게 한다.
- 2026-08-31: **관리자 시험 계정 확인과 코드·운영 데이터 재점검 뒤 기록가·독서가 단계 보상을 전체 학급에 공개한다.**
  현재·미래 학급은 전역 스위치로 열되 학급별 명시적 ON/OFF가 우선하며, 새 학급에는 첫 공용 칭호 시즌을
  자동 생성한다. 보상은 자동 지급하지 않고 학생이 직접 수령한다. 운영 이상 시 전역 OFF로 즉시 중단하고,
  공개 뒤 7일 기술 안정성과 4주 포인트 경제 관측은 계속한다.
- 2026-08-31: **칭호 시즌은 작가·소통·기록가·독서가 네 축이 하나를 공유하고, 1차 단계 포인트는 새 기록가·
  독서가에만 붙인다.** 기존 시즌 ID·마감 스냅샷을 재사용하며 새 학기에는 진행도와 수령 기회만 새로 시작하고
  글·포인트·공방 아이템은 보존한다. 시작 단계는 0P, 이미 오른 단계도 자동 지급하지 않고 학생이 직접 받는다.
  기록가·독서가 각각 2~7단계 `200/400/600/800/1,200/1,800P`, 종목별 누적 5,000P로 구성한다.
- 2026-08-31: **독서록·일기의 `보완 요청`은 교사가 처리한 기록이더라도 `확인 완료`가 아닌 학생 수정 대기 상태로 분리한다.**
  교사 화면과 RPC 모두 `미확인 / 보완 중 / 확인 완료 / 전체` 네 상태를 공용 계약으로 사용하고, 학생이 수정해
  다시 제출한 뒤에만 기존 흐름대로 `미확인`으로 돌아간다.
- 2026-08-31: **학생 홈의 네 칭호는 포인트와 한 줄 5등분하지 않고 독립된 2×2 영역으로 보여 준다.**
  넓은 화면은 왼쪽 포인트·오른쪽 칭호 2×2, 작은 화면은 포인트 한 줄·아래 칭호 2×2로 두고 기존 칭호
  데이터·단계 계산·열기 동작은 그대로 재사용한다. 각 단계 표시는 칭호 종류·이름의 오른쪽 전용 열에서
  상하·좌우 가운데 정렬해 네 카드의 읽는 위치를 맞춘다.
- 2026-08-31: **작가·소통·기록가·독서가 네 칭호는 학생 홈과 같은 학급 친구 아지트에도 함께 공개한다.**
  홈은 이미 받은 공용 칭호 상태를 재사용하고, 친구 화면은 기존 명단 RPC 한 번에 네 원자료를 담아 칭호별 조회나
  민감한 글 내용·포인트 공개를 추가하지 않는다.
- 2026-08-31: **일기와 독서록은 작가 글자 수에서 분리해 자율 활동 전용 칭호로 인정한다.** 작가 칭호는
  교사 과제와 앞으로 추가될 일반 글쓰기를 세고, 기존 댓글·반응 기반 `독자` 축은 실제 의미에 맞게 `소통`
  칭호로 이름을 바꾼다. 기록가는 교사가 확인한 서로 다른 일기 날짜 `0/3/7/14/21/30/40일`, 독서가는
  확인 독서록·서로 다른 책을 함께 만족하는 `0/0, 3/3, 5/4, 8/6, 12/9, 18/13, 25/18`의 7단계를
  사용한다. 이번 시즌도 처음부터 분리 기준으로 즉시 다시 계산하며 작가 단계는 내려갈 수 있다. 포인트·작성 글·
  보유 아이템은 바꾸지 않는다.
- 2026-08-30: **카드 크기 조절은 화면마다 버튼을 복사하지 않고 공통 `card-layout` 모듈을 사용한다.**
  조절기는 상태·데이터를 소유하지 않는 `value/onChange` 컴포넌트라 쉽게 넣고 뺄 수 있어야 한다. 선생님
  과제와 보관함은 같은 작게·보통·크게 선택과 4·3·2열 기준을 공유하고 모바일은 항상 1열로 둔다.
- 2026-08-29: **자비스의 최우선 제품 기준은 기능 수가 아니라 혼자 쓰는 사용자의 업무 절감이다.** 기본
  흐름은 `한 문장 요청 → 오픈클로 자동 처리 → 대시보드에서 결과 확인·정정`으로 두며, 같은 내용을 사람이
  다시 입력하는 화면은 만들지 않는다. 자비스 저장소의 `AGENTS.md`·`ROADMAP.md`·`WORKLOG.md`를 모델 공통
  정본으로 삼고 Claude는 `CLAUDE.md`에서 같은 기준을 읽는다.
- 2026-08-29: **자비스 공유 탭은 사람이 문서를 조립하는 작업대가 아니라 오픈클로 산출물을 확인하는
  자료실로 쓴다.** 화면에는 참고 양식·공유문서·공유 링크 세 구역만 두고 필요한 항목만 펼친다. 수동 양식
  조립기와 자료 수합 편집기는 화면에서 숨기되 데이터·컴포넌트·공개 입력 경로는 삭제하지 않는다.
- 2026-08-29: **Google Sheet 링크 권한은 요구에 범위가 명시될 때만 자동 적용한다.** `편집 가능한 링크`면
  `link_editor`, `보기 링크`면 `link_viewer`, 단순 `공유`면 private를 유지한다. 전체 Drive 대신 자비스가
  만든·선택한 파일만 관리하는 `drive.file` 별도 토큰을 쓰고, 오픈클로는 `/jarvis_google_sheet` 명시 명령만
  허용한다. 일반 대화를 모델이 임의의 외부 쓰기로 자동 연결하지 않는다.
- 2026-08-29: **Google Sheet 직접 입력 문서와 자비스 자료 수합 양식은 다른 기능으로 관리한다.** 직접
  입력 문서는 실제 Spreadsheet를 `공유` 보관함에 등록하고 링크를 받은 사람이 Sheet에서 입력한다.
  새 파일은 소유자 전용으로 만들며, 광범위한 링크 편집 공개는 문서별 명시 승인 없이 자동으로 열지 않는다.
- 2026-08-29: **학년별·부서별 자료 수합은 링크와 공유 대시보드 카드를 여러 개 만들지 않는다.** 필수
  단일선택 항목 하나를 탭 구분으로 삼아 한 입력 링크·한 관리 카드·한 비공개 Google Sheet를 유지하고,
  응답만 선택지별 탭에 나눈다. 자연어를 자동 외부 쓰기로 연결하는 것은 별도 승인 전까지 켜지 않는다.
- 2026-08-29: **자비스는 GitHub에 푸시하지 않고 맥미니 로컬 Git + 통합 백업으로 운영한다.** 배포는
  `~/Jarvis_Brain_Local`에서 로컬 Docker 이미지를 빌드해 프론트 컨테이너만 교체하고 HTTP 스모크로
  확인한다. 개인 자료는 Git에 넣지 않으며, 저장소에 원격을 추가하지 않는다.
- 2026-08-29: **자비스 공개 입력은 투표·자유 의견이 아니라 교직원 자료 수합에만 집중한다.** 양식을
  만들면 같은 열의 Google Sheet를 소유자 전용 비공개로 생성하고, 응답자는 암호가 있는 자비스 링크만
  사용한다. 결과는 DB에 먼저 저장한 뒤 Sheet에 행 추가하며, 공유 탭에서 양식 수정·저장·공개·마감·
  응답 확인을 한곳에서 처리한다. 응답이 들어온 뒤에는 열 구조를 바꾸지 않는다.
- 2026-08-29: **자비스 단축 명령은 확인·조회에 집중하고 공유 자료는 사람이 눈으로 확인한 뒤 직접 만든다.**
  `/week`는 이번 주 일정·업무를 채팅 표로만 보여주며 `app.documents`·`app.document_shares`에 쓰거나
  공유 탭 자료·링크를 자동 생성하지 않는다. 명시적으로 자료 작성을 요청하는 별도 흐름은 유지한다.
- 2026-08-29: **마지막 로그인 90~364일은 장기 미접속, 365일 이상은 휴면계정으로 논리 분류한다.**
  휴면 분류만으로 계정·학급·학생 데이터를 별도 DB로 옮기거나 자동 삭제하지 않고, 다시 로그인하면
  마지막 로그인 시각에 따라 자동 해제한다. 이용약관·개인정보처리방침은 2026-08-29 개정하고 기존의
  7일 고지 규칙을 반영해 2026-09-05부터 시행한다. 시행 전 앱 공지사항 게시가 필요하다.

- 2026-08-29: **관리자 대시보드의 장기 미접속은 90일(3개월) 고정 표시이며 자동 삭제 기준이 아니다.**
  학급 미개설·학생 미등록 여부와 무관하게 모든 가입 교사의 마지막 접속일로 따로 판정하고,
  `정리 후보/정리 대상` 카드·탭·배지는 운영 화면에서 숨긴다. 현재 개인정보처리방침이 교사 계정을
  `회원 탈퇴 시까지` 보유한다고 정했으므로 장기 미접속 목록에서는 되돌릴 수 있는 승인 취소만 제공한다.
  장기 미접속 삭제 정책이 필요해지면 약관·처리방침 개정과 기존 회원 사전 안내·이의 제기·계속 이용 선택
  절차를 먼저 마련한 뒤 별도 작업으로 진행한다.

- 2026-08-27: **허용 목록에 남기는 이유는 "누가 부르는지" 이름을 적어 확인할 수 있게 쓰고, 확인 날짜를
  `reasonCheckedAt` 에 남긴다.** 목록의 값어치는 통째로 "적힌 이유는 믿을 수 있다"에 걸려 있는데,
  사후 점검에서 **이유 8개 중 3개가 사실과 달랐다**(둘은 "RLS 정책이 쓴다"고 적혀 있었지만 정책 64개 중
  0개가 쓰고 있었다). 틀린 이유가 하나 통과하면 검사는 그 함수를 영원히 초록불로 봐주고 다음 사람은
  다시 안 캔다. `check:rpc-surface` 가 확인 날짜가 없거나 120일이 지나면 알려 준다 —
  지금 미확인 2건이 있어 당장은 **막지 않고 경고만** 한다.

- 2026-08-30: **교사 상단 메뉴에서는 `글쓰기 연구소`의 Beta 표기를 제거한다.** 학생 홈의 연구소 활동
  카드는 이번 변경 범위가 아니므로 기존 Beta 표기를 유지하며, 우리반·이웃 아지트는 아직 준비 단계라
  상단 메뉴에는 이름 속 괄호 대신 작은 `BETA` 배지를 붙인다.

- 2026-08-30: **교사 상단 메뉴는 기능 생성 순서가 아니라 실제 업무 흐름으로 묶는다.** 순서는
  `글쓰기 / 글쓰기 연구소 / 우리반 아지트` → `학급 운영 / 학생 / 학급 발자국 / 평가·기록` →
  `아지트 놀이터 / 학급운영도구 / 이웃 아지트` → `설정`으로 고정하고, 구역 사이에는 얇은 시각 경계만 둔다.
  라우트·탭 ID·데이터 요청은 바꾸지 않으며 좁은 화면에서는 가로 스크롤과 선택 메뉴 자동 노출을 유지한다.

- 2026-08-30: **`이웃 아지트`는 설정 항목이 아니라 `학급운영도구` 오른쪽의 독립 교사 메인 메뉴로 둔다.**
  현재는 DB 조회 없는 Beta 준비 화면만 제공하고, 설정의 중복 진입점은 제거한다. 초대·양쪽 교사 승인·학생
  피드가 구현될 때 같은 모듈 진입점 안에서 확장하며 학생 메뉴는 그때 학급별 ON/OFF와 함께 연다.

- 2026-08-30: **이웃 아지트의 옛 두 학급 연결안은 최대 네 학급의 호스트형 초대 공간으로 확장한다.**
  일반 교사는 `internal`에서 위 준비 화면만 보고, 실제 기능은 관리자 `운영 → 기능 공개`에서 먼저 검수한다.
  사용자 명시 승인 전에는 자동으로 전체 공개하지 않으며, `public_beta` 뒤에도 학생 학급 스위치는 기본 OFF다.
  상세 정본은 `NEIGHBOR_AGIT_PLAN.md`다.

- 2026-08-30: **관리자 내부 시험에는 실제 운영 학급을 쓰지 않는다.** 로그인 불가 합성 교사·학생 0명 학급을
  별도 비공개 등록부에 명시해 관리자 후보와 내부 공간 생성 RPC 양쪽을 제한한다. 합성 계정은 실제 교사 수에는
  포함되지만 비밀번호·OAuth identity가 없고 로그인 금지 상태로 유지한다.

- 2026-08-30: **학급운영도구의 기본 순서는 `얘들아, 밥 먹자! → 자리·역할 배치 → QR 코드 관리`로 둔다.**
  첫 진입은 자체 서버에서 동작하는 급식판만 지연 로드하고, 외부 쌤링크 QR 화면은 교사가 직접 선택할 때만
  불러와 불필요한 첫 페이지 요청을 만들지 않는다.

- 2026-08-30: **쌤링크 도구의 메뉴 이름은 `QR 코드 관리`에서 `URL 단축하기`로 바꾼다.**
  핵심 작업인 긴 주소 줄이기를 메뉴에서 먼저 설명하고, 안쪽의 QR 생성·공유 기능과 `samlink` 모듈 ID·주소는
  그대로 유지한다. 최종 도구 순서는 `얘들아, 밥 먹자! → 자리·역할 배치 → URL 단축하기`다.

- 2026-08-30: **`우리반 아지트`는 우리 반 글의 2.5D 전시관과 학급 문집 제작·PDF 내보내기 공간으로 쓴다.**
  교사는 학급·학기·주제별 전시를 여러 개 만들고, 선택한 전시만 만료·해지 가능한 외부 읽기 전용 링크로
  공유할 수 있다. 외부에는 교사가 확인한 가림 이름의 공개 스냅샷만 제공한다. 학급 게시판·투표·채팅·
  자유 이동 메타버스는 범위에서 제외한다. 운영 배포와 사용자 공개를 분리해
  `관리자 내부 → 지정 학급 시범 → 모든 교사 기본 OFF 공개` 순서로 열고, 학생은 관리자 공개 허용·교사
  학급 스위치 ON·공개 콘텐츠 존재를 모두 만족할 때만 홈 카드와 RPC에 접근한다. 상세 정본은
  `CLASS_AGIT_PLAN.md`다.

- 2026-08-27: **위험하지 않은 잔재 함수는 급히 지우지 않되, 허용 목록에는 "쓰는 곳이 없다"고 사실대로
  적는다.** 지우는 것이 원칙이지만 지우는 일 자체가 마이그레이션·배포·검증을 부르므로 위험도로 판단한다.
  권한을 넘기거나 남의 자료를 바꿀 수 있으면 즉시 지우고(`record_comment_ai_review`), `auth.uid()` 기준으로
  부르는 사람이 이미 아는 사실만 돌려주면 미룬다(`auth_user_is_teacher_of`·`is_teacher_of_student`).
  **있지도 않은 용도를 지어내 적는 것**이 이번 문제의 뿌리였으므로, 미루는 것과 거짓 이유를 적는 것은 다르다.

- 2026-08-27: **DB 함수는 마이그레이션으로만 만든다.** psql 로 직접 만든 함수는 저장소에 기록이 남지 않아
  DB 를 다시 세우면 사라지고, 아무도 존재를 모른 채 권한만 열려 있다. 실제로
  `auth_user_is_teacher_of`·`is_teacher_of_student` 는 운영 DB 에 SECURITY DEFINER 로 열려 있는데
  **만드는 마이그레이션이 저장소에 없다.** 함수를 지울 때 보는 곳도 넷으로 못 박는다 —
  이 저장소·다른 앱 배포본·**다른 DB 함수의 본문**·**RLS 정책**.

- 2026-08-26: **새 판(`_vN`)을 만들면 옛 판은 같은 마이그레이션에서 지운다. 못 지우면 이유를 적는다.**
  `ops/rpc-surface-allowlist.json` 에 이름·이유·언제 지울 수 있는지를 적어야 `check:rpc-surface` 가
  통과한다. 사람의 기억이 아니라 푸시 전 검사가 막는다.

- 2026-08-26: **댓글 판정은 서버 대기열 하나만 남기고, 학생이 부를 수 있는 옛 판정 함수는 DB에서 지운다.**
  앱 코드가 부르지 않는 것만 검사하면 부족하다 — 권한이 열린 SECURITY DEFINER 함수는 브라우저에서 그대로
  불린다. 새 판을 만들 때 옛 판을 같은 마이그레이션에서 정리하고, 못 지우면 이유를 주석으로 남긴다.

- 2026-08-26: **DB 함수를 지우기 전에 이 DB 를 함께 쓰는 다른 앱의 배포본까지 확인한다.** 연구소·샘링크·
  자비스·클래스룸툴은 같은 아지트 API 를 부른다. 실제로 `get_student_spelling_entries_v1` 은 아지트
  저장소에는 참조가 없지만 연구소 배포본이 아직 부르고 있어 삭제를 보류했다.

- 2026-08-26: **`우리반 아지트`의 활동 내용과 운영 방식은 아직 정하지 않는다.** 교사 상단에서는
  `글쓰기 연구소` 바로 다음에 Beta 탭을 두되, 방향이 확정되기 전에는 특정 활동을 예고하지 않고
  선택한 학급의 공간을 준비 중이라는 배너만 표시한다. DB·학생 화면은 변경하지 않는다.

- 2026-08-26: **댓글은 별도 독자 칭호 성장으로 인정하므로 앞으로 포인트를 지급하지 않는다.** 기존
  `comment_reward` 원장 9,026건·45,130P와 이미 반영된 학생 잔액은 회수하거나 다시 계산하지 않고 이전
  기록으로 보존한다. 댓글 승인 RPC와 공용 포인트 엔진에서 신규 생성 경로를 닫고 구형 전용 함수는 제거한다.

- 2026-08-26: **모든 학급의 맞춤법 근거는 매주 월요일 한 번 정리하고, 기존 500개 전체를 AI에 보내지 않는다.**
  AI 검사·학생 검색·교사 학급 자료의 짧은 표현과 집계만 모아 기본·공통 자료 정확 일치는 로컬 코드가 제거한다.
  남은 후보와 유사 자료 최대 3개만 AI가 구조화 검수하며 같은 후보 결과는 캐시한다. AI는 게시 권한이 없고,
  관리자가 고른 결과만 모든 학급 공통 자료가 된다.

- 2026-08-26: **선택 미션의 학생별 현황은 24명 기준 색상 카드와 같은 데이터의 확대 모달로 보여 준다.**
  기본 화면은 4열×최대 6행, 확대 화면은 6열×4행으로 구성하고 이름과 상태 문구를 함께 표시해 색에만 의존하지
  않는다. 확대 창은 기존 12초 스냅샷·상태 필터·변화 강조를 그대로 공유하며 별도 RPC나 폴링을 추가하지 않는다.
  전체 활성 과제 범위는 여러 과제의 상태 수를 비교해야 하므로 기존 표를 유지한다.

- 2026-08-26: **실시간 제출 전광판은 `전체 활성 글 과제`를 기본으로 유지하고 필요할 때 미션 하나를 같은
  선택창에서 좁혀 본다.** 선택 미션에서는 학생마다 승인·확인 대기·다시쓰기·미제출 중 현재 상태 하나를 표시하고
  상태별 명단 필터를 제공한다. 전체와 미션별 데이터를 동시에 읽지 않고 v2 RPC 한 건의 범위 인자만 바꾸며,
  12초 스냅샷 사이의 새 제출·상태 변화는 해당 행을 잠시 강조해 전광판처럼 보이게 한다.

- 2026-08-26: **실시간 제출 전광판은 상단 합계 카드와 과제별 진행 막대 대신 `최근 제출`과 `학생별 상태표`에
  집중한다.** 12초 폴링 응답은 과제×학생 원본 행렬을 보내지 않고 활성 글 과제의 승인·확인 대기·다시쓰기·
  미제출 수만 학생별로 합산하며 최대 100명, 최근 제출 8건으로 제한한다. 별도 승인 흐름이 없는 의견 모으기는
  이 상태표 집계에서 제외한다.

- 2026-08-26: **`학급운영도구`의 쌤링크 메뉴 이름은 사용 목적이 먼저 보이도록 `QR 코드 관리`로 표시한다.**
  내부 `samlink` 모듈 ID와 임베드 주소는 유지하며, 메뉴 안에서 기존 쌤링크 서비스를 그대로 사용한다.

- 2026-08-26: **교사 대시보드의 기존 `수업 도구` 탭은 수업용 앱뿐 아니라 자리·역할 배치와 급식·비고처럼
  학급 운영 기능도 함께 담으므로 사용자 표시 이름을 `학급운영도구`로 통일한다.** 내부 `tools` 탭 ID와
  `part: tool` 모듈 계약은 유지하고 메뉴·하위 탭·도움말·도구 화면 머리말이 공용 이름 상수를 사용한다.

- 2026-08-26: **나이스 Open API 인증키는 프론트·Git·문서에 넣지 않고 맥미니의 Git 밖
  `~/agit-supabase/secrets.agit.env`에 `NEIS_API_KEY`로만 보관한다.** 학교 검색과 급식 조회는 서버 함수가
  환경변수의 키를 읽어 대신 호출하며 브라우저에는 키 원문을 절대 전달하지 않는다. 급식 공개 정보는 6시간
  캐시하고 학생별 비고와 물리적으로 분리한다.

- 2026-08-26: **첫 급식 수업 도구의 이름은 `얘들아, 밥 먹자!`, 설명은 `오늘 급식과 우리 반 학생별 비고를
  확인해요`로 한다.** 가입 시 선택한 초등학교를 기본으로 자동 연동하되 현재 학급만 또는 교사 기본 학교를
  바꿀 수 있다. 구조화된 알레르기·건강정보는 수집하지 않고 필요한 학생에게만 최대 300자의 선택 비고를 두며,
  민감한 건강정보 입력을 금지한다. 학생 이름과 비고는 교사 화면에서만 다루고 교실 전체화면에는 급식만 표시한다.

- 2026-08-26: **학교 코드가 없는 기존 교사는 저장된 학교명이 나이스 검색 결과와 정확히 하나만 일치할 때만
  도구 최초 진입에서 기본 학교로 자동 연결한다.** 동명이교 등 결과가 둘 이상이거나 일치하지 않으면 임의로 고르지
  않고, 기존 학교명을 채운 학교 설정 창에서 교사가 직접 선택하게 한다.

- 2026-08-25: **교사가 과제 글을 학생에게 돌려 실제 상태가 `다시쓰기 중`이 되는 전환은 이전 경로와
  관계없이 항상 `writing.rewrite_requested` 알림을 남긴다.** 일반 되돌려주기뿐 아니라 강제 회수 뒤 다시
  주기도 포함하되, 이미 같은 상태인 행의 재저장과 강제 회수 자체는 새 알림을 만들지 않는다.

- 2026-08-25: **교사 글 상세에서 연구소 결과를 보여 주는 바깥 칸은 특정 결과 유형이 아니라
  `연구소 참고자료`라는 공용 자리로 부른다.** 지금은 고정 개요만 표시하지만 다른 연구소 활동 결과가
  이어질 수 있으므로, `글 개요짜기` 같은 구체적인 활동 이름은 각 카드 안쪽에서만 표시한다.

- 2026-08-25: **교사 제출 전광판의 자동 갱신은 최신 8건을 유지하고, 더 긴 제출 기록은 교사가 요청할 때만
  최대 100건을 한 번 읽는다.** 여러 과제가 동시에 진행돼도 과제별 제목은 한 번만 표시하며, 학생 행은 과제
  목록이 아니라 해당 `post_id`의 글 상세를 바로 연다. 100건보다 오래된 기록은 기존 과제별 글 확인에서 찾는다.

- 2026-08-25: **선생님 과제는 `과제 만들기·관리`와 `실시간 제출 현황` 두 내부 탭으로 분리한다.** 작성·수정과
  수업 중 모니터링을 한 화면에 동시에 밀어 넣지 않는다. 폼 상태와 마지막 내부 탭은 유지하고, 전광판 폴링은
  제출 현황을 실제로 열었을 때 즉시 한 번 확인한 뒤에만 12초 주기로 이어 간다. 확인 대기 수는 탭 배지로 알린다.

- 2026-08-25: **선생님 과제 상단 조절기는 열 수가 아니라 미션 카드의 읽기 크기를 선택한다.** 작게·보통·크게
  세 프리셋이 전체 너비 과제 목록의 열 수를 4·3·2열로 바꾸고 카드 내부 여백·제목·태그·버튼 문구를 함께
  조절한다. 모바일은 항상 1열이고 보관함과 제출 현황은 이 설정에서 분리한다.

- 2026-08-25: **초기에는 과제 카드와 제출 전광판을 데스크톱 1:1로 함께 보여 주기로 했으나, 같은 날 내부 탭
  분리 결정으로 대체했다.** 동일한 학급 스냅샷과 제출·승인·확인 대기·다시쓰기 상태 계약은 그대로 유지한다.

- 2026-08-25: **학생 작품 감상은 글 상세에 화면별 전체 화면 코드를 만들지 않고 글쓰기 공용 감상 모달로
  확장한다.** 교사 최초글·최종글 비교 카드, 현재 글 본문과 `학생 글 모아보기`가 같은 모달·클릭 트리거를
  사용하며, 글자 크기 단계와 키보드 접근성·모바일 안전 영역은 모듈이 소유한다. 구조화 글은 장르 전용 읽기
  렌더러를 자식으로 전달한다.

- 2026-08-25: **학생 최상위 메뉴의 부모는 항상 메인 대시보드로 고정한다.** 하단 메뉴끼리 이동해도 직전
  메뉴를 부모로 남기지 않으며, 편집기·상세처럼 실제 하위 화면만 명시한 부모 목록으로 돌아간다. 전체 화면
  오버레이는 메인 위 방문 기록 한 단계와 일회용 열기 신호를 사용하고 현재 하단 메뉴 활성 상태를 함께 갱신한다.
  새 메뉴는 `studentNavigation.js` 단일 원본과 `src/components/student/README.md` 계약을 따른다.

- 2026-08-25: **교사의 과제 글 반려·승인만 학생 앱 전역 상단 배너로 알린다.** 즉시 이동 버튼·읽음 처리는
  넣지 않고 8초간 단순 표시한다. 학생 상시 WebSocket은 현재 600 연결 한도 때문에 사용하지 않으며, 보이는 탭에서
  12초마다 본인 최소 신호만 읽는 코어 폴링 예외를 둔다. 1,000명 기준 평균 83.3회/초 예산이며 최초 시점 분산,
  요청 겹침 금지, 숨은 탭 중단, 복귀 즉시 확인, 오류 후퇴와 기존 홈 동기화 fallback을 함께 유지한다.

- 2026-08-24: **접속 학생과 글쓰기 활동 학생을 같은 숫자로 부르지 않는다.** 접속은 학생 인증 RPC가
  기록하는 `last_login`, 글쓰기 활동은 학생이 남긴 글·발자국 이벤트를 기준으로 별도 집계한다. 대시보드의
  일 단위는 한국 날짜, 글 수는 실제 제출을 기준으로 하며 기록이 없는 서버 값은 0·정상으로 표시하지 않는다.

- 2026-08-24: **어휘의 탑은 1층부터 덱마스터를 통과한 순서대로 다음 층을 연다.** 과거 상위 층 기록이
  있어도 아래층의 빈 통과 기록을 건너뛰어 새 연습·덱마스터를 시작할 수 없다. 다만 규칙 도입 전에 이미
  시작한 판은 학생이 갇히지 않도록 이어서 끝낼 수 있다. 정상 관문은 영구 휘장과 최고 단계 통과 기록으로
  빠진 앞 단계를 복구하고, 받은 별을 유지한 채 통과한 이전 단계 재도전을 허용한다.

- 2026-08-24: **자리·역할의 랜덤 원본은 교사 수정본을 저장해도 지우지 않는다.** 수정본 payload가
  `originalHistoryId`로 원본을 가리키며 지난 기록에서 둘 중 어느 쪽을 열어도 좌우 비교한다. 같은 결과를
  다시 고치면 원본은 유지하고 이전 수정본만 최신 수정본으로 바꾼다. 다음 랜덤 배정의 최근 회차 조건은
  원본·수정본 쌍을 두 번 세지 않고 실제 최종 수정본을 한 회차로 센다.

- 2026-08-24: **자리 중복 회피는 최근 3회, 역할 중복 회피는 최근 4회만 본다.** 표시 문구와 엔진이
  같은 상수를 사용하며 이 조건은 랜덤 생성에만 적용한다. 교사 편집은 수업 상황을 보완하는 교사의 권한이므로
  조건 점수를 다시 계산하거나 저장을 막지 않고 지난 기록에 `교사 편집`으로 표시한다.

- 2026-08-24: **남은 165개도 뜻·예문·내용 난이도·4지선다까지 모두 작성하되, 자동 선별 오답이 포함된
  자료는 편집 완료로 가장하지 않는다.** 기존 직접 검수 20개만 `editorial_review`, 나머지는
  `source_imported + editorial_review_required + teacher_confirmation_required`로 유지한다. 교사는 시범
  20개부터 묶음별로 확인하고, 확인된 항목만 순차 승격한다.

- 2026-08-23: **콘텐츠 편집 검수와 교사 최종 확인을 상태로 구분한다.** 3·4학년 미리 만나기 20개에
  초등학생용 뜻·교실 예문·4지선다를 작성해도 곧바로 `teacher_confirmed`나 `published`로 올리지 않는다.
  `editorial_review + teacher_confirmation_required`로 보존하고, 교사 확인 전에는 운영 DB 시드와 학생 출제를
  연결하지 않는다.

- 2026-08-23: **속담·사자성어의 교육과정 기준 학년과 실제 제공 학년을 분리한다.** 기본팩 185개는 모두
  2022 개정 국어과 5·6학년군의 관용 표현 학습을 기준점으로 두되, 속담은 직접 연결 자료, 사자성어는 어휘
  확장 자료로 표시한다. 쉬운 시범 20개만 3·4학년 미리 만나기를 허용하며, 이는 3·4학년 교육과정 귀속을
  뜻하지 않는다. 같은 항목을 복제하지 않고 학년군별 문제 난이도만 다르게 연결한다.

- 2026-08-23: **속담·사자성어는 일일 미션 전용 복사본을 만들지 않고 공통 콘텐츠 원본·문제 변형·묶음으로
  분리한다.** 오늘의 말 미션과 향후 별도 학습 콘텐츠는 같은 `content_type + collection_key + item_key`를
  기존 공통 학습 엔진에 기록해 첫 확인부터 반복 숙달까지 진도를 이어 간다. 내용 난이도와 문제 난이도를 분리하고,
  학년군·선택형 문제를 사람이 검수하지 않은 원본은 학생에게 제공하거나 DB `published`로 승격하지 않는다.

- 2026-08-23: **학급 활동 도구는 교사 설정 뒤 학급 전체가 함께 보는 공통 전체 화면 모달을 사용하고,
  독립 모듈로 하나씩 추가한다. 첫 구현은 T0 기본 셸과 T5 `함께 만드는 글감 조합기`를 결합한다.** 이야기 글
  5개 항목·부분 재추첨·최종 카드·타이머·지난 기록으로 실제 수업 흐름을 확인한 뒤 공통 셸을 확정한다.
  학생 참여형 도구는 활동 화면을 연 동안만 연결하며, 세부 범위는 `CLASS_ACTIVITY_TOOLS_PLAN.md`를 따른다.

- 2026-08-23: **서바이벌은 자리 배치·역할 나누기의 전체 포맷만 아지트 수업 도구로 가져오고, 나머지는
  원본 데이터로 보관한다.** 새 도구는 아지트 학급 명부와 통합 DB를 사용한다. 기존 주소는 접속 즉시 이전
  알림을 띄우고 2026-09-20까지 가입·이동 안내와 브라우저별 JSON 백업을 제공한 뒤 앱 컨테이너를 내리고
  아지트 도구로 301 연결한다.

- 2026-08-26: **서바이벌 이전 안내·백업 제공 기한은 2026-09-20까지로 앞당긴다.** 안내 팝업과 본문은
  종료일 대신 이용 가능한 마지막 날을 같은 문구로 표시하고, 남은 기간 계산은 9월 21일 00:00(KST)을 기준으로 한다.

- 2026-08-26: **메모리 경고는 누적 스왑량 하나가 아니라 실제 압박 신호로 판단한다.** 차가운 페이지가
  스왑에 오래 남는 정상 상태를 장애로 오인하지 않도록 도커 여유·직전 5분 새 스왑 아웃·PSI·스왑 포화도를
  함께 보고, 맥 본체와 도커 VM 경고를 분리한다. 관리자 자원 카드와 상단 색도 같은 열린 경고를 사용한다.
- 2026-08-23: **서버 자원 지표는 하루 한 번이 아니라 5분마다 재서 그날의 최악값만 남긴다.** 하루 한 번
  기록은 04:50 새벽이라 가장 한가한 때만 보게 되어, 정작 알고 싶은 수업 시간의 나쁜 순간을 놓친다.
  메모리 여유는 가장 낮았던 값(LEAST), 스왑·게이트웨이 CPU 는 가장 높았던 값(GREATEST)을 남긴다.
- 2026-08-23: **kong 워커 수는 CPU 코어 수(기본값)가 아니라 2로 고정한다.** 맥미니가 10코어라 워커
  10개가 뜨고, 워커마다 Lua VM 과 설정 캐시를 따로 들어 게이트웨이 하나가 1.1GB 를 먹었다. 실측으로
  워커 2개가 초당 26,000건을 처리했고 학기 중 최대 부하는 초당 0.09건이다. **늘려야 한다는 신호는
  시간이 아니라 `서버 상태` 화면의 게이트웨이 CPU 가 수업 시간에 계속 70% 를 넘는 것**이다.
- 2026-08-23: **서버 운영 정보는 `운영 > 서버 상태` 한 곳에 모으고, 숫자마다 판단 기준을 함께 적는다.**
  값만 늘어놓으면 "그래서 지금 괜찮은가"를 운영자가 매번 스스로 판단해야 한다.
- 2026-08-23: **학생 알림 표의 인덱스 팽창은 학기마다 한 번 `REINDEX CONCURRENTLY` 로 정리한다.**
  알림은 읽으면 지우는 구조라 넣고 지우기가 반복되는데, 표는 빈 자리를 다시 쓰지만 B-트리 인덱스의 빈
  페이지는 남는다. 2026-08-23 에 733줄짜리 표의 인덱스가 237MB(706배)였고 정리 후 248KB 가 됐다.
  구조상 다시 커지므로 없애려 들지 말고 `서버 상태` 의 DB 크기로 지켜본다.
- 2026-08-23: **푸시 전 검사(`npm run hooks:install`)를 기본으로 건다.** 배포 실패의 상당수가
  **올리지 않은 새 파일** 탓이었는데(2026-08-13·08-19), 로컬에는 파일이 있어 검사가 통과하므로
  로컬 검사로는 절대 잡을 수 없다. 셸·시스템 도구에 기대는 검사를 새로 넣을 때는 배포 관문과 같은
  `docker run --rm -v "$PWD":/app -w /app node:20-alpine npm run test:all` 로 먼저 확인한다.
- 2026-08-23: **정기 서버 재시작 주기는 두지 않는다.** 시간이 아니라 신호로 판단한다 — 도커 VM 스왑을
  쓰기 시작하거나 메모리 여유가 20% 아래로 내려갈 때다. 재시작하면 컨테이너가 자동으로 올라오지 않으므로
  (2026-08-23 확인) 학기 중에는 피하고, 두 스택을 `docker compose up -d` 로 의존 순서대로 올린다.

- 2026-08-21: **현재 글쓰기 연구소 소개는 신규 운영 중인 4대 활동만 기준으로 한다.** 글 개요 짜기·질문
  만들기·좋은 질문 고르기·한줄모아만 학생·교사 안내와 로그인 소개에 노출한다. `hanja_writing` 관련 라벨과
  결과 파서는 기존 방 1개와 학생 기록 2건을 열기 위한 호환 코드이지 현재 제공 기능이 아니므로 홍보 문구에 넣지 않는다.

- 2026-08-21: **독서마라톤은 ON/OFF 기능이 아니라 시작과 종료가 있는 기간형 활동이다.** 시작 전에는 초안으로
  저장하고, 시작한 뒤에는 완주하거나 교사가 중간 종료할 때까지 학생에게 보이며 확인 완료된 독서록을 계속 집계한다.
  진행 중 `사용함 / 안 함` 스위치와 전환 RPC는 두지 않는다. 기존 `paused` 캠페인은 기록·참가자·설정을 보존한 채
  `active`로 복구하며, DB도 시작된 미보관 캠페인이 다시 `paused`로 바뀌지 않도록 보호한다.

- 2026-08-21: **글쓰기 연구소의 한자 단어집은 화면과 라우트만 제거하고 데이터는 보존한다.** 통합 DB의
  `writing_helper.teacher_hanja_word_cards`에는 중복 단어 없이 카드 8개(교사 3명)가 있고, 이관 전 구 DB에는
  카드 6개(교사 2명)가 남아 있다. 통합 DB의 과거 한자 활동방 1개·학생 참여 2건도 삭제하지 않으며 아지트의
  과거 한자 문장 결과 불러오기 호환을 유지한다. 생성 카드 캐시와 한자 반응 데이터는 현재 0건이다.

- 2026-08-21: **맥미니 전체 장애의 같은 주소 자동 점검 안내는 2026–27 겨울방학 후속 과제로 미룬다.**
  현재 맥미니는 사용 기간이 6개월 미만이고 보조 전원이 연결되어 있어, 당장은 앱·내부 프록시 장애를 처리하는
  Caddy 정적 점검 화면만 유지한다. 겨울방학에는 본 서비스 호스팅을 Caddy에 둔 채 외부 Worker를 앞단에 두어
  맥미니 전원·Caddy·가정 인터넷 장애 때만 점검 화면을 반환하는 방식을 우선 검토한다. 그전까지 전체 장애 시
  같은 주소에서 점검 화면이 나오지 않는 한계는 수용하며 DNS·Cloudflare·GitHub Pages 구성은 변경하지 않는다.

- 2026-08-20: **교사가 확인 완료한 독서록·일기는 학생의 완성 기록으로 고정한다.** 학생 서재에서는 계속
  읽을 수 있지만 제목·본문·책/날짜 정보·공개 범위 수정과 삭제는 화면과 DB에서 함께 막는다. 수정이 필요하면
  교사가 `보완 요청`으로 바꾸며, 그때 잠금이 풀린다. 확인 완료 독서록 학급 내보내기는 학생별 반복 조회가 아니라
  담당 학급과 확인 상태를 서버가 검증하는 단일 RPC로 최대 2,000편을 반환한다.

- 2026-08-20: **독서록·일기는 많이 쓰게 하되 포인트 파밍만 교사 확인으로 거른다.** 학생은 하루 보상 상한과 관계없이
  자율 글을 작성 완료할 수 있고, 포인트는 교사가 `확인`한 글에만 제출일 기준 상한 안에서 한 번 지급한다. 교사 반응은
  `확인 / 보완 요청` 두 가지이며 사유·한마디는 선택이다. 기존에 지급된 포인트는 회수하지 않고 중복 지급만 막는다.
- 2026-08-20: **독서록 저장과 독서마라톤 집계 자격은 분리한다.** 교사 확인 전이거나 책 쪽수를 아직 찾지 못한
  상태는 독서록 저장 실패가 아니라 정상적인 집계 대기다. 독서록은 먼저 저장하고, 확인 완료와 유효한 쪽수가 모두
  갖춰진 뒤 기존 트리거가 마라톤에 한 번만 반영한다. 집계용 부가 기능 오류가 원본 글 저장을 막지 않도록 NULL을
  명시적으로 거른다.

- 2026-08-20: **독서마라톤은 개인전·우리 반 전체전·교사 지정 모둠전 세 방식으로 운영한다.** 시작 뒤 경기 방식과
  참가 명단은 결과 안정성을 위해 고정한다. 단체전 메달은 팀 목표만으로 자동 지급하지 않고 `팀 완주 + 교사가 정한
  개인 최소 책/쪽수`를 모두 만족한 학생에게 지급한다. 개인전과 단체전 메달은 모양·색을 구분하며 대표 메달은 학생
  이름 옆, 전체 메달은 나의 아지트에 둔다. 독서록은 별도 ‘마라톤 인정’ 절차 없이 일반 `확인 완료`가 활성 캠페인에
  자동 반영되고 `보완 요청`은 제외된다. 독서록·일기의 두 처리 결과는 모두 기존 학생 활동 알림 원장으로 전달한다.

- 2026-08-20: **사용자 증가에 대비한 조회 최적화는 자료를 조용히 자르는 방식이 아니라 화면별 계약으로 한다.**
  홈은 bootstrap 1회, 사용자 동작은 전용 RPC 1회, 일반 첫 목록은 20~100개 상한을 둔다. 전체 자료가 필요한
  내보내기는 별도 전체/페이지 RPC를 유지한다. 활동 보고서 이력처럼 사람이 같은 이름을 가질 수 있는 데이터는
  이름이 아니라 학생 ID로 저장하며, 일괄 작업은 일부 실패와 저장 실패를 성공으로 표시하지 않는다.

- 2026-08-20: **맞춤법 기본 자료 승격 기준은 `여러 학급에서 되풀이`다.** 한 학급에서 많이 나온 표현은
  그 반의 유행이거나 한 학생의 버릇일 수 있다. 기본값은 2학급·3회이고 관리자가 조절한다.
  **화면이 카탈로그를 자동으로 고치지 않는다** — 밑줄 규칙은 오탐 0을 계약으로 지키므로 예문·문맥을
  사람이 확인해야 한다. 화면은 붙여 넣을 코드 조각까지만 만들고 반영은 배포로 한다.

- 2026-08-19: **맞춤법은 실시간 밑줄(로컬) + 다 쓰고 한 번(AI) 두 겹으로 간다.** 외부 검사기는 실시간에
  못 쓴다(왕복 지연·오탐·동시 요청) 그리고 부산대 무료 엔드포인트는 이미 닫혔다. AI 검사는 **글 한 편에 한 번**,
  **횟수는 서버가** 쥐고(가드 포함), **본문은 서버가 DB에서 읽는다**. 결과는 제안일 뿐 글을 고치지 않는다.
  비용은 실측 기준 한 학기 약 1,300원이라 제약이 아니다.

- 2026-08-19: **검색 기록은 표현으로 보여 주고, 말뭉치는 학급과 끊어서 쌓는다.** 교사 화면이 내부 키와
  `미분류`만 보여 주면 아무 정보가 아니다 — 찾은 항목은 **사전 항목의 표기**를 함께 남긴다(학생 글이 아니라
  우리 사전의 이름이라 원문 비저장 원칙에 걸리지 않는다). 서비스 전체 말뭉치는 학급이 지워져도 남아야 하므로
  **학급·학생 식별자 없이** 따로 쌓되, **40자를 넘는 검색은 담지 않는다**(문장일 수 있다).

- 2026-08-19: **학생 활동은 화면을 넘기지 않고 한 화면에서 끝낸다.** 질문 만들기는 안내→카테고리→카드→쓰기로
  네 번 넘겼는데, 넘길 때마다 앞 단계가 사라져 무엇을 보고 쓰는지 알 수 없었고 화면 폭도 단계마다 달라졌다.
  **왼쪽에서 고르고 오른쪽에서 쓴다**는 한 가지 구성으로 합친다. 담기와 제출은 다른 동작으로 갈라
  마지막 질문을 저장하는 순간 제출되던 것을 없앤다. 새 활동도 이 구성을 따른다.
- 2026-08-19: **질문 카드는 완성된 질문이 아니라 관점을 여는 문장이다.** 완성된 질문을 주면 학생이 그대로
  베껴 내 활동의 목적(스스로 질문 만들기)이 사라진다. AI 생성 프롬프트도 `~ 질문을 만들어 보세요`로
  끝나게 하고 물음표로 끝나는 문장을 금지한다. 화면도 카드를 입력창에 채우지 않고 힌트로만 보여 준다.
- 2026-08-19: **연구소 결과 불러오기는 기본 켜짐이고, 끄면 참고함까지 닫힌다.** 기본이 꺼짐이면 아무도
  켜지 않아 연구소와 글쓰기가 이어지지 않는다. 스위치 하나가 도구 버튼만 닫고 참고함은 열어 두면
  "껐는데 보인다"가 되므로 두 곳이 같은 값을 본다.

- 2026-08-19: **활동 설정의 판정은 화면마다 두지 않고 활동 폴더 한 파일이 갖는다.** 질문 만들기에서
  교사 화면이 방식을 셋으로 나눴는데 서버는 옛 조건(카드 묶음 1개 이상·부연 설명 필수)을 그대로 요구해
  **어느 방식으로도 활동이 만들어지지 않았다.** 정규화 사본도 셋(서버 정의·학생 화면·교사 내용판)이었다.
  `question-generator/config.ts` 하나로 모아 시작 조건·단계 구성·문구를 함께 둔다. 새 활동도 같은 방식을 따른다.
- 2026-08-19: **학생의 첫 단계는 역할이 아니라 질문의 큰 카테고리다.** `탐정 모드`·`상담사 모드` 같은
  역할은 교사 화면에서 지웠지만 DB 와 활동 설정에 남아 학생 화면에는 그대로 떠 있었다. 질문을 고르는
  기준은 **어떤 종류의 질문인가**(상상·반전, 마음·가치 …)이고, 학생이 할 일은 그 질문을 **선생님이 낸
  주제에 맞게 바꿔 쓰는 것**이다. 카테고리는 카드 묶음 **이름의 낱말**로 정해 교사가 이름을 고쳐도 따라가고,
  분류의 원본은 `question-generator/areas.ts` 하나다. `question_card_roles` 표는 카드 묶음을 만드는
  통로로만 남기고 활동·학생 화면에서는 쓰지 않는다.
- 2026-08-19: **학생 홈의 새 표시는 홈 RPC 한 번에 얹는다.** 카드마다 따로 조회하면 홈이 무거워지고
  모듈 성능 계약(`home: 'none'`)이 깨진다. 모듈은 `studentDashboard.newFlagKey` 로 값 이름만 선언한다.
  판정은 **목록 RPC와 같은 조건**을 쓴다 — 배지를 눌러 들어갔는데 아무것도 없으면 배지가 거짓말이 된다.

- 2026-08-17: **공식 도전의 난이도는 시간이 아니라 직접 입력 비중으로 만든다.** 초기안 `12문제·3분`은
  같은 문서의 "빠른 시간·반복 클릭에는 점수를 주지 않는다"와 모순이고, 어휘력이 아니라 타자 속도를 재게 된다.
  **문항당 45초**로 바꿔 답을 찾아볼 여유만 막고, 어려움은 선택형 7 + 직접 입력 5와
  "전체 10/12 **그리고** 직접 입력 3/5" 이중 기준으로 만든다. **2문항까지는 틀려도 통과**한다 —
  자격(익힘 80%)을 정직하게 채운 학생은 대부분 통과해야 하고, 이 시험의 역할은 거르기가 아니라 확인이다.
  실패가 "멀다"가 아니라 "아깝다"에 걸려야 다시 도전한다.
- 2026-08-17: **성취 공개는 완성된 것만 친구에게 보인다(A안).** 본인과 교사는 `7/10` 진행도까지 보고
  친구는 완성된 휘장만 본다. 진행도가 상시 공개되면 은근한 비교가 계속되며, SP 순위를 기본 `내 주변형`으로
  둔 이유와 같다. **서버가 친구용 응답에 진행도 키를 아예 넣지 않는다**(화면에서 가리지 않는다).
  휘장·상징도 엔진이 소유하고 콘텐츠는 이름과 그림만 선언해, 속담·맞춤법이 표시를 다시 만들지 않는다.
- 2026-08-17: **학습 상태·복습·묶음 진도는 콘텐츠가 아니라 공통 엔진이 소유한다.** 어휘의 탑에만 있던
  전이 규칙(힌트 무효화·형태 2종+연속 2회→익힘), 복습 간격(익힘 14일·익숙 3일·그 외 1일), 구간 포인트를
  `learning_engine_*`로 뽑아냈다. **묶음(덱) 개념도 엔진이 갖되 키는 해석하지 않는다** — 몇 개를 한 묶음으로
  볼지는 콘텐츠가 정한다. 학생 진도가 48행뿐인 방학에 옮겨 이관 비용이 없었고, 덱 마스터·학기 점수도
  엔진 위에 만들어 콘텐츠마다 다시 만들지 않는다.
- 2026-08-17: **서바이벌은 통째로 흡수하지 않고 쓸모 있는 것만 골라 가져온다.** 7월 분석의 전제
  (같은 React·localStorage뿐·정리할 게 없음)가 실제와 달랐다 — TypeScript·Tailwind·IndexedDB이고
  **자체 학급·학생 명단을 들고 있어** 그대로 흡수하면 명단이 두 벌이 된다. 대신 ①문제팩(속담 85·사자성어 100)은
  코드가 아니라 **콘텐츠**라 어휘의 탑 덱으로 얹고, ②자리배치·역할배정은 이미 있는 `수업 도구` 슬롯에 넣는다.
  나머지 도구는 3대 기둥에 직접 기여하지 않아 보류한다.
- 2026-08-17: **포인트로 이어지는 값은 절대 클라이언트가 정하지 않는다.** 학생이 자기 글의
  `awarded_*`·`char_count`를 직접 고쳐 승인 때 5만 점을 타는 것을 실제로 재현했다. 원인은 RLS가 본인 글의
  모든 컬럼을 열어 준 것이고, 막는 방법은 **직접 테이블 쓰기에서 서버 소유 컬럼을 되돌리는 트리거 가드**다
  (신뢰 RPC와는 트리거 안 `current_user`로 구분 — 직접 쓰기 `authenticated`, RPC 안 정의자 롤).
  가드는 **반드시 SECURITY INVOKER**여야 한다. DEFINER면 `current_user`가 늘 정의자가 되어 무력화된다.
  승인 RPC의 `COALESCE(글 행, 미션)` 순서는 유지한다 — `awarded_*`는 제출 시점의 약속을 지키는
  스냅샷이므로 순서를 뒤집으면 교사의 미션 수정이 소급 적용된다. 새 활동을 붙일 때도 같은 규칙을 따른다.
- 2026-08-17: **학생 쓰기는 테이블 직접 접근이 아니라 전용 RPC로 모은다.** 독서록·일기는 이미 그랬고
  과제 글쓰기 임시저장만 옛 방식이 남아 위 취약점의 뿌리가 됐다 → `save_my_assignment_draft_v1`로
  이관 완료. 남은 직접 쓰기는 `show_original`(학생 본인의 공개 설정) 하나뿐이며, 학생이 소유하는 값이라
  그대로 둔다. 새 학생 기능도 테이블 직접 쓰기를 열지 않는다.
- 2026-08-17: **학생 알림은 종류를 가리지 않고 `student_notification_events` 원장 하나에 쌓고,
  `module_id`로 갈래만 나눈다.** 20261023의 "친구 반응·댓글은 원장에 넣지 않는다"를 뒤집는다.
  읽음 상태를 `last_feedback_check` 시각 하나로 두면 "1번은 읽고 2번은 안 읽음"을 표현할 수 없어
  알림별 확인이 구조적으로 불가능하고, 배지 조회도 반응·댓글 두 표를 조인해 이력이 쌓일수록 무거워진다.
  원장은 행마다 `read_at`을 갖고 부분 인덱스에 안 읽은 것만 담겨 **11만행→61만행에도 조회 비용이 4페이지로
  동일**했다(실측). 갈래는 `feedback`(헤더 🔔 반응·댓글)과 그 외(홈 패널 승인·반려·포인트)로 나눈다 —
  합치면 승인·반려가 반응 스무 개에 묻힌다. **과거 소식은 소급 생성하지 않는다**(학생당 최대 77건이
  쏟아진다). 전환 시점에 `last_feedback_check`를 현재로 밀어 선을 긋는다.
- 2026-08-17: **알림 원장에 스냅샷·CQRS를 넣지 않는다.** 이 구조는 이벤트 소싱이 아니라 받은 편지함이라
  현재 상태가 그 행의 `read_at`에 그대로 있고 되감아 계산할 게 없다. 개수를 따로 저장하기 시작하면
  원장과 어긋나는 새 버그가 생기는데, 그건 방금 고친 "화면 상태와 서버가 어긋나 배지가 되살아나는" 버그와
  같은 종류다. 홈 RPC 5.2ms 중 알림은 0.5ms이고 나머지는 과제 개수·마라톤 집계라 최적화가 필요해지면
  그쪽이 먼저다. 원장 길이는 90일 정리로 물리적으로 묶는다.
- 2026-08-17: **읽음 처리는 반드시 `studentHomeApi.invalidate()`로 홈 캐시까지 비운다.** 화면 상태만
  바꾸면 학생이 다른 메뉴에 갔다 60초 안에 돌아올 때 대시보드가 새로 만들어지며 옛 개수를 다시 적용해
  배지가 되살아난다(홈 복귀 새로고침은 60초가 지나야 돈다). 확인이 잦은 내 글 소식은 확인 중에는
  캐시만 버리고 창을 닫을 때 한 번만 다시 부른다.
- 2026-08-17: **학생 화면의 홈으로 돌아가는 버튼은 공용 `StudentBackButton` 하나만 쓴다.** 문구는 과제
  글쓰기 기준의 `뒤로 가기`로 통일하고 바꾸는 옵션을 두지 않는다. 옵션을 열면 화면마다 다시 갈라진다.
  홈이 아닌 곳으로 돌아가는 버튼(`← 내 서재`, `놀이터로 돌아가기`)은 목적지를 알려 줘야 하므로
  이 컴포넌트를 쓰지 않고 그 화면의 말로 따로 만든다.
- 2026-08-17: **학생용 놀이 콘텐츠 안내는 놀이터 카드에서만 연다.** 안내 내용은 모듈이 `playground.guide`
  슬롯으로 소유하고 표시는 공용 `StudentModuleGuide`가 맡아, 셸에 모듈별 문구를 넣지 않는다.
  **게임 모듈 실행 화면 안에는 두지 않는다** — 그 화면은 `zIndex: 20000`인데 공용 `Modal`은 9999라 창이
  뒤에 숨고 몸통 스크롤만 잠겨 학생이 닫지도 못한다(2026-08-17 어휘의 탑 지도에서 실제 발생해 제거).
  게임 화면 안에 창이 필요한 기능이 생기면 z-index 층부터 정리한다.
- 2026-08-17: **어휘의 탑 보상·출제 규칙은 교사·학생 도움말로 각각 설명한다.** 화면만 봐서는 포인트가
  익힘 진도로 나뉘어 지급된다는 것과 익힘이 두 유형 연속 성공을 요구한다는 것을 알 수 없다. 교사는
  `teacherGuides.js`의 `vocab-tower` 항목, 학생은 지도의 `StudentTowerGuide`를 쓰며 둘 다 기능 나열이 아니라
  오해 지점만 담는다. 보상 구간이나 출제 규칙을 바꾸면 두 도움말도 같은 커밋에서 고친다.
- 2026-08-17: **틀린 낱말은 같은 연습에서 3문항 뒤 다른 형태로 한 번만 다시 낸다.** 바로 다음 문제로 내면
  정답을 외워 누르므로 간격을 두고, 방금 틀린 유형은 피한다. 방금 틀린 낱말은 직접 입력형으로 올리지 않고
  선택형으로 낮춰 묻는다. 재출제는 별도 대기열 없이 그 연습의 문항·답안 기록에서 계산하며 12문항 예산 안에서
  이뤄진다. 많이 틀린 학생이 새 낱말을 덜 만나는 것은 의도한 결과이고, 쏠림이 심하면 재출제 상한을 뒤에 둔다.
- 2026-08-17: **어휘 V2 포인트는 `한 판 완벽`이 아니라 `층별 익힘 진도`에 건다.** 직접 입력형이 들어가면서
  12/12는 오타 하나로 무너지고, 입력형이 이미 맞힌 낱말에서만 나오므로 첫 연습이 가장 쉽고 성실한 학생이
  가장 불리한 역전이 생겼다. 층마다 익힘 낱말이 25·50·75·100%를 넘을 때 층 총액(교사 설정, 기본 100P)을
  20·20·30·30%로 나눠 지급한다. 한 번 받은 구간은 이후 연습을 망쳐도 사라지지 않고, 총발행량은 늘지 않는다.
  100% 정답률 `정복`은 포인트와 분리해 지도 위 명예 표시로만 남긴다. 이는 위의 `최초 12/12 지급` 결정을 대체한다.
- 2026-08-17: **직접 입력형은 이미 한 유형을 힌트 없이 성공한 낱말에만 연다.** 선택형 3종만 돌면 4지선다를
  찍어도 `서로 다른 두 유형 연속 성공 = 익힘`을 통과할 수 있어 익힘 판정이 실력을 뜻하지 못했다. 처음 만난
  낱말을 입력형으로 맞이하게 하지는 않고, 선택형으로 한 번 성공한 뒤 같은 낱말을 직접 쓰게 해 익힘 근거를 만든다.
  틀리면 `복습 필요`로 내려가 다음에 선택형부터 다시 만나며 감점·차감은 두지 않는다. 채점은 검수된
  `acceptedAnswers`와 대조하되 공백·문장부호·대소문자만 무시하고 글자는 보존해 다른 낱말이 통과하지 않게 한다.
  보기가 없는 문항은 정답 낱말·예문(빈칸형은 뜻까지)을 학생 payload에서 빼고 채점 응답으로만 돌려준다.
  이 변경으로 12/12 완벽 연습 난도가 올라가지만, 최초 완벽 보상을 이미 받은 학생의 기록은 그대로 둔다.
- 2026-08-16: **도커 빌드 캐시는 배포 워크플로가 6GB 상한으로 관리한다.** 도커 VM 디스크는 31.3GB뿐이라
  배포마다 쌓인 캐시 16GB가 잔여 0을 만들어 `agit-kong` 건강검진 1,555회 연속 실패와 Storage 업로드 위험을
  일으켰다. 배포 마지막 단계에서 `docker builder prune -f --max-used-space 6GB`로 초과분만 버린다.
  전량 삭제(`prune -a`)는 다음 배포를 콜드 빌드로 만들어 쓰지 않고, `docker system prune`은 같은 맥미니의
  연구소·다른 프로젝트 볼륨·네트워크까지 지우므로 금지한다. 정리 실패가 배포를 실패로 만들지 않도록
  `continue-on-error`로 두고, 배포가 실패한 회차에도 다음 시도가 살아나도록 `if: always()`로 실행한다.
- 2026-08-16: **학생 어휘의 탑 덱 선택 화면은 카드 목록이 아니라 정상과 입구가 있는 탐험 지도로 표현한다.**
  최고 정답률 100%를 층 정복으로 표시하고 포인트 수령 여부와는 구분한다. 이번 변경은 시각적 탐험 경험만
  다루며 모든 층 자유 선택을 유지한다. `1층 정복 후 2층 개방` 같은 순차 잠금은 별도 정책 결정 뒤 적용한다.
- 2026-08-16: **교사 어휘의 탑 출제자료는 학년별 잠긴 현재 덱 10개로 자동 고정한다.** 교사 대시보드에서
  V1/V2 선택과 V1 전용 설정을 제거하고, 새 학급·설정 누락의 기본값도 V2로 한다. V1 데이터와 전환 RPC는
  화면에 노출하지 않고 운영 비상 롤백용으로만 서버에 보존한다. 이는 아래의 `기본 V1·교사 명시 전환` 결정을 대체한다.
- 2026-08-16: **V2 개인 연습은 각 덱 최초 12/12에 한 번 포인트를 지급한다.** 학급별 교사 설정값을 사용하고
  기본 100P, 설정 범위 0~500P로 한다. 같은 학생의 같은 학년·덱 반복 성공은 0P이며 서버 고정 이벤트 키로
  재클릭·재시도 중복을 막는다. 이는 아래의 초기 `개인 연습 0P` 결정을 대체한다.
- 2026-08-16: **2학기에는 삭제되지 않은 전체 운영 학급을 어휘 V2로 전환하고 학생 모듈도 함께 켠다.**
  기존 다른 모듈 ON/OFF 목록은 보존하고 `vocab-tower`만 추가한다. 학급별 V1 롤백 수단은 유지하며,
  전체 적용 뒤 실제 사용 지표와 이의 문항을 확인한다.
- 2026-08-16: **V2의 첫 학생 게임 단위는 10개 덱 지도와 덱별 12문항 개인 연습으로 한다.** 시간·횟수 제한과
  즉시 포인트를 두지 않고 완료/최고 정답률만 서버에 저장한다. 반복 클릭으로 포인트를 얻지 못하게 하고,
  포인트는 후속 덱 마스터가 숙달 조건을 검증하는 상태 변화에만 연결한다. 기존 V1과 학급별 롤백 전환은 유지한다.
- 2026-08-16: **어휘 V2는 전체 학급을 즉시 교체하지 않고 학급별 시험 전환으로 연다.** 기본은 V1이며
  교사가 V2를 명시 저장해야만 전환된다. V2 문항은 잠긴 덱에서 서버가 발급하고 정답은 제출 전 브라우저에
  보내지 않는다. 전환 시 진행 중 판을 종료하고 덱 10개 잠금을 DB 트리거와 RPC 둘 다에서 강제한다.
- 2026-08-16: **어휘 V2 1,573개를 선생님이 처음부터 전부 작성·대조하지 않는다.** 자동 구조 검사는 전체 문항,
  Codex 1차 편집 검수는 전체 항목에 적용한다. 사람은 자동 수정·복수 의미·유사어·새 예문·쓰임 구별을 우선 보고,
  나머지는 덱별 표본을 확인한다. 표본에서 오류가 나오면 해당 덱만 전체 검수하고, 이상이 없으면 덱 단위로 잠근다.
  검수 UI에는 이 원칙과 원문 대비 수정 표시를 보여주며 학생 V2 연결은 잠긴 덱만 대상으로 별도 구현한다.
- 2026-08-16: 요청자 승인에 따라 전체 검수 후보를 운영 DB에 일괄 반영한다. 직접 확인을 마친 첫 덱과 자동 신호가
  없는 보조 덱은 `교사 확인`, 자동 신호가 남은 덱만 `1차 검수`로 두고 관리자 기본 목록에는 미확인 신호 항목만
  표시한다. 이 자동 반영은 검수 작업공간에만 적용하며 학생 출제 연결이나 덱 잠금으로 해석하지 않는다.
- 2026-08-16: 요청자가 남은 확인 필요 항목도 모두 확인으로 처리하도록 승인해 운영 검수 DB의 40개 덱을 전부
  `교사 확인`으로 전환한다. 문항 완결성은 전환 직전에 서버 검사로 재확인하고, 잠금과 학생 출제 연결은 별도
  단계로 남긴다.
- 2026-08-16: 전체 40개 덱의 교사 확인과 문항 완결성 검사가 끝나 요청자 승인에 따라 모두 잠근다. 잠금은 검수
  작업공간만 고정하며 기존 운영 단어표와 학생 게임은 바꾸지 않는다. 이후 작업은 공통 학습 카드 엔진의 `vocab`
  어댑터와 잠긴 덱 읽기 계약을 먼저 설계·검증한다.

- 2026-08-14: **글쓰기 참고함은 `선생님이 준 기준`과 `학생이 만든 연구소 자료`를 함께 보여주되 빈 자료는 만들지 않는다.**
  선생님 안내·핵심질문은 과제에 값이 있을 때만 기존 데이터로 표시한다. 연구소는 과제별로 `글 개요짜기`와
  `좋은 질문 고르기` 활동방을 각각 하나 연결하고, 학생이 참고함을 열 때만 단일 RPC로 본인 표준 결과 최대 20건을
  읽는다. 연결 결과는 먼저 펼치고 다른 완료 결과는 학생이 모달에서 골라 바꿀 수 있으며 본문에는 자동 삽입하지 않는다.
  연결표는 브라우저에 공개하지 않고 담당 교사 관리 RPC와 실제 학생 연결 기반 본인 조회 RPC로만 접근한다.

- 2026-08-13: **글쓰기 참고 자료는 입력을 가리는 모달에 계속 띄우지 않고, 자료를 고를 때만 모달을 쓰며
  작성 중에는 열고 접는 공통 `글쓰기 참고함`에 펼쳐 둔다.** 1차는 추가 조회 없이 기존 선생님 안내·핵심질문·
  학생 답변만 연결하고, 연구소 결과는 후속으로 기존 표준 결과 RPC를 지연 호출하는 출처 어댑터로 붙인다.
  패널은 조회·저장·본문 삽입을 소유하지 않으며 활동별 RPC를 추가하지 않는다.

- 2026-08-13: **통합 연구소의 기본 학생 입구는 QR이 아니라 아지트 학생 대시보드의 `글쓰기 연구소`다.**
  학생 홈에는 연구소 상세 조회를 추가하지 않고 메뉴를 열 때만 본인 학급의 활성·미만료 활동을 단일 RPC로 읽는다.
  기존 세션 상태에 따라 시작·이어하기·결과 보기로 표시하고 같은 탭의 `/lab/room/{roomId}`에서 자동 입장한다.
  입장용 QR·단축주소는 통합 `/lab`에서 만들지 않으며 구 `helper.` 롤백 흐름에만 남긴다. 학생 개인 결과 공유 QR은
  입장 수단이 아니므로 유지한다.

- 2026-08-13: **통합 연구소의 학급·학생 원장은 아지트 `public.classes`·`public.students` 하나만 사용한다.**
  별도 동기화 작업이나 연구소 학급을 새로 만들지 않는다. 신규 방은 `writing_helper.rooms.agit_class_id`, 신규 결과는
  `student_sessions.agit_student_id`로 직접 연결하고 번호·이름은 당시 표시 스냅샷으로만 둔다. 과거 연구소
  `classes`·`class_students`는 기존 결과 해석과 구 `helper.` 롤백 호환을 위해 삭제하지 않으며, 확정 가능한 과거
  세션만 연결한다. 아지트에서 명단을 바꾸면 `/lab` 재진입·새로고침 때 즉시 반영한다.

- 2026-08-13: **글쓰기 연구소는 교사 상단의 `글쓰기` 바로 다음 독립 업무 메뉴로 연다.** 아지트 안에
  연구소 코드를 이식하거나 iframe으로 넣지 않고 같은 탭의 `/lab/dashboard`로 이동한다. 통합 `/lab`만
  `sb-agit-auth-token` 루트 쿠키를 아지트와 공유하며, 구 `helper.` 빌드는 기존 인증 쿠키를 유지해 병행 검증과
  롤백 시 기존 로그인을 끊지 않는다. 연구소 권한은 JWT 메타데이터가 아니라 DB의 실제 교사 승인 상태로 판정한다.

- 2026-08-13: **연구소는 별도 서비스 관리자 계정과 관리 화면을 두지 않고 아지트 관리자 모드에서 관리한다.**
  아지트 ADMIN만 통합 DB 기준 연구소 현황과 기존 `lab_ai_teacher_links.active` 상태를 제한된 RPC로 읽고 바꾼다.
  이 상태는 AI 브리지 허용에만 사용하며 교사·학급·활동·학생 결과를 삭제하지 않는다. 구 연구소의 서비스 설정·
  감사 테이블은 컷오버 완료 전 롤백 자료로 보존하고, 화면에는 배포 번호를 노출하지 않는다.

- 2026-08-13: **연구소는 별도 Next.js 앱으로 유지하되 교사용 UI는 아지트 공통 디자인 계약을 따른다.**
  기본색·페이지 배경·간격·카드·버튼·포커스·반응형 상단 셸은 아지트와 맞추고, 다섯 활동별 색상은 활동을
  빠르게 구별하는 보조 표현으로만 남긴다. React 컴포넌트를 두 저장소 사이에 복사해 결합하지 않고 각 앱의
  토큰 파일과 자동 계약 검사로 같은 시각 언어를 유지한다.

- 2026-08-13: **연구소는 자체 OpenAI 키를 보관하거나 교사에게 API 키 설정을 요구하지 않는다.** 현재 구 연구소
  로그인 세션을 구 인증 서버에서 재검증하고, 유승현·최원진의 아지트 승인 계정 매핑을 확인한 요청만 비공개
  Docker 네트워크의 아지트 `vibe-ai`로 전달한다. SSO 전환 뒤에는 이 임시 ID 매핑을 공용 아지트 세션 판정으로 단순화한다.

- 2026-08-13: **연구소 통합 대상은 글 개요짜기·질문 만들기·좋은 질문 고르기·한줄모아·한자 활용 문장
  만들기 5종으로 고정한다.** 활동 ID는 `outline_builder`·`question_generator`·`question_voting`·
  `one_line_share`·`hanja_writing`이며 공용 학생 결과 RPC는 이 허용 목록의 표준 결과만 반환한다. `word_game`과
  과학·도덕 별도 활동은 1차 통합에서 제외하고 신규 생성·노출을 중단하되, 기존 데이터와 과거 링크는 회귀 확인 전
  삭제하지 않는다. 좋은 질문 고르기는 학생이 직접 선택한 질문, 한자 활동은 학생이 작성한 문장들을 개인 불러오기
  결과로 제공하며 학급 순위·반응 집계는 별도 운영 자료로 남긴다.

- 2026-08-13: **연구소 통합은 기존 운영 기준 정합화 → 새 DB·`/lab` 병행 검증 → 교사 쿠키 SSO → 학생
  DB 연결 자동 입장 → 표준 결과 RPC 순으로 진행한다.** 현재 연구소 운영 이미지는 커밋이 아닌 로컬 작업 빌드이고
  미적용 DB 컬럼을 참조하므로, 이 상태에서 통합 커밋을 바로 `main`에 푸시하지 않는다. 구·신 DB의 보존 대상 두 교사
  자료 지문은 같아 전체 덤프를 다시 덮지 않고 컷오버 직전 선별 비교만 한다. 학생 신원은 JWT 메타데이터가 아니라
  `students.auth_id=auth.uid()` 실제 연결로 판정하며, 신규 개요 결과는 `student_sessions.answers`가 기준이고
  `outline_queue.result`는 과거 자료 호환에만 쓴다.

- 2026-08-13: **교사 수업 도구의 쌤링크 iframe은 운영 쌤링크 origin 하나만 아지트 CSP `frame-src`에
  허용한다.** 아지트 자체를 다른 사이트가 감싸지 못하게 하는 `frame-ancestors 'none'`과 `X-Frame-Options: DENY`는
  그대로 유지하고, 자식 프레임은 Google 로그인과 `https://xn--9y2br3k43n.kr`만 허용한다. 정적 보안 검사에서
  이 두 출처의 정확한 목록을 고정해 향후 보안 헤더 정리 중 쌤링크가 다시 차단되거나 와일드카드가 열리지 않게 한다.

- 2026-08-14: **말 카드 리그의 학생 순위 공개 기본값은 `내 주변형`으로 하고 교사가 학급별 공개 방식을 선택한다.**
  교사는 개인 성장형(순위 숨김)·내 주변형·제한 경쟁형(상위 3명+내 주변)·모둠 협력형 중 고르며, 학생에게
  전체 개인 순위표를 기본 노출하지 않는다. 교사는 전체 현황을 볼 수 있고, 같은 SP 학생은 속도로 우열을 가르지
  않고 공동 순위로 표시한다.

- 2026-08-14: **어휘의 탑 V2는 핵심 120개만 고르는 대신 3~6학년 필수 어휘 1,573개 전체를 학년별 10개 층
  덱에 넣고, `개인 연습 → 덱 마스터 → 10개 완료 뒤 정상의 어휘 마스터` 2단계 공식 도전으로 구성한다.**
  한 수련에는 보장된 무작위 일부만 보여주되 서버가 모든 낱말 확인·복습을 보장하고, 잘하는 학생은 선택형에서
  직접 입력·빈칸·고쳐쓰기로 올라간다. 덱 마스터 초기안은 12문제·3분이며 속도는 점수에 넣지 않는다. 원본과
  운영 게임은 그대로 두고 자동 진단·검수 완료 덱만 V2에 사용한다.

- 2026-08-13: **어휘·맞춤법 게임은 공통 학습 카드 엔진 위의 콘텐츠 어댑터로 확장하고 경쟁은 주간이 아니라
  학기 단위로 운영한다.** 학생별로 아직 확인하지 않음·연습 중·익힘·자주 헷갈림·복습 예정 상태와 판정 근거를
  보존하고, 개인 수련은 약점·복습·새 항목을 섞은 적응 덱을 사용한다. 틀린 카드는 같은 판의 뒤쪽과 다음 로그인에
  다른 형태로 다시 내며, 드문 접속에도 미접속 감점이나 주간 기회 소멸이 없도록 접속 순서와 경과 시간을 함께 쓴다.
  학생 UI는 `말 카드 리그 입구 / 수호룡 카드 대결 / 결과 정리 / 어휘·맞춤법 카드함` 네 화면을 기준으로 한다.
  포인트는 단순 정답이 아니라 서버가 검증한 숙련·약점 회복·예정 복습 상태 변화에만 지급한다. 경쟁용 학기 SP는
  보유 포인트와 분리하고 공식 관문 최고 기록 일부만 반영해 플레이 횟수 경쟁을 막는다. 현재는 계획만 저장하며
  문항·화면·점수 계약과 미래 콘텐츠 어댑터 범위를 더 검토한 뒤 구현한다.

- 2026-08-13: **학생 아지트 놀이터는 게임 목록이 아니라 포인트 통합 허브로 운영한다.** 상단에 홈 bootstrap의
  포인트 잔액을 재사용하고, 활성 콘텐츠는 매니페스트의 `earn / spend` 분류로 `포인트 모으기 / 포인트 쓰기`에
  자동 배치한다. 최근 내역은 사용자가 펼칠 때만 실제 로그인 학생의 학급·학생 범위를 함께 제한해 최대 20건 조회하며,
  포인트 지급·차감 규칙과 금액은 계속 콘텐츠 전용 서버 RPC만 결정한다.

- 2026-08-13: **친구 아지트 기본 진입은 공개 글 피드 RPC 한 번만 사용한다.** 과제 목록은 `선생님 과제`,
  친구 명단은 `친구 아지트` 탭을 처음 열 때 각각 불러오며, 화면 복귀 때도 실제로 한 번 연 보조 목록만 갱신한다.
  같은 필터의 진행 중 요청은 Promise를 재사용하고 요청 순서 식별자로 늦은 이전 응답이 현재 필터를 덮지 못하게 한다.

- 2026-08-13: **친구 아지트 새 글은 `전체 새 글 / 선생님 과제 / 자율 글` 두 단계 분류로 확장한다.**
  시·보고서·회의 같은 미션 장르는 선생님 과제 안에 자동 포함하고, 독서록·일기 같은 자율 글만 모듈
  매니페스트의 `communityFeed`로 세부 필터를 등록한다. 목록은 유형마다 RPC를 늘리지 않고 로그인 학생의 실제
  학급·제출·공개 상태를 검증하는 단일 커서 RPC로 글·작성자·미션·반응을 함께 읽는다.

- 2026-08-13: **글 반응은 공용 레지스트리의 프로필을 각 장르 매니페스트가 선택하는 모듈 계약으로 관리한다.**
  화면별 반응 배열과 장르 이름 분기를 두지 않고, 학생이 이미 받은 미션 `input_template`로 프로필을 선택해 별도
  DB 조회를 만들지 않는다. 저장 RPC는 같은 값을 서버의 허용 목록과 다시 대조하고 학생 한 명당 글 하나의 반응
  하나만 유지한다. 교사용 상세 목록은 창을 열 때만 학급·미션 직접 범위와 최대 100편 상한으로 한 번 조회한다.

- 2026-08-13: **`내 글 소식`은 새 항목이 있는 개별 탭을 모두 방문하면 자동 읽음 처리한다.** `전체` 탭만 본 것은
  확인으로 세지 않고, 읽음 처리 뒤에도 현재 창의 목록은 유지한 채 닫을 때 정리한다. 원본 반응·댓글은 글에 계속
  남기며 새 소식 목록은 읽음 기준 시각 이후 항목만 다시 조회한다. 수동 비우기와 별도 삭제 원장은 두지 않는다.

- 2026-08-12: **앱 호스팅은 맥미니 Docker+Caddy, 자동 배포는 GitHub Actions self-hosted 러너로 단일화한다.**
  `main` 푸시만 운영 배포를 시작하며 러너가 아키텍처·보안·배포 계약 검사를 포함한 이미지를 빌드하고 `agit-app`을
  교체한 뒤 `127.0.0.1:8300` HTTP 200을 확인한다. Vercel 호스팅 설정과 현재형 의존 문구는 제거하고,
  과거 이관 기록은 당시 사실을 설명하는 감사 기록으로만 남긴다.

- 2026-08-11: **별도 학생 입력 틀이 있는 장르는 같은 작업에서 장르 전용 PDF를 함께 만든다.**
  일반 글은 기존 공용 PDF 양식을 유지한다. `studentEditorEntry` 장르는 매니페스트의 `pdfExport`로 제목·지은이와
  구조화 단위(시의 연, 보고서의 질문·사진·관찰 결과 등)를 살린 렌더러와 인쇄 스타일을 소유한다. 공용 출력기에
  장르별 분기를 넣지 않으며, 전용 PDF 누락과 12pt 미만 글자는 아키텍처 검사에서 차단하고 실제 A4 렌더링까지 확인한다.

- 2026-08-11: **보고서 PDF는 질문 포함 지도형과 질문 없는 완성본 중 선택한다.**
  지도형은 `교사의 질문` 라벨과 질문을 같은 안내 바에 두어 보통 길이 질문을 한 줄에 표시하고, 그 아래 왼쪽
  52mm 5:4 사진과 오른쪽 학생 답변 카드를 둔다. 완성본은 질문·칸 번호·반복 라벨을 빼고 왼쪽 58mm 사진과
  오른쪽 학생 글만 정돈한다. 두 양식 모두 한 칸의 질문·사진·글을 페이지 경계에서 갈라놓지 않는다.

- 2026-08-11: **글 PDF는 일반 글과 보고서를 별도 A4 양식으로 만들고 본문을 12pt 아래로 줄이지 않는다.**
  서버 변환 작업을 두지 않고 교사가 내보내기를 누를 때 브라우저 인쇄 엔진으로 최대 100편을 만든다. 긴 글은
  글자를 억지로 줄이지 않고 다음 페이지로 흐르게 한다. 보고서는 `structured_content`로 별도 양식을 만들며,
  인쇄 창에서 PDF 저장과 실제 프린터 출력을 모두 선택할 수 있게 한다.

- 2026-08-11: **보고하는 글쓰기는 가변 내용 칸과 비공개 최적화 사진으로 저장한다.**
  교사가 정한 칸 문구는 질문으로 유지하고 학생은 `사진 + 사진에 대한 관찰 결과`를 한 칸으로 묶어 최대 12칸까지
  추가·삭제·위아래 이동하며, 사진은 최대 3장만 허용한다. 사진은 학생 기기에서 긴 변 720px·최대 256KB
  WebP(미지원 브라우저는 JPEG)로 줄인 뒤
  비공개 Storage에 두며, DB `structured_content`에는 사진 경로와 크기 정보만 둔다. 사진 열람은 글 소유 학생,
  담당 교사, 제출·학급 공개 조건을 만족하는 같은 반 학생으로 제한하고 목록/홈에서는 사진을 불러오지 않는다.

- 2026-08-11: **맞춤법 500개는 관리용 6분류와 정확도용 3검출 방식으로 나누되, 글쓰기는 공용 후보 색인 한 번으로 검사한다.**
  큰 분류는 문장 규칙·용언 활용·뜻 구별·낱말 표기·합성어/사이시옷·외래어이고, 각 항목은 세부 분류와
  `exact`(단어)·`phrase`(어구)·`context`(문맥) 중 하나를 반드시 가진다. 앞으로 추가할 때 분류나 검출 방식이
  빠지거나 패턴과 맞지 않으면 자동 검증을 실패시키며, 5,000자 평균 검사 시간은 2ms 이내로 유지한다.

- 2026-08-11: **맞춤법 기본 자료를 기존 300개와 겹치지 않는 200개를 더해 총 500개로 운영한다.**
  추가 자료도 항목별 학습 라벨·큰 분류·바른 예문 2개·국립국어원 출처·밑줄 규칙을 갖추며, 퀴즈는
  500개 전체에서 열 때마다 중복 없는 5개만 뽑는다. 통계로 확정된 ‘빈도 순위’ 목록은 없으므로
  국립국어원의 자주 묻는 표현·공공언어 오류 사례·외래어 오기 사례와 학생 글쓰기 활용도를 기준으로 고른다.

- 2026-08-11: **맞춤법 검사는 항목 ID·학습 라벨·큰 분류·검사 표현을 분리하고 후보 색인을 사용한다.**
  학생 글에서 모든 규칙을 반복 검색하지 않고 검사 표현을 한 번 훑어 실제로 등장한 라벨 후보만 문맥 판정한다.
  기본 300개와 학급 승인 자료는 같은 색인 계약을 쓰며, 기존 350ms 입력 지연·오탐 0·최대 50개 결과는 유지한다.

- 2026-08-10: **댓글 관리 상단은 유연한 제목 열과 폭이 제한된 검색 열로 구성한다.** 검색창은 항상 컨테이너 안에서 줄어들고, 좁은 화면에서는 제목 아래 한 줄로 내려 우측이 잘리지 않게 한다. 도움말은 제목과 일정한 간격을 둔다.

- 2026-08-10: **학생 명단 머리말은 제목·인원 배지·도움말을 하나의 정렬 그룹으로 유지한다.** 인원은 제목과 구분되는 작은 중립색 배지로 표시하고, 도움말은 같은 행의 제목 바로 뒤에 배치한다. 좁은 화면에서는 세 요소의 크기와 간격을 함께 줄인다.

- 2026-08-10: **평가·기록도 글쓰기처럼 바깥 반복 제목을 표시하지 않는다.**
  `학생 평가·평어 도우미` 화면은 각 기능의 자체 머리말을 사용하고 공통 제목 줄은 제거한다.
  도움말은 각 자체 제목 옆으로 옮겨 기존 안내 접근성을 유지한다.
- 2026-08-10: **학급 운영도 글쓰기처럼 바깥 반복 제목을 표시하지 않는다.**
  왼쪽 세부 메뉴와 각 카드의 자체 제목으로 위치를 알 수 있으므로 `운영 현황·최근 활동·학생 댓글` 위의
  공통 제목 줄은 제거한다. 학생 댓글 도움말은 기능 카드의 자체 제목 옆에 유지한다.
- 2026-08-10: **설정 화면도 글쓰기 메뉴처럼 현재 탭 제목을 한 번만 표시한다.**
  설정 허브의 제목·도움말을 기준 머리말로 사용하고 하위 카드의 같은 대제목과 별도 도움말은 제거한다.
  카드 안에는 현재 학급·기능 소제목·상태처럼 실제 관리에 필요한 정보만 남긴다.
- 2026-08-10: **설정 화면도 현재 선택한 탭 제목 오른쪽에 공용 `💡 도움말`을 표시한다.**
  학급 관리·피드백/평어 기준·글쓰기 창 관리·맞춤법 배움 데이터·이웃 아지트의 안내는
  `TeacherGuideButton`과 `teacherGuides`에서 관리하고 메뉴 목록에는 도움말 아이콘을 넣지 않는다.
- 2026-08-10: **맞춤법 배움 데이터는 `항목 만들기`와 `등록 데이터` 탭으로 구분한다.**
  등록 데이터에서는 우리 반 항목 전체를 적용 중·초안 상태로 필터링하고 학생용 설명과 바른 예문까지 확인한다.
  기존 학급 범위 workspace RPC 응답을 재사용해 추가 DB 조회나 마이그레이션은 만들지 않는다.
- 2026-08-10: **Tier 2 `gpt-4o-mini` 일괄 피드백은 교사당 1개씩·2초 간격·학생 글 2건 묶음으로 처리한다.**
  클라이언트는 청크 완료 즉시 DB에 저장하고 일부 실패를 구분한다. 운영 Edge/DB에서는 조직 전체 기준으로
  동시 요청 3건, 30 RPM, 150,000 TPM을 적용하고 교사별 순환 대기열, `Retry-After` 우선 재시도,
  응답 한도 헤더 20% 이하 자동 감속을 구현한다. 이 서버 제어는 맥미니에서 배포·검증할 잔여 작업이다.
- 2026-08-10: **독서록·일기의 무승인 완료는 쓰기 유창성과 부담 완화를 위한 교육적 운영 원칙으로 안내한다.**
  교사 확인·한마디는 평가나 승인이 아니라 학생 글을 읽었다는 기록과 격려로 설명한다.
- 2026-08-10: **독서록 도움말에도 포인트 기본값과 책 단위 중복 방지 예시를 표시한다.**
  기본 완료 조건·보상·하루 지급 편수와 같은 책의 수정·재작성 미지급을 안내하되 실제 정책 값은 학급 설정을 따른다.
- 2026-08-10: **일기 도움말에는 포인트 기본값과 중복 방지 예시를 함께 표시한다.**
  기본 완료 조건·보상·하루 지급 편수와 수정·재작성 시 미지급을 교사가 바로 이해하도록 안내하되 실제 정책 값은 기존 설정을 따른다.
- 2026-08-10: **자체 머리말이 있는 글쓰기 화면은 공통 탭 제목을 중복 출력하지 않는다.**
  기존 화면 제목을 한 번만 유지하고 `💡 도움말`은 그 머리말 오른쪽 작업 영역에 배치한다.
- 2026-08-10: **독서록·일기 도움말은 일반 과제와 다른 완료 흐름을 명확히 안내한다.**
  학생 완료 시 포인트가 처리되고 교사의 확인은 승인이나 추가 지급이 아니라 읽음 기록이라는 점을 현재 탭 도움말에 적는다.
- 2026-08-10: **교사 메뉴 도움말은 메뉴 목록이 아니라 현재 탭 제목 오른쪽에 `💡 도움말`로 표시한다.**
  공용 `TeacherGuideButton`·`GuideInfoButton`을 확장해 사용하고, 안내가 있는 현재 화면에만 버튼 하나를 노출한다.
- 2026-08-10: **교사 대시보드의 현재 메뉴는 브라우저 탭 범위의 `sessionStorage`에 보관한다.**
  새로고침에는 복원하되 브라우저 탭을 닫으면 초기화하며, 저장값은 등록된 메뉴 ID인지 검증하고 DB에는 기록하지 않는다.
- 2026-08-10: **AI 연결 점검은 상시 상태가 아니라 문제 해결용 보조 기능으로 둔다.**
  화면 진입마다 연결 여부를 요구하거나 자동 호출하지 않고, 실제 AI 기능에 문제가 있을 때 교사가 수동 점검 버튼을 사용한다.
- 2026-08-10: **AI 프롬프트 설정은 `피드백·평어 기준` 메뉴 하나에서 관리한다.**
  메뉴명에서 두 용도를 함께 드러내고 내부의 `학생 피드백`·`평어 작성` 탭으로 기준 종류를 구분하며, 기존 저장 형식은 유지한다.
- 2026-08-10: **교사 프로필의 백그라운드 인증 갱신 중에는 기존 대시보드를 언마운트하지 않는다.**
  전체 로딩 화면은 저장된 프로필이 없는 최초 진입에만 표시해 창 복귀·토큰 갱신 때 현재 선택 메뉴와 화면 상태를 유지한다.
- 2026-08-10: **작가 수호룡의 1회 재선택은 작가 3단계를 처음 통과한 성장 축하 확인 직후 자동으로 안내한다.**
  1→4처럼 단계를 건너뛴 경우도 포함하되, 이미 3단계 성장을 확인한 기존 학생이나 재선택을 사용한 학생에게는
  강제로 다시 띄우지 않는다. 수호룡 방 안의 수동 `다시 고르기` 버튼도 남긴다.
- 2026-08-10: **LLM 작업 문서는 `작은 활성 컨텍스트 + 필요시 원문 조회` 방식으로 운영한다.** Codex와 Claude의
  SessionStart 훅은 `AGENTS.md` 전체와 WORKLOG 최신 항목을 반복 주입하지 않고 공용 `SESSION_CONTEXT.md`만 넣는다. 상세 규칙·계획·이력은
  `docs/wiki/README.md`에서 작업별 정본으로 라우팅하며, 활성 컨텍스트에는 현재 상태를 짧게 유지한다.
- 2026-08-10: **화면에 설명을 덧붙이는 정보 아이콘은 공용 `GuideInfoButton`만 사용한다.** 문자
  `ⓘ`·`!`·`?`나 화면별 버튼은 폰트에 따라 그림과 클릭 범위의 중심이 어긋나므로 직접 만들지 않는다.
  교사 메뉴 사용법은 이를 포함한 `TeacherGuideButton`과 `constants/teacherGuides.js`에 모으며,
  작가 수호룡처럼 등록형 모듈의 안내도 모듈 ID로 같은 체계를 사용한다.
- 2026-08-10: **프로젝트 Codex 셸 훅은 운영체제별 실행 명령을 둔다.** POSIX는 기존 `/bin/bash`,
  Windows는 `commandWindows`에서 설치된 Git Bash를 명시적으로 사용한다. 공통 스크립트 본문은 하나로
  유지하고 저장소 루트는 실행 시 `git rev-parse`로 찾는다.
- 2026-08-10: **프로젝트의 모든 셸 훅은 Git 체크아웃 시 LF를 강제한다.** Windows의
  `core.autocrlf=true` 환경에서 CRLF로 바뀌면 Bash 구문 검사가 실패하므로 `.gitattributes`의
  `*.sh text eol=lf` 규칙을 공통 안전장치로 둔다.
- 2026-08-10: **Codex에도 프로젝트 로컬 `SessionStart` 훅을 둔다.** Codex가 원래 자동으로 읽는
  `AGENTS.md`를 명시적으로 재주입하고 WORKLOG 최신 항목 1개를 함께 넣어, 새 세션뿐 아니라
  `resume`·`clear`·`compact` 뒤에도 모델 교대 규칙과 직전 인수인계를 복원한다. 훅은 저장소의
  `.codex/`에 버전 관리하며 새로 생기거나 바뀐 해시는 사용자가 `/hooks`에서 한 번 신뢰한다.
- 2026-08-09: **아지트온클래스를 "숨김 보관"에서 완전 삭제로 재결정.** 2026-07-27에 "나중에 필요하면
  `available: true`로 되돌린다"는 전제로 보관했으나, 굳이 다시 쓸 계획이 없어 유지 비용(코드·DB 테이블·
  모듈 등록이 계속 남아 매번 "이거 왜 있지" 재확인해야 함, 오늘도 죽은 실시간 구독 하나가 이걸로 생겼었음)이
  되살릴 가능성보다 크다고 판단했다. 나중에 비슷한 컨셉(학급 공동 목표 게임)이 다시 필요해지면 새로 만든다.
- 2026-08-08: 앞으로 새 콘텐츠는 `PERFORMANCE_HARNESS.md`의 **1,000명 성능 하네스**를 기본 개발 계약으로
  사용한다. 학생 홈은 공용 bootstrap, 상세 화면은 열 때 전용 RPC, 쓰기 행동은 트랜잭션형 전용 RPC 한 번을 원칙으로
  하며 짧은 폴링·기능별 Realtime·무제한 목록·N+1을 금지한다. 정적 검사는 Docker 빌드를 막고, SQL은 실제 스키마에서
  기능 스모크 후 전부 롤백한다.
- 2026-08-08: 포인트는 선택형 게임 모듈이 아니라 **항상 켜지는 공용 코어 엔진**으로 운영한다. 새 콘텐츠는
  전용 RPC 하나가 자기 권한·완료 조건을 검증하고 내부 `point_engine_apply()`를 호출한다. 화면은 `pointApi`만 사용하며,
  `event_key`로 재클릭·통신 재시도 중복을 막는다. 포인트 원장과 범용 증감 함수는 클라이언트 직접 쓰기를 금지한다.
- 2026-08-08: 과제 승인·승인 취소와 포인트 원장은 **클라이언트 직접 변경을 금지하고 서버 RPC의 단일
  트랜잭션으로만** 처리한다. 서버가 `post_id`에서 학생·학급·과제·보상 스냅샷을 다시 확인하며, 재시도는
  같은 보상을 중복 지급·회수하지 않는다. 기존 이상 데이터는 자동 차감하지 않고 별도 검증 후 보정한다.
- 2026-08-08: 비로그인 첫 진입 화면은 외부 도구 모음으로 사용하지 않고 **학생·선생님 로그인과 현재 아지트 안에서
  할 수 있는 일만** 보여 준다. 연구소·문해력 활동은 앱 내부 통합 뒤 해당 기능의 내부 동선에서 제공하고, 교사용·학생용
  외부 가이드는 장차 앱 안의 상황별 안내로 대체한다. 기존 수호룡 상단 이미지는 유지하되 화면은 외곽 700px·본문
  620px의 세로형으로 줄여 이미지와 로그인 영역이 과도하게 넓어지지 않게 한다.
- 2026-08-07: 교사 화면의 학생 기능 ON/OFF는 학생 일기에서 확정한 읽기 쉬운 공용 스위치로 통일한다. 스위치는
  상태·학생 노출 설명·접근성만 담당하고, 저장과 기존 데이터 보존은 각 기능 모듈이 소유한다.
- 2026-08-07: 일기는 개인정보가 포함될 수 있으므로 **학급별 선택 기능**으로 운영한다. 교사 관리 탭은 다시 켤 수 있도록
  항상 남기고, OFF는 학생 노출과 작성·수정만 막으며 기존 일기·초안·보상 데이터는 삭제하지 않는다.
- 2026-08-07: 교사의 학생 독서록 관리는 **학생 독서록 확인 / 독서록 완료조건·포인트 / 독서록 이벤트** 세 탭으로
  나눈다. 독서마라톤 같은 기간형 활동은 완료 조건이나 일반 글쓰기 동기부여 설정에 섞지 않고 이벤트 탭에서 관리한다.
- 2026-08-07: 독서마라톤은 한 캠페인 안에서 **개인 거리 순위 경쟁과 학급 공동 목표 달성의 투 트랙**으로 운영한다.
  학생 화면에는 상위 3명과 본인 순위만, 교사 화면에는 전체 순위를 보여 준다. 1쪽=10m이고 같은 학생의 같은 판본은
  캠페인당 한 번만 센다. 공동 목표 완주 뒤에도 새 캠페인 전까지 개인 기록은 계속되며 별도 포인트는 지급하지 않는다.
- 2026-08-07: 독서마라톤 페이지 수는 학생에게 받지 않고 `book_catalog`의 판본 정보로 관리한다. 국립중앙도서관 API 대신
  Google Books를 연결해 책 선택 때 쪽수를 보여 주고, 저장 뒤 서버가 ISBN 일치 여부를 다시 확인한 값만 기록한다.
  Google에 쪽수가 없는 판본은 교사가 보정하며 교사 보정값은 Google 자동 조회가 덮어쓰지 않는다.
- 2026-08-07: 교사 대시보드는 특정 PC 폭에 고정하지 않고 가용 화면을 모두 사용하는 유동형 작업 공간으로 유지한다.
  단, 읽기용 문장·설정 폼·모달처럼 너무 넓어지면 사용성이 떨어지는 개별 요소의 제한 폭은 그대로 둔다.
- 2026-08-07: 학생 화면 확인을 대형 공용 미리보기로 먼저 만들지 않는다. 현재는 교사가 설정 결과를 꼭 확인해야 하는
  `글쓰기 창 관리`와 `과제 만들기·수정`에만 작은 전용 미리보기를 두고, 실제 필요가 확인될 때 화면별로 확장한다.
- 2026-08-07: 연구소 통합의 교사 화면은 역할을 분리한다. 새 **`학생 글쓰기 지도` 탭**에서 연구소 기능과 활동을 관리하고,
  `설정 → 글쓰기 창 관리`에서는 연구소 결과물을 학생 글쓰기 창으로 불러오는 기능의 ON/OFF와 미리보기만 맡는다.
  OFF는 불러오기 진입만 숨기며 연구소 활동·결과 데이터는 보존한다.
- 2026-08-07: 교사 학생 화면 확인은 학생 계정 **대리 로그인이나 실제 학생 데이터 조회가 아니라 안전한 샘플 미리보기**로
  제공한다. `글쓰기 창 관리`는 실제 공용 입력 부품을 재사용한 글쓰기 창만 보여 주며, 학생 홈 같은 전체 화면 확인은
  이 설정과 분리된 공용 학생 화면 미리보기에서 다룬다. 글쓰기 보조 기능은 `classes.writing_editor_settings.enabled_tools`의 매니페스트 ID로
  학급별 ON/OFF하며, 설정 조회 실패 시 기존 기능을 유지하도록 기본 ON으로 둔다.
- 2026-08-07: 어휘의 탑 V2는 **문항을 먼저 정비하고 게임 수정은 가장 마지막에** 한다. 기존 1,573개를
  자동 진단한 뒤 학년별 샘플과 핵심 낱말을 사람이 직접 검토하며, 특히 구별 문제는 `검토 완료` 상태만
  출제한다. 현재 운영 게임은 검토 문항이 준비될 때까지 유지한다.
- 2026-08-07: 포인트를 소비하는 신규 놀이 콘텐츠보다 **어휘의 탑 리뉴얼을 먼저 완료·안정화**한다.
  어휘의 탑은 오답 복습형 탐험과 서버 보상 상한을 적용하고, 이후 콘텐츠는 포인트를 새로 많이 발행하기보다
  학생이 재미있게 소비할 수 있는 독립 모듈을 우선 검토한다.
- 2026-08-07: 수호룡 공방 유료 상품을 27종에서 **40종**으로 늘리고 5개 슬롯마다 8종씩 배치한다.
  등급은 입문 8·일반 12·희귀 10·영웅 7·전설 3종의 피라미드로 구성하고 기존 구매 ID와 소유권은 보존한다.
- 2026-08-22: 일기·독서록을 일일 제출로 운영할 수 있는 현재 활동량에 맞춰 공방 일반 상품은
  **입문 300P/작가 1, 일반 700P/작가 3, 희귀 1,500P/작가 5, 영웅 3,000P/작가 7**로 조정한다.
  전설 장식은 판매하지 않고 작가 10·독자 7을 모두 달성한 학생에게 5종 완성 세트로 한 번만 선물한다.
  가격 인하 전 구매자는 실제 결제액과 새 가격의 차액을 환급하며 기존 소유권은 유지한다.
- 2026-08-22: 성장 단계에 막힌 학생과 포인트를 많이 모은 학생 모두 선택지가 있도록 작가 단계 제한 없는
  자유 구매 완성 세트 3종을 둔다. 세트별 아이템 가격은 800P·1,000P·1,200P로 고정하며 등급 체계와 분리한다.
- 2026-08-22: 장르 글쓰기는 전용 틀을 늘리지 않고 **글 종류 프리셋**으로 넓힌다. 틀을 새로 만드는 기준은
  ①출력물 모양이 본문과 다른가 ②글자 말고 다른 것을 담는가 ③칸별로 따로 꺼내 쓰는가 셋 중 하나다.
  이 기준으로 남은 후보는 극본뿐이고, 나머지는 안내 질문과 최소 문단 수로 충분하다. 제출이 시작된
  미션의 안내 질문은 잠근다 — 학생 답이 질문 번호로 저장되고 초안은 학생 기기에만 있어 되돌릴 수 없다.
- 2026-08-22: 어휘의 탑 층당 진도 보상 총액의 위쪽 상한(500P)을 없앤다. 학급마다 어휘 학습 비중이 달라
  교사가 상한 없이 정하고, 서버는 0P 미만과 빈 값만 보정한다(0P는 보상 끄기). 값 보정의 원본은
  `vocab_tower_v2_floor_reward_points_v1`과 `vocab-tower/rewardPolicy.js` 두 곳뿐이며, 남는 한계는
  정책이 아니라 integer 컬럼의 기술적 한계다.
- 2026-08-07: 꾸준히 참여한 학생의 포인트 획득 기준을 **주 1,000P**로 결정했다. 초기 배분은 글쓰기 700P,
  모든 학습 게임 공용 상한 250P, 행사·교사 보너스 50P다. 18주 학기 획득 기준은 18,000P이며,
  공방 유료 상품 전체 가격 목표는 그 3배인 54,000P 이상으로 잡는다.
- 2026-08-07: Stage 2(연구소 연동+서바이벌 흡수) 착수를 앞두고 전체 코드베이스를 분석했다. 결론:
  핵심 루프(글쓰기→드래곤 육성→교사 확인) 자체보다 그걸 감싸는 **과제형 글쓰기 파이프라인**(모듈화
  이전 구조, `useMissionManager.js` 1,383줄)과 **교사 확인 화면**(`components/teacher/` 40개 파일
  11,032줄, 학생 쪽 `MyPostEngagementPanel` 같은 통일 부품 없음)이 계속 커지는 게 문제이고, 다음 주
  통합이 정확히 이 두 곳을 건드린다(2e는 모듈 계약 위에, 2d는 학생 에디터에 얹힘). 이 분석을 원래
  모듈화 계획(Stage 3a/3b)·진행 현황과 하나로 합쳐 **Stage 3 상단 요약 + 3c(남은 정리) 우선순위**로
  정리했다. 아직 실행 여부·순서는 미정 — 다음 세션에서 결정.

- 2026-08-06: 같은 날 정한 비초기화 시즌 결정을 제품 목표에 맞게 변경한다. 수호룡은 **한 학기 동안 한 마리**를 키우고
  `성장 중 → 작별 편지 → 시즌 보관`으로 마무리한 뒤, 새 학기는 작가·독자 성장과 수호룡 종류를 1단계 알부터 다시 시작한다.
  단, 기존 글·포인트·구입/장착 소품은 지우지 않으며 최종 수호룡·칭호·편지는 불변 스냅샷으로 보관한다.
  작별 편지는 성장·보상에 포함하지 않고 최종 수호룡이 들어간 PNG를 브라우저에서 생성한다. 아지트 온 클래스의
  `agit_season_history`와는 계속 분리한다.

- 2026-08-06: 기능의 구현·DB 적용·자동검증·운영 배포가 끝났다면 기능 체크박스는 `[x]`로 닫는다. 태블릿에서의
  글자 크기·배치·입력감이나 긴 동선 확인은 기능 미완료와 섞지 않고 `현재 위치 5번`의 독립 인수 검사표에서 관리한다.
  AI 댓글 오탐률처럼 표본 시간이 필요한 것은 미완료가 아니라 날짜가 있는 운영 측정 일정으로 남긴다.

- 2026-08-05: 포인트 꾸미기는 관리가 복잡한 자유 배치·신체 액세서리 대신 **프레임·받침대·좌우 소품·문패 5개 고정 슬롯**으로
  운영한다. 작가 칭호=수호룡 성장, 독자 칭호=빛 효과, 포인트=생활 공간 꾸미기로 역할을 분리한다. 장착 모습은 나의 방과
  친구 아지트가 같은 공용 장면을 읽어 자동으로 일치시킨다. 중앙은 중립 실내로 고정하고 기존 구매 배경은 저장 ID를 유지한
  네 모서리 프레임 소유권으로 보존한다. 소품은 일반 가구보다 수호룡 세계관의 서가·룬석·불꽃·관측구·둥지를 우선한다.

- 2026-08-05: 좌우 소품은 작은 CSS 기호로 표현하지 않는다. 왼쪽은 기록·마법, 오른쪽은 교감·관측·보물·휴식으로 역할을
  나누고 드래곤 몸체와 같은 부드러운 3D 판타지 WebP 에셋을 사용한다. 학생이 이미 산 상품은 ID·가격·소유권을 유지한 채
  표시 이름과 모습만 개선한다.

- 2026-08-05: 문패는 단순 색상 변형이 아니라 재질·실루엣·테두리 효과가 다른 수집 상품으로 운영한다. 에셋 중앙에는
  글자를 굽지 않고 학생·수호룡 이름을 HTML로 겹쳐 재사용한다. 기존 4종 ID·소유권은 유지하고 전설 문패는 작가 10단계
  장기 목표로 둔다.

- 2026-08-05: 아지트 꾸미기와 독자 효과는 시각 언어를 분리한다. 포인트 상품은 물질적인 배경·가구·소품, 독자 칭호는
  빛·별·룬·궤도·수호 문장을 사용한다. 새 배경은 `readerTone` 대비 계약을 반드시 선언하고 독자 효과의 보호 무대와
  단계 인장은 앞쪽 꾸미기에도 가리지 않는 고정 레이어로 둔다.

- 2026-08-05: 수호룡 성장 축하 연출은 학생이 다음에 홈에 들어와 공용 칭호 상태를 확인할 때 표시하고, 서버에 마지막으로 본
  단계를 저장해 기기마다 반복하지 않는다. 테스트 칭호의 확인 기록은 실제 칭호와 분리해 운영 성장 이력을 오염시키지 않는다.

- 2026-08-05: 수호룡 실계정 단계 검증은 승인 글이나 글자 수를 위조하지 않고 별도 **관리용 칭호 테스트 오버라이드**로 한다.
  원본 통계와 포인트는 유지하고 공용 칭호 조회에만 합성하며, 테스트가 끝나면 오버라이드 행만 삭제해 즉시 원복한다.

- 2026-08-05: 작가 수호룡은 **4종 × 작가 성장 10단계 × 독자 효과 7단계**로 시작한다. 처음에는 알 4개 중 하나를
  선택하고 작가 3단계부터 한 번만 다시 고를 수 있다. 종·작가 단계는 개별 WebP, 독자 단계는 경량 CSS 효과로 합성해
  280조합을 표현하며 현재 이미지와 다음 단계 한 장만 읽는다. 선택 횟수와 자격은 서버 RPC가 강제한다.

- 2026-08-05: 드래곤은 별도 먹이 레벨 게임이 아니라 **작가 칭호 10단계와 함께 자라는 작가 수호룡**으로 운영한다.
  먹이 비용과 미접속 퇴화는 없애고 교감은 무료 기록, 포인트는 아지트 꾸미기에만 쓴다. 이름·구매 배경·보유 아이템은
  보존하며 친구 공개 화면도 같은 공용 성장값과 표현 모듈을 사용한다.

- 2026-08-05: 학생 홈은 카드마다 다른 배경색과 그라데이션으로 구분하지 않고 **흰색 공통 표면 + 중립 테두리**를 기본으로 한다.
  색은 칭호 뱃지, 메뉴 아이콘 배경, 완료·주의·긴급 같은 상태 전달에만 제한한다.
- 2026-08-05: 새 모달·패널의 아이콘형 창 닫기는 글자 `×/✕`를 직접 쓰지 않고 **공용 `ModalCloseButton`의 SVG**를 쓴다.
  버튼 색·크기는 화면에 맞게 조정할 수 있지만 `grid` 중앙 정렬과 고정 SVG 좌표는 유지한다.
- 2026-08-05: 누적 독서록 교사 관리는 **검토 대기 / 학생별 책장 / 전체 기록**으로 나누고 기본은 검토 대기로 한다.
  학생별 내보내기는 콘텐츠마다 Google·Excel 코드를 복사하지 않고 **공용 서버 행 계약 + 콘텐츠 export profile + 공용 전송부**를
  사용한다. 기존 미션 내보내기는 즉시 교체하지 않고 운영 호환을 유지하며, 회귀 검증 뒤 같은 계약으로 이동한다.
- 2026-08-05: 독서록 기본 정책은 **최소 200자·최소 1문단·기본 100P·하루 작성 완료 1편**으로 한다.
  새 학급과 기존 기본값 그대로인 학급에는 새 값을 적용하되, 교사가 따로 조정한 학급 정책과 과거 보상은 덮어쓰지 않는다.
- 2026-08-05: 독서록 정책의 `daily_reward_limit`는 호환을 위해 DB 이름은 유지하되 의미를 **하루 포인트 보상 상한에서
  하루 새 독서록 작성 완료 상한**으로 바꾼다. 학생 화면은 완료/설정/남은 편수를 표시하고, 새 완료는 원장 수를 서버에서
  강제한다. 완료한 기존 글 수정은 새 편수로 세지 않으며 글을 삭제해도 그날 사용한 편수는 복구하지 않는다.
- 2026-08-05: 과제와 독서록은 같은 글쓰기 정책을 사용하더라도 **완료 흐름은 분리**한다. 과제는 교사 승인 뒤 보상하고,
  독서록을 포함한 자율 글쓰기는 서버가 완료 조건을 검증한 최초 완료 시 즉시 1회 보상한다. 공용화 대상은 최소 글자·문단,
  포인트·보너스 정책, 분량 계산·검증, 설정·진행 UI이며 보상 원장은 글 출처별 유일 제약으로 중복 지급을 막는다.
- 2026-08-05: Google Docs 내보내기는 별도 API 키와 번들형 `gapi-script` 대신 **기존 공개 Web OAuth 클라이언트 ID +
  Google Identity Services 토큰 모델 + Docs REST API**로 유지한다. OAuth 클라이언트 ID와 anon 키는 공개 프론트 설정이지만,
  OAuth 시크릿은 서버 Auth에만 두고 Vite 빌드에 절대 넣지 않는다. 권한 요청은 팝업 정책을 위해 사용자 클릭 직후 실행한다.
- 2026-08-05: 디자인 2차 전에 기존 ESLint 경고를 정리하고 **0경고·0오류를 새 기준선**으로 삼는다. 동적 키가 필요한
  집계는 학생 이름·ID를 일반 객체 키로 직접 쓰지 않고 `Map` 또는 검증된 `Reflect` 접근을 사용한다. 게임 보상 Effect는
  의존성을 숨기지 않고 실행 가드로 중복 지급을 막는다. 외부 `gapi-script` 번들 내부 `eval` 안내는 앱 린트와 별개이며,
  실제 구글 내보내기 인증 스모크 없이 패키지를 교체하지 않는다.
- 2026-08-10: 학생 맞춤법 감지는 **350ms 입력 지연 + 한 번의 결합 정규식 순회 + 최대 50개 결과 상한**으로 유지한다.
  설명·예문 수첩은 학생이 열 때만 지연 로드하고, 감지 규칙이 가리키는 수첩 ID와 결과 상한은 `spelling:check`에서 자동 검증한다.
- 2026-08-12: 학생 알림은 `내 글 소식`(친구 반응·친구/선생님 댓글), `지금 할 일`(시작 전·작성 중·다시 쓸 글),
  `활동 알림`(다시쓰기 요청·승인+지급·승인 취소+회수·교사 포인트 조정)으로 책임을 분리한다. 활동 알림은 학생별
  단일 이벤트 원장 + 내부 발행 함수 + 홈 bootstrap 요약 + 열 때 목록 RPC 구조로 확장하며 기능별 폴링·Realtime은 추가하지 않는다.
  과제 글 반려·승인 전역 배너는 전체 목록이 아닌 최소 신호만 읽는 12초 코어 예외로 별도 관리한다.
- 2026-08-10: 맞춤법 확장은 **`spelling-learning` 등록 모듈**이 학급 항목·검색 집계·교사 AI 초안·학급 발자국 카드를 소유한다.
  학생 입력 중 DB 쓰기는 금지하고 수첩을 닫을 때 최대 20항목을 RPC 한 번으로 합친다. AI 초안은 담당 교사가 확인·승인해야
  학생 밑줄과 검색에 적용된다. 교사 관리는 수업 도구가 아니라 **통합 설정의 모듈 설정 슬롯**에 두며,
  학급 데이터를 공통 데이터로 승격하는 관리자 검수 흐름은 후속으로 둔다.
- 2026-08-05: UI 정리는 전면 재디자인이나 원시 값 일괄 치환이 아니라 **의미 기반 `--ui-*` 토큰 + 공용 Button/Card/
  CenteredDialog를 먼저 고정한 뒤 화면 단위로 점진 적용**한다. 클릭되지 않는 카드는 움직이지 않고, 태블릿 버튼은 44px 터치
  높이를 보장하며, 중앙 모달은 Esc·바깥 클릭·스크롤 잠금·Fullscreen 컨테이너·SVG 닫기를 공통 규격으로 한다.
  학생 글쓰기 저장·제출 코어는 건드리지 않고 입력 주변 UI에만 디자인 규격을 적용한다.
- 2026-08-04: 교사 **학급 운영·학급 발자국의 신규 지표는 대시보드 본체에 직접 카드를 추가하지 않고
  `manifest.dashboardCards` 확장 계약으로 등록**한다. DB의 새 필드를 임의로 노출하지 않으며 제목·단위·순서·공개 화면을
  카드 한 곳에서 명시한다. 기존 배열의 새 유형은 자동 표시하고, 새 카드는 등록 한 번으로 기본/전체/모달에 함께 반영한다.
  가벼운 핵심 집계는 기존 RPC 이름 공간을 확장하되 무거운 선택 기능은 모듈 전용 지연 조회로 분리한다.
- 2026-08-04: 학생의 **질문 없는 과제·질문 있는 과제·독서록 입력창은 질문 있는 과제의 넓은 폭으로 통일**한다.
  세 화면이 공용 바깥 폭 `1200px`를 사용하며 PC의 실제 제목·본문은 약 `1016px`, 모바일은 화면 너비에 맞춰 줄어든다.
- 2026-07-31: 학생 글쓰기의 맞춤법 지원은 **앱 내부 수첩 밑줄 + 학생 주도 `맞춤법 찾아보기`**로 한다.
  기기 안의 확정 규칙은 제목·본문에서 명확한 오류 후보만 표시하되 글을 자동 변경하거나 외부로 전송하지 않는다.
  검색은 낱말·짧은 문장을 수첩에서 직접 찾고, 학생이 찾기를 명시적으로 누른 낱말·짧은 구만 인증 서버 함수를 통해
  국립국어원 표준국어대사전에서 조회한다. 문장 전체를 외부로 보내지 않고 비용이 드는 공용 AI를 호출하지 않으며,
  OS 자동 수정도 끄고 저장·제출 코어와 분리된 `writing/tools` 슬롯으로 확장한다.
- 2026-07-30: 칭호를 제외한 글쓰기 발자국은 `writing-footprint`의 **공용 시각화 + 학생 본문 컴포넌트**로 유지한다.
  교사 화면은 같은 표현 규칙을 재사용하되 현재 학년도 학급 집계만 별도 RPC로 받고, 학생 간 등수 대신 개인별 참여·최근 기록을 표시한다.
  교사용 `학급 발자국`은 평가의 하위 화면이 아니라 아지트 놀이터 다음의 독립 상단 업무 탭으로 둔다.
- 2026-07-30: 학생 자신의 **작가·독자 칭호는 `writing/title-status` 공용 모듈만 조회·표시**한다. 나의 아지트와
  글쓰기 발자국에서 개별 레벨 계산이나 독자 점수 조회를 다시 만들지 않고, 공용 RPC·캐시·뱃지·설명판을 그대로 재사용한다.
- 2026-07-30: 친구 아지트 `최신 글`은 과제와 독서록을 한 피드에 섞지 않고 **`선생님 과제 최신글 / 독서록 최신글` 고정 탭**으로
  나눈다. 과제 탭을 기본값으로 하며 기존 과제별 필터는 그 안의 보조 탐색으로 유지한다. 두 유형은 받은 뒤 화면에서 거르지 않고
  `student_posts.class_id`와 유형 조건을 함께 건 별도 최신순 조회로 페이지를 나눈다.
- 2026-07-30: 친구 아지트는 친구의 모든 활동을 보여 주는 정보창이 아니라 **공개 자기표현과 둘 사이의 관계를 보는 공간**으로
  축소한다. 목록은 드래곤 성장치 대신 이름·작가/독자 칭호로 찾고, 상세는 칭호·반려 드래곤·공개 서재·서로의 공개 글에 남긴
  승인 댓글/반응만 표시한다. 글쓰기 전체 통계와 포인트 활동 횟수는 기존 모듈을 삭제하지 않고 친구 화면 연결만 끊는다.
- 2026-07-30: 독자 칭호 경계는 한 학기만에 중앙 학생이 상위권에 닿지 않도록, 최원진 교사의 현재 운영 학급 실데이터를
  기준으로 **`0 / 1 / 20 / 50 / 120 / 200 / 300점`**으로 재보정한다. 1점의 `첫 독자`는 즉시 성취감 때문에 유지하고,
  1년 뒤 최소 절반이 Lv5에 닿되 Lv6·Lv7은 꾸준한 학생에게 남긴다. 점수·댓글·반응은 변경하지 않고 계산 경계만 바꾼다.
- 2026-07-30: **작가 칭호가 세는 글의 정의를 한 곳으로 고정한다** — 과제글은 미션별 가장 최근 한 편,
  자율글(`mission_id IS NULL`)은 각 글을 한 편. 클라이언트는 `writerLevels.js`의 `collectWriterPosts()`,
  SQL은 `get_my_writing_footprint_detail`의 `level_posts` CTE가 짝이며 한쪽만 고치면 안 된다.
  활동 통계(달력·연속 기록·이달의 활동·가장 길게)는 중복 제거 없이 승인 글 전체를 센다 — 재작성한 날도 글을 쓴 날이다.
- 2026-07-30: **칭호 뱃지는 256px WebP로만 제공한다**(화면 최대 76px × DPR 3). 512px PNG는 40~76px 표시에
  과했고 설명판 한 번에 3.3MB를 내려받았다. `<picture>` PNG 폴백은 두지 않는다 — WebP는 2020년 이후 브라우저에서
  보편이고, 파일을 두 벌 유지하면 단계 추가 때 어긋난다. 변환은 새 의존성 없이 설치된 Edge/Chrome을
  Playwright로 띄워 쓴다(`npm run badges:optimize`).
- 2026-07-30: 후속 사용자 확인으로 바로 아래의 `축별 기본 훈장 2종 + LV` 결정을 폐기하고,
  **작가 10단계·독자 7단계 각각 다른 뱃지 이미지 17종**을 적용한다. 기본 화면에는 현재 단계 이미지만 간결하게 표시하고,
  설명판에서는 달성 이미지는 원색, 미달성 이미지는 잠금·회색 처리해 성장 방향을 미리 보여 준다.
- 2026-07-30: 나의 아지트 칭호 이미지는 단계마다 17종을 따로 만드는 계획 대신 **작가/독자 축별 훈장 2종 + 현재 LV 표식**으로 바꾼다.
  기본 화면은 두 뱃지와 현재 이름만 간결하게 보여 주고, 성장 공식·전체 단계·다음 조건은 뱃지를 눌러 여는 설명판으로 이동한다.
- 2026-07-29: 포인트·놀이 확장은 **게임 폴더 + 매니페스트 + 레지스트리 등록 한 줄**을 표준으로 한다.
  공통 셸이 학급 ON/OFF와 카드·로딩·오류를 담당하고, 모듈은 교사 관리(`teacherEntry`)와 학생 실행(`studentEntry`) 및
  전용 데이터만 소유한다. 신규 모듈은 열 때만 조회하며 OFF는 데이터를 지우지 않는다. 기존 드래곤·어휘탑은 회귀를 막기 위해
  우선 `game/legacy` 호환 어댑터로 격리하고, 기능별 실기기 검증 후 각 모듈의 `teacherEntry`로 옮긴다.
- 2026-07-29: 평어 도우미의 성취기준 범위는 여러 교과로 넓히지 않고 **2022 개정 국어과**를 기준으로 한다.
  성취기준은 개별 학년이 아니라 교육과정 단위인 **3~4학년군 / 5~6학년군**으로 선택·저장하며, 기존 `grade` 값은 읽을 때
  학년군으로 변환하고 다음 저장부터 `grade_band`를 사용한다. 직전 국어·사회·과학 확장 계획은 이 결정으로 철회한다.
- 2026-07-29: **글쓰기 자체를 독립 평가 영역으로 취급하지 않는다.** 학생 평가에는 국어 활동에서 나온 글과 실제 평가 결과·교사 관찰을
  근거로 남기고, 평어 도우미에서 교사가 평가계획에 맞는 국어 성취기준을 직접 확정한 뒤
  그 근거로 앞뒤 문장에 자연스럽게 이어 붙일 짧은 평어 문장을 생성한다. AI의 성취기준 자동 추정은 추천에만 사용하고
  최종 선택은 교사가 하며, 결과 화면은 학생마다 문장을 바로 복사할 수 있게 한다.
- 2026-07-29: 직전 최근 활동 기본 기간 1일 결정을 사용자 확인에 따라 **7일로 변경**한다.
  하루 동안 활동이 없을 때 빈 화면이 먼저 보이는 문제를 줄이기 위한 UI 기본값 변경이며, 1·14·30일 선택과 DB 기간 제한 구조는 유지한다.
- 2026-07-29: 최근 활동의 기본 기간은 **최근 24시간(1일 이내)**으로 하고 7·14·30일로 넓혀 본다.
  기간은 화면에서 목록을 받은 뒤 거르지 않고 RPC가 원본 조회 단계에서 제한한다. PC 카드 열 수는 글쓰기 관리와 같은 조작감으로
  브라우저에 저장하되 2~6칸을 허용하고, 모바일은 선택값과 무관하게 1열로 표시한다.
- 2026-08-04: 학급 전체 판단과 학생 개인 관리를 분리한다. **학급 운영**은 최상위 업무 영역으로 두고 운영 현황·최근 활동을,
  **학생**은 학생 명단·코드·포인트 같은 개인 관리만 담당한다. 글쓰기 탭과 학급 발자국의 독립 위치는 유지한다.
- 2026-07-29: **최근 활동은 운영 통계와 분리된 학생 관리 3번째 메뉴**로 둔다. 학급 운영 현황은 집계와 교사의 할 일만,
  최근 활동은 과제·독서록·댓글의 실제 시간순 피드만 담당한다. 댓글 피드는 글쓰기 발자국 도입 이후 이벤트가 아니라
  `post_comments`의 승인된 학급 댓글 원본을 사용해 기존 댓글도 보여 주되, 목록은 미리보기만 반환하고 전문은 선택 시 조회한다.
- 2026-07-29: 학급 운영 현황은 학생 순위를 강조하지 않고 **기간별 학급 활동 + 교사가 지금 확인할 일 + 미션별 진행 상태**로 구성한다.
  수시 집계가 필요한 교사 화면은 원천 목록을 내려받지 않고 전용 RPC로 계산하며, 고쳐쓰기·댓글·피드백은 글쓰기 발자국 도입 이후 자료만 사용한다.
  마감일 데이터가 아직 없으므로 `지각/기한 초과`로 표현하지 않고 **현재 미제출**로만 표시한다.

- 2026-07-27: 자율 독서록은 미션 승인·점수·평가 흐름에 넣지 않고 **가벼운 교사 확인**으로 관리한다.
  교사 대시보드의 `미션 관리`를 `글쓰기 관리`로 바꾸고 내부를 `선생님 과제 / 학생 독서록`으로 나눈다.
  검토 기록은 `reading_log_teacher_reviews`에 분리해 학생 글·공개 설정을 교사가 수정하지 않으며, 목록은 요약 RPC로 읽고
  본문은 선택 시 한 건만 조회한다. 학생에게는 자기 독서록의 `확인했어요` 또는 교사 한마디만 보여준다.
- 2026-07-27: 친구 아지트의 포인트 정보는 잔액·총 획득액·순위 대신 **활동 횟수**만 공개한다.
  공개 유형은 글쓰기 보상·회의 참여·어휘탑·드래곤 돌봄·아지트 꾸미기이며, 신규 환영 포인트와 교사 수동 조정의 금액·사유는 제외한다.
  자유 형식 `reason`을 화면에서 해석하지 않고 `point_logs.activity_type`과 같은 반 전용 요약 RPC를 사용한다.
- 2026-07-27: 친구 아지트 상세는 `FriendProfileShell`과 카드 매니페스트로 구성한다. 드래곤·발자국·공개 책장·포인트 활동을 활성화하고,
  관계 카드는 공개 정책이 확정될 때까지 `planned` 슬롯으로 둔다. 카드 캐시는 보는 학생과 대상 친구를 함께 키로 사용하며,
  각 카드는 독립 오류 경계를 가져 일부 데이터 장애가 정보창 전체를 막지 않게 한다.

- 2026-07-27: 글쓰기 발자국은 **기능 적용 이후 활동만 0부터 기록**하고 원문·댓글 내용·평가 점수는 이벤트에 넣지 않는다.
  같은 글을 같은 날 여러 번 저장한 고쳐쓰기는 1회로 계산하며, 댓글은 글/사람 조합을 중복 제거한다. 원천 이벤트는 직접 공개하지 않고
  매일 00:10 만든 학생별 스냅샷만 본인·같은 반 친구에게 역할별 RPC로 제공한다.
- 2026-07-27: 친구 커뮤니티의 **1차 목표는 지금 작성한 과제 글을 바로 공유해 함께 읽는 것**, 2차 목표는 학생별 아지트를
  방문해 드래곤·공개 글·향후 통계 정보를 보는 것으로 정한다. 따라서 기본 탭은 `우리 반 글 나눔`으로 두고 본인 글도 포함하며,
  `친구 아지트 방문`은 학생마다 선택한 꾸미기 정보를 드러내는 별도 탭으로 유지한다.
- 2026-07-27: 친구 아지트 공개 책장은 **친구 목록에서 모든 글을 미리 집계하지 않고**, 특정 친구의 정보창을 연 뒤에만
  요약 목록을 지연 조회하며 상세 본문은 글을 선택할 때 조회한다. 공개가 취소되면 열린 상세도 주기·화면 복귀 시 다시 확인해 닫고,
  댓글·반응 직접 조회도 원글 공개 범위를 따르게 한다.
- 2026-08-04: 독서록은 **API 도서 정보 → 학생별 나의 책 → 독서록 한 편** 3단계로 관리한다.
  공통 도서와 책장 데이터는 독립 모듈 테이블에 두고 `student_posts`는 글 본문 저장소로 유지한다. 국내 도서 검색은
  카카오 책 검색을 1순위로 사용하되 키를 프론트에 노출하지 않고 Edge Function에서만 호출하며, 검색 실패·미등록 도서는 직접 입력을 항상 제공한다.
- 2026-07-27: **학생 자율 글쓰기는 독서록 하나로 시작**한다. 일기·자유글·자율 시쓰기는 당분간 넣지 않고,
  선생님 과제는 기존 자유글·시·회의 안건 등 장르 미션으로 확장한다. 자율 독서록은 시스템 미션을 만들지 않고
  `student_posts`의 `writing_context='self'`, `self_writing_type='reading_log'`로 구분한다. 공개 기본값은 2026-07-28 결정에 따라
  친구 공개이며 학생이 원하면 비공개로 전환한다.
- 2026-07-27: **친구 아지트는 게임의 캐릭터 정보창 같은 모듈 셸로 발전**시킨다. 아바타·드래곤·공개 글 책장·
  처음/최종 글·글쓰기 발자국·사회적 반응·포인트 활동을 독립 카드로 지연 로드하고, 향후 게임/드래곤 기능도 매니페스트 등록으로 추가한다.
  글쓰기 발자국은 2026-07-30 실측 후 일별 스냅샷을 폐기하고 원천 이벤트를 실시간 집계한다. 평가 향상도는 적은 평가 횟수 때문에 보조 지표로만 쓴다.
- 2026-07-27: 글쓰기 발자국과 피드백 횟수는 **기능 도입 이후 자료부터** 기록한다. 기존 댓글 중복 자료는 삭제하지 않고
  통계에서 `DISTINCT`로 완화한다. 끄적끄적 아지트 헬퍼 통합은 친구 정보창과 글쓰기 구조가 안정된 마지막에 RPC 데이터 연동으로 진행한다.
- 2026-07-27: **권한 회수는 `PUBLIC` 과 역할 양쪽 모두에서 해야 한다.** `FROM anon` 만 회수해도 PUBLIC 권한이
  남아 있으면 anon 은 계속 호출 가능하고, `FROM PUBLIC` 만 회수해도 명시적 `anon=X` 가 남으면 마찬가지다.
  회수 후에는 반드시 `has_function_privilege('anon', oid, 'EXECUTE')` 로 **실효를 확인**한다.
- 2026-07-27: **`auth.uid() IS NULL` 을 "서버 내부 호출"로 신뢰하지 않는다.** anon 키 요청도 uid 가 NULL 이다.
  서버 배치를 허용하려면 `current_user = 'service_role'` 처럼 **롤을 직접 확인**한다.
  이 전제를 신뢰한 탓에 익명 사용자가 학생 포인트를 임의 조작할 수 있었다.
- 2026-07-27: **`SECURITY DEFINER` 함수는 `REVOKE ... FROM PUBLIC` 만으로 보호되지 않는다.**
  Supabase 자체호스팅 스택이 `anon`·`authenticated` 에게 EXECUTE 를 기본 부여하므로 **역할을 명시해 회수**해야 한다.
  또한 "내부 구현용 함수라 호출자가 검사한다"는 전제를 두지 않고 **모든 DEFINER 함수가 스스로 권한을 검사**한다(이중 방어).
  이 규칙을 어겨 당일 권한 상승 취약점 1건이 발생했다(`admin_withdraw_teacher_internal`).
- 2026-07-27: **탈퇴는 `auth.users` 까지 지워야 완결된다.** `profiles` 만 지우면 재로그인 시 프로필이 재생성되고
  자동승인으로 부활한다. `classes.teacher_id`·`point_logs.teacher_id` 는 `auth.users` 를 `NO ACTION` 으로 참조하므로
  **학급·포인트로그 → 프로필 → auth.users** 순으로 지운다. 관리자가 취소한 계정은 `approval_revoked_at` 로 표시해
  자동승인 대상에서 제외한다.
- 2026-07-27: **관리자 대시보드의 계정 집계는 전부 DB RPC에서 한다.** 브라우저가 `classes`·`students`를
  전량 받아 세던 방식을 버리고 `admin_get_teacher_usage` 한 번 호출로 대체한다(학생이 늘어도 전송량은 교사 수 고정).
  계정은 `학급 미개설 > 학생 미등록 > 장기 미접속 > 활동 중 > 조용함` 5단계로 판정한다.
  **정리(삭제)는 학급·학생이 모두 없는 계정에만 허용**하고, 자료가 남은 계정은 되돌릴 수 있는 `승인 취소`로만 다룬다.
  서버 RPC가 `p_only_empty`로 한 번 더 막으므로 화면 실수로 실사용 계정이 지워질 수 없다. 상세는 [ADMIN_DASHBOARD_GUIDE.md](ADMIN_DASHBOARD_GUIDE.md).
- 2026-07-27: **아지트온클래스는 교사·학생 UI에서 완전히 숨긴다.** 코드·기존 설정·데이터는 보존하고 매니페스트의 `available:false`로 격리해 나중에 필요할 때 복구한다.
  놀이터의 별도 `학급 기능 켜기/끄기` 패널은 제거하고, 드래곤과 어휘의 탑은 각각 자신의 관리 카드에서 ON/OFF와 세부 설정을 함께 관리한다.
- 2026-07-27: AI 결과물은 국어 교과 평어 전체가 아니라 교사가 작성한 기존 평어의 앞이나 뒤에 붙이는 **글쓰기 평가 덧붙임 문장**으로 한정한다.
  학생 이름이나 `학생`으로 시작하지 않고, 문맥 의존 접속어 없이 성취기준과 실제 평가 근거를 반영한 완결형 평어체 1~2문장만 작성한다.
- 2026-07-27: `2193b86`의 `보관 미션 제출 완료 글 전체를 평어에 포함`하는 결정을 철회한다.
  국어 평어는 활성·보관 여부와 관계없이 **평가 결과가 있는 학생 글만** 근거로 사용한다. 제출물은 있으나 평가가 없으면 입력 경고로 생성을 막고,
  제출물 자체가 없는 학생만 결석 가능 대상으로 분류해 교사 확인 후 제외한다. 성취기준은 미션별 글 종류 추천을 참고하되 교사가 최종 선택한다(`a1a950a`).
- 2026-07-26: 국어 평어 대상 글은 활성 미션에서는 승인되었거나 평가된 제출물만 사용하고,
  **보관 미션에서는 승인 상태와 관계없이 제출 완료 글 전체**를 사용한다. 이전 수업 자료를 학기말 평어 근거로 누락하지 않기 위한 정책이다.
- 2026-07-26: 평가 루브릭은 자유·시·회의 모두가 `writing/evaluation` 공통 컴포넌트를 사용한다.
  루브릭 JSON에 실제 지도 학년(3–6)과 2022 개정 국어과 성취기준 코드를 함께 저장하고, 1차는 글쓰기 미션으로 관찰 가능한
  **쓰기 영역 + 시 표현 문학 기준**만 제공한다. 이 기준·평가 수준·교사 의견·학생 글은 `국어 평어` AI 초안의 근거로 사용하며 최종 문장은 교사가 검토한다(`2ce1778`).
- 2026-07-26: 회의 안건의 교사 제안 검토 목록은 **학생 이름·제목만 빠르게 선택**하는 구조로 두고,
  선택한 제안은 학생들과 함께 읽을 수 있는 화면 대부분 크기의 상세 모달로 표시한다(`609c340`).
- 2026-07-26: 학생 미션 목록은 모든 글 유형에서 **내 글 동작과 친구 글 보기를 나란히 제공**한다.
  친구 글은 선택한 미션으로 필터링된 친구 아지트에서 열며, 회의 안건만 버튼 문구·색으로 구분한다(`fd77777`).
- 2026-07-26: 회의 안건도 학생이 **쓸 때와 내 글을 볼 때는 공통 `StudentWriting` 흐름**을 사용한다.
  친구 제안 탐색·선택만 친구 아지트로 분리하고, 회의 전용 상태·반응 문구·즉시 제출 포인트는 장르 매니페스트 확장점으로 유지한다(`77ab8c5`).
- 2026-07-26: **회의 안건 제안도 친구 아지트에서 다른 글과 함께 탐색**한다.
  별도 목록으로 격리하지 않고 기존 친구 글 카드·상세 보기 방식을 재사용하되, 보라색 후보 카드와 `마음에 들어요` 선택 수로 성격을 구분한다(`9503d35`).
- 2026-07-26: 학생의 같은 학급 학생 정보 조회는 JWT `app_metadata.class_id` 존재 여부에 의존하지 않고,
  `students.auth_id`를 해석하는 `auth_user_class_id()`를 기준으로 한다. 제안·글·반응의 작성자 조인도 이 학급 범위 안에서 허용한다.
- 2026-07-26: 미션 생성은 **자유 글쓰기(기존 제목·본문 입력 + 하위 글 종류)**와 **장르 글쓰기(전용 입력 틀)**로 구분한다.
  장르 유형은 레지스트리 등록형으로 추가하며 1차는 `시 쓰기`와 `회의 안건 만들기`만 제공한다. 시는 연별 입력과 교사 기준을
  제공하고, 아이디어마켓은 사용자 명칭을 회의 안건 만들기로 바꾸되 기존 `meeting` 데이터·전용 토론 흐름은 재사용한다.
  나머지 장르는 자유형으로 운영한 뒤 실제 수업 효과가 확인된 유형만 전용 틀을 추가한다(`c894b41`).
- 2026-07-26: 회의 안건도 일반 글쓰기 미션과 **같은 통합 목록·수정·보관·삭제 동선**을 사용한다.
  학생 제안·투표·결정은 전용 모듈로 유지하되, 회의 카드의 `학생 제안 보기`에서 바로 열고 내부의 별도 안건 선택·관리 탭은 사용하지 않는다(`e1e3af8`).
- 2026-07-26: 활용도가 낮은 **아지트온클래스는 삭제하지 않고 기본 OFF 독립 모듈로 격리**한다.
  아이디어마켓은 아지트온클래스에서 분리해 `writing/idea-market`으로 옮기고, 기존 `mission_type='meeting'`과
  `student_posts`를 재사용하는 **글쓰기 입력미션 모듈**로 유지한다. 후속 결정에 따라 학급 기능 ON/OFF와 교사 독립 탭은
  사용하지 않고, 교사 **미션 관리**의 `아이디어 입력 미션 만들기`에서 생성·관리하며 생성된 미션만 학생 목록에 노출한다(`8047133`).
- 2026-07-26: **친구 아지트는 학급별 ON/OFF 없이 항상 사용하는 고정 기능**으로 확정. 모듈 폴더·레지스트리·lazy chunk는
  유지하되 매니페스트를 `core: true`로 지정하고 교사 토글 및 학생 진입점 조건을 제거한다. 직전 ON/OFF 결정은 폐기.
- 2026-07-26: `한줄모아`는 아지트 내부 학급 커뮤니티 기능이 아니라 랜딩에서 연구소로 이동하는 연결 항목이다.
  Stage 3 모듈화·정리 후보에서 제외하고, 연구소 `한줄쓰기` 결과물 연동은 Stage 2d/4b에서 다룬다.
- 2026-07-26: 친구 아지트를 `community/friends-hideout` 모듈로 이전하고 ON/OFF 게이트를 만들었다.
  이후 사용자 결정으로 고정 기능으로 전환되어 게이트는 제거됨(`b1acef5`).
- 2026-07-26: 어휘의 탑 학생 노출의 기준은 `classes.enabled_modules`로 일원화한다. 기존
  `vocab_tower_enabled`는 미설정 학급 초기값·구버전 롤백용 호환 미러로만 유지하며 교사 UI에는 노출하지 않는다.
- 2026-07-24: 교사 Google 로그인 후 대시보드 진입까지 사용자 실기기 확인 완료. 다음 세션은 신규 개발보다
  컷오버 후 교사·학생 핵심 글쓰기 전 과정, Realtime, 기존 기능의 스모크 테스트를 우선한다.
- 2026-06: 코드베이스 병합 없이 도메인·DB·인증 레벨 통합 / 전부 맥미니+로컬 DB(A안) 확정
- 2026-07-09: 장애 원인 = Auth IP 속도제한 + DB 부하. Realtime 무혐의 → 채널 감축은 Stage 5로 격하
- 2026-07-16: 작업 순서 확정 — **완전 이관 먼저, 통합·재편은 그 후** (원인 분리 원칙)
- 2026-07-16: 비전 4대 방향 확정 — ①앱+DB 완전 이관 ②연구소 SSO 통합+하위 활동 결과물을 글쓰기로 불러오는 구조
  ③글쓰기 외 기능 파트별 모듈화 ④코어 셸 불변, 글쓰기 확장도 모듈(슬롯)로만
- 2026-07-16: 연구소 연동은 활동 유형 무관 **표준 결과물 인터페이스(RPC)** 방식으로 — 코드 이식 금지 재확인
- 2026-07-17: **Stage 1.5 신설** — 교사·학생 대시보드 속도 개선을 이관 직후 정식 스테이지로 승격.
  진단: 느림 = 직렬 왕복 세대 수(교사 6, 학생 4) × 무료 티어 DB 지연. 해법 = 이관(지연 감소) + 부트스트랩 RPC 통합·청크
  프리로드(세대 감소) + Caddy 서빙 최적화(Vercel 자동 기능 승계). 측정 기준선 ①현 Vercel ②이관 후 ③패키지 후
- 2026-07-17: 연동 1차 대상 활동 확정 — **개요짜기(학생별 개요→에디터), 질문만들기(→미션 가이드 질문), 한줄쓰기**.
  연구소 입장 방식 개편 확정 — QR+수동 입력 → **아지트 JWT 인증 시 명단 자동 매칭 입장** (미인증 접속은 기존 방식 폴백).
  저장 위치 조사 완료: 개요=`student_sessions.answers`+`outline_queue.result`, 한줄=`one_line_entries.content`,
  질문=`selections[].remixedQuestion`
- 2026-07-23: Stage 0 대청소 완료. 발견 사항 — eslint에 `react/jsx-uses-vars` 규칙이 빠져 있어
  `<motion.div>`처럼 JSX 멤버 표현식으로만 쓰이는 소문자 import(~44건)가 미사용으로 오탐되고 있었음.
  규칙 추가로 해결. `scripts/`(브라우저 콘솔용 보안 테스트)는 린트 대상에서 제외.
  console 제거는 프로덕션 빌드에만 적용(dev는 유지). 남은 린트 경고(exhaustive-deps 19건 등)는 계획대로 Stage 3 이후.
- 2026-07-24: Phase 0 진척 — ①학교망 관문 통과(기호스팅 연구소로 검증) ②도메인 계획 확정: 본=`끄적끄적아지트.site`
  (`/`아지트+`/lab`연구소), 테스트=`test.`, API=`api.끄적끄적아지트.site` 신설(Kong, `supabase.샘링크.kr` 병행 유지),
  `helper.`→`/lab` 301 ③연구소 데이터: 유승현·최원진 계정만 UUID 매핑, 나머지 삭제 ④빈 접미사 스택 제거 완료.
  잔여: Cloud 전체 백업(pg_dump), Vercel 설정 기록
- 2026-07-24: Phase 0 완료 — Cloud 백업 완료(행수 검증 포함). ⚠️ 클라우드 PG 17.6 vs 로컬 스택 PG 15.8 확인
  → Phase 1에 "로컬 스택 PG 버전 정합" 선행 작업 추가 필요. Storage는 비어 있어 파일 백업 불필요
- 2026-07-24: PG 버전 격차 해법 확정(B안) — 라이브 스택 제자리 업그레이드 대신 **새 PG 17 스택 신설 후
  이중 복원(클라우드+로컬 유지분) → 라우팅 전환**하는 blue-green 방식. 연구소 무중단, 실패 시 즉시 롤백 가능
- 2026-07-24: **Phase 1 DB 이관 대부분 완료** — 새 PG17 스택(`~/agit-supabase/`, project `agit`, Kong:8100/DB:5433)에
  클라우드 덤프(users 2,896·students 1,398·posts 2,769·point_logs 17,286) + 연구소 스키마 복원. 익명로그인 ON·속도제한 300.
  연구소 UUID 매핑(유승현→yoo, 최원진→wonjin) + 나머지 삭제. 검증: 행수 일치·RLS anon 노출 0·익명signup 200.
  잔여: Edge Functions 4개 로컬 배포+시크릿, 아지트 프론트를 로컬 스택 URL/anon키로 재빌드(Phase 2와 연계)
- 2026-07-24: **🎉 컷오버 완료** — 본 도메인 `끄적끄적아지트.site`가 맥미니를 가리킴. 가비아 네임서버로 이전(Vercel→가비아),
  A레코드 5개(apex/api/helper/survival/umami→180.228.70.202). Caddy가 Let's Encrypt 인증서 발급 완료.
  전수 검증 통과: 학생 익명로그인(signup 200·bind_student_auth 200)·교사 구글OAuth 리다이렉트·HTTPS 5개 도메인.
  ⚠️ 8.8.8.8 apex 전파 진행중(구글 리졸버 NS캐시) — 완료 전까지 일부 사용자는 구 Vercel 앱을 봄.
  잔여: 전파 완료 확인 → 1~2주 모니터링 → Vercel/클라우드 해지. 교사 AI피드백 실사용 1건 확인 권장(공용키 167명)
- 2026-07-24: **이번 방학 사용자 우선순위 4가지 로드맵 반영** — ①학생 자율 글쓰기(독서록·일기)=Stage 4c
  ②포인트·드래곤 시스템 고도화=**Stage 4d 신설** ③연구소↔글쓰기 통합=Stage 2+4b ④기능추가 시 성능유지=Stage 1.5+3(모듈화·지연로딩).
  ①③④는 기존 스테이지에 매핑, ②만 신규. 포인트 고도화는 코어 셸(포인트) 개선이라 정합성 검증 신중히.
- 2026-07-24: **제품 집중 3대 기둥 확정** — ①교사 글쓰기 지도 ②학생 자율 글쓰기·제출 ③포인트 동기부여.
  아지트온클래스·아이디어마켓을 처음 정리 후보로 분류했으며, 2026-07-26 후속 결정에서
  아지트온클래스=기본 OFF 보관, 아이디어마켓=입력미션 유지, 친구 아지트=고정 유지로 확정.
  코드 청소(죽은 코드·대형 파일 분할·exhaustive-deps)도 3c에 포함.
- 2026-07-24: **기둥3(포인트 동기부여) 확장 방향** — 드래곤 기르기 다이나믹화 + 포인트로 즐기는 신규 엔터테인먼트 활동
  다양화(뽑기·미니게임·꾸미기·학급목표 등). 신규 활동은 모듈로 추가(지연로딩·학급별on/off로 성능 유지),
  "글쓰기 동기부여" 목적에 종속·교육용 수위 유지. Stage 4d로 통합.
- 2026-07-24: 미사용 **Umami Docker 서비스 중단·제거** — 자동 재생성하던 `com.jarvis.umami` LaunchAgent 비활성화 후
  앱·DB 컨테이너와 네트워크 제거. 복구를 위해 DB 볼륨·이미지·Compose 설정은 보존, Caddy/DNS 완전 정리는 추후 확정.
- 2026-07-24: 모바일 `www` 접속 호환을 위해 가비아 CNAME 추가 후 Caddy에서 apex 영구 리디렉션 적용.
  런타임 TLS·HTTP/2 301 검증 완료, `/etc/caddy/Caddyfile` 영구 저장은 관리자 sudo 실행 대기.
- 2026-07-24: 운영 Supabase Google OAuth 클라이언트를 Google 콘솔의 현행 Web 클라이언트와 정합화하고 `agit-auth`만 재생성.
  Auth healthy·callback URI 일치·Google 승인 응답 확인. 당시 대기하던 실제 교사 로그인은 2026-08-23 후속 확인 완료.
- 2026-07-24: 통합 스택 Realtime 503/tenant 오류 수정 — 구 연구소 스택과 컨테이너 이름 충돌을 피하기 위해
  `agit_default` 내부 DNS 별칭으로 공식 tenant host를 제공. publication 8개 테이블 복원 후 WS 101·실구독 확인.
- 2026-07-25: **3개 앱 통합 방식 분석·확정** — 앱 성격이 근본적으로 다름(연구소=Next.js·자체DB/인증,
  서바이벌=React·localStorage·무인증). 따라서 **연구소는 별도 앱 유지+SSO·데이터 연동(코드이식X), 서바이벌은
  아지트 모듈로 흡수**(같은 React·백엔드 없어 효율적, 점수→포인트). Stage 2를 2a(연구소 새스택+구글인증)
  ·2b(SSO)·2c(학생 자동입장)·2d(결과물 연동)·2e(서바이벌 흡수)로 재구성. "무조건 다 합치기"보다 앱별 최적 방식.
