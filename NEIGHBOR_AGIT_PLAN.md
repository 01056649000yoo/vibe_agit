# 이웃 아지트 점검 및 보완 계획

> **상태: 2026-09-05 점검 후 N0·N1과 N2 기본 조건을 보완하고 배포 대상으로 확정. SQL `20261240` 운영 적용 완료.**
> 기준 커밋 `2459f35`(기능 변경 `66dc461`). 이 문서가 앞으로의 범위·순서·합격 기준의 정본이다.
> 이전 구현·배포 이력은 [WORKLOG.md](WORKLOG.md), 큰 진행표는 [ROADMAP.md](ROADMAP.md)를 참조한다.
> 후속 요청으로 검사·공개·매칭 보완을 구현했고 배포 요청에 따라 롤백 검증 후 SQL을 운영 적용했다. 기존 제한 공개 범위는 유지한다.

## 1. 현재 위치와 이번 점검의 범위

이웃 아지트는 공간·초대·글 교류의 기본 구현과 세 활동 확장까지 있는 상태다.
“세 활동 운영 반영 전”이라는 종전 문서 머리말은 최신 이력과 맞지 않아 정리했다.

| 확인 대상 | 2026-09-05 확인 결과 |
|---|---|
| Git | 시작 시 작업트리 깨끗함. 로컬 `main`, 최근 20커밋 확인. 현재 보유한 원격 추적 브랜치에 main 미병합 커밋 없음. 원격 fetch는 하지 않음 |
| 최근 기능 이력 | `1001389` 세 활동 → `7c99165` 상호 승인 → `b1d5e73` 활동 탭 → `66dc461` 교사 직접 공유·승인형 매칭 |
| 운영 DB 적용 원장 | `npm run migrate:status`: 282개/282개, 대기 0. 이웃 기능과 무관한 `20261229_class_board_meal_columns.sql` 체크섬 경고 1건은 남음 |
| 공개 범위 | 최근 운영 기록상 `테스트`·`테스트2`, `limited_beta`, 학생 공개 ON. 이번 점검에서 공개 단계 변경 없음. 현재 공개 원장 자체의 별도 재조회는 하지 않음 |
| 정적·단위 검사 | `node --test tests/neighborAgit*.test.mjs tests/teacherNeighborAgitTab.test.mjs tests/teacherClassAgitTab.test.mjs`: **75/75 통과** |
| 기본 역할 검사 명령 | `npm run smoke:neighbor-agit`: **실패**. 이미 적용된 `create_neighbor_activity_v1`의 동일 시그니처 재생성에서 중단 |
| 현재 스키마 역할 검사 | 마이그레이션 재실행 없이 `node scripts/run-rollback-smoke.mjs tests/sql/20261201_neighbor_limited_beta.smoke.sql`: **통과·ROLLBACK 확인** |
| 화면 | `?dev-lab=neighbor-agit-teacher`의 실제 교사 컴포넌트로 글 검토·후보 불러오기·매칭 명단 확인. 1440/900/390px에서 기능 영역 가로 넘침 없음·콘솔 오류 0건. 샘플 자료이며 실제 두 교사·학생 로그인 연결 검증은 아님 |

위 표는 최초 점검 당시의 기준이다. 후속 로컬 보완 결과는 다음과 같다.

- N0: 적용 원장/checksum 기반 선행 SQL 선택. 적용된 8개는 건너뛰고 새 `20261240`만 롤백 적용하여 기초·제한 공개 스모크 모두 통과했다. 옛 필명·내부 시험 학급 기대값도 현재 계약으로 고쳤다.
- N1: 비공개 자율 글 제외·비공개 전환 시 공유 회수, 전문 열람과 원글 버전 비교, 입력한 반려 사유 저장, pending 전용 오래된 순서의 100편 묶음과 전체 대기 수를 구현했다. 묶음을 처리하면 다음 대기가 이어지며 임의 페이지 이동은 N3에서 다룬다.
- N2: 호스트 포함 두 학급, 로그인 가능한 활성 학생 각 1~100명·두 배 이내 조건을 화면과 생성 RPC에 적용했다. 일반 과제 화면의 변경으로 승인/매칭 상태와 제목·안내가 어긋나는 것을 DB에서 차단한다.
- 화면: 전문을 별도 검토 창에서 읽고 공개하거나 사유를 적어 반려한다. 학급 전환은 교사 모듈을 새로 마운트하며 검토 창의 늦은 조회는 버린다. 학급·학생 전체 비동기 흐름은 N3의 나머지 범위다.
- 검증: 이웃 전용 79개, 구조 260개, 전체 보안 검사, 마이그레이션 롤백 및 이웃 역할 스모크 통과. 린트·빌드와 DB 없는 PC/모바일 상호작용도 확인했다. 실제 기기에서 교사 둘과 학생이 로그인하는 운영 인수는 별도로 남아 있다.
- 보안 참조: 연구소·샘링크·클래스룸툴·자비스 배포본에 기존 직접 검토 RPC 호출이 없음을 확인했다. 클래스룸툴의 실제 정적 배포 경로 `/srv`를 RPC 검사 설정에 반영했다. 비공개 조건과 대기열 조건을 일부러 깨뜨렸을 때 새 회귀 검사가 실패하는 것도 롤백으로 확인했다.
- 남은 N2/N4 경계: 진행 중 호스트 이전·참여 학급 퇴장/공간 종료·전입/전출에 따른 활동 정리 및 명단 갱신 정책, 임의 페이지 조회, 전체 장르 읽기 계약. 이번 변경으로 모두 해결했다고 표시하지 않는다.

## 2. 두 아지트의 역할

| 기능 | 이웃 아지트 | 우리반 아지트 |
|---|---|---|
| 목적 | 초대로 연결된 여러 학급이 글을 읽고 교류 | 우리 학급의 글을 선정해 전시하고 문집 발행 |
| 대상 | 참여 학급의 로그인한 교사·학생 | 담당 교사·학생, 별도 공개본을 보는 외부 방문자 |
| 주요 활동 | 글 나눔 공간 / 함께 쓰는 주제 / 글짝 교환 활동 | 2.5D 전시관 / 학급 문집 / 읽기 전용 외부 전시 |
| 원본 | 학생의 원래 `student_posts`에 보존 | 같은 원본을 참조하고 전시·발행 시 승인본 고정 |
| 교류 | 댓글 하나·공감 하나·간직하기 | 1차 전시·문집은 읽기에 집중 |
| 다른 반 글 | 참조 간직하기만 | 다른 반 글의 복제·수록은 1차 범위에서 제외 |

2.5D 전시관을 이웃 피드에 함께 넣지는 않는다. 이웃의 `글 나눔 공간`은 교류용 피드로 유지한다.

## 3. 현재 사용 과정과 목표 흐름

### 공간 연결

호스트 교사가 공간 생성 → 일회용 초대키 전달 → 게스트 교사가 자기 학급으로 신청 →
호스트 승인 → 각 담임이 자기 학급 학생 공개 ON → 학생 홈 카드로 입장.

- 공간당 최대 4학급, 학급당 활성 공간 1개.
- 초대 기본 24시간·일회성·해시 저장·실패 시도 제한을 유지한다.
- 학생 접근은 공개 단계·제한 학급·학급 모듈·학생 공개 스위치·활성 참여·활성 공간·2학급 이상을 모두 확인한다.
- `internal / limited_beta / public_beta / paused`를 유지하고 모든 학급의 기본값은 OFF다.
- 전체 교사 공개는 사용자가 따로 정하는 단계다. 기능 수정이나 계획 검토로 자동 전환하지 않는다.

### 글 나눔 공간

현재: 학생이 제출 글을 골라 신청 → 담임 승인 → 모든 참여 학급이 읽기.
또는 교사가 `우리 학급 글 불러오기` → 글 선택 → 즉시 공유.

보완 후: **공유 자격 확인 → 전문·표시 이름·공개 범위 미리보기 → 공개**를 두 경로 모두에 적용한다.
돌려보낼 때 이유를 직접 쓰고 학생은 그 이유와 재신청 동선을 볼 수 있게 한다.

### 함께 쓰는 주제

참여 교사 제안 → 제안 시점 참여 학급의 나머지 교사 전원 승인 → 각 학급의 기존 미션 열림 →
학생이 기존 편집기에서 작성·제출 → 이웃 활동으로 복귀 → 나눔 요청 → 담임 검토 → 활동 피드.

제출 완료와 이웃 공개 요청은 서로 다른 단계다. 복귀 시 원래 활동을 다시 열고
`제출했어요 · 선생님께 공개 확인 요청`으로 다음 행동을 드러내는 것을 목표로 한다.

### 글짝 교환 활동

현재: 두 학급·주제·공유 범위 제안 → 상대 교사 활동 승인 → 호스트 매칭안 →
상대 교사 매칭 승인 → 미션 열림 → 학생 제출·공개 요청 → 담임 검토 → 글·댓글 교환.

- 로그인 가능한 학생을 학급당 최대 100명, 학생당 상대 1~2명으로 연결한다.
- 인원이 2배를 넘게 차이 나면 매칭 불가다. **제안 전에** 인원·참여 대상을 확인할 수 있게 보완한다.
- 1차 보완 권장안은 **호스트가 포함된 두 학급만** 선택하게 만드는 것이다. 게스트끼리의 교환은 활동 진행자를
  별도로 두는 후속 확장으로 남긴다. 현재 UI와 서버 조건이 서로 다른 상태를 먼저 없앤다.
- `글짝끼리만`과 `두 학급 전체`는 현재 생성 시 정하는 공개 범위다. 현재 코드에는 “교환 완료 후 전체 공개”로
  바꾸는 별도 단계가 없다. 첫 보완에서는 문구를 실제 동작에 맞추고, 단계 전환이 필요하면 양 교사의 승인과
  변경 이력을 갖춘 별도 작업으로 구현한다.

## 4. 우선 보완할 사항과 코드 근거

P1은 제한 공개 운영의 신뢰성에 영향을 주어 먼저 처리할 항목, P2는 그다음 사용 흐름 보완이다.
“코드 확인”은 현재 제어 흐름에서 확인한 사실이며 실제 개인정보 유출을 확인했다는 뜻은 아니다.

| ID | 우선 | 확인한 문제와 영향 | 수정 방향·검증 |
|---|---|---|---|
| N-01 | P1 | 비공개 일기도 공유 후보가 될 수 있다. 후보·직접 공유는 제출 여부만 검사하며 `visibility`를 확인하지 않는다. 일기는 `private`여도 저장 시 제출 완료다 | 교사 열람 권한과 이웃 공개 자격을 분리. 비공개 일기 직접 공유 차단, 학생의 명시적 공유 선택 기준 정의. 후보·공개·상세·원문 회수에서 같은 정책 검증 |
| N-02 | P1 | 검토 대기 글은 180자 발췌와 공개/돌려보내기 버튼만 보인다. 기존 교사 상세 RPC도 `published/hidden`만 받아 승인 전 전문을 열 수 없다. 직접 공유 후보도 전문 진입점 없음 | 자기 학급 후보·대기 글의 전문 미리보기, 공개 범위 확인, 반려 사유 입력. 다른 학급 미승인 글은 계속 차단 |
| N-03 | P1 | 게스트 B가 게스트 C와 활동을 만들 수 있지만 매칭 제안은 호스트 A 포함을 요구한다. 3~4학급에서 생성 후 진행 불가 | 제안 UI와 생성·매칭 RPC의 참여 조건 통일. 호스트+게스트, 게스트+게스트, 호스트 이전 각각 검사 |
| N-04 | P1 | 검토용 100개를 `pending/published/returned/hidden` 전체에서 최신순으로 자른 뒤 화면에서 pending만 고른다. 최근 처리 글에 밀린 대기 글이 사라지고 `검토 0편`으로 보일 수 있음 | DB에서 대기를 먼저 좁혀 커서 조회. 상태별 실제 총수와 현재 페이지 길이를 분리. 100편 이후 오래된 대기 글 회귀 검사 |
| N-05 | P1 | 기본 `smoke:neighbor-agit`가 운영 적용 후 재실행되지 않는다. `20261239`가 옛 6인자 함수만 DROP하고 새 7인자 함수를 `CREATE FUNCTION`으로 다시 만듦 | 적용 원장을 보고 미적용 마이그레이션만 롤백 적용하는 검사 경로와 현재 스키마 스모크를 분리. 이미 적용된 SQL은 수정하지 않음. 새 DB 최초 적용·최신 DB 재검사 둘 다 검증 |
| N-06 | P2 | 교사 후보는 최근 100편 안에서만 이름·제목을 검색하고 학생 후보는 최근 50편으로 끝난다. 공개/숨김 관리도 50편 이후 이동 없음 | 서버 검색·학생/과제/상태 필터·커서. 후보 전문은 선택 시에만 조회 |
| N-07 | P2 | 학생은 간직하기 토글을 할 수 있지만 저장한 글만 모아보는 목록이 없다 | `간직한 글` 보기 추가. 회수·퇴장·숨김 뒤 본문은 차단하고 비공개 흔적 정책 적용 |
| N-08 | P2 | 교사 학급 전환, 학생 상세 닫기·다른 글 열기·활동 전환에 늦게 온 응답을 버리는 장치가 없다. 닫은 상세가 다시 열리거나 다른 활동 응답이 현재 목록을 덮을 수 있음 | 요청 식별값/취소 및 클래스·활동 키 확인. 쓰기 완료 응답도 요청 당시 글과 현재 상세가 같을 때만 반영. 지연·응답 역전 검사 |
| N-09 | P2 | 활동 제출 후 `neighbor_agit`로만 돌아가 선택 활동 정보가 소실된다. 공동 활동에는 공개 회수 동선이 없고 숨긴 글도 다시 요청 버튼이 노출됨 | 원래 활동·목록 위치 복귀, 제출/대기/반려/숨김/회수/종료별 다음 행동 통일. 닫힌 활동의 불가능한 요청은 표시하지 않음 |
| N-10 | P2 | 공간 종료·나가기·활동 종료가 `window.confirm` 3곳에 남아 있음 | 공용 `useConfirmDialog`·`useNotice`로 변경. 취소·실패·잠금 해제 순서 확인 |
| N-11 | P2 | 호스트 이전 RPC·운영 이력 표가 있지만 교사 화면에는 연결이 없다. 초대 취소·강제 퇴장도 화면 없음 | 호스트 이전, 초대 취소, 나가기/종료와 활동 처리, 간단한 운영 기록을 권한별로 연결. 없는 RPC는 신규 여부를 따로 확인 |
| N-12 | P2 | 댓글 상세는 활성 참여 학급만 보여 주지만 `comment_count/reaction_count` 일부는 원장 전체를 센다 | 탈퇴·퇴장·숨김·비활성화 후 목록/상세/쓰기 응답의 집계 기준 통일. 보이지 않는 활동이 숫자에 남지 않는지 검사 |

핵심 근거:

- N-01: [직접 공유 후보·공개](supabase/migrations/20261239_neighbor_teacher_sharing_exchange_matching.sql#L319),
  [학생 공유](supabase/migrations/20261237_neighbor_activity_spaces.sql#L625),
  [일기 저장](supabase/migrations/20260907_diary_writing_type.sql#L179).
  [원문 동기화 트리거](supabase/migrations/20261199_neighbor_agit_data_foundation.sql#L2739)에도 `visibility` 변경이 빠져 있다.
- N-02: [교사 검토 화면](src/modules/community/neighbor-agit/TeacherEntry.jsx#L583),
  [교사 상세 RPC](supabase/migrations/20261237_neighbor_activity_spaces.sql#L1158).
- N-03/N-05: [활동 생성](supabase/migrations/20261239_neighbor_teacher_sharing_exchange_matching.sql#L126),
  [매칭 구성 조건](supabase/migrations/20261239_neighbor_teacher_sharing_exchange_matching.sql#L578).
- N-04/N-06: [작업 공간 조회](supabase/migrations/20261201_neighbor_limited_beta.sql#L500),
  [교사 화면 검색](src/modules/community/neighbor-agit/TeacherEntry.jsx#L300).
- N-07~N-10: [학생 화면](src/modules/community/neighbor-agit/StudentEntry.jsx),
  [교사 화면](src/modules/community/neighbor-agit/TeacherEntry.jsx), [제출 복귀](src/hooks/useMissionSubmit.js#L326).
- N-11/N-12: [호스트 이전](supabase/migrations/20261199_neighbor_agit_data_foundation.sql#L1228),
  [상세 댓글·합계](supabase/migrations/20261237_neighbor_activity_spaces.sql#L1034).

## 5. 구현 때 함께 검증할 경계

다음은 추가 설계·재현 대상이다. 이번 점검에서 모두 실패한다고 확정한 항목은 아니다.

- 일반 과제 화면에서 이웃 연결 미션의 보관 해제·수정·삭제를 했을 때 상호 승인·주제·종료 상태가 어긋나지 않는가.
- 두 교사가 동시에 승인/종료하거나 같은 요청을 재시도할 때 중복 생성·낡은 매칭 승인이 생기지 않는가.
- 글 전문을 읽은 뒤 학생이 고친 경우, 교사가 실제 읽은 버전만 승인되는가. `expected_source_revision` 등 버전 대조가 필요하다.
- 매칭 승인 후 전입·전출·로그인 해제 시 기존 교환 글은 어떻게 보존하고 새 학생은 다음 회차에 어떻게 참여하는가.
- 4학급 공간 안 2학급 글짝 활동을 나머지 학급의 교사·학생이 어디까지 볼 수 있는가. 학생은 참여 두 학급으로
  제한하고, 다른 담임의 전문 열람/긴급 숨김 범위는 명시적으로 정한다.
- 공개를 회수한 원문, 퇴장한 작성 학급, 숨긴 댓글의 조건이 읽기뿐 아니라 댓글·반응·간직하기 쓰기에도 같은가.
- 장르별 `structured_content`를 그대로 응답하는 대신 허용 필드만 반환하는가. 사진 제외 단계에서 Storage 경로나
  다른 내부 참조가 섞이지 않게 하고, 실제 표현은 장르가 소유하는 읽기 계약을 사용한다.

## 6. 수정 순서와 단계별 합격 기준

한 단계씩 로컬에서 구현·점검한다. 사용자가 배포/마무리/확정을 말하면 그때 누적 변경을 한 번 마감한다.

| 단계 | 작업 | 다음 단계로 넘어갈 조건 |
|---|---|---|
| N0 검사 복구 | N-05. 현재 스키마와 미적용 변경 검증 분리, 합성 사례 확충 | 적용 완료 DB에서 전용 검사가 반복 통과하고 시험 데이터·공개 단계 변경이 남지 않음 |
| N1 공개·검토 보완 | N-01/N-02/N-04. 공개 자격, 전문 검토, 반려 사유, 대기 목록 | 비공개 일기 차단, 전문 확인 후 승인, 원문 변경 시 재확인, 오래된 대기 글 도달 가능 |
| N2 활동 운영 보완 | N-03과 5절 활동 경계. 참여 조건·매칭 사전 확인·미션 승인 일치 | 두 교사 제안→상호 승인→매칭→학생 제출→검토→댓글을 실계정으로 완료 |
| N3 학생·교사 동선 | N-06~N-10/N-12. 검색·간직한 글·회수·복귀·응답 역전·공용 확인창·집계 | 목록→상세→복귀, 다른 활동 전환, 느린 응답, 숨김/회수 후 표시까지 일관됨 |
| N4 운영 인수 | N-11, 도움말·운영 기록, 2~4학급·상이한 인원·모바일 | 호스트 이전/퇴장/종료/긴급 중지, 다른 학급 권한 실패, PC·태블릿·모바일 인수 완료 |

- N1 후보·공개 자격 정책은 우리반 전시·문집에서도 재사용할 최소 계약으로 정리한다.
  학급 열람·이웃 공개·외부 공개의 권한을 하나로 합치지는 않는다.
- 정적 문자열 검사를 추가하는 데서 끝내지 않고 실제 역할 SQL, UI 상호작용, 늦은 응답 사례를 함께 검증한다.
- DB 변경 시 `migrate:check`·`test:security`·`check:rpc-surface`, 학생 흐름 변경 시 `test:architecture`를 적용한다.
  새 검사는 해당 조건을 일부러 깨뜨렸을 때 실패하는지 확인한다.
- 새 마이그레이션 번호는 작업 시작 때 실제 파일과 적용 원장을 확인해 정한다. 적용된 파일 수정·함수 임시 수동 생성 금지.

## 7. 유지할 기술 계약과 변경 지점

- 모듈: `src/modules/community/neighbor-agit/`. 활동 이름은 `activityTypes.js`, 화면 정책은 `policy.js`.
- 원본은 `student_posts`, 공개 연결은 `neighbor_shared_posts`. 기존 학급 SELECT RLS를 넓히지 않는다.
- 공개·권한은 DB가 실제 담당 교사/학생·학급 연결로 재검사한다. 브라우저에 전용 표 직접 권한을 주지 않는다.
- 학생 홈은 기존 bootstrap의 작은 요약만 사용하고 상세 RPC·폴링·Realtime을 추가하지 않는다.
- 목록 최초 20편·최대 50편, 전문은 선택 시 1회, 행동은 전용 RPC 1회와 응답 상태로 마친다.
- 교사는 `teacherApi.js`의 workspace/action/candidate/roster를, 학생은 `api.js`를 거친다.
- 최신 매칭 경로는 `propose_neighbor_exchange_matches_v1` → `review_neighbor_exchange_matches_v1`이다.
  옛 `match_neighbor_exchange_v1`은 `20261239`에서 삭제되었으므로 다시 연결하지 않는다.
- 함수 정리 전 저장소·외부 앱·다른 DB 함수·RLS 호출처 네 곳을 확인한다. `*_core_날짜` 내부 래퍼도 실제 호출을 확인한다.
- UI는 teacher 작업공간/활동/검토/공개글, student 피드/활동/상세로 나누되 상태·API를 복제하지 않는다.
- 도움말은 `teacherGuides.js`와 `teacherGuideJourneys.js`의 `guideRef`, 공용 `TeacherGuideButton`을 함께 맞춘다.

## 8. 이번 계획 검토에서 선택할 내용

아래는 권장안이며 아직 사용자 확정으로 간주하지 않는다.

1. **글짝 대상:** 우선 호스트 포함 두 학급으로 맞춘 뒤, 필요할 때 게스트끼리 활동 진행자를 추가.
2. **글짝 공개 시점:** 1차는 생성 시 `글짝 전용/두 학급 전체` 선택. “교환 후 확대 공개”가 필요하면 양 교사 승인 단계 추가.
3. **비공개 일기:** 교사 직접 공유에서 제외. 학생이 별도 공유 의사를 선택하는 흐름은 공개 범위 정책을 정한 뒤 추가.

채팅·대댓글·순위·포인트·사진·외부 비로그인 이웃 피드·다른 학급 문집 수록·다중 공간 동시 참여는 후속 범위다.
우선 N0~N2를 끝내고, [우리반 아지트 계획](CLASS_AGIT_PLAN.md)의 C0~C1로 이어가는 순서를 권장한다.

2026-09-05 추가 점검: 미적용 `61240`의 원글 버전 충돌을 `PT409`로 반환한다. 현재 PostgREST의 `40001` 자동
재시도로 응답이 끝나지 않는 문제를 우리반 아지트 HTTP 검사에서 확인해 동일 경로도 함께 보완했다.
