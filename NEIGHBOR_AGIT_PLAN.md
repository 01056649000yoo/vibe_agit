# 이웃 아지트 구현 계획

> **상태**: Step 0~6 구현 완료, Step 7 운영 `internal` 배포와 관리자 테스트 교사·학급 2세트 준비 완료·실화면 인수 대기 (2026-08-30)
>
> **이번 주 목표**: 2026-08-31~09-06에 여러 학급이 초대로 참여하는 글 교류 MVP를 구현하고,
> 운영 서버에는 **관리자 내부 단계**로만 배포한다. 사용자가 관리자 화면에서 확인하고 명시적으로 승인하기
> 전에는 실제 기능을 모든 교사에게 공개하지 않는다.
>
> **한 문장 정의**: 한 교사가 만든 공간에 최대 네 학급이 초대로 참여하고, 각 학급 학생이 자기 글을
> 그대로 소유하면서 교사가 승인한 글만 함께 읽고 한 줄 댓글·공감·간직하기를 하는 안전한 글 교류 공간이다.

이 문서는 `이웃 아지트`의 제품 범위·권한·데이터·RPC·공개 단계·구현 순서의 정본이다. 큰 진행 상황은
[`ROADMAP.md`](ROADMAP.md), 실제 작업 이력은 [`WORKLOG.md`](WORKLOG.md)에 남긴다.

## 1. 이번 주 MVP 범위

### 포함

1. 승인된 교사는 누구나 공간을 만들 수 있고, 만든 교사는 해당 공간의 호스트가 된다.
2. 호스트가 만료·일회성 초대 키를 만들고, 게스트 교사는 로그인 후 자기 학급을 선택해 참여를 신청한다.
3. 호스트 승인 후 학급이 참여하며, 한 공간에는 호스트 학급을 포함해 최대 네 학급이 들어온다.
4. 각 학급 교사는 자기 학급의 학생 공개 스위치와 자기 학생 글의 승인·회수·숨김을 관리한다.
5. 학생은 하나의 공간 피드에서 승인된 글을 읽고, 이웃 글을 쓰고, 한 줄 댓글·공감 1종·간직하기를 사용한다.
6. 이웃 아지트에서 쓴 글의 원본은 처음부터 해당 학생의 `student_posts`와 원래 학급에 남는다.
7. 관리자 화면에서 공간·참여 학급·공개 글·댓글·반응 상태와 교사/학생 화면 미리보기를 확인한다.

### 이번 주 제외

- 외부 비로그인 공개, 학급·학생 순위, 포인트·칭호, 채팅·대댓글, 여러 반응 종류
- 사진·첨부파일, 공동 문서 편집, 알림 메일, Realtime·폴링
- 학생이 상대 학급을 골라 보는 분리 피드, 공간 간 통합 검색
- 다른 학급 글의 복제·다운로드·문집 수록. 다른 학급 글은 간직하기 링크만 허용한다.

초기에는 **학급당 활성 공간 1개**로 제한한다. 여러 공간 동시 참여는 실제 사용에서 필요성이 확인된 뒤
확장한다. 공간의 활성 학급이 두 곳 미만이 되면 학생 공개는 자동 중지한다.

## 2. 역할과 권한

| 역할 | 할 수 있는 일 | 할 수 없는 일 |
|---|---|---|
| 관리자 | 내부 미리보기, 전체 현황, 공개 단계 변경, 긴급 전체 중지 | 학생 글 원본을 임의 수정 |
| 호스트 교사 | 공간 설정, 초대 발급, 참여 승인·퇴장, 호스트 이전, 공간 종료, 전체 긴급 숨김 | 게스트 학급 학생 관리·원문 수정 |
| 게스트 교사 | 자기 학급 학생 공개 ON/OFF, 자기 학생 글 승인·회수, 퇴장, 긴급 숨김 | 다른 학급 학생·원문 관리 |
| 학생 | 승인된 피드 읽기, 자기 글 작성, 댓글 1개, 반응 1개, 간직하기 | 다른 학생 글 수정·삭제, 공간·학급 관리 |

- 모든 참여 교사는 문제 글·댓글을 즉시 숨길 수 있다.
- 숨긴 자료의 복구와 최종 처리는 원래 학급 교사가 담당하고, 공간 호스트는 공간 노출만 중단한다.
- 호스트가 나갈 때는 다른 참여 교사에게 호스트를 이전하거나 공간을 종료해야 한다.
- 학생 화면의 학급명·학생명은 교사가 정한 공개용 학급 이름과 학생 필명만 사용한다.

## 3. 공개 단계

운영 서버에 코드를 배포하는 것과 사용자에게 기능을 공개하는 것을 분리한다.

| 단계 | 일반 교사 | 학생 | 관리자 |
|---|---|---|---|
| `internal` | 현재의 정적 `준비 중` 화면만 표시 | 메뉴·직접 URL·RPC 모두 차단 | 실제 관리 화면과 교사/학생 미리보기 사용 |
| `public_beta` | 모든 승인 교사에게 실제 화면 표시 | 자기 학급 교사가 ON한 경우만 표시 | 현황·긴급 중지·공개 관리 |
| `paused` | 점검 중 안내만 표시 | 즉시 차단 | 자료 보존 상태로 원인 확인·복구 |

- 초기값과 이번 주 내부 배포값은 반드시 `internal`이다.
- 날짜·배포 완료·테스트 통과만으로 `public_beta`로 자동 전환하지 않는다.
- 관리자 `운영 → 기능 공개 → 이웃 아지트`에서 합격 항목을 모두 확인한 뒤, 사용자의 명시적 승인과
  2단계 확인을 거쳐야 `public_beta`로 바뀐다.
- 전체 교사 공개 뒤에도 `classes.enabled_modules`의 `neighbor-agit`은 기존·신규 학급 모두 기본 OFF다.
- 화면 숨김만 믿지 않고 모든 교사·학생 RPC가 현재 공개 단계와 학급 스위치를 다시 확인한다.

최종 학생 접근 조건은 다음과 같다.

```text
공개 단계 = public_beta
AND 자기 학급의 neighbor-agit ON
AND 공간 = active
AND 자기 학급 참여 = active
AND 공간의 활성 학급 수 >= 2
```

관리자 내부 미리보기는 별도의 관리자 권한 분기로만 통과하고, 일반 교사·학생에게 이 예외를 재사용하지 않는다.

## 4. 공간 흐름

```text
승인 교사 공간 생성 → 생성 교사 = 호스트
        ↓
만료·일회성 초대 키 발급
        ↓
게스트 교사 로그인 → 자기 학급 선택 → 참여 신청
        ↓
호스트 최종 승인
        ↓
각 학급 교사가 자기 학급 학생 공개 ON
        ↓
교사가 확인한 글만 공간 피드 공개
        ↓
퇴장·종료 시 접근 차단, 원본 글은 원래 학급에 보존
```

초대 키는 혼동 문자를 제외하고 충분한 무작위성을 가지며, DB에는 원문이 아닌 해시만 저장한다. 기본 만료는
24시간, 한 번 사용하거나 호스트가 취소하면 즉시 무효화한다. 실패 시도는 서버에서 속도 제한한다.

## 5. 글·댓글·반응·간직하기 원칙

### 글 원본

- 글은 기존 `student_posts`에 학생과 원래 `class_id` 소유로 저장한다.
- `neighbor_shared_posts`는 어느 공간에 글을 공개했는지와 승인·숨김 상태만 연결한다.
- 다른 학급에 복사본을 만들지 않으므로 공간 종료 뒤에도 글은 내 아지트와 원래 학급에 남는다.
- 호스트·게스트 교사는 다른 학급 원문을 수정하지 못하고 공간에서 숨기기만 할 수 있다.

### 댓글과 반응

- 학생 한 명은 글 한 편에 한 줄 댓글 하나를 작성하고 자기 댓글만 수정·삭제할 수 있다.
- 학생 한 명은 글 한 편에 공감 1종 하나만 선택하며 다시 누르면 취소한다.
- 승인되어 현재 보이는 댓글·반응만 합계에 포함한다.
- 학생에게는 전체 합계만, 교사에게는 전체와 참여 학급별 합계를 제공한다. 학생·학급 순위는 만들지 않는다.
- 기존 학급 댓글·반응 RLS와 횟수에는 합치지 않고 이웃 아지트 전용 표와 RPC를 사용한다.

### 간직하기

- 자기 글은 이미 원래 학급에 있으므로 별도 복사하지 않는다.
- 다른 학급 글은 이웃 아지트 안의 참조만 간직한다. 원작자나 원래 교사가 공개를 회수하면 본문과 상대
  활동은 숨기되, 내가 간직했던 활동 기록은 제목 없는 비공개 흔적으로 보존할 수 있다.
- 다른 학급 글을 우리반 전시·문집으로 가져오는 기능은 저작자·원래 교사 동의가 설계될 때까지 제외한다.

## 6. 데이터 구조

같은 통합 DB의 전용 표를 사용하고 별도 물리 DB를 만들지 않는다.

| 표 | 책임 | 핵심 제약·인덱스 |
|---|---|---|
| `neighbor_rollout_state` | 전체 공개 단계 단일 행 | `internal/public_beta/paused`, 브라우저 직접 권한 없음 |
| `neighbor_spaces` | 공간, 호스트, 상태, 공개용 이름 | 호스트 학급 직접 범위, `status` |
| `neighbor_space_classes` | 참여 학급, 역할, 승인, 학생 ON/OFF | `UNIQUE(space_id, class_id)`, 활성 학급 인덱스 |
| `neighbor_invites` | 초대 해시, 만료, 사용·취소 상태 | 원문 미보관, 만료·일회성 |
| `neighbor_shared_posts` | 원본 글과 공간의 공개 연결 | `UNIQUE(space_id, post_id)`, 공간·상태·공개시각 커서 |
| `neighbor_comments` | 한 줄 댓글 | `UNIQUE(shared_post_id, student_id)`, 길이 상한 |
| `neighbor_reactions` | 공감 1종 | `UNIQUE(shared_post_id, student_id)` |
| `neighbor_saves` | 간직하기 참조 | `UNIQUE(shared_post_id, student_id)` |
| `neighbor_feed_visits` | 학생별 마지막 피드 방문 | `UNIQUE(space_id, student_id)`, 학급·학생 복합 범위 |
| `neighbor_space_events` | 초대·승인·숨김·종료 운영 이력 | 짧은 유형·대상·시각만, 글 본문 금지 |
| `neighbor_invite_attempts` | 계정별 초대 실패 제한 | 10분 5회 뒤 30초 차단, 키 원문 금지 |
| `neighbor_rollout_events` | 관리자 공개 단계 변경 감사 이력 | 전·후 단계·점검표·변경자·시각, 브라우저 직접 권한 없음 |
| `neighbor_internal_test_classes` | 관리자 내부 시험에 쓸 합성 학급 등록부 | 브라우저·`service_role` 직접 권한 없음, 운영 학급 후보 제외 |

모든 학급 관련 표는 `class_id`를 직접 저장하고, 학급 표끼리 조인할 때 `class_id`도 함께 비교한다. 브라우저
역할에는 전용 표 직접 권한을 주지 않고 RLS를 켠 뒤 기능 전용 RPC만 실행하도록 한다.

## 7. 전용 RPC 계약

### 관리자

- `get_neighbor_admin_dashboard_v1` — 탭을 열 때 한 번만 공개 단계·요약·최근 공간 20개·등록된 관리자 테스트
  학급 최대 100개·선택 공간의 안전한 글 미리보기 최대 20편 반환
- `create_neighbor_internal_trial_v1` — 실제 DB의 `ADMIN`만 `internal`에서 등록된 테스트 학급 2~4개를 학생 공개 OFF로 연결
- `set_neighbor_acceptance_check_v1` — 권한·PC·태블릿·모바일·성능·운영 준비 여섯 항목만 저장
- `change_neighbor_rollout_v1` — 실제 DB의 `ADMIN`만 단계를 전환하며, `public_beta`는 여섯 항목과 정확한
  확인 문구를 서버에서도 요구하고 `neighbor_rollout_events`에 변경 이력을 남김

### 교사

- `create_neighbor_space_v1`, `create_neighbor_invite_v1`
- `request_neighbor_join_v1`, `review_neighbor_join_v1`
- `set_neighbor_class_access_v1`, `leave_neighbor_space_v1`, `close_neighbor_space_v1`
- `review_neighbor_shared_post_v1`, `moderate_neighbor_item_v1`

교사 RPC는 실제 `classes.teacher_id = auth.uid()`를 확인하고, 관리 대상이 자기 학급인지 또는 자신이 호스트인
공간 관리인지 구분한다. 클라이언트가 보낸 교사 ID는 신뢰하지 않는다.

### 학생

- `request_neighbor_post_share_v1`, `recall_my_neighbor_shared_post_v1` — 기존 제출 글의 공개 신청·본인 회수
- `get_neighbor_space_feed_v1(p_space_id, p_limit, p_cursor_at, p_cursor_id)` — 첫 20편, 절대 상한 50
- `get_neighbor_shared_post_v1(p_space_id, p_shared_post_id)` — 선택한 글 전문과 보이는 댓글
- `save_neighbor_comment_v1`, `toggle_neighbor_reaction_v1`, `toggle_neighbor_save_v1`

학생 피드 RPC는 다음 세 단계로 유지한다.

1. `auth.uid()`로 활성 학생과 실제 학급을 찾는다.
2. 그 학급이 공간의 활성 참여 학급이고 학생 공개가 허용됐는지 확인한다.
3. 참여 학급의 `published` 공유 글만 안정 커서로 반환한다.

`student_posts`의 기존 학급 범위 RLS는 넓히지 않는다. 전용 조회 RPC가 공간·참여 학급·공개 연결·원본 글의
제출 상태를 함께 확인하고 제목·본문·필명 등 허용 필드만 반환한다. 함수는 `search_path`를 고정하고
`PUBLIC/anon` 실행 권한을 회수하며 `authenticated`에 필요한 함수만 허용한다.

## 8. 화면 구성

### 관리자 `운영 → 기능 공개 → 이웃 아지트`

- 현재 공개 단계와 `전체 교사 공개 안 됨` 상태를 가장 위에 표시
- 공간·참여 학급·공개 글·보이는 댓글·숨김 건수 요약
- 내부 시험 공간 만들기와 시험 학급 2~4곳 선택
- 같은 화면 컴포넌트를 이용한 `교사 화면 미리보기`와 읽기 전용 `학생 피드 미리보기`
- 권한·모바일·성능·운영 스모크 합격 체크
- 합격 전에는 비활성인 `전체 교사 Beta 공개` 버튼
- 공개 시 영향 범위와 학생 기본 OFF를 다시 보여 주는 2단계 확인
- 즉시 사용할 수 있는 `긴급 중지` 버튼

### 교사

- `internal`에서는 현재 준비 화면을 그대로 유지한다.
- `public_beta`에서는 `공간 / 초대·참여 / 글 검토 / 운영 기록`만 제공한다.
- 공간 생성·호스트 여부, 참여 학급 수, 학생 공개 스위치, 숨김·퇴장 상태를 한눈에 보여 준다.
- 담당 교사가 아닌 학급의 학생 명단과 내부 ID는 보여 주지 않는다.

### 학생

- 학생 홈에는 최종 접근 조건이 참일 때만 `이웃 아지트` 카드 한 칸을 추가한다.
- 홈에서 별도 조회하지 않고 기존 bootstrap에 `공간 있음/새 글 수`의 작은 요약만 추가한다.
- 카드를 열 때 공간 피드를 RPC 한 번으로 읽고, 글 전문은 누를 때 지연 조회한다.
- 하단 메뉴 증설은 이번 주 범위에서 제외하고, 실제 사용 빈도를 본 뒤 결정한다.

## 9. 이번 주 구현 스텝

날짜는 작업 순서를 보여 주는 기준이며, 앞 단계의 보안·회귀 검사가 실패하면 다음 단계로 넘어가지 않는다.

### Step 0 — 8월 30일: 계약과 실패 검사 고정

- [x] 다중 학급 공간·호스트/게스트·원본 보존·댓글/반응 기준 확정
- [x] `internal → 사용자 승인 → public_beta`, 학생 기본 OFF 계약 확정
- [x] 공개 단계·최대 네 학급·학급당 활성 공간 1개의 단일 원본 정의 (2026-08-30)
- [x] 일반 교사에게 준비 화면만 보이고 학생 진입점·운영 데이터 요청이 생기지 않는 실패 회귀 검사 작성
      (2026-08-30, 실제 RPC 권한 실패 검사는 Step 1 마이그레이션과 함께 추가)

### Step 1 — 8월 31일: 데이터·권한 기반

- [x] 공개 단계 원장을 포함한 전용 표 9개·제약·인덱스 설정 마이그레이션 작성 (2026-08-30)
- [x] 직접 표 권한 회수와 관리자/호스트/게스트/학생 모두의 브라우저 직접 접근 차단, 최대 4학급·학급당
      활성 공간 1개·활성 공간 호스트 1곳 권한 스모크 작성 (2026-08-30)
- [x] `npm run migrate:check`로 실제 운영 스키마 전체 롤백 검증. 운영 적용은 내부 배포 단계까지 대기
      (2026-08-30, 파일 241/적용 240/대기 1)

### Step 2 — 9월 1일: 공간 생성과 여러 학급 초대

- [x] 승인 교사 공간 생성, 호스트 권한, 최대 네 학급·학급당 활성 공간 1개 구현 (2026-08-30)
- [x] 혼동 문자 제외 키·SHA-256 해시 보관·24시간 만료·일회성 사용과 계정별 10분 5회/30초 실패 제한 구현
      (2026-08-30)
- [x] 게스트 학급 선택→참여 신청→호스트 승인, 퇴장·종료·호스트 이전 구현 및 실제 관리자·교사 역할
      전체 흐름 롤백 검증 (2026-08-30)

### Step 3 — 9월 2일: 글 원본과 교사 검토

- [x] 기존 편집기·임시 저장을 복제하지 않고 재사용하는 모듈 `writingBridge` 계약 추가 (2026-08-30)
- [x] 원본은 `student_posts`, 공개 상태는 `neighbor_shared_posts`로 분리 (2026-08-30)
- [x] 자기 학급 교사의 승인·돌려보내기·회수와 모든 참여 교사의 긴급 숨김 구현 (2026-08-30)
- [x] 기존 `student_posts`·학급 댓글·반응 RLS 불변 정적 검사와 실제 교사·학생 역할 롤백 스모크 통과
      (2026-08-30)

### Step 4 — 9월 3일: 학생 단일 피드

- [x] 학생 bootstrap 최소 요약과 조건부 홈 카드 구현 (2026-08-30)
- [x] 피드 20편·커서 상한 50, 상세 지연 조회와 공간별 공개용 필명 구현 (2026-08-30)
- [x] 다른 공간·퇴장 학급·교사 OFF·숨김 글·ID 추측 접근 실패 역할 스모크 (2026-08-30)
- [x] 폴링·Realtime·학생 홈 추가 직접 조회 없음 확인 (2026-08-30)

### Step 5 — 9월 4일: 댓글·공감·간직하기

- [x] 한 학생·한 글·한 줄 댓글과 자기 댓글 수정·삭제 (2026-08-30)
- [x] 공감 1종 토글, 간직하기 참조, 보이는 건만 합계 반영 (2026-08-30)
- [x] 원글 회수·공간 종료 시 본문 차단과 활동 기록 보존 확인 (2026-08-30)
- [x] 학급별 교사 집계와 순위·포인트 미연결 확인 (2026-08-30)

### Step 6 — 9월 5일: 관리자 내부 화면과 종합 검증

- [x] 관리자 `운영 → 기능 공개` 화면, 학생 공개 OFF인 2~4학급 내부 시험 공간, 교사/학생 읽기 전용
      미리보기와 긴급 중지 구현 (2026-08-30)
- [x] 모의 운영 자료로 PC 1440px·태블릿 900px·좁은 모바일 390px 화면의 한 열 전환·가로 넘침 없음·
      공간 선택 뒤 두 미리보기 갱신·콘솔 오류 0건 확인 (2026-08-30)
- [x] 관련 단위 검사, `test:architecture`, `test:security`, `migrate:check`, lint, build 통과 (2026-08-30)
- [x] 운영 적용 전 백업 확인, 적용 뒤 운영 DB 역할별 롤백 스모크 재실행 (2026-08-30)

### Step 7 — 9월 6일: 관리자 전용 운영 배포와 인수

- [x] 오늘자 백업 3앱 3/3·산출물/세 사본·복구 PASS와 디스크 107GB를 확인하고 운영 마이그레이션
      241/241 적용 (2026-08-30)
- [x] 운영에는 `internal`로 배포하고 실제 일반 교사 관리자 RPC 차단, 실제 학생 홈 미노출·새 글 0을
      운영 DB 역할로 확인 (2026-08-30)
- [x] 실제 관리자 RPC의 `version=1`·`mode=internal`·공간 0과 DB 원장, 익명 401·직접 표 권한 없음,
      앱/Caddy HTTP 200·최근 오류 0건을 대조 (2026-08-30)
- [x] 로그인 불가 합성 교사 2명과 학생 0명 테스트 학급 2개를 만들고 직접 접근 불가 등록부로 실제 운영 학급을
      관리자 후보·내부 공간 생성 양쪽에서 제외. 관리자 후보가 두 테스트 학급만 반환함을 운영 DB에서 확인 (2026-08-30)
- [ ] 사용자가 실제 관리자 계정으로 `운영 → 기능 공개` 화면을 열어 PC·태블릿·모바일 표시와 내부 시험
      공간 생성을 확인. 현재 연결된 자동화 브라우저가 없어 로그인 실화면 클릭은 사용자 확인으로 남김
- [x] 사용자 확인용 인수 순서를 제공하고, 승인 전 `public_beta`로 바꾸지 않고 0/6 점검 상태에서 정지
      (2026-08-30)

### 승인 이후 — 별도 작업

- [ ] 사용자의 명시적 `전체 교사 공개` 승인 기록
- [ ] `public_beta` 전환 전 최종 백업과 보안 스모크
- [ ] 모든 승인 교사에게 실제 메뉴 공개, 모든 학급 학생 스위치 기본 OFF 확인
- [ ] 24시간·7일 추이 관찰 후 오류·사용성 보완, 필요하면 즉시 `paused`

## 10. 관리자 인수 합격선

- 일반 교사는 `internal`에서 구현 완료 여부를 알 수 없고 현재 준비 화면만 본다.
- 학생은 메뉴·직접 URL·RPC를 모두 사용할 수 없다.
- 최대 네 학급 초대와 학급당 활성 공간 1개 제한이 DB에서 강제된다.
- 호스트는 게스트 학급 학생과 글 원본을 수정할 수 없다.
- 비참여 학생, 퇴장 학급, 교사 OFF 학급은 공간 ID와 글 ID를 알아도 읽지 못한다.
- 공개 글은 원래 학급·원작자 소유를 유지하며 공간 종료 뒤에도 자기 학급에 남는다.
- 댓글·반응은 이웃 전용 횟수로만 집계되고 기존 학급 횟수와 섞이지 않는다.
- 첫 피드는 RPC 한 번·20편, 안정 커서·최대 50편이며 폴링·상시 WebSocket이 없다.
- 다른 학급 학생 실명·로그인 정보·내부 관리 메모가 응답과 화면에 나오지 않는다.
- 관리자 긴급 중지 즉시 학생 RPC가 거부되고 데이터는 삭제되지 않는다.
- PC·태블릿·좁은 모바일에서 핵심 흐름과 관리자 홈 복귀가 가능하다.
- `public_beta` 전환은 자동 일정이나 배포 스크립트가 아니라 사용자 승인 뒤 명시적 관리자 동작으로만 가능하다.

## 11. 구현 중 변경 금지선

- 기존 `student_posts`의 학급 범위 SELECT RLS를 이웃 학급 전체로 넓히지 않는다.
- 기존 글쓰기 코어 셸을 복제하거나 연구소 코드를 이식하지 않는다.
- 공간 기능 때문에 학생 홈에 직접 DB 요청·폴링·Realtime을 추가하지 않는다.
- 공개 단계를 프론트 상수나 이메일·교사 ID 하드코딩으로 판정하지 않는다.
- 테스트 편의를 위해 일반 교사·학생에게 관리자 내부 RPC 실행 권한을 열지 않는다.
- 내부 배포 완료를 전체 교사 공개 승인으로 간주하지 않는다.
