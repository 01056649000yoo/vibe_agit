# 끄적끄적 아지트 실기기 수동 점검표

> 목적: 자동 테스트로 확인할 수 없는 실제 학생·교사 동선, 태블릿 입력감, 화면 배치를 한 번에 확인한다.
> 기능 개발 완료 여부와 분리된 **운영 인수 검사표**다. 확인한 항목은 `[x]`로 바꾸고 오류가 있으면 맨 아래 기록표에 남긴다.
>
> **현재 상태(2026-08-23)**: 사용자 확인에 따라 현재 구현·배포된 기능의 실기 점검 항목을 모두 완료 처리했다.
> 점검 준비·사후 정리 항목은 다음 인수 점검에서도 다시 사용하는 운영 절차이므로 체크 전 상태로 유지한다.

## 0. 점검 준비

- [ ] 가능하면 실제 수업 학급이 아닌 `테스트` 학급을 사용한다.
- [ ] 교사 계정 1개, 같은 학급의 학생 A·학생 B 계정을 준비한다.
- [ ] 테스트 글 제목 앞에 `[수동점검]`을 붙인다.
- [ ] 학생 A의 시작 포인트와 작가·소통·기록가·독서가 칭호를 적어 둔다.
- [ ] 갤럭시탭 또는 아이패드 Chrome을 준비하고 가능하면 PC Chrome도 함께 연다.
- [ ] 점검 날짜, 기기, 브라우저 버전을 아래에 기록한다.

| 점검일 | 기기·화면 방향 | 브라우저 | 교사 | 학생 A / B |
|---|---|---|---|---|
|  |  |  |  |  |

### 0-1. 비로그인 학습지원소프트웨어 안내

- [x] 로그인 화면 맨 아래 `학교 도입 안내` 링크가 보이고 공개 페이지로 이동한다.
- [x] PC와 모바일에서 제품 개요와 개인정보보호 9개 세부 기준을 읽을 수 있으며, 모바일 표는 가로 스크롤된다.
- [x] 각 증빙 링크가 공개 개인정보 처리방침으로 이동한다.
- [x] `제출 양식 내려받기`를 누르면 `학교운영위원회_안건_상정_자료.hwpx`가 내려받아지고 한글에서 정상적으로 열린다.
- [x] 안내 페이지의 `메인 화면으로 가기`가 로그인 화면으로 돌아간다.

### 0-2. 비로그인 첫 화면·기능 소개

- [x] PC와 모바일에서 대표 이미지, `쓰고, 읽고, 키우며 함께 자라는 우리 반 아지트`, 학생·교사 로그인이 먼저 보인다.
- [x] 대표 이미지에서 수호룡과 `끄적끄적 아지트`, 하단 설명 문구가 과도하게 잘리지 않고 원래 구도로 보인다.
- [x] 장황한 기능 카드 없이 `쓰고 다듬는 글 / 읽고 채우는 책장 / 활동으로 키우는 수호룡` 세 버튼이 한눈에 보인다.
- [x] 세 기능이 별도 큰 카드가 아닌 하나의 3분할 선택 바로 보이고, 제목은 같은 두 줄 높이이며 `＋` 대신 화살표가 보인다.
- [x] 일반 노트북과 모바일 세로 화면에서 하단 링크까지 보기 위해 페이지를 추가로 스크롤할 필요가 없다.
- [x] 각 버튼을 누르면 해당 설명 모달이 열리고 상단 세 탭으로 다른 설명을 전환할 수 있다.
- [x] 모달은 닫기 버튼·바깥 클릭·ESC로 닫히고 닫은 뒤 눌렀던 기능 버튼에 키보드 초점이 돌아온다.
- [x] 작은 모바일 화면에서도 모달 내용이 잘리지 않고 내부 스크롤로 끝까지 읽힌다.
- [x] 하단 `학교 도입 안내 / 개인정보 처리방침 / 이용약관` 링크가 각각 올바른 공개 페이지로 이동한다.

### 0-3. 글쓰기 연구소 교사용 공통 UI

- [x] 연구소 로그인 뒤 상단에 `끄적끄적 아지트 · 글쓰기 연구소`와 교사 이름·아지트 홈·로그아웃이 한 줄로 보인다.
- [x] `학급·활동 / 질문 카드 / 도움말`을 이동할 때 현재 메뉴만 파란 배경과 밑줄로 표시된다.
- [x] 대시보드→학급→활동 만들기→활동 운영→학생 결과가 같은 배경·콘텐츠 폭·카드·버튼 모양으로 이어진다.
- [x] 현재 네 활동은 공통 카드 틀 안에서 활동별 보조색만 달라 빠르게 구별되고, 사용 안내에 네 활동이 모두 보인다.
- [x] 태블릿 세로와 모바일에서 상단 메뉴가 잘리지 않고 가로 이동되며, 주요 버튼 높이와 키보드 초점 표시가 충분하다.

### 0-4. 연구소 학생 자동 입장·결과 불러오기

- [x] 학생 홈의 `과제 글쓰기` 옆에 `글쓰기 연구소` 카드가 보이고, 홈을 새로 열었을 때 연구소 활동 목록 추가 요청은 없다.
- [x] `글쓰기 연구소`를 열면 우리 반의 활성·미만료 활동만 보이고 시작 전·활동 중·완료가 각각 `활동 시작 / 이어서 하기 / 결과 보기`로 표시된다.
- [x] 참여할 활동이 없으면 같은 화면에 `지금 참여할 활동이 없어요`가 보이고, 선생님이 새 활동을 연 뒤 `새로 고침`하면 나타난다.
- [x] 아지트 학생 A가 로그인한 채 자기 학급의 `/lab/room/...` 링크를 열면 번호·이름 입력 없이 활동으로 바로 들어간다.
- [x] 학생 홈의 연구소 활동을 눌러도 같은 자동 입장이 동작하고, 브라우저 뒤로 가기 뒤 아지트 학생 홈으로 돌아온다.
- [x] 학생 A가 다른 학급의 연구소 링크를 열면 활동이나 학생 명단이 보이지 않고 내 학급 활동이 아니라는 안내가 나온다.
- [x] URL의 `session` 값을 학생 B 결과로 바꿔도 학생 A가 결과를 읽거나 저장·반응할 수 없다.
- [x] 개요·질문 만들기·좋은 질문 고르기·한줄모아·한자 활용 문장 완료 뒤 아지트 글쓰기의 `연구소 결과 불러오기`에 나타난다.
- [x] 결과 도구를 열기 전에는 연구소 결과 DB 조회가 없고, 도구를 열면 최근 20개만 먼저 보이며 `이전 결과 더 보기`가 동작한다.
- [x] 일반 글에서는 학생이 `넣기`를 누른 내용만 커서 위치에 들어가고 기존 본문은 덮어쓰지 않는다.
- [x] 보고서·시 같은 구조화 글에서는 본문 삽입 대신 복사만 제공해 장르별 저장 내용이 어긋나지 않는다.
- [x] 교사가 `글쓰기 창 관리`에서 연구소 결과 도구를 OFF로 저장하면 학생 글쓰기에서 진입 버튼만 사라지고 기존 결과는 유지된다.
- [x] 통합 `/lab` 교사 활동 화면에는 입장용 QR·단축주소 대신 학생 대시보드 안내가 보이고, 구 `helper.`에서는 기존 QR 입장이 유지된다.

### 0-5. 관리자 백업 상태

- [x] 관리자 대시보드의 `💾 백업 상태` 탭을 열면 일일 백업과 실제 복구 리허설 카드가 한 화면에 보인다.
- [x] 일일 카드에 내장·Drive·외장 SSD가 모두 정상이고 필수 파일이 8개로 표시된다.
- [x] 복구 카드에 아지트·연구소 DB 테이블 수와 Storage 파일 수가 표시되고 최근 상태가 정상이다.
- [x] 최근 실행 내역은 최신순이며 새로고침 버튼을 눌러도 중복 요청이나 화면 깜박임이 없다.
- [x] 일반 교사·학생 계정으로 백업 상태 RPC나 원장 테이블을 직접 읽을 수 없다.

### 0-6. 이웃 아지트 선택 학급 제한 공개

- [ ] 관리자 `운영 → 기능 공개`에서 실제 교사 학급 두 곳을 선택하고 `선택한 학급으로 제한 공개 시작`으로 전환한다.
- [ ] 선택하지 않은 교사 학급은 `이웃 아지트(제작 중)` 메뉴를 눌러도 공간 자료 없이 닫힌 화면만 보인다.
- [ ] 첫 교사가 공간과 일회용 초대키를 만들고, 둘째 교사가 자기 학급으로 신청한 뒤 첫 교사가 승인한다.
- [ ] 두 교사 화면에서 연결 학급이 같게 보이고, 각 교사가 자기 학급의 학생 공개를 따로 켤 수 있다.
- [ ] 학생 공개 전에는 학생 홈 카드가 없고, 공개 후에는 해당 학급 학생에게만 `이웃 아지트` 카드가 나타난다.
- [ ] 여섯 인수 항목을 확인한 뒤 `제한 공개 ↔ 정상 공개` 스위치를 켜면 정상 공개, 끄면 같은 선택 학급 제한 공개로 돌아간다.
- [ ] 학생이 `공개할 내 글 고르기`를 누른 뒤에만 제출 글 목록이 나타나며 공개 요청·회수가 동작한다.
- [ ] 원학급 교사가 요청 글을 공개·돌려보내기 할 수 있고, 참여 교사가 문제 글·댓글을 숨길 수 있다.
- [ ] 관리자에서 한 학급을 선택 해제하면 그 학급 학생 카드와 교사 실제 화면 접근이 즉시 닫힌다.
- [ ] PC·태블릿·모바일에서 교사 세 관리 탭과 학생 피드가 가로로 넘치지 않고 조작된다.

## 1. 빠른 핵심 점검 순서

시간이 부족하면 아래 순서만 먼저 확인한다. 전체 점검은 2번부터 이어서 진행한다.

- [x] 학생 A 로그인 → 학생 홈이 흰 화면 없이 열린다.
- [x] 질문 없는 과제를 열어 2~3분 입력해도 화면이 주기적으로 새로고침되지 않는다.
- [x] 글을 제출하고 교사가 한마디·승인을 남긴다.
- [x] 학생 A의 오늘 할 일에서 완료 글이 `다시 쓸 글`로 잘못 나타나지 않는다.
- [x] 학생 A가 독서록 1편을 완료해도 포인트는 그대로이고, 교사가 `확인`한 뒤에만 한 번 지급된다.
- [x] 학생 A가 일기 1편을 완료해도 포인트는 그대로이고, 교사가 `확인`한 뒤에만 한 번 지급된다.
- [x] 학생 B가 친구 아지트에서 학생 A의 공개 글을 열고 정상 댓글을 남긴다.
- [x] 정상 댓글은 `확인 중 → 공개`, 무작위 문자열 댓글은 `선생님 확인 대기`가 된다. (2026-08-06 확인)
- [x] 학생 A의 나의 아지트에서 글·칭호·수호룡·장착 장식이 최신 상태로 보인다.

## 2. 학생 홈·로그인

- [x] 학생 로그인 후 홈이 빈 화면이나 무한 로딩 없이 열린다.
- [ ] 보유 포인트는 별도 칸, 작가·소통·기록가·독서가 칭호는 2×2로 겹치지 않고 보이며 각 레벨은 이름 오른쪽 영역의 상하·좌우 가운데에 정렬된다.
- [x] 오늘 할 일에는 실제로 해야 할 과제와 다시쓰기만 나온다.
- [x] 승인 완료·교사 회수·보관 과제가 `다시 쓸 글`에 나타나지 않는다.
- [x] 하단 메뉴가 홀수 개면 마지막 카드가 2칸 너비, 짝수면 모두 1칸 너비가 된다.
- [x] 홈 우측 수호룡 카드를 한 번 누르면 나의 아지트를 거치지 않고 수호룡 방이 열린다.

## 3. 과제 글쓰기

### 3-1. 질문 없는 과제

- [x] 제목·본문 입력 영역이 질문 있는 과제와 같은 최대 폭으로 보인다.
- [x] 빈 본문은 약 8줄 높이이며 글이 길어지면 입력창 안쪽 스크롤 없이 아래로 늘어난다.
- [x] 2~3분 동안 입력해도 내용이 사라지거나 화면이 주기적으로 새로고침되지 않는다.
- [x] 모바일 키보드가 올라와도 현재 커서와 입력 줄이 가려지지 않는다.
- [x] 화면을 나갔다 돌아오면 로컬 임시저장이 복원된다.
- [x] `제출 전 검토` 창이 문서 상단이 아니라 현재 화면 중앙에 나타난다.
- [x] 검토창·알림창의 원형 닫기 버튼 안에서 X가 정중앙에 보인다.
- [x] 제출 후 학생 화면의 상태와 교사 제출 목록이 함께 갱신된다.

### 3-2. 질문 있는 과제

- [x] 안내 질문과 답변 칸, 제목·본문이 태블릿 세로/가로에서 잘리지 않는다.
- [x] 질문 답변에도 맞춤법 후보 밑줄이 본문과 같은 위치에 표시된다.
- [x] 질문 없는 과제와 본문 폭·글자 크기·저장/제출 버튼 위치가 자연스럽게 통일돼 있다.

### 3-3. 보고하는 글쓰기

- [x] 교사가 `미션 만들기 → 보고하는 글쓰기`에서 교사의 질문이 될 기본 칸을 추가·삭제·위아래 이동하고 학생 미리보기를 연다.
- [x] 학생이 `사진 + 사진에 대한 관찰 결과` 한 입력창으로 작성하고 새 칸 추가·삭제·위/아래 이동을 해도 내용과 사진이 함께 움직인다.
- [x] 아이패드·갤럭시탭에서 카메라 사진을 넣으면 화면이 멈추지 않고 긴 변 720px·장당 최대 256KB의 WebP 또는 JPEG로 최적화된다.
- [x] 사진 3장을 넣으면 네 번째 사진 추가가 차단되지만 기존 세 장의 교체·삭제는 가능하다.
- [x] 세로·가로 사진의 방향과 비율이 유지되고, 사진 교체·삭제 뒤 새로 열어도 이전 사진이 되살아나지 않는다.
- [x] 사진이 있는 칸의 관찰 결과가 비어 있으면 제출을 막고 몇 번 칸인지 알려준다.
- [x] 제목과 각 칸 관찰 결과의 맞춤법 밑줄이 타이핑을 방해하지 않고 표시된다.
- [x] 제출 전 검토, 학생 내 서재, 같은 반 친구 글, 교사 제출 상세·최근 활동에서 칸 순서·사진·관찰 결과가 동일하다.
- [x] 다른 학급 학생과 비로그인 사용자는 보고서 사진 주소를 열 수 없다.
- [x] 원본 10MB 안팎 사진 3장을 넣어도 3,000자 보고서 타이핑과 칸 이동이 버벅이지 않는다.
- [x] 보고서 PDF `질문 포함 지도형`은 교사 질문을 한 줄 안내 바에, 사진과 학생 글을 그 아래에 배치한다.
- [x] 보고서 PDF `질문 없는 완성본`은 교사 질문·칸 번호 없이 사진과 학생 글만 보고서 형태로 배치한다.

### 3-4. 맞춤법 수첩

- [x] PC·갤럭시탭·아이패드 Chrome에서 확정 규칙의 붉은 물결 밑줄이 보인다.
- [x] 맞는 표현에는 불필요한 밑줄이 생기지 않는다.
- [x] 기본 자료의 대표 오류(예: `김치찌게`, `카드로 결재`, `선생님 말씀데로`)에 밑줄이 표시된다.
- [x] `숙제는 반듯이 해야 해요`·`규칙을 반듯이 지켜야 해요`에는 밑줄이 생기고, `책을 반듯이 놓았어요`에는 생기지 않는다.
- [x] 3,000~5,000자 글에서 입력을 계속하는 동안 키가 밀리지 않고, 손을 멈춘 뒤 밑줄이 갱신된다.
- [x] 밑줄 후보를 누르면 `이렇게 고쳐 써요`와 어린이용 설명이 열린다.
- [x] 궁금한 낱말·짧은 구를 직접 검색하면 국립국어원 사전 결과가 나온다.
- [x] `랜덤 5문제` 탭에서 오답을 고르면 정답과 설명이 표시되고 다음 문제로 이동한다.
- [x] 5문제 뒤 결과 화면에 맞힌 개수가 표시되고 `새 문제 5개 풀기`가 동작한다.
- [x] 수첩을 닫았다 다시 열면 500개 후보 중 새로운 5문제가 중복 없이 나온다.
- [x] 교사 `등록 데이터`에서 기본 자료를 펼치면 학습 라벨과 큰 분류가 보이고 두 값으로 검색된다.
- [x] 태블릿 세로·가로와 모바일에서 문제 문장·선택지·다음 버튼이 잘리지 않는다.
- [x] 문장 전체를 AI가 자동 수정하거나 학생 글을 대신 고쳐 주지 않는다.

### 3-5. 교사 승인·다시쓰기·보관

- [x] 교사가 한마디를 남기면 학생이 자기 글 상세에서 확인할 수 있다.
- [x] 교사가 다시쓰기를 요청하면 학생 오늘 할 일에 정확히 1건만 나타난다.
- [x] 학생이 다시 제출하고 교사가 승인하면 다시쓰기 할 일에서 사라진다.
- [x] 교사가 과제를 보관하면 아직 제출하지 않은 학생은 새로 저장·제출할 수 없다.
- [x] 보관 과제를 복구하면 필요한 학생의 작성 동선이 다시 정상화된다.
- [x] 승인 취소·재제출·재승인 후 승인일이 새 날짜로 기록된다.

## 4. 독서록

### 4-1. 책 선택·초안

- [x] 책 A를 선택했다가 `다른 책 검색하기`로 책 B를 선택하면 제목이 책 B 이름으로 바뀐다.
- [x] 책만 선택하고 나간 경우 다음 새 독서록에 지난 책이 되살아나지 않는다.
- [x] 본문을 쓴 뒤 나가면 같은 기기에서 임시저장이 복원된다.
- [x] `저장` 후 다른 기기에서 열면 서버 초안을 이어 쓸 수 있다.
- [x] 카드가 `책만 저장 / 작성 중 / 작성 완료` 상태에 맞는 색과 문구를 표시한다.
- [x] 책장 카드가 큰 화면 4열, 태블릿에서 자연스러운 열 수, 모바일 1열로 정리된다.

### 4-2. 분량·교사 확인 보상

- [x] 기본 정책이 200자·1문단·확인 보상 100P·하루 보상 1편으로 보인다.
- [x] 200자 또는 1문단에 미달하면 저장은 되지만 `작성 완료`는 차단된다.
- [x] 작성 완료만 했을 때는 포인트가 지급되지 않고 교사가 `확인`한 뒤 100P가 지급된다.
- [x] 같은 글을 다시 확인하거나 수정·삭제·같은 책 재등록을 해도 포인트가 다시 지급되지 않는다.
- [x] 하루 보상 상한을 채워도 새 독서록을 계속 작성 완료할 수 있고, 상한을 넘은 글은 포인트만 지급되지 않는다.
- [x] 학생 홈과 독서록 화면에 오늘 작성 편수와 교사 확인 보상 편수가 같은 값으로 보인다.

### 4-3. 공개·교사 확인·삭제

- [x] 공개 독서록은 친구 아지트 독서록 책장에서 보이고 비공개 글은 보이지 않는다.
- [x] 교사가 확인만 하면 학생 카드에 간단한 확인 표시가 나타난다.
- [x] 교사가 `확인`을 누르면 포인트가 지급되고 학생 홈 `활동 알림`에 확인 알림이 1건 나타난다.
- [x] 교사는 `확인 / 보완 요청` 두 가지만 선택할 수 있고, 두 경우 모두 사유·한마디를 비워도 저장된다.
- [x] 교사가 `보완 요청`을 누르면 사유 유무와 관계없이 학생 홈에 보완 알림이 나타나고, 글 카드·상세에 `보완 요청`으로 보인다.
- [x] 활동 알림의 `내 독서록 확인하기` 또는 `선생님 의견 보기`로 연 상세창을 닫으면 나의 아지트를 거치지 않고 학생 홈으로 돌아간다.
- [x] 보완 요청된 독서록은 학생 홈 `지금 할 일 → 다시 쓸 글` 개수에 포함되고, 누르면 해당 독서록 수정 화면으로 열린다.
- [x] 독서록을 수정 완료하면 `다시 쓸 글` 개수가 1개 줄고 교사 검토 대기로 돌아간다.
- [x] 교사 한마디가 있으면 카드에는 `선생님 한마디 있음`만 보이고 누르면 중앙 모달로 전문이 열린다.
- [x] 독서록 수정 화면 하단에서도 선생님 한마디 전문이 보인다.
- [x] `책만 저장`, `작성 중`, `작성 완료` 카드 모두 삭제할 수 있다.
- [x] 독서록을 삭제하면 연결된 책도 책장에서 사라진다.

### 4-4. 독서마라톤 개인전·단체전·메달

- [x] 교사 `학생 독서록 → 독서록 이벤트`에서 `개인전 / 우리 반 전체전 / 모둠 대항전` 세 가지가 보인다.
- [x] 모둠 대항전에서 모둠을 추가·삭제하면 학생이 모둠별로 균등 재배정되고, 모둠 카드의 학생 선택 상자로 개별 이동할 수 있다.
- [x] `랜덤 배정`을 누를 때마다 학생 구성이 섞이면서도 모둠별 인원 차이는 최대 1명이다.
- [x] `균등 재배정 / 랜덤 배정 / 크게 보기`가 한 줄에 있고, 큰 화면에서 세 가지 배정 방식을 선택해 결과를 함께 확인할 수 있다.
- [x] 학생 전원을 한 모둠에 한 번씩 배정해야 시작되고, 첫 독서 실적 반영 뒤 경기 방식·모둠 배정은 잠긴다.
- [x] 테스트할 때는 목표를 1km, 페이지가 확인된 책을 사용한다(1쪽=10m이므로 100쪽이면 1km).
- [x] 마라톤을 사용하지 않는 동안 독서록 확인 화면과 학생 화면에 `마라톤 인정` 같은 불필요한 문구가 없다.
- [x] 독서록을 작성 완료한 직후에는 거리가 늘지 않고, 교사가 `확인 완료`한 뒤에만 거리·책·쪽수가 늘어난다.
- [x] 확인 완료한 글을 `보완 요청`으로 바꾸면 아직 보관하지 않은 현재 경기의 거리에서 제외된다.
- [x] 개인전은 학생마다 자기 목표 진행률과 개인 순위가 보이고, 목표를 채운 학생만 원형 골드 메달을 받는다.
- [x] 우리 반 전체전은 학급 합계가 목표에 도달하고 개인 최소 요건을 채운 학생만 각진 블루 단체 메달을 받는다.
- [x] 모둠 대항전은 학생 화면에 자기 모둠 진행률·모둠 순위가 보이고, 교사 화면에는 모둠별 결과와 모든 학생 참여도가 보인다.
- [x] 학생의 접힌 마라톤 카드에는 `우리 모둠 거리`, 상세에는 모둠 합계 거리·책·쪽수와 `내 기여`가 구분되어 보인다.
- [x] 완주 뒤 학생 홈 이름 오른쪽에 대표 메달이 보이고 누르면 나의 아지트 `독서마라톤 메달함`에서 전체 메달을 확인한다.
- [x] 개인 최소 요건을 못 채운 학생은 팀이 완주해도 메달이 없고, 이후 조건을 채우면 메달이 생긴다.
- [x] `지난 마라톤 결과`에서 경기 방식, 학생별 최종 기록, 모둠전이면 모둠별 최종 결과가 유지된다.

## 5. 일기

- [x] 학생 홈의 `일기`에서 새 글을 작성할 수 있다.
- [x] 기본 정책이 150자·1문단·확인 보상 80P·하루 보상 1편이며 기본 공개 상태는 비공개다.
- [x] 분량 미달은 저장할 수 있지만 완료할 수 없다.
- [x] 작성 완료만 했을 때는 포인트가 지급되지 않고 교사가 `확인`한 뒤 80P가 한 번만 지급된다.
- [x] 같은 일기를 다시 확인하거나 삭제 후 다시 써도 추가 보상이 없다.
- [x] 나의 아지트에 `일기 책장` 탭이 생기고 완성 일기가 들어간다.
- [x] 교사가 사유 없이도 `확인 / 보완 요청`을 누를 수 있고, 학생 홈 활동 알림과 일기 카드에 같은 상태가 나타난다.
- [x] 활동 알림의 `내 일기 확인하기` 또는 `선생님 의견 보기`로 연 상세창을 닫으면 나의 아지트를 거치지 않고 학생 홈으로 돌아간다.
- [x] 보완 요청된 일기는 학생 홈 `지금 할 일 → 다시 쓸 글` 개수에 포함되고, 누르면 해당 날짜의 일기 수정 화면으로 열린다.
- [x] 일기를 수정 완료하면 `다시 쓸 글` 개수가 1개 줄고 교사 검토 대기로 돌아간다.
- [x] 공개로 바꾼 일기만 친구 책장에서 보인다.
- [x] 임시저장 후 같은 기기와 다른 기기에서 이어 쓸 수 있다.
- [x] 교사 `학생 일기` 탭에서 확인·한마디·학생별 보기·Excel·PDF·Google Docs 내보내기가 동작한다.
- [x] 교사 동기부여 설정 변경이 학생 진행 표시와 완료 판정에 반영된다.

## 6. 나의 아지트·내 서재·글쓰기 3축

- [ ] 상단 상태창에 보유 포인트와 작가·소통·기록가·독서가 네 칭호가 2×2로 겹치지 않고, 각 레벨은 이름 오른쪽 영역의 상하·좌우 가운데에 보인다.
- [ ] 네 칭호 카드를 누르면 단계별 설명이 중앙 모달로 열린다.
- [ ] 작가 칭호가 학생 홈·나의 아지트·교사 학생 아지트에서 동일하다.
- [ ] 소통 칭호와 소통 점수가 학생 홈·나의 아지트·수호룡 효과에서 동일하다.
- [ ] 이번 시즌 작가 글 수·글자 수·레벨에서 기존·새 일기와 독서록이 모두 빠지고, 포인트·작성 글·보유 아이템은 유지된다.
- [ ] 일기는 교사가 확인한 뒤 작성 날짜별 한 번만 기록가에 반영되며, 같은 날짜의 두 편은 1일로 센다.
- [ ] 독서록은 교사가 확인한 뒤 편수와 서로 다른 책 수에 반영되며, 같은 책의 두 편은 2편·1권으로 센다.
- [x] 내 서재의 `과제 / 독서록 / 일기` 책장 탭이 분리돼 있다.
- [x] 긴 책 제목이 책등 중앙에 보기 좋게 배치되고 `제목 전체 보기`로 전부 읽을 수 있다.
- [x] 과제·독서록·일기 책을 열면 아래 3가지가 같은 모양과 순서로 나타난다.
  - [ ] 제출/확인 상태
  - [ ] 선생님 의견
  - [ ] 친구 댓글
- [x] 나의 아지트 책장에서 직접 연 과제·독서록·일기 상세창을 닫으면 학생 홈이 아니라 나의 아지트 책장으로 돌아간다.
- [x] 비공개 글과 보관 과제는 본인에게만 올바른 상태로 보인다.
- [x] 글쓰기 발자국에서 포인트 획득과 사용처가 모두 표시된다.

## 7. 친구 아지트·댓글·반응

- [x] 친구 아지트 화면이 흰 화면 없이 열린다.
- [ ] 친구 목록과 친구 상세에서 이름과 작가·소통·기록가·독서가 네 칭호가 같은 단계로 보인다.
- [x] `최신 글`에서 선생님 과제와 독서록 탭이 분리돼 최신순으로 보인다.
- [x] 친구 상세에는 공개 책장과 둘이 나눈 기록만 보이고 민감한 전체 통계·포인트 잔액은 보이지 않는다.
- [x] 친구의 과제·독서록·공개 일기가 올바른 책장 탭에 들어간다.
- [x] 학생 B가 학생 A 글에 남긴 반응과 댓글이 학생 A 화면에 30초 안에 반영된다.
- [x] 정상 댓글 예시 `주인공의 마음이 잘 드러나서 재미있게 읽었어.`가 공개된다.
- [x] 무작위 문자열 예시 `asdf qwerty`는 삭제되지 않고 교사 확인 대기로 남으며 친구에게는 보이지 않는다.
- [x] 댓글 작성자 본인은 `확인 중` 또는 `선생님 확인 대기` 상태를 볼 수 있다.
- [x] 친구 글을 닫으면 방문 중이던 친구 아지트로 돌아온다.

## 8. 교사 대시보드

### 8-1. 글쓰기·댓글 관리

- [x] 과제 제출·다시쓰기·승인 상태가 학생 화면과 일치한다.
- [x] `학급 운영 → 학생 댓글`에서 처리할 것·승인됨 탭과 3일/7일/2주/30일/전체 필터가 동작한다.
- [x] 상단 댓글 개수와 실제 목록 개수가 일치한다.
- [x] AI가 막은 댓글을 교사가 승인하면 친구에게 공개된다.
- [x] 교사가 댓글을 삭제하면 목록·개수·학생 화면에서 함께 사라진다.

### 8-2. 독서록·일기 관리

- [ ] 독서록과 일기 모두 화면 맨 위에 `미확인 / 보완 중 / 확인 완료 / 전체` 통계가 같은 모양으로 보이고, 실제 글 수와 일치한다.
- [ ] `보완 요청`한 글은 두 화면 모두 `보완 중` 수와 목록에만 들어가고 `확인 완료` 수·목록에는 들어가지 않는다.
- [x] 두 화면 모두 기본 화면이 `검토 대기 / 학생별 / 전체 기록`으로 나뉘며 검토 대기 첫 20편이 먼저 보인다.
- [x] 검토 대기는 PC 4열·넓은 PC 5열·태블릿 2~3열·모바일 1열의 압축 카드로 보여, PC 한 화면에서 약 20편을 빠르게 훑을 수 있다.
- [x] 글별 체크 또는 `현재 목록 전체 선택` 뒤 `선택 글 확인`을 한 번 누르면 선택한 독서록·일기가 모두 확인된다.
- [x] 일괄 확인 직후 미확인 통계와 목록이 줄고, 각 학생에게 확인 알림과 확인 보상이 글당 한 번만 반영된다.
- [x] 20편보다 많으면 `더 보기`로 다음 글을 불러오며, 이미 선택한 글이 사라지거나 중복되지 않는다.
- [x] 전체 기록의 독서록은 PC 3열·태블릿 2열·모바일 1열 카드로 보인다.
- [x] 학생별 독서록 Excel·PDF·Google Docs 내보내기가 해당 학생 글만 포함한다.
- [x] 독서록 확인과 동기부여 설정이 상단 탭으로 분리되고 미저장 설정 이동 경고가 나온다.
- [x] 일기 확인·한마디·학생별 관리·내보내기가 독서록과 섞이지 않는다.

### 8-3. 학생 아지트

- [x] `학급 운영 → 학생 아지트` 진입 시 빈 화면이나 무한 로딩 없이 학생 한 명이 선택된다.
- [x] PC 화면 첫 진입에서 수호룡 방·칭호·포인트·활동 통계와 책장 상단이 한눈에 들어오며, 수호룡 방이 과도하게 커지지 않는다.
- [x] 학생 검색·선택 시 이름, 칭호, 포인트, 성장 상태, 실제 방·꾸미기 상태가 해당 학생 기준으로 바뀐다.
- [x] 완성 글 책장 탭의 글 수와 공개 범위 표시가 학생의 최근 완료 글과 맞는다.
- [x] 책등을 누르면 해당 글의 제목·본문·글자 수가 읽기 전용 상세창에 나오고, 독서록 책 정보·보고서 구성·처음글 비교가 보존된다.
- [x] 새로고침을 눌러도 학생 포인트·글·꾸미기 데이터가 변경되지 않는다(읽기 전용).
- [x] `학생 관리 → 학생 명단 → 아지트 보기`를 누르면 해당 학생이 선택된 학생 아지트 화면으로 이동한다.
- [x] PC·태블릿·모바일 폭에서 학생 목록, 방, 책장이 겹치거나 잘리지 않는다.

### 8-4. 학급 발자국·내보내기

- [x] 학급 발자국 전체화면이 화면비에 따라 글자·그래프가 찌그러지지 않는다.
- [x] 학생별 현황 카드 내용이 카드 밖으로 잘리지 않는다.
- [x] 학생 카드와 달력·그래프·포인트 사용처 카드를 누르면 중앙 확대 모달이 열린다.
- [x] 전체화면에서 하단에 큰 빈 공간 없이 주요 정보가 배치된다.
- [x] 승인 글 Excel·PDF·Google Docs 내보내기에 승인일이 표시된다.
- [x] 일반 글 PDF는 제목·글쓴이·과제명·본문이 A4 양식에 나오며 본문 글자가 12pt보다 작아지지 않는다.
- [x] 한 페이지를 넘는 긴 글은 글자를 축소하지 않고 다음 페이지에 이어지며 문장이 잘리지 않는다.
- [x] 시 과제 PDF는 보고서 양식을 묻지 않고 제목·지은이·연 단위 시구를 시 전용 양식으로 내보낸다.
- [x] 사진 보고서 Excel은 `내용` 뒤 `사진 1~3` 열의 같은 글 행에 실제 이미지가 보인다.
- [x] 사진 보고서 Google Docs는 각 글의 제목·글쓴이·본문 뒤, 글의 마지막에 사진이 원래 순서대로 보인다.
- [x] Google Docs 내보내기 뒤 교사의 Drive에 `끄적끄적-임시사진` 파일이나 공개 권한이 남지 않는다.
- [x] PDF 인쇄 창에서 `PDF로 저장`과 실제 프린터 출력이 모두 가능하다.
- [x] 근거가 없는 과거 승인 글은 임의 날짜 대신 `승인일 기록 없음`으로 표시된다.

## 9. 수호룡·아지트 공방

- [x] 최초 알 선택에서 수호룡 4종 중 하나를 고를 수 있다.
- [x] 작가 칭호 1~10단계에 맞춰 같은 종의 외형이 단계적으로 달라진다.
- [x] 작가 3단계에서 한 번만 수호룡 종류를 다시 선택할 수 있다.
- [ ] 소통 효과 1~7단계가 크기뿐 아니라 빛·별·룬·궤도·오로라 형태로 구분된다.
- [x] `오늘의 이야기 들려주기`가 오늘 완료한 글 제목에 반응한다.
- [x] 오늘 글이 없으면 글쓰기 이동 버튼을 보여 주고 교감 자체로 포인트를 지급하지 않는다.
- [x] 공방에서 프레임·받침대·왼쪽 소품·오른쪽 소품·문패 5슬롯을 각각 미리 볼 수 있다.
- [x] 상품 구입 시 표시 가격만큼 포인트가 한 번 차감되고, 구입과 장착이 구분된다.
- [ ] 장착한 프레임과 소품이 수호룡·소통 효과·문패 글자를 과하게 가리지 않는다.
- [x] 문패 8종의 재질·광택이 구분되고 학생·수호룡 이름이 중앙에 선명하게 보인다.
- [x] 교사 `아지트 놀이터 → 작가 수호룡`에는 예전 `공용 성장 정책`·먹이·퇴화·레벨 초기화가 보이지 않는다.
- [x] 교사 화면에서 현재 시즌 시작일·참여 학생·완성 글·글자 수·작가 10단계 분포가 보인다.
- [ ] 학생 카드를 누르면 수호룡 종류, 작가·소통·기록가·독서가 단계, 이번 시즌/누적 글, 교감, 장착한 5개 소품을 중앙 모달에서 확인할 수 있다.
- [ ] 교사가 `성장 마감 · 작별 편지 열기`를 누르면 그 순간의 학생 네 칭호 단계와 수호룡 모습이 더 이상 변하지 않는다.
- [x] 학생 수호룡 방에 작별 편지 칸이 나타나고 50자 미만 완성은 막히며 작성 중 저장·완성이 구분된다.
- [x] 완성한 편지의 `기념 이미지 받기`가 학생 이름·최종 수호룡·작가/독자 단계·편지를 담은 PNG를 내려받는다.
- [x] 교사 화면에서 작별 편지 완성 인원과 학생별 완성 표시가 보인다.
- [x] 시즌 종료 뒤 학생의 지난 수호룡 기록과 완성 편지는 계속 보이고 수정은 막힌다.
- [x] 교사 `지난 시즌` 탭에서 `학생별 수호룡 보기`를 누르면 그 시즌 학생 카드 목록이 펼쳐지고,
  카드를 누르면 상세 모달 상단이 `STUDENT GUARDIAN` 대신 `SEASON n · 종료일 기준`으로 표시된다.
- [x] 교사가 새 학기를 별도로 시작하면 학생 칭호·수호룡은 1단계 알로 돌아가고 4종을 다시 선택할 수 있다.
- [x] 새 학기 전후 학생의 기존 글·포인트·구입 및 장착한 5개 소품은 그대로 유지된다.
- [x] 장착한 5개 슬롯이 나의 아지트 작은 카드와 친구 아지트에 같은 모습으로 보인다.
- [x] 친구 화면에는 장착 변경이 30초 안에 반영된다.

## 9-2. 어휘의 탑 — 덱마스터·어휘 마스터

> **확인 기록**: 2026-08-18 PC 실계정(테스트 학급)에서 정상 관문 **1단계 통과**를 확인했다 —
> 19/20 정답 · 직접입력 7/8 · 229초(문항당 평균 11.5초, 제한 45초). 통과 직후 별이 ⭐☆☆ 로 늘고
> 다음 단계가 2단계로 열리는 것까지 확인했다. 2·3단계는 문항 구성만 다르고 경로가 같아 생략했다.
> 확인에 쓴 진도는 같은 날 되돌렸다(운영에 남은 테스트 데이터 없음).

> 2026-08-18 추가. 이 절의 숫자는 전부 **학급 설정**이라 반마다 다를 수 있다(교사 화면 `어휘의 탑`).
> 아래는 기본값 기준이다.

### 층 연습과 진도

- [x] 지도에서 원하는 층을 골라 12문항을 풀 수 있고, 1층부터 순서대로 갈 필요가 없다.
- [x] 낱말 상태 네 칸(처음 볼·연습 중·다시 볼·완전히 익힘)의 합이 그 층 낱말 수와 맞는다.
- [x] 한 번만 맞힌 낱말은 `완전히 익힘`이 되지 않는다(서로 다른 두 형태를 연달아 맞혀야 한다).
- [x] 한 판에서 틀린 낱말이 서너 문제 뒤에 **다른 형태로** 다시 나온다.

### 덱마스터 (층 시험)

- [x] 자격이 없어도 `🔒 덱마스터 도전` 버튼이 **보인다**. 눌러도 시험이 시작되지 않는다.
- [x] 잠긴 버튼을 누르면 조건이 펼쳐지고, 익힘 수와 아직 만나지 않은 낱말 수가 **실제 진도와 맞는다**.
- [x] 익힘 80%를 채워도 **만나지 않은 낱말이 남아 있으면 열리지 않는다**(두 조건 모두 필요).
- [x] 두 조건을 다 채우면 버튼이 `🏆 덱마스터 도전`으로 바뀐다.
- [x] 시험 중에는 정답을 알려 주지 않고, 한 문제를 답하면 되돌아갈 수 없다.
- [x] 문항마다 남은 시간이 줄고, 다 되면 빈 답으로 넘어가며 시험이 멈추지 않는다.
- [x] 결과 화면에 정답 수·직접 쓰기 수가 나오고, 틀린 낱말에서 낱말 카드함으로 이어진다.
- [x] 통과한 층은 버튼이 `🏆 덱마스터 다시 도전`으로 바뀐다.

### 재도전 제한 (보충 수련)

- [x] 시험에서 틀린 낱말이 `다시 볼 낱말` 칸으로 옮겨 간다(익힘 수가 그만큼 준다).
- [x] 불합격 뒤 버튼이 `📚 낱말 N개 더 익히기`로 바뀌고 곧바로 재도전할 수 없다.
- [x] 그 버튼을 누르면 `N/M개 다시 익혔어요`가 나오고 숫자가 실제 진도와 맞는다.
- [x] 그 층을 연습하면 틀렸던 낱말이 **먼저** 나온다.
- [x] 일부만 다시 익히면 아직 잠겨 있고, 전부 익히면 도전이 다시 열린다.
- [x] **통과했을 때도** 틀린 낱말은 `다시 볼 낱말`이 된다. 받은 덱마스터는 사라지지 않는다.
- [x] 시험 도중에 나가면(중도 종료) 보충 대상이 생기지 않는다.
- [x] 정상 관문에서 떨어져도 같은 방식으로 잠기고, 틀린 낱말이 여러 층에 흩어져 있다.
- [x] 정상 관문 보충 안내에 `어느 층에 있냐면 3층 1개 · 5층 1개`처럼 층별 개수가 나오고 실제와 맞는다.
- [x] 정상 노드 제목 옆 `?`를 누르면 도전 규칙이 화면 안에서 펼쳐진다(창이 뒤로 숨지 않는다).
- [x] 그 도움말의 단계별 직접입력 수·합격선이 교사가 정한 학급 설정과 같다.
- [x] 교사 화면에서 조건(익힘 비율·문항 수·합격선·시간)을 바꾸면 **재배포 없이** 학생 화면에 반영된다.

### 어휘 마스터 (정상 관문)

- [x] 덱마스터를 다 채우지 못한 학생에게도 꼭대기 `🔒 어휘 마스터 도전`이 보이고, 누르면 남은 개수가 나온다.
- [x] 10개 층을 모두 통과하면 `👑 어휘 마스터 1단계 도전`으로 바뀐다.
- [x] 10번째 덱마스터를 통과한 결과 화면에 **휘장이 아니라 `탑의 정상이 열렸어요`**가 나온다.
- [x] 1단계를 통과하기 전에는 2단계에 도전할 수 없다.
- [x] 단계가 오를수록 직접 쓰는 문제가 늘어난다(8 → 14 → 20개). 3단계는 보기가 하나도 없다.
- [x] 문제가 **열 층 전체에서 고르게** 나온다(20문항이면 층마다 2문항).
- [x] 한 단계를 통과할 때마다 별이 하나씩 는다(⭐ → ⭐⭐ → ⭐⭐⭐).
- [x] 3단계까지 통과하면 꼭대기에 `어휘 마스터 완성!`이 뜨고 도전 버튼이 사라진다.

### 휘장 표시 (세 화면)

- [x] 나의 아지트에 휘장 카드가 있고, **정상 이름이 가장 큰 글자**로 나온다.
- [x] 아직 못 받았어도 `👑 어휘 마스터 ☆☆☆`와 여는 방법이 보인다(카드가 사라지지 않는다).
- [x] 층 띠가 카드 폭을 채우고 칸마다 층 번호가 있으며, **통과한 층 번호만** 금색이다.
      (띄엄띄엄 통과했을 때 앞에서부터 채워지지 않는지 꼭 확인한다.)
- [x] 친구 아지트에서도 친구의 휘장과 덱마스터 진도가 보인다.
- [x] 친구 화면에 시험 점수·오답은 나오지 않는다.
- [x] 교사 `학생 아지트 보기`의 성장 상태에서 같은 휘장이 보인다.

### 도움말

- [x] 놀이터의 어휘의 탑·수호룡 카드 **오른쪽 위**에 `💡 도움말`이 또렷하게 보인다(글씨가 보여야 한다).
- [x] 어휘의 탑 도움말에 덱마스터·어휘 마스터 3단계·휘장 설명이 들어 있다.
- [x] 수호룡 도움말에 **포인트로는 수호룡이 자라지 않는다**는 설명이 들어 있다.
- [x] 교사 대시보드의 도움말 버튼이 **모든 메뉴에서 같은 모양**이다(작가 수호룡·어휘의 탑 포함).
- [x] 교사 수호룡 도움말에 **시즌 마감 3단계와 칭호 초기화 시점**이 설명되어 있다.

## 10. 태블릿·모달 공통 화면

아래 화면은 태블릿 세로와 가로에서 각각 한 번 확인한다.

- [x] 글쓰기·독서록·일기의 본문 글자 크기와 터치 버튼이 충분히 크다.
- [x] 가로/세로 회전 직후 폭과 카드 열 수가 자동으로 다시 맞는다.
- [x] 화면 하단 키보드가 입력 중인 문장과 주요 버튼을 가리지 않는다.
- [x] 모든 중앙 모달이 현재 보고 있는 화면 중앙에 나타난다.
- [x] 긴 모달은 창 전체가 화면 밖으로 나가지 않고 모달 내부만 스크롤된다.
- [x] 원형 닫기 버튼의 X가 중앙에 있고 터치 영역이 충분하다.
- [x] 책등·학생 카드·그래프의 글자와 데이터가 카드 밖으로 잘리지 않는다.
- [x] 로딩 중 여러 번 눌러도 중복 저장·중복 포인트·중복 댓글이 생기지 않는다.

## 11. 점검 후 정리

- [ ] `[수동점검]` 과제와 글은 필요한 화면 증거를 남긴 뒤 정리한다.
- [ ] 포인트가 예상과 다르면 글이나 기록을 바로 삭제하지 말고 오류 기록을 먼저 남긴다.
- [ ] 막힌 댓글은 AI 오탐률 측정을 위해 이유 없이 일괄 삭제하지 않는다.
- [ ] 확인한 체크박스를 저장하고 `WORKLOG.md`에 확인 날짜와 기기를 기록한다.

## 오류 기록표

| 번호 | 중요도 | 계정·기기 | 화면 | 재현 순서 | 기대 결과 | 실제 결과 | 캡처 |
|---|---|---|---|---|---|---|---|
| 1 | 치명/높음/보통/낮음 |  |  |  |  |  |  |

오류는 다음 우선순위로 처리한다.

1. **치명**: 로그인 불가, 글 유실, 다른 학급 자료 노출, 포인트 중복·무단 획득
2. **높음**: 제출·승인·완료·댓글·내보내기 등 핵심 기능 실패
3. **보통**: 태블릿 잘림, 모달 위치, 상태 표시 불일치
4. **낮음**: 색상·간격·문구·애니메이션 미세 조정
