# 보안 하네스

새 기능을 붙일 때 성능 하네스와 함께 아래 검사를 통과시킨다.

## 우리반 아지트 C1~C5 경계 (2026-09-05 로컬 구현)

- 전시·항목·확인 원장·공개 단계 표는 RLS를 켜고 직접 권한을 모두 닫는다. internal 단계는 승인된 DB ADMIN, pilot 단계는 지정된 최대 두 학급의 승인된 ADMIN/TEACHER가 현재 담당하는
  학급에서만 전용 관리 RPC를 허용한다. 학생은 실제 학급 연결·활성 상태·학급 ON·공개 상태를 매번 검사한다.
- 일반 과제 글·시의 제출/확인/공개 자격과 표시본·해시는 서버 정본 하나에서 구성한다. 클라이언트 본문이나
  원글 상태 주장을 저장하지 않으며 source revision·전시 expected revision·명시적 학급 수록 확인을 요구한다.
- 초안 저장은 공개판을 덮어쓰지 않는다. 원글 회수·삭제·학생 비활성·수록 철회는 다음 조회에서 작품을 숨기며,
  철회 이후 새 확인 식별자를 사용해 초안 저장만으로 과거 공개판이 다시 열리지 않게 한다.
- 공개 RPC는 방당 12편의 표시 필드만 반환한다. 원글/학생 ID·확인 원장·내부 스냅샷 원본은 내보내지 않는다.
  외부 공유는 별도 표시본·작품별 확인·해시 토큰을 사용한다. 문집은 확정 콘텐츠·설정을 보관하며 PDF 파일 서버 보관은 하지 않는다.
- `test:class-agit`과 `test:security:static`의 저장 계약 검사, `smoke:class-agit` 또는 미적용 SQL의
  `migrate:check`가 실제 합성 계정 역할 경계를 검증한다. `20261241`은 롤백 검증 후 2026-09-05 사용자 요청으로 운영 적용했다. 진남초 4학년 1반만 pilot·학급 ON이며 외부 공유는 OFF다. 앱 배포·실계정 인수는 별도다.

- C2 학생 목록·방 요약·전문 RPC는 `students.auth_id=auth.uid()`로 학급을 구한다. 클라이언트가 학급/학생 ID를
  고르지 않으며 실제 활성 상태·현재 담당 교사 승인과 제한 운영 범위·학급 ON·내부 공개 단계를 매번 검사한다.
- 방/전문은 공개판 행을 공유 잠금한 뒤 같은 수록 철회·확인 세대 필터를 사용한다. 작품 번호는 판 번호와 묶으며
  새 판 뒤 과거 번호로 다른 글을 잘못 여는 요청을 거부한다. 응답은 허용된 표시 필드만 투영하고 화면도 이를 검사한다.
- 홈에는 공개 전시 또는 문집 존재 Boolean만 전달한다. 목록/방 응답에 전문을 포함하지 않고, 읽기 실패나 학급 전환·요청 취소
  뒤 예전 전문을 화면에 복구하지 않는다. 실시간 철회 구독은 없으며 다음 조회부터 차단한다.

- 문집/항목/확정판/공개 이력/외부 공유/외부 항목/익명 요청 예산/시범 학급 표도 RLS ON·직접 권한 OFF다.
  수록 철회 이후 재확인은 새 consent_id이며 원글 회수/삭제·학생 비활성도 문집·외부 항목을 지속 철회한다.
- 익명 읽기 RPC 하나만 anon에 연다. 외부 이름은 별도 가림 이름이며 학생/원글/학급 ID·비공개 경로를 응답에 넣지 않는다.
  32바이트 난수 토큰은 DB SHA-256, URL fragment, POST 본문만 사용하며 로그·분석·브라우저 저장소에 쓰지 않는다.
- 공개 문서와 콘텐츠의 no-store/no-referrer/noindex를 적용한다. 기본 외부 OFF, 최대 30일 만료·해지·재발급,
  전역 3,000/분·공유 600/분·3초 statement timeout·1초 lock timeout. JSON 오류 반환으로 제한 카운터를 보존한다.
- 격리 DB의 `smoke:class-agit:http`가 익명 200/404/409/429·교사/학생 403·직접 표 차단과 헤더·시간 제한을 실제 HTTP로 검사한다.
  운영 DB에는 스키마 읽기만 하며 테스트 DB와 컨테이너는 종료 시 제거한다.

## 기본 원칙

1. 권한은 화면이나 JWT 메타데이터가 아니라 DB의 실제 사용자·학급 연결과 승인 상태로 판정한다.
2. 학생 쓰기는 본인 데이터만, 교사 관리는 담당 학급만 허용한다. `SECURITY DEFINER` RPC도 함수 안에서 다시 확인한다.
3. 외부 비용이 생기는 AI·메일은 승인 사용자 확인, 입력 상한, 서버 속도 제한을 모두 둔다.
4. 새 Edge 함수는 `scripts/check-operational-security.mjs` 허용 목록에 의도적으로 등록한다. 사용하지 않는 함수는 운영 경로에서 격리한다.
5. 브라우저에는 재사용 가능한 학생 코드·비밀 키를 저장하지 않는다.
6. 새 외부 도메인을 연결하면 CSP를 필요한 항목에만 추가하고, 빌드 후 실제 응답 헤더를 확인한다.
7. 운영 의존성은 `npm audit --omit=dev` 0건을 기준으로 한다. 수정판이 없는 패키지는 사용 경로를 제거하거나 교체한다.
8. 맞춤법 학습 통계는 학생 글·문장 원문을 저장하지 않는다. 기존 수첩 항목 ID와 사전·문장 검색은 횟수만 집계하고,
   미등록 검색 후보는 서버가 다시 검증한 2~15자·최대 2어절의 한글 표현만 보관한다. 교사 화면에는 여러 학생이
   찾거나 반복된 후보만 상한 안에서 반환하며, 학급 항목은 담당 교사 승인 뒤에만 학생에게 노출한다. AI 검사·검색
   후보의 공통 승격·중지와 검토 원장 조회는 실제 `profiles.role='ADMIN'`인 관리자 전용 RPC만 허용한다. 학생은
   승인된 공통 자료와 자기 학급 자료를 합친 상한 100개만 받고, 같은 표현은 공통 자료를 우선해 한 번만 적용한다.
   주간 AI 검수는 기본 500개 전체나 학생 글을 보내지 않고, 서버가 모은 40자 이하 표현과 집계 숫자·로컬에서
   고른 유사 자료 최대 3개만 보낸다. 한 번에 후보 200개·AI 요청당 12개로 제한하고 같은 후보 결과는 캐시하며,
   원장·캐시 표는 브라우저에 직접 공개하지 않고 관리자 선택 뒤에만 공통 자료로 게시한다.
9. Google Docs 사진 내보내기는 교사의 명시적 내보내기 동작에서만 실행한다. 호환 변환용 Drive 임시 파일은 검색
   불가 공개 권한으로 만들고 Docs 삽입 응답 직후 파일 삭제, 실패 시 공개 권한 삭제까지 반드시 시도한다.
10. 학생 활동 알림 원장은 브라우저에 테이블 권한을 주지 않는다. 기능 전용 업무 RPC가 같은 트랜잭션에서 내부 발행
   함수를 호출하고, 학생은 실제 `auth_id` 연결로 찾은 본인 학급·학생 범위 조회/읽음 RPC만 실행한다. `event_key`로
   재시도 중복을 막고 payload는 짧은 표시 정보만 두며 글 본문·비밀 값은 넣지 않는다.
   반려·승인 상단 배너 폴링도 원장을 직접 읽지 않고 전용 RPC만 사용한다. RPC는 현재 글 상태까지 다시 확인하고
   브라우저에는 이벤트 ID·유형·대상 ID·시각만 반환하며 기존 payload의 제목·피드백·포인트를 반환하지 않는다.
11. 학생 글 반응은 `post_reactions` 직접 쓰기 권한을 주지 않고 전용 RPC로만 변경한다. RPC는 실제 학생 연결,
   같은 학급의 제출·공개 글, 과제 `input_template`에 대응하는 허용 반응 프로필을 모두 다시 검증한다. 반응 프로필
   서버 카탈로그는 브라우저에 공개하지 않는다.
12. 친구 공개 글 피드는 클라이언트가 보낸 학급 ID를 신뢰하지 않는다. 실제 `auth_id`로 연결된 학생 학급을 서버에서
    구하고 `student_posts.class_id`·제출·`visibility='class'`를 직접 제한한다. 처음 글은 학생이 공개를 켠 경우만
    반환하고, 익명 실행과 등록되지 않은 자율 글 유형은 거부한다.
13. 학생 포인트 내역은 클라이언트가 학생·학급 ID를 보내지 않는다. 실제 `auth_id`로 연결된 활성 학생의
    `point_logs.class_id`·`student_id`를 함께 제한하고 최근 목록 상한을 둔다. 화면에 필요 없는 `event_key`·
    `metadata`와 내부 식별 정보는 반환하지 않는다.
14. 어휘 V2 공용 검수표는 브라우저에 테이블 권한을 주지 않고 ADMIN 전용 RPC로만 다룬다. 권한은 JWT 메타데이터가
    아니라 실제 `profiles.role`로 확인한다. 한 덱은 최대 50개, 문항 JSON·텍스트·보기·허용 정답에 상한을 두며,
    항목·덱 버전으로 동시 수정을 막는다. 잠긴 덱은 수정할 수 없고 잠금만으로 학생 출제에 연결하지 않는다.
15. 어휘 V2 낱말별 학습 상태는 브라우저에 테이블 권한을 주지 않는다. 실제 학생·학급·실행·서버 발급 문항이
    모두 일치하는 답안 저장 트랜잭션에서만 갱신하고, 지도 집계와 적응 출제도 인증 학생의 학급·학년·덱을 서버가
    고정한다. 정답은 제출 전 응답에 포함하지 않으며 과거 답안 백필도 서버에 보존된 문항 스냅샷만 사용한다.
    개인 연습 출제 정책표도 브라우저 역할과 `service_role`에 직접 열지 않고, 모든 현재·미래 학급을 정책 2로
    등록한 뒤 새 판을 만들 때 실행 행에 정책 버전을 스냅샷한다. 정책 등록이 바뀌어도 이미 시작한 판의 문제 비율은
    중간에 바뀌지 않는다.
16. 백업 상태 원장은 브라우저 역할에 테이블 권한을 주지 않고 실제 `profiles.role='ADMIN'`을 확인하는 전용 RPC로만
   읽는다. 호스트 기록기는 고정된 상태·숫자·짧은 코드만 저장하며 원문 로그, 파일 경로, 시크릿을 DB에 넣지 않는다.
17. 공통 학습 콘텐츠 카탈로그는 문제 정답을 포함하므로 익명·학생·교사 브라우저와 `service_role`에 표 직접 권한을
   주지 않는다. 학년군·내용/문제 난이도·검수 신호가 해결된 `published` 자료만 기능 전용 RPC가 사용하며, 학생에게
   문제를 줄 때는 정답을 빼고 답안 제출 RPC가 실제 학생·학급·서버 발급 문항을 다시 확인한다.
18. Realtime WebSocket은 통합 연구소의 활성 질문 결과 화면에만 허용한다. Kong은
    `/realtime/v1/api/openapi`와 `/realtime/v1/api/tenants`를 `request-termination` 403으로 먼저 차단한 뒤
    일반 `/realtime/v1/` WebSocket route를 처리해야 한다. `npm run test:security:ops`는 운영 설정과 실제
    anon 키 403을 함께 확인한다.
19. 서비스 정기점검·CVE 원장은 브라우저 역할과 `service_role`에 표 직접 권한을 주지 않는다. 호스트 CVE
    검사기는 Docker 소켓 없이 이미지 tar를 하나씩 읽고, DB에는 제한된 이미지 식별자·노출 등급·개수와 원본
    해시만 기록한다. 원본 JSON·패키지 경로·호스트 파일 경로는 브라우저에 반환하지 않는다. 점검 결과 수정과
    완료는 실제 `profiles.role='ADMIN'` 관리자 RPC만 허용하며, 미확인 항목이 있으면 완료할 수 없다.
20. 이웃 아지트의 `neighbor_` 전용 표는 브라우저 역할과 `service_role`에 직접 권한을 주지 않는다. 공개 단계는
    기본 `internal`이며 실제 `profiles.role='ADMIN'` 관리자만 내부 화면과 단계 변경 RPC를 사용한다. 공간당 최대
    4학급·학급당 활성 공간 1개를 DB에서 강제하고, 모든 공유 글·댓글·반응·간직하기는 공간·학급·학생 복합
    외래키로 원래 소유 범위를 고정한다. 일반 교사·학생 RPC는 공개 단계, 실제 담당 학급, 활성 참여와 학급
    학생 스위치를 서버에서 다시 확인하며 기존 `student_posts` 학급 SELECT RLS를 넓히지 않는다. 초대 원문은
    혼동 문자를 뺀 키를 생성 응답 한 번에만 반환하고 SHA-256 해시만 저장한다. 24시간·일회성으로 제한하며,
    잘못된 키는 인증 계정별 10분 5회 뒤 30초 차단 원장에 예외 롤백 없이 기록한다. 학생 피드·상세는 공간 ID와
    글 ID를 알아도 현재 접근 조건과 `published` 상태를 다시 확인하고 원본 글/학생/학급 ID는 반환하지 않는다.
    초대·교사 승인으로 닫힌 공간 안에서는 참여 교사가 책임 있게 확인할 수 있도록 등록 학생 이름과 공개용
    학급명을 표시한다. 교사의 직접 전시는 자기 학급의 제출 완료 일반 글만 허용한다. 자율 글은 학급 공개 상태여야 하며 비공개 전환 시 이웃 공개도 회수한다. 전문 조회의 원글 버전과 공개/반려 시점의 버전을 서버에서 비교하며 기존 직접 검토 RPC는 내부 호출 전용으로 닫는다. 함께 쓰는 주제는 참여 학급만
    읽는다. 글짝 교환 명단은 활동별 SHA-256 불투명 키와 등록 이름만 호스트에게 최대 학급당 100명 반환하며 원본 학생
    ID는 내보내지 않는다. 호스트의 1:1·1:2 매칭안은 상대 학급 교사가 승인해야 하고, 그전에는 학생 요약·미션·직접
    피드 호출을 모두 막는다. `글짝 전용`은 서버가 배정한 본인·상대만, `두 학급 전체`는 해당 활동 참여 학급만
    목록·상세·댓글·반응에 접근한다. 공동 활동도 제안 학급 외 참여 교사 승인 전에는 열지 않는다. 활동·참여 학급·
    교사 승인·글짝 원장도 브라우저 직접 권한 없이 전용 RPC만 쓴다.
    마지막 피드 방문 원장도 직접 접근 없이 전용 피드 RPC에서만 갱신한다.
    한 줄 댓글은 한 학생·한 글 한 행, 줄바꿈 없이 1~300자로 제한하고 학생은 자기 댓글만 저장·삭제한다. 공감 1종과
    간직하기도 학생·현재 공개 글을 서버가 다시 확인하는 토글 RPC만 사용하며, 내 글 간직하기는 막는다. 참여 교사는
    문제 댓글을 숨길 수 있지만 작성 학급 교사만 복원하고, 숨김·삭제 댓글은 학생 합계에서 제외한다. 원글 회수나 공간
    종료는 본문 접근만 차단하고 댓글·공감·간직하기 행을 임의 삭제하지 않는다. 학생 순위·포인트는 연결하지 않는다.
    관리자 현황·내부 시험 생성·제한 공개 학급 선택·인수 점검·단계 전환 RPC는 JWT 관리자 표기 대신 실제
    `profiles.role='ADMIN'`을 확인한다. 내부 시험은 `internal`에서만 직접 접근 불가
    `neighbor_internal_test_classes` 등록부의 2~4학급·학생 공개 OFF로 만들며 실제 운영 학급은 후보와 생성
    RPC에서 모두 제외한다. `limited_beta`는 관리자가 고른 실제 교사 학급만 직접 접근 불가
    `neighbor_limited_classes`에 최대 8개까지 등록하고 두 학급 이상일 때만 시작한다. 제한 공개 후보에는 승인
    교사 학급과 현재 실제 `ADMIN`이 직접 소유한 학급만 포함하며, 다른 관리자 소유 학급과 내부 합성 시험 학급은
    제외한다. 교사·학생 RPC와 학생 홈
    bootstrap은 매번 현재 학급이 이 등록부에 있는지 서버에서 다시 확인한다. 학급 선택 해제는 그 학급의
    `neighbor-agit` 모듈과 학생 공개 스위치도 같은 트랜잭션에서 끄며, 선택·해제는 별도 비공개 감사 원장에
    남긴다. 관리자 미리보기는 최근 공간 20개·내부 시험 학급 100개·제한 공개 후보 100개·글 20편 상한과 안전
    필명만 반환한다. `public_beta` 전환은 여섯 인수 항목과 정확한 확인 문구를 서버에서도 요구하고 전·후 단계·
    점검표·변경자를 직접 접근 불가 감사 원장에 기록한다.
21. 우리 반 스크린의 학생 이름표는 로그인한 담당 교사·관리자의 편집 화면과 열린 스크린에만 최대 100명까지 반환한다.
    교사가 교실 화면에 제출 여부를 함께 보여 주는 기능이므로 이름은 허용하지만 학생 ID·인증 ID·글 ID·제목·본문·
    피드백·최근 제출 시각은 반환하지 않는다. 보드 저장 RPC와 비공개 이미지 Storage도 실제 담당 학급을 다시
    확인하며, 보드 JSON에는 살아 있는 학생 현황을 저장하지 않는다. 교사가 고른 현황 항목은 서버가 허용
    목록(`mission·daily·dailyNames·titles·reactions`)으로 다시 거르고, 켠 항목만 계산해 20초 폴링이
    무거워지지 않게 한다. 오늘 자율 글 명단과 새 칭호는 활성 학생 이름 최대 100명·칭호 30건까지만
    반환하며 학생 ID·보상 포인트·글 내용은 내보내지 않는다. 서로 읽어 준 정도는 승인된 댓글과 공감의
    개수만 세고 누가 무엇에 남겼는지는 반환하지 않는다. 배경색과 구성 항목도 저장 payload 검증이
    허용 목록과 중복 여부를 다시 확인한다.
22. 우리 반 스크린의 학생 무작위 뽑기도 실제 담당 교사·관리자만 전용 RPC로 활성 학생 이름 최대 100명을 한 번
    읽는다. 학생 ID·인증 ID·학생 코드와 뽑기 결과·순서는 반환하거나 보드 JSON에 저장하지 않는다. 날씨 위젯은
    기기 위치 권한을 사용하지 않으며 교사가 검색해 고른 국내 지역 이름과 공개 좌표만 저장한다. 브라우저는 CSP에
    명시한 Open-Meteo의 지오코딩·예보 HTTPS origin만 호출하고 학생·학급·사용자 식별자는 보내지 않으며 30분 캐시와
    무폴링을 지킨다. 오늘과 내일은 한 요청으로 함께 읽으므로 `내일`을 켜도 요청 수가 늘지 않고, 보여 줄 날은
    `today`·`tomorrow` 허용 목록과 중복 여부를 저장 payload 검증이 다시 확인한다. 신규 이미지 업로드는 클라이언트
    재인코딩과 비공개 Storage 양쪽에서 1MB 이하를 강제하되, 기존 1~2MB 이미지가 든 보드는 계속 수정할 수 있게
    저장 payload 호환만 유지한다.
23. 숨긴 우리 반 스크린의 목록과 복구도 브라우저의 테이블 직접 접근 없이 전용 RPC만 쓴다. 실제 학급의 담당
    교사 또는 관리자만 최대 20개의 제목·숨김 시각을 읽고, 복구 RPC는 해당 학급의 기존 활성 탭을 내린 뒤 선택한
    숨긴 화면 하나만 활성 탭으로 되돌린다. 익명 역할은 두 RPC를 실행할 수 없다.
24. 우리 반 스크린 식단표는 기존 `얘들아, 밥 먹자!` 담당 교사 작업공간 RPC로 학교와 공개 알레르기 표만 읽고,
    학생 이름·비고를 위젯에 전달하거나 보드 JSON에 저장하지 않는다. 나이스 키는 기존 Edge 함수 안에만 두며
    화면을 열 때 오늘 급식 1회만 요청한다. 알림장은 제목 80자·허용 색상만 보드 JSON에 저장하고
    서버 payload 검증을 통과해야 한다.
25. 우리 반 스크린 알림장의 내용은 보드 JSON이 아니라 `class_board_notices`(학급+날짜 기본 키)에 두고,
    브라우저의 표 직접 접근 없이 전용 RPC 두 개로만 읽고 쓴다. 두 RPC 모두 실제 담당 교사 또는 관리자만
    허용하고 익명 역할에는 열지 않는다. 읽기는 화면을 열 때 한 번, 지정 날짜 하나와 최근 날짜 최대 30개의
    날짜·미리보기 40자만 반환하며 작성자 ID나 다른 학급 내용은 내보내지 않는다. 쓰기는 본문 2,000자,
    오늘 기준 ±365일 날짜 범위를 서버에서 강제하고 빈 내용은 그 날짜 기록을 지운다. 알림 저장은 보드
    revision을 건드리지 않으므로 화면 배치 저장 권한과 분리된다. 알림장 도구의 날짜 목록도 같은 담당 교사
    경계를 쓰는 전용 RPC로만 읽고, 한 쪽 40개·미리보기 120자 상한과 날짜 커서를 서버에서 강제하며 본문
    전체나 작성자 ID는 목록에 담지 않는다. 표는 학급에만 매여 있어 스크린 탭이나 알림장 위젯을 지워도
    기록이 남고, 학급을 지울 때만 함께 지워진다. 교사가 도구 창에서 적고 교실 화면을 다른 창에 띄운
    경우를 위해 저장 결과를 같은 브라우저의 다른 창까지 `BroadcastChannel`로 전달한다. 서버나 저장소를
    거치지 않고 같은 기기·같은 출처 안에서만 오가며, 지원하지 않는 브라우저에서는 자기 창에서만 동작한다.
26. 글쓰기 보상 계산은 화면이 보낸 금액을 믿지 않는다. 기본 보상·1회 추가 보너스·구간별 반복 보너스를
    합치는 계산은 `calculate_writing_reward_total_v1` 하나만 쓰고, 이 함수는 `anon`·`authenticated`에
    열지 않는다(과제 승인·자율 글 확인·승인 알림 세 경로가 모두 같은 함수를 부른다). 반복 횟수 상한은
    서버가 20회로 묶는다. 학생이 제출하는 순간 그때의 보상 설정을 `student_posts`에 스냅샷한다(최소 글자 수도 함께 찍는다 —
    반복 구간이 여기서 시작하므로 이것만 빠지면 지급액이 흔들린다). 이후
    교사가 과제나 학급 설정을 바꿔도 이미 낸 글의 지급액은 달라지지 않는다. 학생 쓰기 가드는 스냅샷 열을
    학생이 직접 넣거나 고치지 못하게 막고, 스냅샷 트리거가 가드보다 뒤에 돌도록 이름 순서를 스모크가
    확인한다(순서가 뒤집히면 스냅샷이 조용히 지워진다).

## 실행

```bash
npm run test:security:static   # 코드 계약
npm run migrate:check         # 미적용 SQL + SQL 권한 스모크, 전부 ROLLBACK
npm run smoke:security-boundary # 적용된 핵심 역할 조합도 매번 ROLLBACK 재검사
npm run smoke:writing-references # 과제↔연구소 연결의 교사·학생·다른 학급 경계를 ROLLBACK 재검사
npm run smoke:learning-content # 공통 학습 원본·문제·묶음의 승격 제약과 직접 권한을 ROLLBACK 재검사
npm run test:security:ops      # 맥미니 포트·권한·Edge 함수 허용 목록
npm audit --omit=dev           # 운영 의존성
```

운영 DB 적용 뒤에는 해당 SQL 스모크를 `scripts/run-rollback-smoke.mjs`로 한 번 더 실행한다.
