# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea

# 개인 도구 권한 설정 (사람마다 다를 수 있어 공유 저장소에는 안 올림)
.claude/settings.local.json
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Test artifacts
playwright-report/
test-results/
testsprite_tests/tmp/

# Supabase CLI local metadata
supabase/.temp/

# Environment variables
.env
.env.*
.env_backup
*_backup
*.bak
*.config

# 관리자 계정 정보가 포함될 수 있는 마이그레이션 (Git에 올리지 말 것)
supabase/migrations/20260224_final_security_fix.sql

# 편지지·PDF 미리보기 등 눈으로 확인하려고 뽑아 둔 산출물
output/

# 눈으로 확인하려고 잠깐 만든 스크립트·화면 사진. 확인이 끝나면 지운다.
# 실제로 여러 날 남아 저장소를 어지럽힌 적이 있어 아예 올라가지 않게 막는다.
*.tmp.mjs
*.tmp.json
