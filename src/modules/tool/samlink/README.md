# QR 코드 관리 (쌤링크)

교사 `학급운영도구` 런처에는 `QR 코드 관리`로 표시하고, 안쪽에서 외부 쌤링크 서비스를 임베드하거나 새 창으로 여는 도구 모듈이다.
`manifest.js`가 표시 순서와 실행 방식을, `TeacherEntry.jsx`가 실제 진입 화면을 소유한다.

인증 정보나 비밀 값을 프론트에 넣지 않는다. 임베드가 차단되거나 외부 서비스가 응답하지 않아도 다른 교사 화면은
공통 오류 경계 안에서 계속 동작해야 한다.
