# 이웃 아지트

다른 학급과 글을 나누는 후속 독립 모듈이다. 학급 생성·삭제를 담당하는 `ClassManager` 안에 기능 카드를 넣지 않는다.

현재는 `manifest.settingsEntry`로 교사 설정의 독립 진입점만 제공한다. 초대·양쪽 교사 승인·학생 피드·댓글·공감·간직하기는
DB와 Edge 함수 작업이 가능한 환경에서 전용 테이블/RPC를 설계한 뒤 연결한다.
