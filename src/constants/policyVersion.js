/*
 * 약관·개인정보 처리방침의 **현재 판**. 단 하나의 자리.
 *
 * 왜 상수로 두나 (2026-09-11):
 *   교사가 가입할 때 "어느 판에 동의했는지"를 DB 에 남긴다. 판 이름이 화면 문구·처리방침 머리말·
 *   동의 기록 세 곳에 흩어지면 하나만 고쳐져 "동의한 판"이 실제 시행판과 어긋난다.
 *   개정할 때는 **이 값과 처리방침 본문의 개정 이력**을 함께 올린다.
 *   `tests/policyConsent.test.mjs` 가 두 곳이 같은지 본다.
 *
 * 형식은 시행일(YYYY-MM-DD)이다. 처리방침 제12조가 "시행 7일 전 공지"를 약속하므로
 * 공지일로부터 7일 뒤 이후여야 한다.
 */
export const POLICY_VERSION = '2026-09-21';
