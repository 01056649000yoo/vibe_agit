/**
 * 서비스 정체성 문구의 단일 원본.
 *
 * 왜 모았나: 로그인 화면에서 이 앱이 무엇인지 말하는 문장이 세 군데에 흩어져 서로 다른 말을 했다
 * (2026-08-28 점검). 큰 제목은 `쓰고, 읽고, 키우며`, 소개 카드는 `다양하게 쓰기`, 정작 정체성을
 * 가장 정확히 말하는 `글쓰기를 중심으로…` 는 히어로 이미지의 `alt` 안에만 있어 **눈에 보이지 않았다.**
 * 그 결과 화면에 `글쓰기` 라는 낱말이 한 번도 나오지 않았다.
 *
 * 저장소가 말하는 정체성은 세 곳에서 같다.
 *   README.md               — 초등 글쓰기 통합 플랫폼
 *   ROADMAP.md 제품 3대 기둥 — ①교사의 글쓰기 지도 ②학생의 자율 글쓰기·과제 ③포인트 동기부여
 *   학습지원소프트웨어 안내   — 글쓰기 활동을 기반으로 한 학급 경영·게이미피케이션
 *
 * 문구를 바꿀 때는 여기만 고친다. `tests/landingPage.test.mjs` 가 화면에 보이는 자리에
 * `글쓰기`·`초등` 이 남아 있는지 지킨다.
 */

/** 큰 제목 바로 아래 — **이 앱이 무엇인지** 한 줄로 말한다. 눈에 보이는 자리여야 한다. */
export const SERVICE_IDENTITY_LINE = '선생님이 지도하고 학생이 꾸준히 쓰는, 초등 학급 글쓰기 공간이에요.';

/** 브라우저 탭과 검색 결과에 쓰는 기본 제목. */
export const SERVICE_PAGE_TITLE = '끄적끄적 아지트 | 초등 학급 글쓰기 지도 플랫폼';

/** 서비스 이름에 쓰인 `아지트` 가 무슨 뜻인지. 처음 온 사람은 이 말을 모른다. */
export const AGIT_MEANING = '아지트는 학생마다 하나씩 갖는 나만의 글쓰기 공간이에요';

/** 히어로 이미지 대체 텍스트. 눈에 보이는 문장과 같은 것을 말해야 한다. */
export const HERO_IMAGE_ALT = `끄적끄적 아지트 — ${SERVICE_IDENTITY_LINE}`;
