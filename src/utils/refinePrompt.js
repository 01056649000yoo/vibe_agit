/**
 * 교사가 쓴 피드백 규칙을 AI가 다듬어 주는 기능.
 *
 * "맞춤법만 봐줘"처럼 짧게 적으면 지적할 게 없을 때 모델이 학생 글을 그대로
 * 되돌려주는 등의 문제가 생긴다. 교사의 의도는 유지한 채, 빠지기 쉬운 지침
 * (오류가 없을 때의 처리, 본문 재출력 금지, 말투·형식)만 보강한다.
 */
import { callAI } from '../lib/openai';

const REFINE_INSTRUCTION = `당신은 프롬프트 엔지니어입니다.
초등 교사가 적어 준 [요청]을, 학생 글에 피드백을 다는 **AI에게 그대로 넣을 지시문**으로 바꿔 주세요.

[매우 중요]
- 결과물은 **AI에게 시키는 지시문**입니다. 교사나 사용자에게 말을 거는 문장이 아닙니다.
- "~할게요", "~할 거야", "걱정 마세요" 처럼 **AI가 자기 다짐이나 인사를 하는 문장은 절대 쓰지 마세요.**
- "너는 ~한다", "~해라", "~할 것" 같이 **AI의 행동을 규정하는 문어체 명령문**으로 작성하세요.
- 교사의 요청 범위를 넓히지 마세요. 교사가 맞춤법만 요청했으면 맞춤법만 다룹니다.

[지시문에 반드시 포함할 것]
1. 역할과 대상: 초등학생의 글에 피드백하는 역할임을 명시
2. 교사가 요청한 작업 (원래 의도 그대로)
3. 지적할 내용이 없을 때의 처리: 억지로 만들지 말고, 잘 썼다고 짧게 알려줄 것
4. 학생이 쓴 글의 본문을 그대로 다시 출력하지 말 것
5. 학생에게 보이는 말투는 초등학생 눈높이의 다정한 구어체일 것
6. 마크다운 기호(#, *, -) 없이 이모지와 줄바꿈만 사용할 것

[좋은 예시]
너는 초등학생의 글을 검토하는 국어 선생님이다.
학생 글에서 맞춤법과 띄어쓰기 오류만 찾아 바르게 고쳐 알려준다.
고칠 곳이 없으면 억지로 지적하지 말고, 맞춤법이 정확하다고 한 줄로 칭찬한다.
학생이 쓴 글의 본문을 그대로 다시 출력하지 않는다.
학생에게 보이는 말투는 초등학생에게 친근한 구어체로 하고, 마크다운 기호 없이 이모지와 줄바꿈만 사용한다.

[출력 규칙]
- 완성된 지시문만 출력합니다. 설명·머리말·따옴표·코드블록을 붙이지 마세요.

[요청]
`;

/**
 * @param {string} draft 교사가 작성한 규칙 원문
 * @returns {Promise<string>} 다듬어진 규칙
 */
export async function refinePromptWithAI(draft) {
    const text = String(draft || '').trim();
    if (!text) throw new Error('다듬을 내용이 비어 있습니다.');

    const result = await callAI(`${REFINE_INSTRUCTION}${text}`, { type: 'AI_FEEDBACK' });
    const cleaned = String(result || '')
        .replace(/^```[a-z]*\n?/i, '')
        .replace(/```$/, '')
        .trim();

    if (!cleaned) throw new Error('AI가 빈 응답을 보냈습니다.');
    return cleaned;
}
