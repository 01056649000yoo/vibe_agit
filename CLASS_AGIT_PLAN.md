# 우리반 아지트 계획

> **상태**: 방향 확정, 구현 전 (2026-08-30)
>
> **한 문장 정의**: 우리 반 학생들의 글을 골라 가벼운 2.5D 전시관에 전시하고, 한 학기·한 해의
> 학급 문집으로 편집해 PDF로 내보내는 학급 출판 공간이다.

이 문서는 `우리반 아지트`의 제품 범위·공개 방식·구현 순서의 정본이다. 진행 여부와 큰 단계는
[`ROADMAP.md`](ROADMAP.md), 실제 작업 이력은 [`WORKLOG.md`](WORKLOG.md)에 남긴다.

## 1. 제품 경계

### 하는 일

1. **우리반 글 전시관** — 교사가 학급·학기·주제별 전시를 여러 개 만들고, 고른 학생 글을 실제
   전시실처럼 둘러보고 읽는다.
2. **학급 문집 제작실** — 전시 글 또는 완료된 글을 골라 한 권으로 편집하고 PDF로 내보낸다.
3. **학기·학년도별 보관** — 지난 전시와 발행한 문집을 학급의 글쓰기 기록으로 남긴다.
4. **외부 읽기 전용 전시** — 교사가 명시적으로 선택한 전시 한 편만 만료·해지가 가능한 링크로 공유한다.

### 하지 않는 일

- 일반 학급 게시판·공지·투표·설문·채팅
- 학생 인기 순위나 경쟁형 투표
- 자유 이동 아바타·다중 사용자 위치 동기화 같은 무거운 메타버스
- 글쓰기와 무관한 학급 운영 기능

`우리반 아지트`는 내 아지트·친구 아지트·드래곤 아지트와 같은 세계관을 쓰지만 역할은 겹치지 않는다.

| 공간 | 역할 |
|---|---|
| 내 아지트 | 학생 개인의 글과 성장 |
| 친구 아지트 | 학급에 공개한 새 글 탐색과 관계 |
| 드래곤 아지트 | 작가·독자 성장의 시각화와 동기부여 |
| **우리반 아지트** | 교사가 큐레이션한 학급 글의 전시와 문집 발행 |

## 2. 전체 흐름

```text
학생이 글 작성·제출
        ↓
교사가 전시 후보를 선택
        ↓
전시실 구성·학생 화면 공개
        ↓
전시 글 또는 완료 글에서 문집 작품 선택
        ↓
표지·목차·작품 순서 편집
        ↓
내용 확정 → PDF 내보내기 → 발행본 보관
```

전시는 현재 글을 보여 주는 공간이고, 문집은 특정 시점의 글을 고정해 오래 남기는 발행물이다.

## 3. 공개와 권한: 서로 독립된 공개 상태

운영 서버에 배포하는 것과 실제 사용자에게 공개하는 것을 분리한다.

### 3.1 서비스 공개 단계 — 관리자 소유

| 단계 | 교사 노출 | 학생 노출 |
|---|---|---|
| `internal` | 관리자만 메뉴·미리보기 확인 | 항상 차단 |
| `pilot` | 허용 목록의 교사·학급만 스위치 확인 | 교사가 켠 허용 학급만 |
| `public_beta` | 모든 교사가 스위치 확인 | 교사가 켠 학급만 |
| `general` | 모든 교사가 사용 | 교사가 켠 학급만 |

- 초기값은 `internal`이다.
- 시범 교사·학급은 서버의 관리자 전용 설정에 저장하고 이메일·학급 ID를 프론트에 하드코딩하지 않는다.
- 관리자 판정은 JWT 문구만 믿지 않고 DB의 실제 관리자 연결 상태로 확인한다.

### 3.2 학급별 사용 스위치 — 담당 교사 소유

- 모듈 ID는 `class-agit`으로 두고 기존 `classes.enabled_modules` 계약을 재사용한다.
- 기존·신규 학급 모두 기본값은 OFF다.
- OFF여도 교사는 전시와 문집 초안을 만들 수 있고 기존 자료는 삭제되지 않는다.
- OFF이면 학생 카드가 사라지고 학생용 조회 RPC도 거부한다.
- 관리자의 공개 단계가 허용하지 않으면 교사가 스위치를 켜도 학생에게 노출되지 않는다.

### 3.3 콘텐츠 공개 상태 — 담당 교사 소유

- 전시: `초안 → 공개 → 지난 전시`
- 문집: `편집 중 → 내용 확정 → 내보내기 완료`, 필요할 때만 학생 서가에 공개
- 학급 스위치가 ON이어도 공개 상태인 전시·문집만 학생이 볼 수 있다.

학생 노출의 최종 조건은 다음과 같다.

```text
관리자 공개 허용
AND 학생 소속 학급의 class-agit ON
AND 공개된 전시 또는 문집 존재
```

UI를 숨기는 데서 끝내지 않고 학생·교사·관리자 RPC가 같은 조건을 다시 검사한다.

### 3.4 외부 읽기 전용 링크 — 전시별 교사 선택

학생 앱 공개와 외부 링크 공개는 서로 다른 선택으로 둔다.

- 교사는 자신이 만든 전시마다 `외부 공유 OFF/ON`을 따로 결정한다. 기본값은 OFF다.
- 교사 한 명은 담당 학급별로 전시를 여러 개 만들고, 여러 전시를 동시에 보관할 수 있다.
- 전시에는 `class_id`와 `creator_teacher_id`를 함께 두어 학급 범위와 만든 교사를 모두 확인한다.
- 학생 화면 스위치가 OFF여도 교사는 외부 공유를 별도로 켤 수 있지만, 관리자 공개 단계와 외부 공유
  전체 중지 설정은 항상 우선한다.
- 링크는 로그인 없이 전시 한 편의 **공개 스냅샷만** 읽는다. 원본 글·학생 프로필·다른 전시로 이동하지 않는다.
- 외부 공유를 켤 때 현재 전시의 제목·소개·작품 제목·본문·표시 이름·배치를 스냅샷으로 고정한다.
  이후 원문 수정은 자동 반영하지 않고 교사가 `공개본 새로 반영`을 눌러야 한다.
- 기본 만료는 30일이며 교사가 만료일을 바꾸거나 즉시 해지·링크 재발급할 수 있다. 만료 없음은 경고를
  다시 확인한 경우만 허용한다.
- 링크 토큰은 충분히 긴 무작위 값으로 만들고 DB에는 원문 대신 해시를 저장한다.
- 외부 공개본은 학생 실명 대신 필명·가림 이름을 기본으로 하며 학교명·학급·내부 ID를 자동으로 싣지 않는다.
- `noindex`, `nofollow`, `noarchive`와 캐시 제한을 적용하고 공개 조회에 속도 제한을 둔다.
- 외부 방문자는 읽기만 가능하다. 댓글·반응·로그인 이동·원본 파일 접근·교사용 기능은 제공하지 않는다.
- 교사 관리 화면에는 링크 복사·만료일·즉시 해지·재발급·마지막 열람 시각·누적 조회 수만 보여 준다.
  방문자의 IP·기기 정보는 별도 분석 자료로 보관하지 않는다.

외부 노출의 최종 조건은 다음과 같다.

```text
관리자 외부 공유 허용
AND 전시 공개 스냅샷 존재
AND 전시별 외부 공유 ON
AND 링크 미만료·미해지
```

## 4. 교사 화면

현재 `우리반 아지트` 준비 화면에는 확정된 네 가지 업데이트 개요만 표시한다. 실제 구현 단계에서는 이를
다음 구조로 바꾼다.

### 공통 상단

- 선택한 학급 이름
- `학생 화면 사용` 스위치
- 현재 공개 상태와 관리자 공개 단계 안내
- `학생 화면 미리보기`
- 공용 `TeacherGuideButton` 도움말

### 탭 1 — 글 전시관

- 학급·학기·주제별 여러 전시 만들기·복제·지난 전시 보관
- 내 전시 목록에서 초안·학생 공개·외부 공개·만료 상태를 한눈에 확인
- 전시 제목·소개·기간·전시실 테마 선택
- 전시 가능한 글 검색·선택
- 전시실과 벽면별 작품 배치·순서 조정
- 공개 전 학생 화면 미리보기
- 초안/공개/지난 전시 상태 변경
- 전시별 외부 읽기 전용 링크 발급·복사·만료 변경·해지·재발급

### 탭 2 — 학급 문집

- 새 문집 만들기
- 전시 작품 또는 완료 글에서 수록 작품 선택
- 학생별·장르별·주제별 묶기와 작품 순서 조정
- 문집 제목·부제·표지·학기·발행일 설정
- 교사 여는 글·반 소개 추가
- 목차 자동 생성과 인쇄 미리보기
- 내용 확정·PDF 내보내기·발행본 보관

## 5. 학생 화면

- 학생 홈 대시보드에 `우리반 아지트` 카드 한 칸을 **조건부**로 추가한다.
- 이미 하단 메뉴가 여섯 개이므로 1차에는 일곱 번째 하단 탭을 만들지 않는다.
- 홈 카드 때문에 별도 DB 조회를 추가하지 않는다. 필요한 최소 공개 여부는 기존 학생 홈 bootstrap에
  함께 싣고, 전시 상세는 카드를 실제로 열 때 지연 조회한다.
- 입장 뒤에는 `현재 전시`, `지난 전시`, 공개된 `우리반 문집 서가`만 보여 준다.
- 전시 작품을 누르면 글 전문을 읽고 원래 위치로 돌아온다.
- 댓글·순위·투표는 1차 범위에서 제외한다. 반응이 필요해지면 기존 글 반응 모듈의 한 글 한 반응 계약을 재사용한다.

## 6. 2.5D 전시관 방식

자유 이동형 3D가 아니라 브라우저에 맞는 **CSS·DOM 기반 2.5D 전시실**로 만든다.

- 원근감 있는 벽·바닥 배경 위에 작품 액자 DOM을 배치한다.
- 화살표·미니맵·가로 이동으로 전시실 사이를 이동한다.
- 액자를 누르면 읽기 쉬운 글 상세 모달 또는 상세 화면을 연다.
- 배경 자산은 최적화한 WebP/AVIF를 사용하고 전시실 진입 때 지연 로딩한다.
- 전시실 한 곳은 최대 12편, 첫 목록은 최대 100편 계약보다 더 작은 전용 상한을 둔다.
- 상시 폴링·Realtime·WebSocket·아바타 위치 동기화는 사용하지 않는다.
- 저사양 기기·동작 줄이기 설정에서는 원근 애니메이션을 줄이고 같은 내용을 카드형 목록으로도 접근할 수 있게 한다.

## 7. 작품 원본과 발행본 원칙

- 제출되지 않은 초안과 비공개 글을 자동으로 후보에 포함하지 않는다.
- 일기 등 민감할 수 있는 글은 학생이 학급 공개로 설정했거나 별도 동의를 거친 경우만 전시·수록한다.
- 교사는 작품을 선정하고 배치하지만 학생의 원본 글을 이 공간에서 임의로 고치지 않는다.
- 전시는 원본 글 ID를 참조하고, 원문 변경을 전시에 반영할지는 교사가 확인한다.
- 문집은 `내용 확정` 때 제목·본문·작성자 표시·배치 정보를 스냅샷으로 고정한다.
- 확정 뒤 원문이 바뀌어도 발행본은 바뀌지 않는다. 수정이 필요하면 새 판을 발행한다.
- 학생 이름 표시 방식과 표지의 학급 정보는 PDF 확정 전에 교사가 미리보기로 확인한다.
- 외부 전시는 원본을 직접 익명 조회하지 않고, 교사가 확인한 공개 스냅샷만 별도 읽기 경로로 제공한다.
- 글이 삭제·비공개로 바뀌어도 기존 외부 공개본을 몰래 바꾸지는 않되 교사에게 재확인 대상으로 표시하고,
  교사·관리자는 링크를 즉시 해지할 수 있다.

## 8. 기술 경계

### 모듈

- 구현은 `src/modules/class-agit/` 아래에 둔다.
- 공통 셸은 공개 단계·학급 스위치·지연 로딩·오류 격리만 담당한다.
- 전시관과 문집은 각각 자기 화면·데이터 API·내보내기를 소유한다.
- 기존 글쓰기 코어와 연구소 코드는 수정·이식하지 않고 승인된 글 ID와 RPC로만 연결한다.

### 데이터와 조회

- 전시·문집·수록 항목은 모두 해당 테이블의 `class_id`로 직접 제한한다.
- 학급 테이블끼리 조인할 때도 `class_id`를 조인 조건에 넣는다.
- 전시 목록은 안정 정렬·페이지 상한을 갖고, 작품 전문은 열 때 가져온다.
- 학생 홈에 새 `supabase.from()`·`rpc()`를 직접 추가하지 않는다.
- 캐시는 `dataCache`와 `classKey()` 계약을 사용한다.
- 외부 브라우저에는 전시·작품·학생 표 직접 권한을 주지 않는다. 해시 토큰을 검증하는 전용 읽기 경로가
  상한이 적용된 공개 스냅샷만 반환한다.
- 공개 링크 하나가 반환하는 전시는 최대 5개 전시실·전시실당 12편으로 제한하고 작품 전문은 액자를 열 때
  별도 상한 조회한다.
- 1차 외부 공유는 텍스트 작품과 서비스가 제공하는 전시 배경만 지원한다. 학생 사진이 포함된 장르의 외부
  공유는 공개용 복사본·동의·해지 시 자산 정리가 설계된 뒤 별도 단계로 연다.

### PDF

- 1차 내보내기는 A4 PDF 한 가지에 집중한다.
- 12pt 미만으로 글자를 줄이지 않고 긴 글은 자연스럽게 다음 페이지로 넘긴다.
- 표지·목차·작품·교사 여는 글·반 소개의 페이지 구조를 전용 내보내기 모듈이 소유한다.
- Google Docs·DOCX·주문 인쇄 연동은 실제 요구가 확인된 뒤 별도 단계로 판단한다.

## 9. 구현 단계

### Phase 0 — 계약과 관리자 내부 셸

- [ ] `class-agit` 매니페스트·성능표·공개 단계의 단일 원본 정의
- [ ] 현재 일반 교사에게 보이는 Beta 메뉴를 `internal`에서 관리자 전용으로 전환
- [ ] 관리자 시험 학급 선택과 학생 화면 미리보기
- [ ] 학급 스위치·학생 카드·RPC 접근 조건의 회귀 검사부터 작성

### Phase 1-a — 여러 전시 데이터와 권한

- [ ] 교사·학급별 여러 전시와 전시 작품 데이터, 안정 정렬·페이지 상한
- [ ] 담당 교사·학급 범위 RLS/RPC와 다른 교사·다른 학급 실패 검사

### Phase 1-b — 교사 편집과 학생 기본 보기

- [ ] 교사 작품 선택·배치·공개 화면
- [ ] 학생 홈 조건부 카드와 지연 로딩 전시관

### Phase 1-c — 2.5D 전시실

- [ ] CSS·DOM 2.5D 기본 테마 1종과 접근 가능한 카드형 대체 보기
- [ ] 관리자 내부 PC·태블릿·모바일 점검

### Phase 1-d — 외부 읽기 전용 공유

- [ ] 공개 스냅샷·해시 토큰·기본 30일 만료·즉시 해지/재발급
- [ ] 로그인 없는 전시 한 편 읽기 화면과 검색 차단·속도 제한
- [ ] 가림 이름 기본값·내부 ID 미노출·익명 표 직접 권한 없음 검사
- [ ] 관리자 외부 공유 전체 중지와 교사 링크 관리 화면

### Phase 2 — 학급 문집 MVP

- [ ] 문집 프로젝트·작품 선택·정렬·목차·표지
- [ ] 확정 시점 콘텐츠 스냅샷과 판 관리
- [ ] A4 미리보기·PDF 내보내기
- [ ] 발행본 보관과 선택적 학생 문집 서가 공개

### Phase 3 — 시범 운영

- [ ] 지정 교사·학급 1~2곳을 `pilot` 허용 목록에 등록
- [ ] 교사가 OFF→ON을 직접 선택하고 실제 학생 흐름 점검
- [ ] 학급 간 권한, 저사양 기기, 긴 글 PDF, 기능 OFF 후 자료 보존 확인
- [ ] 시범용 외부 링크의 만료·해지·재발급·검색 차단·모바일 읽기 확인
- [ ] 오류·사용성 보완 뒤 공개 베타 진입 여부 결정

### Phase 4 — 공개 베타와 정식 운영

- [ ] 모든 교사에게 스위치를 기본 OFF로 공개
- [ ] 사용 학급·전시 입장·PDF 실패율을 개인정보 없이 집계
- [ ] 안정화 뒤 Beta 표시 제거 여부 결정
- [ ] 하단 메뉴 편입·추가 테마·반응 기능은 실제 사용 빈도를 보고 판단

## 10. 첫 공개 합격선

- `internal`에서 일반 교사·학생은 메뉴, 카드, 직접 URL, RPC 모두 접근할 수 없다.
- `pilot`에서 허용하지 않은 학급은 노출되지 않는다.
- 교사 스위치 OFF 즉시 학생 카드와 학생 조회가 막히고 자료는 보존된다.
- 다른 학급의 전시·문집·학생 글을 ID 추측으로 읽거나 바꿀 수 없다.
- 한 교사가 여러 전시를 만들고 초안·현재 공개·지난 전시를 섞지 않고 관리할 수 있다.
- 외부 링크는 지정한 공개 스냅샷 한 편만 읽고 원본·학생 프로필·다른 전시로 이동할 수 없다.
- 외부 링크를 해지하거나 만료시키면 기존 주소가 즉시 더 이상 내용을 반환하지 않는다.
- 외부 공개본은 기본적으로 학생 실명·학교명·학급명·내부 ID와 사진을 포함하지 않는다.
- 학생 홈에 추가 네트워크 요청·폴링·상시 WebSocket이 생기지 않는다.
- 전시실 첫 진입은 목록 RPC 한 번과 최대 12편으로 끝난다.
- PC·태블릿·좁은 모바일에서 전시와 글 읽기, 홈 복귀가 가능하다.
- 문집의 선택→정렬→미리보기→내용 확정→PDF 다운로드가 한 흐름으로 끝난다.
- 확정 문집은 학생 원문의 후속 수정으로 바뀌지 않는다.

## 11. 구현 전에 마지막으로 정할 항목

아래는 방향을 바꾸지 않는 세부 선택이므로 Phase 0에서 화면 시안과 함께 확정한다.

1. 2.5D 첫 전시실의 시각 테마와 한 화면 액자 수
2. 학생 이름을 실명·이름 일부·필명 중 무엇으로 기본 표시할지
3. 문집 기본 판형(A4 세로 기준)과 표지 기본 양식
4. 문집 PDF를 학생 서가에 직접 보여 줄지, 교사 다운로드만 먼저 열지
5. 외부 공개 링크의 만료 없음 선택을 정식 운영에도 유지할지
