# 우리반 아지트 구현 계획

> **상태: 2026-09-05 C0~C4와 C5 제한 운영 제어를 구현·검증하고 배포 대상으로 확정했다. 운영 SQL `20261241`을 적용하고 진남초 4학년 1반만 시범 학급·학급 ON으로 설정했다. 실제 학급 인수는 남아 있다.**
> 목표는 **학생 글을 전시하는 2.5D 전시관 → 학급 문집 제작 → 읽기 전용 전시의 외부 공유**다.
> `/?dev-lab=class-agit`은 C0 전시실 시안, `/?dev-lab=class-agit-persistence`는 C1 저장·공개·충돌·재확인 샘플, `/?dev-lab=class-agit-student`는 C2 학생 감상 샘플이다. `/?dev-lab=class-agit-release`는 문집·공유·제한 운영 통합 샘플이다. 이 샘플들은 실제 운영 학급 설정과 별개다. 다른 학급과 외부 공유는 OFF다.

### 현재 운영 설정 (2026-09-05)

- 사용자 지정 학급: 여수진남초등학교 **진남초 4학년 1반** (`a4b4512b-6378-4edf-aa25-58e46c82c87a`).
- `class_agit_rollout`: `mode=pilot`, `external_enabled=false`, `revision=2`. 허용 목록은 해당 학급 1개다.
- 해당 학급의 기존 `__configured__`, `dragon`, `vocab-tower`를 유지하고 `class-agit`만 추가했다. 다른 학급의 모듈 설정은 변경하지 않았다.
- 우리반 SQL `20261241`과 허용 학급·학급 ON을 한 트랜잭션으로 반영했다. 이웃 SQL `20261240`도 배포 전 적용해 전체 284개·대기 0이다.
- 학생 글을 자동 수록·발행하지 않았다. 학생 홈 카드는 교사가 전시나 문집을 공개한 뒤 나타난다.
- 사용자 배포 요청으로 새 화면을 배포 대상으로 확정했다. 실제 교사·학생 계정과 학교망 인수는 후속 작업이다.

이 문서는 우리반 아지트의 제품 범위·사용 과정·권한·구현 순서 정본이다.
연결 기능은 [이웃 아지트 점검 계획](NEIGHBOR_AGIT_PLAN.md), 기존 이력은 [WORKLOG.md](WORKLOG.md)를 참조한다.

## 1. 지금 있는 것과 새로 만들 것

| 영역 | 현재 코드에서 확인한 상태 |
|---|---|
| 교사 메뉴 | `teacherNav.js`의 `class-agit`, 연구소 옆 메뉴·BETA 표시 |
| 교사 진입 | `TeacherClassAgitHub.jsx`는 일반 교사 준비 안내를 유지하고 허용된 관리자/시범 교사에게 모듈 화면을 지연 로딩한다. 서버가 실제 승인·현재 담당·공개 단계를 다시 검증 |
| 실제 모듈 | `src/modules/class-agit/`의 후보 페이지·전문·초안 저장·공개판·철회 연결과 C0 공용 2.5D 뷰어. SQL `20261241` 운영 적용 완료·새 화면 배포 대상 확정 |
| 재사용 가능 기반 | 기존 글쓰기 원본·장르 매니페스트·학생 메뉴/뒤로가기·bootstrap·학급 모듈 ON/OFF·공용 도움말 |
| 출력 기반 | 기존 `writingPdfExport.js`의 장르별 렌더 계약과 A4 인쇄. 문집 프로젝트·확정판과 실측 페이지·차례 쪽수 연결 |
| 검사 | `test:class-agit`의 화면/저장 계약 검사, `smoke:class-agit`의 합성 계정·실제 역할별 RPC 롤백 검사. 브라우저 샘플은 DB 권한 검사를 대체하지 않음 |

준비 화면과 실제 기능 접근을 구분한다. 일반 교사에게 보이는 준비 안내를 유지할지는 UI 검토 때 정하고,
실제 편집·학생 조회·외부 공유는 관리자 내부 확인부터 시작한다.

### C0 로컬 구현 범위

- [x] 기본 OFF 매니페스트, 후보/초안/표시 데이터의 모듈 계약과 교사 공용 도움말.
- [x] 일반 과제 글·시를 검색하고 전문 확인 뒤 학급 수록 확인을 거쳐 담기. 시의 기존 연·행 변환 재사용.
- [x] 밝은 벽·나무 바닥 테마, 12편씩 최대 5개 방, 순서 변경·삭제·선정되지 않은 학생 표시.
- [x] 로비→전시실/목록→평면 전문, 글자 크기 조절, 좁은 화면의 큰 액자 가로 탐색.
- [x] 외부 수록 확인과 가림 이름을 별도로 지정한 뒤 읽기 화면 미리보기. 실제 링크 발급은 없음.
- [x] 0/1/12/60편 개발 시나리오와 전용 자동 검사. 초안은 화면 메모리에만 보관.
- [x] 후속 진행 요청에 따라 C1 실제 데이터·권한·저장/공개판으로 연결(로컬).

초안 편집의 revision과 미리보기 복사본은 C0 계약이며 서버 동시 편집·공개판 저장을 뜻하지 않는다.
현재 본문은 확인된 글의 `title/content/structured_content`를 사용한다. 교사 수정 제안이나 처음 원문으로
자동 교체하지 않으며, C1 실제 RPC에서 표시본과 원글 버전 재확인을 연결했다.

로컬 검증: 전용 30개·아키텍처 272개 통과, 변경한 구현/새 검사 lint 0경고, Vite 빌드 통과.
전체 lint는 오류 0건이며 기존 영역 경고 39건이 남아 있다. 브라우저에서 0/1/12/60편,
작품 담기→학생 열람→외부 가림 이름, 시 연/행, 390px 긴 글(24문단), Escape·Tab 순환·액자 초점 복귀를 확인했다.
외부 범위 필터를 제거한 변이도 회귀 단언이 탐지함을 확인했다. 운영 DB·공개 설정은 변경하지 않았다.

### C1 로컬 구현 범위

- [x] 전시 20개·전시당 60편, 초안/공개/보관 상태, 서버 revision과 생성 요청 ID.
- [x] 과제 글·시 후보 20편 페이지(서버 최대 50), 제목/학생 검색, 선택한 글의 전문·원글 해시 재확인.
- [x] 클라이언트의 본문을 저장하지 않고 서버가 실제 확인된 `title/content/structured_content`로 표시본 생성.
- [x] 학급 수록 확인·철회 원장과 현재 공개판 고정. 초안 삭제/순서 변경은 다음 갱신에만 반영.
- [x] 원글 수정 시 공개 갱신 차단, 전문 재확인·저장·갱신으로 복구. 회수/삭제/학생 비활성·수록 철회는 다음 조회부터 차단.
- [x] 철회 후 재확인해도 이전 공개판이 자동 부활하지 않도록 수록 확인 식별자를 교체.
- [x] 승인된 관리자 본인 학급에서만 내부 운영, 학생 공개 기본 OFF, 다른 학급·일반 교사·익명 접근 차단.
- [x] 공개 중단·보관·복원, 저장 충돌 시 입력 유지, 미저장 편집을 버릴 때 확인.
- [x] 공용 2.5D 장면으로 실제 RPC 공개판을 방당 12편씩 확인하는 교사 열람 화면과 DB 없는 동일 화면 샘플.
- [x] 사용자 지정 시범 학급 활성화를 위해 운영 DB `20261241` 적용.
- [ ] 앱 배포 후 실제 로그인·실기기 인수.

서버 표는 `class_agit_rollout`, `class_agit_exhibitions`, `class_agit_items`, `class_agit_consent_events`다.
모든 표 직접 권한을 닫고 전용 RPC만 연다. 방은 저장된 순서에서 12편씩 계산하므로 별도 방 표가 없다.
현재 학급 공개판 1개와 판 번호를 보관하며 갱신 시 교체한다. **과거 공개판 전체 보관이나 문집 판 이력은 아직 없다.**
수록 범위 중 C1은 `class`만 저장한다. 외부 가림 이름 시안은 C0에 남겨 두며 외부 동의·토큰 구현을 뜻하지 않는다.

C1 교사 공개판 확인은 요청당 한 방 12편의 전문까지 받는다. C2 학생용 조회는 방 요약과 선택한 작품 1편의 전문으로 분리했다.
학급 전환은 편집 호스트를 새로 만들며, 현재 저장 캐시는 두지 않고 쓰기 후 공용 학급 캐시를 무효화한다.
운영 마이그레이션은 2026-09-05 적용했다. 검증 명령과 경계는 모듈 README에 정리한다.

### C2 로컬 구현 범위

- [x] 공용 홈 bootstrap에 `class_agit_available` Boolean만 추가. 홈 전용 추가 요청 없이 조건부 카드·사용법을 연결.
- [x] 전시 목록 최대 20개 → 로비 → 방당 최대 12편 요약 → 선택한 작품 1편 전문. 하단 메뉴 6개 유지.
- [x] 방·전시실/목록 보기·이전/다음 작품은 같은 방문 단계에서 교체. 닫기·Escape는 원래 방과 액자로 복귀.
- [x] 실제 학생 연결·활성 학급·승인된 관리자 담당·학급 ON·내부 공개 단계·현재 발행판을 서버에서 재확인.
- [x] 수록 철회·공개 중단·판 갱신 후 전문 차단, 실패 시 이전 전문 제거, 화면을 떠난 요청의 늦은 응답 무시.
- [x] 미설정 학급 ON은 공용 매니페스트로 기존 메뉴를 초기화하며, 이미 설정된 서버 목록을 덮지 않음.
- [x] 0/1/12/60편 샘플, 390px·26px 글자·시 연/행·Tab 순환·Escape·초점 복원과 지연 응답 검증.
- [ ] 운영 적용 후 실제 학생 로그인·기기 뒤로가기·저사양 실기기 인수. 1,000명 부하 합격을 뜻하지 않음.

C1 SQL이 아직 미적용임을 `migrate:status`로 확인한 뒤 동일 `20261241`에 C2를 함께 작성했다.
미적용 파일을 각각 독립 롤백하는 검사에서 C2가 C1 표를 누락하지 않도록 한 구성이다. 적용된 SQL은 고치지 않았다.
매니페스트 `available: true`는 로더 등록 상태다. 기본 OFF·DB 내부 단계·관리자 본인 학급 제한은 유지한다.
학생에게 원글/학생/수록 확인 ID나 본문 전체 목록을 전달하지 않으며, 작품 번호와 발행판 번호로 전문을 요청한다.
공개 철회는 다음 조회부터 반영한다. 이미 표시 중인 글을 실시간으로 회수하는 구독은 추가하지 않는다.

합성 긴 글 60편의 다섯 번째 방은 로컬 DB 측정에서 약 67ms·4,648바이트였다. 방 내부 자격 검사에는 최대 60편을
확인하지만 브라우저 응답은 12편 요약이다. 단일 요청 측정이며 실제 학급 규모·동시접속 성능 인수는 별도다.
전용 45개·아키텍처 287개·보안 정적 223개와 전체 보안 하네스/롤백 검사 통과. 문집·외부 익명 공유는 후속 범위다.

### C3~C5 로컬 구현 범위

- [x] 독립 문집 프로젝트(학급당 20권)·최대 100편 선정·전시 가져오기·중복 제거·미수록 학생·학생/주제별 정렬.
- [x] 표지·부제·학기·발행일·여는 글·순서 저장, 별도 문집 수록 확인, 확정판 최대 20판과 출력 설정 버전 보관.
- [x] 공용 장르 PDF 렌더러 재사용. 초안 인쇄 미리보기는 수록 상태를 검증하고 확정판을 만들지 않는다. 글꼴 준비 후 A4 페이지를 실측하고 차례 쪽수·페이지 번호를 붙인다. 본문 12pt·시 14pt.
- [x] 교사가 선택한 확정판만 학생 문집 서가→차례→작품 1편으로 조회. 홈 추가 요청 없음.
- [x] 외부 전용 표시본·가림 이름·작품별 외부 확인·방문자 미리보기·256비트 토큰 해시·최대 30일·재발급/만료 변경/해지.
- [x] `/exhibition#토큰`은 인증 셸 없이 열며 토큰은 POST 본문으로 전달. 공개 응답 no-store/no-referrer/noindex, 익명 직접 표/관리 RPC 차단.
- [x] 관리자 실제 권한으로 internal/pilot/disabled, 시범 학급 최대 2개, 외부 공유 전역 스위치(기본 OFF).
- [x] 원글 회수·삭제·학생 비활성은 모든 발행 범위에서 다음 조회를 막는다. 문집 재수록은 새 확인 세대이므로 과거 판이 자동 복원되지 않는다.
- [x] 사용자 지정 진남초 4학년 1반 하나만 운영 DB 허용·학급 ON 설정. 외부 공유 OFF.
- [ ] 앱 배포 후 관리자 실제 계정→진남초 4학년 1반에서 작품 선정·감상·문집 인수. 외부 공유는 별도 허용 뒤 해지까지 확인하고, 실제 저사양 기기/학교망 점검 후 공개 범위를 결정한다.

검증 자료와 명령은 모듈 README·성능/보안 하네스에 둔다. 운영 자료·비밀 키·공유 토큰 원문을 기록하지 않는다.

## 2. 사용자가 하게 될 일

### 교사: 글 전시관

학급 선택 → `새 전시` → 제목·소개 입력 → 학생 글 검색·전문 확인 → 작품 담기 →
전시실에 자동 배치 → 순서·방·작가 표시 조정 → 학생 화면 미리보기 → 학급에 공개.

- 한 학급에서 학기·주제별 여러 전시를 만든다. 초안·공개·지난 전시를 구분한다.
- 후보에서 학생별 작품 수와 아직 작품을 고르지 않은 학생을 교사만 확인할 수 있게 한다.
- 작품 수를 인기 순위로 보여 주지 않고 교사가 학급 전체의 참여를 챙기는 용도로 쓴다.
- 글을 읽고 선정하는 과정에서 학생 원문을 덮어쓰지 않는다. 수정 필요 시 기존 글쓰기 지도 흐름으로 연결한다.
- 게시 기간이 끝나면 지난 전시로 보관하며 학생 공개 여부는 교사가 고른다.

### 학생: 전시 감상

홈의 `우리반 아지트` 카드 → 현재 전시 선택 → 전시실 이동 → 액자 선택 → 글 전문 읽기 →
원래 액자 위치로 복귀. 현재/지난 전시와 공개된 문집을 열람한다.

하단 메뉴를 늘리지 않고 조건부 홈 카드로 시작한다. 전시실이나 상세에서 뒤로가기는
`글 상세 → 해당 전시실 → 로비 → 전시 목록 → 학생 홈`으로 일관되게 동작해야 한다.

### 교사: 학급 문집

`새 문집` → 전시 작품 가져오기 또는 우리 학급 완료 글 선택 → 학생별/주제별 순서 조정 →
표지·여는 글·목차 편집 → 인쇄 미리보기 → 내용 확정 → A4 PDF로 저장 → 발행판 보관.

전시를 먼저 만들지 않아도 문집만 만들 수 있다. 전시와 문집은 같은 작품 선정·공개 기준을 사용하지만
별도 프로젝트이며, 전시 수정이 이미 발행한 문집을 바꾸지 않는다.

### 교사·외부 방문자: 읽기 전용 전시 공유

교사가 전시에서 `외부 공개본 만들기` → 공개 작품·본문·표시 이름·전시 소개 확인 →
만료일 선택 → 외부 방문자 미리보기 → 링크 발급·복사 → 방문자가 로그인 없이 해당 공개 전시만 감상.

외부 공개본은 학급용 전시와 별도로 관리한다. `공개본 갱신`, `즉시 해지`, `새 링크 발급`을 제공한다.
외부 전시에 댓글·공감·학생 검색·학생 프로필·원문 다운로드 버튼·교사 기능은 넣지 않는다.

## 3. 2.5D 전시관의 구체적인 모습

첫 테마 권장안은 밝은 벽·나무 바닥·작은 작품 액자가 있는 글 전시실이다. 최종 색·테마는 C0 시안에서 고른다.

| 요소 | 1차 설계 |
|---|---|
| 로비 | 전시 제목·짧은 소개·입장·전시실 목록 |
| 전시실 | 벽·바닥 원근 배경과 실제 HTML 버튼으로 된 작품 액자 |
| 방 이동 | 이전/다음 방 버튼, 작은 방 목록. 자동 이동·아바타 조작 없음 |
| 액자 | 제목·표시 이름·짧은 발췌. 전문을 작게 구겨 넣지 않음 |
| 글 읽기 | 원근 효과 밖의 평면 읽기 화면. 글자 크기 조정·이전/다음 작품·닫기 |
| 모바일 | 방 전체를 축소하지 않고 보이는 액자 수를 줄여 옆으로 탐색. 큰 읽기 화면 유지 |
| 접근성 | 모든 이동·액자에 키보드 접근, 닫을 때 포커스 복귀, 동일 작품의 목록 보기, 동작 줄이기 |
| 배치 | 고정 슬롯에 자동 배치가 기본. 순서 이동 버튼을 항상 제공하고 드래그는 보조 수단 |

- **CSS·DOM 기반 2.5D**로 시작한다. 기존 React/Vite와 연결하고 WebGL·실시간 위치 서버는 추가하지 않는다.
- 방당 최대 12편, 첫 전시는 최대 5개 방·60편을 권장한다. 한 학급이 여러 전시를 보관하는 것은 허용한다.
- 액자 위치·크기는 전시실 템플릿의 한 원본에서 계산한다. 교사 편집·학생 보기·외부 보기에서 같은 좌표를 사용한다.
- 전시용 2.5D 컴포넌트는 데이터 조회를 직접 소유하지 않는다. 내부·외부 전용 API가 준 안전한 작품 목록을 받는다.
- 원근 효과는 CSS `perspective`로 구성할 수 있다. 해당 속성은 stacking context와 fixed 요소의 기준도 바꾸므로
  글 상세를 전시 벽 안에 넣지 않고 별도 평면 레이어로 설계한다. [MDN perspective](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/perspective)
- 동작 줄이기 설정에는 이동 애니메이션을 줄인다. [MDN prefers-reduced-motion](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/At-rules/@media/prefers-reduced-motion)

이는 기술 선택안이다. 저사양 기기의 부드러움은 C2에서 실측하며 CSS를 썼다는 이유만으로 성능 합격으로 보지 않는다.

## 4. 작품을 고르는 기준과 공개 권한

이웃 아지트 점검에서 **담임이 읽을 수 있는 글과 다른 사람에게 공개할 수 있는 글은 다르다**는 문제가 나왔다.
우리반 아지트는 후보 선정부터 이를 구분한다.

| 원본 상태 | 학급 전시·문집 권장 기준 | 외부 전시 |
|---|---|---|
| 제출 전 초안·회수 글 | 후보 제외 | 제외 |
| 교사 확인을 마친 과제 글 | 전문 확인 뒤 선택 | 별도 외부 공개 확인을 거친 작품만 |
| 완료된 독서록 등 자율 글 | 유형별 공개 가능 조건 확인 뒤 선택 | 학급 공개를 외부 동의로 간주하지 않음 |
| `나만 보기` 일기 | 기본 후보에서 제외. 교사가 조회 가능해도 직접 공개 불가 | 제외 |
| 학급 공개 일기 | 민감 내용 확인과 전시·수록 의사 확인을 추가한 뒤 지원 | 1차는 제외 권장 |
| 다른 학급의 이웃 글 | 참조 간직하기만. 가져오기·문집 수록 불가 | 제외 |

- 1차 지원은 일반 과제 글과 시 등 **사진 없는 텍스트 작품**에 집중한다. 지원 장르는 공용 매니페스트로 선언한다.
  독서록·일기·사진 보고서는 각 공개·표현 계약을 갖춘 순서로 추가하며 본문 일부를 조용히 빼서 전시하지 않는다.
- 후보 검색·항목 추가·학급 공개·문집 확정·외부 발행 때마다 서버가 같은 원본 자격을 재확인한다.
- 학급 내부 전시, 문집 수록, 외부 공개 의사를 서로 다른 범위로 기록한다. 학생/보호자 확인이 필요한 운영 절차와
  증빙 방식은 C0에서 정하고, 확인 문구 한 번을 법적 동의 전체의 대체물로 취급하지 않는다.
- 공개 확인 기록은 대상 작품·범위·확인 주체·확인 시점·철회 여부로 최소화한다. 불필요한 개인정보 사본은 저장하지 않는다.
- 교사 미리보기는 **학생에게 보일 제목·본문·작가 표시**와 동일해야 한다. 교사 수정본/원문 선택과 장르 구조를
  서로 다른 화면에서 따로 계산하지 않도록 공용 작품 표현 계약을 둔다.

## 5. 원본·편집본·발행본

원본 글, 전시 배치, 고정 발행본을 따로 관리한다.

1. **작업 중 전시·문집**은 원본 글 ID·원본 버전·순서·표시 이름을 참조한다.
2. **학급 전시 공개본**은 교사가 확인한 버전의 내용을 고정한다. 원문 변경은 `내용 바뀜`으로 표시하고 교사가
   다시 확인해 반영한다. 배치 편집 중간 상태가 학생에게 보이지 않게 한다.
3. **문집 확정판**은 제목·본문·장르 구조·작가 표시·작품 순서·표지·목차·출력 설정을 고정한다.
4. **외부 전시 공개본**은 승인된 필드만 별도 스냅샷으로 만든다. 익명 조회에서 원본 표를 직접 읽지 않는다.

- `expected_revision`으로 저장·공개·확정 충돌을 감지한다. 다른 탭에서 수정된 작업을 조용히 덮어쓰지 않는다.
- 원문이 삭제·회수·비공개로 전환되거나 공개 의사가 철회되면 **해당 온라인 작품을 즉시 비노출** 처리하고
  교사에게 알린다. 고정판의 내용을 몰래 편집하지 않고 별도의 접근 중지 상태로 관리한다.
- 이미 내려받거나 인쇄한 PDF는 링크 해지로 회수되지 않는다. 교사 관리 화면은 온라인 공개 중지와 PDF 발행을 구분한다.
- 확정판 수정은 새 판으로 만든다. 원본이 바뀌어도 이전 PDF의 내용·순서가 달라지지 않는다.
- 사진 지원 시 발행판에 묶인 비공개 자산 사본·보관/정리 정책이 필요하다. 만료되는 서명 URL을 스냅샷에 저장하지 않는다.
- 학급 보관·삭제·담임 변경 시 현재 담당 교사와 학급 상태로 접근을 다시 판정한다. `creator_teacher_id`만으로
  과거 담임에게 영구 권한을 주지 않는다.

이 기준은 종전 계획의 “삭제·비공개 변경은 교사에게 알리기만 함”을 보완한 **권장 변경안**이다.

## 6. 학급 문집 MVP

### 책 구성과 편집

표지 → 교사 여는 글(선택) → 작품 목차 → 작품 본문 → 발행 정보.

- 제목·부제·학기·발행일·표시 학급명·표지 테마 1종부터 시작한다.
- 학생별/주제별 묶기, 작품 순서 바꾸기, 중복 수록 방지, 미수록 학생 확인을 제공한다.
- 전시를 그대로 문집으로 가져온 뒤 작품을 추가·제외할 수 있다. 원본 글 작성·수정 기능은 복제하지 않는다.
- 본문 12pt 이상, 한글·시의 연/행·긴 제목·긴 글의 자연스러운 페이지 넘김을 지킨다.
- 한 권 최초 상한은 기존 출력 계약과 같은 100편으로 둔다. 초과분은 여러 권으로 나누는 안내를 제공한다.

### 페이지·PDF는 별도 검증

기존 `writingPdfExport.js`는 브라우저 A4 인쇄용 HTML과 장르 렌더러를 제공한다. **정확한 책 목차 쪽수와
고정된 발행 PDF 파일 보관까지 제공하는 엔진은 아니다.**

- 구현한 `anthology/pagination.js`가 글꼴 준비 후 페이지를 실측한다. 100편·117쪽 실제 PDF에서 차례 쪽수 100/100과 문단·연 누락 0건을 확인했다. 추가 상한 검사(긴 표지/제목·한 문단·130행 시)는 131쪽 전체 차례·본문·12pt·잘림 검사를 통과했다.
- 쪽수가 확정되지 않는 단계는 제목형 목차로 표시하고, 계산하지 않은 쪽수를 넣지 않는다.
- 장르별 `pdfExport { id, load }`와 `renderEntry/styles`를 재사용한다. 공용 출력 파일에 장르별 분기를 복제하지 않는다.
- 첫 내보내기는 기존 계약처럼 **교사가 인쇄 창에서 PDF로 저장**하는 경로를 권장한다. 인쇄 창을 열었다는
  사실만으로 “파일 저장 완료”를 기록하지 않는다.
- 발행판 보관의 1차 의미는 **확정 콘텐츠·설정·판 번호 보관**이다. PDF 바이트를 서버에 저장해 학생이 내려받는 기능은
  파일 생성·비공개 Storage·용량·다운로드 권한까지 구현한 별도 단계로 둔다.
- 학생 문집 서가는 먼저 확정판을 읽는 웹 화면으로 제공할 수 있다. 교사가 선택한 판만 보인다.
- 자동 PDF 파일 생성·파일 보관, DOCX·Google Docs, 주문 인쇄, 사진 문집은 1차 검증 뒤 확장한다.

## 7. 외부 읽기 전용 공유

### 접근과 공개본

- 전시별 외부 공유 기본 OFF, **기본 30일 만료** 권장. 1차는 만료 없음 옵션을 두지 않는다.
- 외부 공개는 별도 미리보기·작품 범위 확인 뒤 교사가 명시적으로 발행한다.
- 학생 실명 대신 가림 이름/필명, 학교·실제 학급명 자동 삽입 없음이 기본이다. 제목·본문·소개 안의 개인정보도
  교사가 확인한다. 이름 칸을 가린 것만으로 본문까지 익명화된 것으로 표시하지 않는다.
- 긴 무작위 토큰을 발급하고 DB에는 해시만 저장한다. 재발급 시 이전 토큰은 즉시 해지한다.
- 익명 방문자는 전시 한 편의 공개 스냅샷만 상한 조회한다. 학생·전시 원본 표에 `anon` 권한을 열지 않는다.
- 공개 경로는 앱 로그인·학생 홈 bootstrap과 분리한다. 외부 페이지 로딩 때문에 로그인 데이터가 조회되지 않게 한다.
- 토큰의 URL/접속 로그·Referrer 유출을 줄이도록 **고정 경로+URL fragment, 읽기 요청 본문 전달**을 우선 검토한다.
  실제 라우팅·Caddy·Edge 로그 설정까지 확인한 후 정한다. 토큰을 분석 이벤트나 오류 로그에 넣지 않는다.

### 해지와 제한

- 모든 공개 조회에서 토큰·만료·공유 ON·관리자 허용·작품 접근 중지 상태를 재검사한다.
- 콘텐츠 응답은 `Cache-Control: no-store`, 공개 페이지는 `Referrer-Policy: no-referrer`를 기준으로 검증한다.
- 서버 속도 제한·응답 크기/페이지 상한·요청 시간 제한을 둔다. 학교의 공유 IP를 고려해 토큰별 제한과 전체 예산을
  함께 설계하고 IP·기기별 방문 분석 기록은 만들지 않는다.
- 관리자는 외부 공유를 전체 중지할 수 있다. 교사는 링크 해지·재발급·만료 변경을 한다.
- 링크 해지 뒤 **다음 조회에서** 내용 반환이 차단되어야 한다. 이미 열린 페이지나 내려받은 내용을 지우는 기능으로
  설명하지 않는다. 상시 연결·폴링을 추가하지 않는다.
- `noindex/nofollow/noarchive`는 검색 노출 감소용이다. 토큰·권한 검사를 대체하지 않는다.
  robots.txt로 크롤링을 막으면 검색기가 noindex를 읽지 못할 수 있으므로 응답 헤더와 함께 검증한다.
  [Google 검색 색인 차단 안내](https://developers.google.com/search/docs/crawling-indexing/block-indexing)
- 읽기 전용은 수정·댓글 권한이 없다는 뜻이다. 화면 캡처·수동 복사 방지를 보장하지 않는다.
- 사진과 사용자 업로드 배경의 외부 공개는 공개용 자산·확인·철회·정리까지 갖춘 다음 단계로 미룬다.

## 8. 모듈·데이터 설계안

`src/modules/class-agit/` 아래에 `teacher/`, `student/`, `gallery/`, `anthology/`, `public/`, `api/`를 둔다.
공통 셸은 진입·공개 단계·지연 로딩·오류 격리만 맡고, 새 기능은 등록 매니페스트로 연결한다.

아래 표·RPC 이름은 설계용 예시다. 구현 시 기존 스키마·외부 앱 호출과 충돌을 확인해 확정한다.

| 데이터 | 책임 |
|---|---|
| 전시·전시실·작품 항목 | `class_id`, 초안/공개/보관 상태, 원본 참조, 배치·순서, revision |
| 전시 공개판·작품 스냅샷 | 학급용/외부용 공개판, 승인 시점 내용, 스키마 버전, 온라인 비노출 상태 |
| 문집 프로젝트·확정판·수록 항목 | 표지·목차·순서, 확정 콘텐츠와 출력 설정, 판 번호 |
| 공개 확인·철회 기록 | 작품·공개 범위·확인 주체·시각. 본문/민감정보 사본 없이 필요한 연결만 |
| 외부 링크 | 공개판 ID, 토큰 해시, 만료·해지·발급 시각. 원문 토큰 저장 금지 |
| 기능 공개 설정 | 관리자 공개 단계·허용 학급·외부 공유 전체 중지 |

- 모든 학급 데이터에 직접 `class_id`를 두고 학급 테이블 조인에도 포함한다. 복합 FK와 안정 정렬 인덱스를 둔다.
- 교사용 후보 목록·작품 상세·저장/정렬·공개/철회·문집 확정, 학생용 목록/방/상세, 외부용 공개본 읽기를 구분한다.
- 후보 선정과 내용 표현의 최소 계약은 이웃 기능과 공유하되, 공간 회원 권한과 외부 토큰 권한은 분리한다.
- 생성/정렬/공개/확정은 각 동작당 RPC 1회와 같은 트랜잭션으로 끝낸다. 재시도 키와 revision으로 중복·충돌을 막는다.
- 캐시는 `dataCache + classKey()`를 사용하며 권한·학급 전환·발행/철회 때 무효화한다.
  외부 비로그인 응답을 학급 인증 캐시와 섞지 않는다.
- 새 테이블·RPC·외부 엔드포인트 도입 시 `SECURITY_HARNESS.md`, 장르 출력은
  `src/modules/writing/export/README.md`, 학생 이동은 `src/components/student/README.md`를 따른다.

## 9. 공개 단계와 성능 예산

기능 공개(`internal → pilot → public_beta → general`), 학급 사용 ON/OFF, 콘텐츠 공개 상태,
외부 링크 ON/OFF를 독립적으로 둔다. 긴급 중지는 별도 관리자 제어로 모든 허용보다 우선한다.

- 내부 기능은 관리자만, 시범 기능은 허용 학급만 사용한다. 일반 교사에게 공개해도 학생 기본 OFF.
- 교사 학급 스위치 OFF여도 허용된 교사는 초안을 편집한다. 학생 조회는 차단한다.
- 외부 링크는 별도 선택이며 학생 카드 OFF만으로 켜지거나 꺼지지 않는다. 이 차이를 교사 관리 화면에 표시한다.
- 교사 공개 허용이 철회되거나 학급이 삭제되면 외부 링크도 서버에서 닫힌다.

| 동작 | 초기 예산안 |
|---|---|
| 학생 홈 | 추가 요청 0회. 기존 bootstrap에 공개 존재 여부·작은 요약만 |
| 전시 목록 | 첫 20개·상한 50개, 커서 조회 |
| 전시 입장/방 이동 | 선택한 방 12편 요약과 방 목록을 RPC 1회로 조회 |
| 작품 열기 | 전문 1편을 1회 조회. 닫기·같은 방 이동은 불필요한 재조회 없음 |
| 외부 전시 | 한 링크 5개 방·60편, 전문은 작품 선택 시 조회 |
| 문집 | 한 권 100편 이내, 본문·출력 자산은 편집 상세/내보내기 때만 로드 |
| 배경 | 기본 테마 1종, 지연 로딩. 첫 방 이미지 합계 1MB 이하를 초기 목표로 측정 |

폴링·Realtime·상시 WebSocket·위치 동기화 없음. 목표 수치는 C2에서 실제 기기·네트워크로 측정하며
`PERFORMANCE_HARNESS.md`에 측정치와 함께 확정한다.

## 10. 한 단계씩 구현할 순서

이웃 아지트 N0~N2의 검증·공개 자격 보완을 먼저 처리한 뒤 시작하는 것을 권장한다.

| 단계 | 실제 산출물 | 합격 기준 |
|---|---|---|
| C0 계약·화면 시안 | 공개 자격/확인 범위, 역할·상태도, 등록 매니페스트, DB 없는 전시실 1종 시안 | 교사→학생→외부 방문자 흐름과 첫 지원 장르·이름 표시·PDF 방식 검토 완료 |
| C1 글 선정·내부 공개 | 전시/방/작품 데이터, 후보 검색·전문·자동 배치·초안 저장·공개판 | 다른 학급/미제출/비공개 글 차단, revision 충돌·원문 회수 처리, 관리자 내부에서 전 과정 완료 |
| C2 2.5D 전시 감상 | 로비·방 이동·액자·평면 상세·목록 대체 보기, 학생 홈 연결 | PC·태블릿·390px, 키보드·포커스·뒤로가기, 0/1/12/60편과 긴 글, 추가 홈 요청 없음 |
| C3 학급 문집 | 출력 시제품 → 작품 선정·표지·목차·확정판·A4 PDF 저장·발행판 보관 | 100편/긴 글/시/한글 렌더, 12pt 이상, 잘림·겹침 없음, 확정 뒤 원문 변경에도 내용 고정 |
| C4 외부 읽기 공유 | 공개 확인·외부 전용 스냅샷·해시 토큰·30일 만료·해지/재발급·익명 뷰어 | 익명 직접 표 접근 차단, 다른 전시 ID 추측 실패, 철회·만료·캐시·검색/Referrer 헤더·모바일 확인 |
| C5 제한 운영·확장 판단 | 지정 1~2학급 인수, 오류/부하 점검, 교사 도움말 | 실제 작품 선정→학생 감상→문집→외부 링크 해지까지 한 흐름으로 완료 후 공개 범위 결정 |

C0의 시안은 실제 문집/외부 공유 완료를 뜻하지 않는다. C1~C4는 각각 로컬 구현·검증 뒤 검토하며,
사용자가 `배포/마무리/확정`을 말할 때만 WORKLOG·ROADMAP·전체 마감 검사·푸시를 수행한다.

## 11. 첫 완성판의 인수 시나리오

- 서로 다른 두 학급 교사·학생, 관리자, 로그아웃 방문자로 직접 URL·ID 추측·OFF 상태 접근을 확인한다.
- 글 선정→수정 충돌→공개→원문 변경 알림→재확인→비공개 전환→온라인 비노출을 확인한다.
- 전시 초안 편집이 기존 공개판을 흔들지 않고, 다른 학급 자료가 섞이지 않아야 한다.
- 학생이 액자를 열고 기기 뒤로가기를 눌러 같은 방·같은 위치로 돌아와야 한다.
- 문집에는 아직 선정하지 않은 학생을 교사만 확인하고, 동일 글 중복 수록과 빈 책 확정을 막는다.
- 인쇄 직전 글꼴을 로드하고 실제 A4 PDF를 만든 뒤 페이지별로 렌더링해 한글·쪽수·12pt·잘림을 확인한다.
- 외부 링크는 새 판 발행/해지/재발급/만료와 공개 의사 철회 후 다음 조회에서 옛 내용을 주지 않아야 한다.
- 브라우저 미리보기, 자동 권한 검사, 실제 수업 기기 인수를 서로 대체하지 않는다.

## 12. 이번 검토에서 정할 기본값

| 선택 | 권장안 | 다른 선택을 하면 달라지는 일 |
|---|---|---|
| 첫 전시 테마 | 밝은 작은 미술관 1종 | 도서관·아지트 방 테마도 가능. 데이터 구조는 동일 |
| 첫 작품 범위 | 일반 과제 글·시 등 사진 없는 텍스트 | 독서록·일기·사진은 별도 공개/표현 계약을 함께 구현 |
| 학생 이름 | 학급 안 등록 이름, 외부 가림 이름/필명 | 외부 실명은 별도 확인 절차와 미리보기 필요 |
| 문집 첫 출력 | A4 세로, 교사 인쇄 창에서 PDF 저장 | 자동 파일 다운로드/서버 보관은 파일 생성·Storage 단계 추가 |
| 외부 링크 | 기본 30일, 해지/재발급, 만료 없음 제외 | 장기 전시는 재발급/갱신 방식부터 운영해 보고 확대 |
| 학생 문집 서가 | 교사가 고른 확정판의 읽기 화면 | PDF 파일 다운로드는 실제 파일 저장 기능과 함께 후속 추가 |

여러 테마·자유 이동 아바타·실시간 방문자·방명록·인기 순위·포인트·사진 외부 전시·다른 반 글 수록은
첫 완성판 이후에 검토한다. 2.5D 전시·문집·외부 읽기의 기본 흐름부터 완성한다.
