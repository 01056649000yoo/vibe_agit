node_modules
dist
.git
playwright-report
test-results
e2e
testsprite_tests
# Docker builder가 전체 회귀 테스트를 실행하므로 tests가 읽는 scripts와 Markdown 계약 문서를 포함한다.
# 최종 runner에는 dist만 복사되어 운영 이미지 크기에는 영향을 주지 않는다.
