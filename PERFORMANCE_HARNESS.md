# 1,000명 성능 하네스

이 문서는 성능을 한 번 고치고 끝내는 계획이 아니라, 앞으로 콘텐츠를 추가할 때마다 지켜야 하는 합격 조건이다.

## 새 콘텐츠의 데이터 계약

1. 학생 홈에는 상세 조회를 붙이지 않는다. 홈에 꼭 필요한 작은 숫자·ON/OFF만 공용 bootstrap에 넣는다.
2. 콘텐츠 상세는 학생이 카드를 열었을 때 기능 전용 RPC 한 번으로 읽는다.
3. 저장·제출·구매·보상 같은 한 번의 행동은 전용 RPC 한 번에서 끝낸다.
4. 목록은 `class_id`/`student_id`를 직접 조건으로 사용하고 정렬+상한 또는 커서 페이지를 둔다.
5. 콘텐츠별 `setInterval`과 Realtime 구독은 기본 금지다. 화면 복귀·수동 새로고침·서버 응답으로 수렴시킨다.
6. 공용 홈 데이터가 바뀌면 `studentHomeApi.invalidate()`만 호출한다. 화면별 재조회 함수를 연쇄 호출하지 않는다.

## 콘텐츠를 추가할 때 반드시 적는 성능표

모든 등록 모듈의 `manifest.js`에는 `performance`를 적는다. 한 항목이라도 빠지거나 첫 목록 상한이 100을 넘으면
아키텍처 테스트와 Docker 빌드가 실패한다.

```js
performance: {
  home: 'none',          // 홈에 꼭 필요한 작은 요약만 있으면 'summary'
  load: 'on-open',       // 상세 데이터는 사용자가 열 때만
  writes: 'rpc',         // 쓰기 없음은 'none', 아직 이전 중이면 'legacy-bounded'
  realtime: 'none',      // 핵심 알림 채널을 공유할 때만 'core-only'
  maxInitialRows: 50     // 첫 목록 0~100개, 그 이상은 페이지/커서 사용
}
```

새 콘텐츠 작업 완료 조건은 다음 네 가지다.

1. 매니페스트 성능표 작성
2. 홈 추가 조회 0회인지 확인하고, 상세 읽기·행동 쓰기는 각각 1회 RPC를 목표로 설계
3. 목록의 사용자/학급 직접 조건, 정렬, 상한 또는 커서 페이지 확인
4. `npm run test:architecture`, `npm run build` 통과. 새 SQL은 `npm run migrate:check`로 ROLLBACK 검증

## 자동 방어선

`npm run test:architecture`는 다음 회귀를 막는다.

- 학생 홈 컴포넌트의 직접 DB 호출
- 학생 홈/편집 설정의 짧은 고정 폴링
- 과제 목록·글쓰기 화면의 학생별 Realtime 채널 재도입
- 교사 보관함의 미션별 count N+1
- 범용 포인트 함수의 화면 직접 호출
- bootstrap/제출 RPC의 권한·트랜잭션 계약 누락
- 신규 모듈의 성능표 누락·100개 초과 첫 목록
- 교사 과제 승인·회수 뒤 전체 목록 재조회 회귀
- 교사 첫 화면 bootstrap과 과제·제출글 상한 누락

Docker 이미지 빌드 전에 이 검사를 실행한다. 즉 로컬에서 놓쳐도 운영 배포 단계에서 실패한다.

DB SQL은 운영 적용 전에 `npm run migrate:check`로 검사한다. 이 명령은 아직 적용하지 않은 SQL을 실제 DB 구조에서
실행하고, 함께 있는 `tests/sql/<마이그레이션명>.smoke.sql`까지 확인한 뒤 전체를 `ROLLBACK`한다.

## 1,000명 부하 합격선

- 50명/초로 1,000명까지 증가, 10분 유지
- 사용자 비율: 글쓰기 70%, 홈 15%, 과제 목록 10%, 친구/게임 5%
- 오류율 1% 미만, API p95 500ms 미만
- PostgreSQL 연결 80/100 미만, CPU 지속 70% 미만, swap 0
- 제출 누락 0, 중복 포인트 0, 다른 학생/학급 데이터 노출 0

실제 학생 비밀 값은 파일에 저장하지 않는다. 부하 도구는 환경변수로 시험용 URL·토큰을 받고 운영 학생 대신
격리된 시험 학급을 사용한다.

학생 홈 읽기 부하는 `npm run load:test:student-home`으로 측정한다. 기본값은 50명/초 증가, 1,000명,
10분 유지다. 원격 서버는 명시적인 안전 확인 환경변수 없이는 실행되지 않는다.
