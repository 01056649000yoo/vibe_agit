# 1,000명 성능 하네스

이 문서는 성능을 한 번 고치고 끝내는 계획이 아니라, 앞으로 콘텐츠를 추가할 때마다 지켜야 하는 합격 조건이다.

## 새 콘텐츠의 데이터 계약

1. 학생 홈에는 상세 조회를 붙이지 않는다. 홈에 꼭 필요한 작은 숫자·ON/OFF만 공용 bootstrap에 넣는다.
2. 콘텐츠 상세는 학생이 카드를 열었을 때 기능 전용 RPC 한 번으로 읽는다.
3. 저장·제출·구매·보상 같은 한 번의 행동은 전용 RPC 한 번에서 끝낸다.
4. 목록은 `class_id`/`student_id`를 직접 조건으로 사용하고 정렬+상한 또는 커서 페이지를 둔다.
5. 콘텐츠별 `setInterval`과 Realtime 구독은 기본 금지다. 화면 복귀·수동 새로고침·서버 응답으로 수렴시킨다.
6. 공용 홈 데이터가 바뀌면 `studentHomeApi.invalidate()`만 호출한다. 화면별 재조회 함수를 연쇄 호출하지 않는다.

## 화면별 요청 예산

기능을 넓힐 때 아래 횟수를 넘기면 이유와 1,000명 환산 부하를 이 문서에 먼저 기록한다.

| 흐름 | 기본 예산 | 현재 방식 |
|---|---:|---|
| 학생 홈 첫 진입 | DB API 1회 | `get_student_home_bootstrap_v1` |
| 학생 활동 알림 | 홈 요약 추가 요청 0회, 창을 열 때 목록 1회(최대 50건), 확인 행동당 쓰기 RPC 1회 | 단일 이벤트 원장, 별도 폴링·Realtime 없음 |
| 학생 백그라운드 동기화 | 4~6분당 인증 1회 + 홈 1회 | 무작위 분산, 글쓰기 중 중단 |
| 학생 과제 목록 열기 | DB API 1회, 최대 100개 | `get_student_mission_list_v1` |
| 학생 과제 글쓰기 열기 | DB API 1회 | 과제+내 기존 글 `get_student_assignment_workspace_v1` |
| 학생 저장·제출·구매 | 행동당 쓰기 RPC 1회 | 기능 전용 RPC |
| 교사 첫 화면 | DB API 2회 | 교사 bootstrap 1회 + 과제 개요 1회 |
| 교사 승인·회수 | 행동당 쓰기 RPC 1회 | 응답으로 로컬 상태 갱신 |
| 학생 독서록 책장 | DB API 1회, 책 50권 | `get_my_reading_library_v1` |
| 친구 반응·댓글 쓰기 | 행동당 쓰기 RPC 1회 | 학생·학급·공개 글을 서버에서 재검증 |
| 장르별 글 반응 | 프로필 추가 조회 0회, 반응 모아보기 창을 열 때 RPC 1회(최대 100편) | `input_template`로 프런트 매니페스트를 즉시 해석, 저장 때만 서버 프로필 검증 |
| 교사 글 상세 | DB API 1회 | 글·학생·과제·반응·댓글 `get_teacher_post_detail_v1` |
| 활동·평가 보고서 자료 | 활동 200행 커서 / 과제 평가 100행 | 전용 RPC, 1,000행 고정 절단 금지 |
| 친구 글 상세 | 처음 열 때만 조회 | 고정 폴링·개별 Realtime 없음, 화면 복귀 시 60초 stale 갱신 |
| 맞춤법 학급 규칙 | 글쓰기/수첩을 열 때 RPC 1회, 최대 100개 | 모듈 API Promise 캐시로 제목·본문 중복 조회 방지 |
| 맞춤법 검색 기록 | 수첩을 닫을 때 배치 RPC 1회, 최대 20항목 | 입력 중 쓰기 없음, 세션 메모리에서 항목별 횟수 합산 |
| 교사 PDF 내보내기 | 사용자 동작 때 최대 100편 | 브라우저에서 A4 생성, 보고서 사진 URL은 50개씩 서명, 서버 PDF 생성 없음 |

학생 상시 WebSocket은 사용하지 않는다. 1,000명 목표가 현재 Realtime 동시 사용자 한도보다 크기 때문에,
승인·포인트·반려는 단일 이벤트 원장의 최신 미확인 요약을 공용 홈 스냅샷에 실어 4~6분 무작위 주기로 반영한다.
로그인·새로고침은 즉시 읽고, 다른 화면에서 홈으로 돌아올 때는 60초 stale 기준으로 재검증한다. 즉시성이 꼭 필요한 미래 기능은
전교생 상시 구독이 아니라 해당 화면을 연 사용자만 연결하고, 동시 연결·초당 이벤트 예산을 먼저 제시해야 한다.

## 콘텐츠를 추가할 때 반드시 적는 성능표

모든 등록 모듈의 `manifest.js`에는 `performance`를 적는다. 한 항목이라도 빠지거나 첫 목록 상한이 100을 넘으면
아키텍처 테스트와 Docker 빌드가 실패한다.

```js
performance: {
  home: 'none',          // 홈에 꼭 필요한 작은 요약만 있으면 'summary'
  load: 'on-open',       // 상세 데이터는 사용자가 열 때만
  writes: 'rpc',         // 쓰기 없음은 'none', 아직 이전 중이면 'legacy-bounded'
  realtime: 'none',      // 핵심 알림 채널을 공유할 때만 'core-only'
  maxInitialRows: 50     // 첫 목록 0~100개, 그 이상은 페이지/커서 사용
}
```

새 콘텐츠 작업 완료 조건은 다음 네 가지다.

1. 매니페스트 성능표 작성
2. 홈 추가 조회 0회인지 확인하고, 상세 읽기·행동 쓰기는 각각 1회 RPC를 목표로 설계
3. 목록의 사용자/학급 직접 조건, 정렬, 상한 또는 커서 페이지 확인
4. `npm run test:architecture`, `npm run build` 통과. 새 SQL은 `npm run migrate:check`로 ROLLBACK 검증

## 자동 방어선

`npm run test:architecture`는 다음 회귀를 막는다.

- 학생 홈 컴포넌트의 직접 DB 호출
- 학생 홈/편집 설정의 짧은 고정 폴링
- 과제 목록·글쓰기 화면의 학생별 Realtime 채널 재도입
- 교사 보관함의 미션별 count N+1
- 범용 포인트 함수의 화면 직접 호출
- bootstrap/제출 RPC의 권한·트랜잭션 계약 누락
- 신규 모듈의 성능표 누락·100개 초과 첫 목록
- 교사 과제 승인·회수 뒤 전체 목록 재조회 회귀
- 교사 첫 화면 bootstrap과 과제·제출글 상한 누락
- 학생 상시 WebSocket 재도입과 친구 글 상세 고정 폴링
- 학생·교사 과제 목록 통합 RPC와 활성 과제 복합 인덱스 누락
- 핵심 RPC 실패 시 과거 다중 조회 폴백 재도입
- 친구 반응·댓글의 직접 테이블 쓰기
- 독서록 책장의 다중 병렬 조회, 교사 글 상세의 반응·댓글 분리 조회
- 활동 보고서의 1,000행 고정 절단
- 학생 활동 알림의 기능별 폴링·Realtime·홈 추가 요청, 무제한 목록, 직접 원장 접근

Docker 이미지 빌드 전에 이 검사를 실행한다. 즉 로컬에서 놓쳐도 운영 배포 단계에서 실패한다.

DB SQL은 운영 적용 전에 `npm run migrate:check`로 검사한다. 이 명령은 아직 적용하지 않은 SQL을 실제 DB 구조에서
실행하고, 함께 있는 `tests/sql/<마이그레이션명>.smoke.sql`까지 확인한 뒤 전체를 `ROLLBACK`한다.

## 1,000명 부하 합격선

- 50명/초로 1,000명까지 증가, 10분 유지
- 사용자 비율: 글쓰기 70%, 홈 15%, 과제 목록 10%, 친구/게임 5%
- 오류율 1% 미만, API p95 500ms 미만
- PostgreSQL 연결 80/100 미만, CPU 지속 70% 미만, swap 0
- 제출 누락 0, 중복 포인트 0, 다른 학생/학급 데이터 노출 0

실제 학생 비밀 값은 파일에 저장하지 않는다. 부하 도구는 환경변수로 시험용 URL·토큰을 받고 운영 학생 대신
격리된 시험 학급을 사용한다.

학생 홈 읽기 부하는 `npm run load:test:student-home`으로 측정한다. 기본값은 50명/초 증가, 1,000명,
10분 유지, 학생별 4~6분 동기화다. 더 강한 스트레스 시험은 `AGIT_LOAD_TEST_SYNC_MIN_MS`와
`AGIT_LOAD_TEST_SYNC_MAX_MS`를 명시해 별도로 실행한다. 원격 서버는 명시적인 안전 확인 환경변수 없이는 실행되지 않는다.

## 측정 기록

시험할 때마다 한 줄씩 추가한다. 위 합격선과 바로 비교할 수 있도록 같은 항목만 남긴다.

| 날짜 | 대상/범위 | 사용자 수 | 요청 수 | 오류율 | p50 | p95 | p99 | DB 연결 | CPU 최대 | 판정 |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---|
| 2026-08-09 | 학생 홈 읽기(`get_student_home_bootstrap_v1`)만, 로컬 Kong(`127.0.0.1:8100`) | 1,000 | 2,547 | 0% | 13ms | 87ms | 292ms | 63/100(시험 전후 동일) | 11%(Kong) | ✅ pass |

- 아직 못 한 것: 실제 비율(글쓰기 70%·과제 목록 10%·친구/게임 5%) 혼합 부하, 제출·승인 등 쓰기 경로 포함 시나리오.
  지금까지는 가장 자주 열리는 읽기 경로 하나만 확인했다.

### 클라이언트 글쓰기 검사 측정

| 날짜 | 대상/범위 | 글 길이 | 변경 전 | 변경 후 | 판정 |
|---|---|---:|---:|---:|---|
| 2026-08-11 | 맞춤법 500개 6분류·3검출 방식 통합 후 `npm run spelling:benchmark`(5회×5,000회) | 5,000자 | — | 평균 0.106ms·초기 모듈 로드 12.08ms | ✅ 2ms 자동 합격선 통과 |
| 2026-08-11 | 맞춤법 기본 500개(학습 라벨 483개·검사 표현 487개), 로컬 Node 10회×5,000회 미세 측정 | 5,000자 | — | 평균 0.174ms | ✅ 입력 지연 350ms 대비 무시 가능한 수준 |
| 2026-08-11 | 맞춤법 기본 300개(학습 라벨 283개·검사 표현 287개), 로컬 Node 미세 측정 | 5,000자 | 평균 0.422ms | 평균 0.084ms | ✅ 후보 색인 약 5.05배 |

- 실제 태블릿 브라우저 성능은 별도 실기기 검사 대상이다. 입력 중에는 검사하지 않고 기존처럼 350ms 멈춘 뒤 실행하며 결과는 최대 50개다. 맞춤법 자료를 늘릴 때에는 `npm run spelling:benchmark`의 5,000자 평균 2ms 합격선을 함께 지킨다.
