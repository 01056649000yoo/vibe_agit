# 교사 활용 안내서 AI 길잡이 계획

> **상태**: 관리자 선공개 구현·배포
>
> **목표**: 활용 안내서 오른쪽에서 교사가 사용법을 자연어로 묻고, 기존 도움말에 근거한 짧은 답을 받은 뒤
> 가장 가까운 상세 안내 또는 실제 기능 화면으로 바로 이동한다. 계정당 하루 AI 호출은 5회로 제한하며,
> 첫 운영은 관리자에게만 열고 검증을 통과한 뒤 승인 교사 전체에 공개한다.

## 1. 제품 원칙

- AI가 새 사용법을 만들어 내지 않고 `src/constants/teacherGuides.js`의 기존 도움말만 근거로 답한다.
- 기존 도움말의 상세 내용을 다른 파일이나 DB에 복사하지 않는다. 검색 색인은 원본에서 실행 시점에 만든다.
- 긴 상담보다 `질문 1개 → 짧은 답 → 관련 안내 펼치기/화면 열기`에 집중한다.
- AI가 없어도 기존 활용 안내서와 화면 이동은 그대로 작동한다.
- 질문·답변 원문은 DB, 브라우저 영구 저장소, 운영 로그에 남기지 않는다.
- 공개 단계는 서버가 소유하며, 1차에는 실제 `profiles.role='ADMIN'`인 계정만 화면과 API를 사용할 수 있다.

## 2. 화면과 사용자 흐름

### PC

- 기존 `목적별 안내 / 상세 내용` 오른쪽에 320~360px 폭의 `AI 사용법 길잡이` 패널을 둔다.
- 첫 화면에는 질문 예시 3~4개, 질문 입력창, `오늘 5회 남음`을 표시한다.
- 답변에는 짧은 설명과 가능한 행동만 표시한다.
  - `관련 안내 펼치기`: 해당 `guideRef`와 `sectionRef`로 안내서 위치를 바꾼다.
  - `해당 화면 열기`: 검증된 `TEACHER_GUIDE_TARGETS`로 이동하고 안내서를 닫는다.
- 전송 중에는 버튼과 중복 제출을 잠그며, 실패하면 재시도 가능 상태로 되돌린다.

### 좁은 화면

- 3열을 억지로 유지하지 않는다. 하단의 `AI에게 물어보기` 버튼으로 별도 시트 또는 전체 화면 패널을 연다.
- 닫은 뒤에는 원래 보고 있던 흐름과 펼친 도움말, 스크롤 맥락을 유지한다.
- 키보드 포커스, Esc 닫기, 스크린리더 상태 안내, `prefers-reduced-motion`을 지원한다.

### 사용 횟수 안내

- 성공 응답마다 `오늘 N회 남음`을 즉시 갱신한다.
- 5회를 모두 쓰면 입력을 잠그고 `오늘 사용할 수 있는 AI 안내 5회를 모두 사용했어요. 기존 활용 안내서는 계속 볼 수 있습니다.`라고 안내한다.
- 분당 제한에 걸리면 일일 한도 소진으로 표시하지 않고 잠시 뒤 다시 시도하도록 구분한다.

## 3. 최소 토큰 검색 전략

1. 브라우저에서 `TEACHER_GUIDES`와 `TEACHER_GUIDE_JOURNEYS`로 검색 문서를 만든다.
2. 제목, 섹션 제목, 단계, 주의사항, 흐름 제목·목적을 한국어 공백·기호 기준으로 정규화한다.
3. 질문과의 제목 일치, 연속 구문, 희소 키워드, 단계·주의사항 일치에 가중치를 주어 점수를 계산한다.
4. 점수 차이에 따라 1~3개 후보만 AI에 보내고, 각 후보도 질문과 맞는 문장 위주로 잘라 보낸다.
5. 위치가 명확한 질문은 로컬에서 안내 이동 답변을 만들어 AI 호출과 일일 횟수를 사용하지 않는다.
6. 후보별 본문과 전체 요청에 각각 길이 상한을 적용한다. 도움말 전체 약 79KB를 한 요청에 보내지 않는다.
7. 이전 대화 전체는 다음 요청에 다시 보내지 않는다. 화면에 보이는 최근 질답도 현재 모달 메모리에만 둔다.

1차에는 임베딩, 벡터 DB, 별도 검색 서버를 두지 않는다. 실제 질문에서 로컬 검색 적중률이 부족하다는 측정이
쌓일 때만 후속 단계로 검토한다.

## 4. AI 요청·응답 계약

### 요청 상한

- 질문: 앞뒤 공백 제거 후 1~200자
- 검색 후보: 최대 3개
- 후보별 전달 텍스트: 최대 450자
- 전체 참고 문맥: 최대 1,200자
- 전송 내용: 질문, 후보의 `guideRef`·선택적 `sectionRef`, 제한된 도움말 문맥만
- 교사·학급·학생 이름, 학생 글, 평가 결과 등 현재 화면의 업무 데이터는 보내지 않는다.

### 모델 호출

- 서버가 저비용 모델을 고정하며 클라이언트의 모델·키·토큰 상한 재정의를 받지 않는다.
- 온도는 0, 출력은 최대 120토큰으로 제한한다.
- 시스템 지시는 `제공된 도움말만 근거로 답하기`, `모르면 모른다고 말하기`, `짧게 답하기`로 고정한다.
- 참고 문맥은 명령이 아닌 인용 자료로 구분하여 프롬프트 삽입의 영향을 줄인다.

### 구조화 응답

```json
{
  "answer": "학생 등록 화면에서 학생을 추가한 뒤 개인별 접속 코드를 전달할 수 있습니다.",
  "guideRef": "students",
  "sectionRef": null,
  "actionLabel": "학생 등록 안내 보기",
  "confidence": "high"
}
```

- `answer`: 최대 300자, 2~4문장
- `guideRef`: 전달한 후보 중 하나 또는 `null`
- `sectionRef`: 실제 해당 도움말 섹션 또는 `null`
- `actionLabel`: 최대 30자
- `confidence`: `high | medium | low`
- 클라이언트는 `teacherGuideRegistry.js`로 참조를 다시 검증한다. 존재하지 않는 참조나 후보 밖 참조는 버리고 답변만 표시한다.
- 신뢰도가 낮으면 단정하지 않고 `관련 안내를 정확히 찾지 못했어요`와 가까운 후보를 제안한다.

## 5. 권한과 사용량 제한

기존 `ai_request_events`와 service-role 전용 소비 함수를 새 마이그레이션으로 확장한다. 이미 적용된 마이그레이션은
수정하지 않는다.

### 확정 제한

- 새 범위: `teacher_guide_chat`
- 1차 대상: 실제 `profiles.role='ADMIN'`인 관리자만
- 전체 공개 뒤 대상: 승인 상태인 `TEACHER` 또는 `ADMIN`
- 공개 단계: 서버 설정의 `admin_only | public` 허용 목록으로 관리하며 기본값은 `admin_only`
- 권한 판정: 클라이언트 플래그나 JWT 메타데이터가 아니라 Edge 함수가 DB의 실제 프로필과 서버 공개 단계를 확인
- 일일 제한: 계정당 5회
- 일일 경계: `Asia/Seoul` 기준 00:00~24:00
- 순간 제한: 계정당 분당 3회
- 동시 요청: 교사 ID와 범위 기준 advisory transaction lock으로 5회 초과 방지
- 횟수 소비: OpenAI 호출 직전에 원자적으로 선점
- 실패 복구: 연결 실패, 시간 초과, 상위 서비스 429·5xx, JSON 검증 실패 때 해당 이벤트를 안전하게 취소
- 성공 응답: `remainingToday`, `dailyLimit`, 필요한 경우 `retryAfterSeconds` 반환
- `teacher_ai` 범위와 분리하여 기존 평가·피드백 AI 사용량과 서로 막지 않게 한다.

관리자 선공개 중에는 일반 교사 화면에서 패널과 진입 버튼을 표시하지 않는다. 직접 API를 호출해도 서버가 403으로
거부한다. 전체 공개는 코드 재배포 없이 서버의 공개 단계를 명시적으로 `public`으로 바꾸되, 변경 RPC는 실제 관리자만
실행할 수 있게 한다. 긴급 중지는 공개 단계를 `admin_only`로 되돌리는 것으로 처리한다.

사용량 원장은 교사 ID, 범위, 시각과 최소 상태만 보관한다. 질문·답변·후보 도움말 원문은 저장하지 않는다.
무제한 재시도에 의한 비용 증가를 막기 위해 클라이언트 자동 재시도는 두지 않고 교사가 명시적으로 다시 누르게 한다.

## 6. 구현 위치

- `src/guides/teacherGuideSearch.js`: 원본 도움말을 검색 문서로 바꾸고 후보를 고르는 순수 함수
- `src/components/teacher/TeacherGuideAssistant.jsx`: 질문, 답변, 남은 횟수, 이동 행동 UI
- `src/components/teacher/TeacherGuideCenter.jsx`: 오른쪽 패널 배치와 안내서 위치 이동 연결
- `src/components/teacher/TeacherGuideCenter.css`: PC 3열과 모바일 시트 반응형
- `src/lib/teacherGuideAssistantApi.js`: 전용 요청과 오류·응답 정규화
- `supabase/functions/vibe-ai/index.ts`: `TEACHER_GUIDE_CHAT` 요청 타입, 권한·상한·구조화 출력
- 새 SQL 마이그레이션: `teacher_guide_chat` 일일·분당 원자적 소비와 실패 복구 계약

기존 `callAI`의 일반 자유문장 경로에 억지로 섞지 않고, 구조화 응답과 남은 횟수를 보존하는 전용 API 래퍼를 둔다.

## 7. 단계별 구현

### Phase 1 — 로컬 검색과 안내 이동

- 검색 문서 생성·한국어 정규화·점수 계산 구현
- 상위 3개 후보와 일치 근거를 반환하는 순수 함수 작성
- `guideRef + sectionRef`로 목적 흐름을 고르고 상세 도움말을 펼치는 내부 이동 추가
- AI 없이 검색 후보를 개발 미리보기에서 확인

### Phase 2 — 서버 제한과 AI 계약

- 새 사용 범위와 하루 5회·분당 3회 마이그레이션 추가
- 실패한 선점만 정확히 복구하는 service-role 전용 함수 추가
- 승인 교사 검증 뒤에만 사용량을 소비하도록 Edge 함수 연결
- 입력·후보·문맥·출력 상한과 고정 모델·JSON 응답 검증 적용

### Phase 3 — 챗봇 UI

- PC 오른쪽 패널과 모바일 시트 구현
- 남은 횟수, 전송 중, 분당 제한, 일일 소진, 외부 장애 상태 분리
- 관련 안내 펼치기와 실제 화면 열기 연결
- 현재 모달 안에서만 최근 질답을 유지하고 닫을 때 폐기

### Phase 4 — 관리자 선공개 검증

- 관리자 계정에서만 패널 노출, 질문, 남은 횟수, 안내 펼치기와 화면 이동을 확인한다.
- 일반 교사 계정은 화면 미노출과 직접 API 403을 함께 확인한다.
- 대표 질문 세트의 상위 3개 검색 적중, AI 답변 근거성, 잘못된 이동, 평균 입력·출력 토큰을 기록한다.
- 하루 5회 경계, 자정 초기화, 분당 3회, 동시 요청, 외부 실패 복구를 운영과 같은 조건에서 확인한다.
- 공개를 막을 오류가 없을 때만 관리자가 공개 단계를 `public`으로 바꾼다.

### Phase 5 — 전체 공개와 조정

- 개발용 대표 질문 세트로 검색 상위 3개 적중률을 확인한다.
- 잘못 연결되는 질문은 별도 답변을 추가하지 않고 원본 도움말의 제목·단계 표현이나 검색 가중치를 보완한다.
- 실제 토큰 사용량과 평균 응답 길이를 측정해 후보 길이와 출력 상한을 필요할 때 더 낮춘다.
- 전체 공개 뒤에도 계정당 하루 5회·분당 3회 제한은 관리자와 승인 교사에게 똑같이 적용한다.
- 심각한 오답, 비정상 비용 또는 반복 장애가 생기면 즉시 `admin_only`로 되돌리고 기존 안내서는 유지한다.

## 8. 검사와 완료 기준

### 자동 검사

- 도움말 25개가 검색 색인에 빠짐없이 들어가며 검색용 상세 원본이 별도로 생기지 않는다.
- 후보가 3개, 후보별 450자, 전체 문맥 1,200자, 질문 200자를 넘지 않는다.
- 전체 도움말 원문을 AI 요청에 넣는 경로가 없다.
- 존재하지 않는 `guideRef`·`sectionRef`와 후보 밖 AI 참조로 이동하지 않는다.
- 학생·미승인 교사·변조한 요청을 거부한다.
- 관리자 선공개에서는 일반 교사에게 UI가 보이지 않고 직접 API 호출도 403이며, `public` 전환 뒤에만 승인 교사가 사용한다.
- 공개 단계 변경은 실제 관리자만 가능하고 허용 목록 밖 값은 거부한다.
- 한국 시간 하루 5회, 분당 3회, 동시 요청 상한이 정확하다.
- 성공만 일일 횟수로 남고 외부 실패·잘못된 JSON은 복구된다.
- 질문·답변 원문이 DB와 로그에 기록되지 않는다.
- 기존 안내서는 AI 호출이나 별도 데이터 조회 없이 계속 열린다.
- 키보드, 초점, 모바일 레이아웃과 AI 장애 시 기존 안내서 이용을 확인한다.

### 실행할 검사

1. `node --test tests/teacherGuides.test.mjs tests/teacherGuideCenter.test.mjs`와 새 검색·챗봇 테스트
2. `npm run test:architecture`
3. `npm run migrate:check`
4. `npm run test:security`
5. `npm run lint`
6. `npm run build`
7. DB 없는 `src/dev/` 미리보기에서 PC·모바일·한도 상태를 눈으로 확인

### 완료 정의

- 교사가 대표 질문을 입력하면 짧은 근거 답변과 올바른 도움말 또는 화면 이동을 받는다.
- 한 계정이 한국 시간 하루 5회를 넘겨 외부 AI를 호출할 수 없다.
- 관리자 선공개 인수 검사를 통과하기 전에는 일반 교사가 기능을 발견하거나 호출할 수 없다.
- AI 장애나 한도 소진 중에도 기존 활용 안내서를 온전히 사용할 수 있다.
- 새 도움말을 `teacherGuides.js`에 추가하면 별도 챗봇 지식 수정 없이 검색 대상에 자동 포함된다.

## 9. 구현 순서와 보류 범위

첫 구현은 Phase 1 → Phase 2 → Phase 3 → Phase 4 관리자 선공개 순서로 진행한다. 관리자 검증 결과를 확인한 뒤에만
Phase 5 전체 공개로 전환한다. DB 제한을 우회할 수 있는 UI를 먼저 운영에 내지 않는다.
대화 영구 보관, 교사별 즐겨찾기, 질문 통계, 벡터 검색, 음성 입력, 학생용 챗봇은 1차 범위에서 제외한다.
