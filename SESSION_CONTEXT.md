# 세션 활성 컨텍스트

> 이 파일은 Codex `SessionStart` 훅이 자동 주입하는 **짧은 핫 컨텍스트**다.
> 상세 규칙과 이력은 복제하지 말고 [LLM 위키 인덱스](docs/wiki/README.md)를 따라 필요한 부분만 읽는다.
> 작업을 마칠 때 현재 상태가 달라졌다면 이 파일도 짧게 갱신한다.

## 시작 순서

1. `git log --oneline -20`과 `git branch -a`로 다른 모델의 최근 작업과 미병합 브랜치를 확인한다.
2. [ROADMAP.md](ROADMAP.md)의 `현재 위치`와 최신 `결정 기록`을 읽고 실제 코드와 대조한다.
3. [WORKLOG.md](WORKLOG.md)의 최신 항목 몇 개를 읽는다. 이 파일의 요약만으로 작업 완료 여부를 단정하지 않는다.

## 절대 규칙 요약

- 비밀 값은 코드·문서·로그에 쓰지 않는다. 위치만 기록한다.
- 글쓰기 코어 셸은 직접 확장하지 않고 모듈·슬롯으로 연결한다. 연구소 코드는 이식하지 않고 RPC 데이터만 연동한다.
- 기능 삭제보다 모듈화와 기본 OFF를 우선한다.
- 학급 글 조회, 학생 홈/RPC, 포인트, DB·인증·외부 API 변경은 각각의 성능·보안 계약을 먼저 확인한다.
- 설명 정보 아이콘은 `GuideInfoButton`, 교사 메뉴 안내는 `TeacherGuideButton`과 `teacherGuides.js`를 사용한다.
- 기존 사용자 변경을 보존하고, 관련 검증을 통과한 뒤 `WORKLOG.md`와 `ROADMAP.md`를 갱신한다.

## 현재 위치

- Stage 0~1은 완료했고 Stage 1.5와 Stage 3은 대부분 완료했다.
- 2026-08-16에 어휘의 탑 V2를 문항 검수부터 운영 전면 적용까지 마쳤다. 전체 활성 학급이 V2이고 교사 화면의
  V1 선택은 제거했으며(V1 데이터·RPC는 비상 롤백용으로만 서버 보존), 학생 화면은 낱말별 익힘 상태·적응 복습,
  덱별 최초 12/12 포인트, 지그재그 탐험 지도까지 반영했다. 상세는 [WORKLOG.md](WORKLOG.md) 최신 항목을 읽는다.
- 2026-08-11의 맞춤법 500개·보고서 장르·전용 PDF·Excel/Google Docs 내보내기는 구현을 마쳤고 실계정 확인만 남았다.
- 2026-08-17에 내 글 소식(친구 반응·댓글)을 활동 알림과 같은 `student_notification_events` 원장으로
  통합했다. `module_id='feedback'`이 헤더 🔔, 나머지가 홈 활동 알림이다. 학생은 알림별 `✓ 확인`과
  `모두 확인`으로 정리한다. 과거 소식은 소급 생성하지 않고 전환 시점에 선을 그었다(방학이라 손실 없음).
  읽음 처리 뒤에는 반드시 `studentHomeApi.invalidate()`로 홈 캐시를 비워야 배지가 되살아나지 않는다.
- 2026-08-17 기본 로직 점검에서 **학생이 자기 글의 포인트 지급액을 고칠 수 있는 취약점**을 찾아 막았다
  (`20261117_guard_student_post_server_columns.sql`). 포인트로 이어지는 값은 서버만 쓴다. 가드는
  SECURITY INVOKER여야 하고(DEFINER면 무력화), 과제 임시저장도 `save_my_assignment_draft_v1` RPC로
  옮겨 학생 화면이 보상·상태 값을 아예 보내지 않는다. 학생 쓰기는 전부 전용 RPC를 쓴다.
- 2026-08-17에 임시 저장 성공 알림을 대화상자에서 화면 안 표시로 바꿨다(과제·독서록·일기). 태블릿에서
  `alert`은 키보드를 닫아 화면이 튀고, 반복되면 브라우저가 조용히 막는다. **실패 알림만 대화상자로 남긴다.**
- 운영 `agit-db`는 마이그레이션 155개 적용·대기 0이고, 보고서 사진은 `agit-storage-data` named volume에
  저장된다. DB와 Storage를 건드릴 때는 최신 운영 상태를 다시 조회한다.
- 러너 4개(`vibe_agit`·`writing-helper`·`classroom-tools`·`URL`) plist에 `KeepAlive`를 넣어 죽으면 스스로
  되살아난다(2026-08-17. 그전까지 3개가 조용히 죽어 있었다. GitHub 기본 템플릿에 KeepAlive가 없다).
  일부러 멈출 때는 `launchctl bootout`을 쓴다 — `stop`만으로는 다시 뜬다.
- 2026-08-17에 **공통 학습 엔진을 분리**했다(`learning_item_progress`·`learning_engine_*`). 어휘의 탑이
  첫 콘텐츠이고 읽기·쓰기 모두 엔진을 쓴다. 묶음은 `collection_key`(어휘 `g3:d1`)로 두고 엔진은 해석하지 않는다.
  그 위에 **덱마스터**(공식 도전)를 콘텐츠 중립으로 구현했다 — 자격 익힘 80%, 12문항(선택 7+직접입력 5),
  합격 10/12 그리고 직접입력 3/5, 문항당 45초. 전부 학급 설정이라 마이그레이션 없이 조정한다.
  2026-08-18에 **휘장 표시**를 세 화면(나의 아지트·친구 아지트·교사 학생 아지트)에 붙였고, 같은 날
  **도전 조건 6개를 교사 대시보드에서 정하도록** 화면을 붙였다(RPC가 학급 열을 읽으므로 재배포 없이 적용).
  공개 범위는 화면이 아니라 **보는 사람마다 다른 RPC**로 가른다 — 친구용 응답에는 진행도가 아예 없다.
  ⚠️ 자격은 비율만이 아니다 — **그 층 낱말을 모두 한 번씩 만나야**(`v_seen >= v_items`) 열린다.
  같은 날 **어휘 마스터 정상 관문**도 만들었다(`20261124`·`20261125`). 10개 덱마스터를 채우면
  휘장이 아니라 **지도 꼭대기의 관문이 열리고**, **1·2·3단계를 순서대로** 통과해야 별이 하나씩 늘어난다
  (⭐ → ⭐⭐ → ⭐⭐⭐). 난이도 축은 **직접입력 비중 하나**다(8 → 14 → 20개). 문항 수·합격선·시간은
  세 단계가 같다 — 이 저장소는 시간으로 난이도를 올리지 않는다.
  자격은 **현재 학년 10개 층**으로 좁혀서 본다(엔진 판정은 학년을 모른다). 도전 버튼은 자격이
  없어도 늘 보이고, 누르면 시험 대신 **무엇이 모자란지**가 열린다.
  ⚠️ **자격 계산을 화면에서 하지 않는다** — 개요 RPC의 `master_eligible`·`summit` 을 받아 쓴다.
  휘장 카드는 **정상이 주인공**이고 관문은 아래 진행 띠다. 잠긴 정상도 늘 보인다(친구 아지트 포함) —
  목표는 보여야 향해 간다. 진행 숫자만 친구용 응답에서 빠진다.
  **다음은 재도전 제한(보충 수련 뒤 열기)**이고, 그 뒤 속담 85개를 두 번째 콘텐츠로 붙여 추상화를 검증한다.
- **테스트 학급에 심어 둔 진도가 있다**(2026-08-18). 자격을 채운 학생이 운영에 하나도 없어
  테스트 학급 유예담(`006c9aeb`) 한 명의 **4학년 10개 층 전체** 진도를 엔진과 같은 모양으로 채우고,
  덱마스터 10개를 실제 RPC 로 치러 통과시켰다. 그래서 그 계정은 지도 꼭대기의 정상 관문이 열려 있다
  (휘장은 아직 없다 — 시험을 통과해야 받는다). 확인이 끝나면 되돌린다:
  시도 삭제 → vocab 진도 삭제 → `public._deck_master_test_backup_20260818`(손대기 전 48행) 복원 → 표 삭제.
  자세한 순서는 WORKLOG 해당 항목에 있다.
- 그 밖의 후보: 자리배치 → 수업 도구(ROADMAP 2e-2), `나의 아지트 디자인 3차`.
  서바이벌 13종 전체 흡수 계획은 2026-08-17에 폐기했다(전제가 실제와 달랐다 — ROADMAP 2e 머리말).
- Caddy `/lab` 분기와 `helper.` 301 리다이렉트는 **디스크(`/etc/caddy/Caddyfile`)에 반영 완료**다
  (2026-08-18 00:04). 어댑트 결과가 실행 중 설정과 일치하는 것을 확인했으므로 재시작에도 살아남는다.
- 옛 연구소 독립 실행판은 컨테이너·CI 배포 단계까지 제거했다(2026-08-17). `helper.` 주소는 `/lab` 301.
  공용 `supabase-kong`은 샘링크·자비스가 쓰므로 **유지**하되 `127.0.0.1`로 조였고, 샘링크는
  `kong:8000`(도커 네트워크)으로 직접 붙는다. 두 kong 모두 이제 로컬 바인딩이다.
- 덱마스터는 DB·화면 모두 **배포 완료**(2026-08-18). GitHub 429가 이어져 **CI 없이 컨테이너에 직접 배포**했다
  (워크플로 단계를 그대로 실행). Actions에 `ba0a284`가 실패로 남아 있지만 실제 운영은 최신이다.
  **하루에 배포를 열 번 넘게 돌리면 429에 걸린다 — 변경을 묶어 한 번에 배포한다.**
- ⚠️ **화면이 걸린 변경은 브라우저로 한 번 열어 보고 배포한다.** 2026-08-18에 덱마스터가
  `게임을 여는 중 문제가 생겼어요`로 죽었다 — 선언보다 먼저 쓴 변수(TDZ)라 빌드도 린트도 통과했다.
  Playwright로 해당 화면 단계까지 도달시키는 검사가 유효했다.
- 디스크는 세 겹으로 지킨다(2026-08-18): ①`scripts/preflight-disk.sh` 가 여유 10GB 미만이면 **배포를 막고**
  ②04:00 백업 스크립트가 Node 컴파일 캐시(>2GB)·도커 빌드 캐시(>3GB)·Electron 앱 캐시(>8GB)를
  **기준을 넘을 때만** 비우며 ③여유 10GB 미만이거나 inode 80% 이상이면 **화면 알림**이 뜬다.
  기준을 넘을 때만 비우는 이유는 매일 지우면 캐시 이득이 사라져 늘 느려지기 때문이다.
- 실기기 인수 검사는 [MANUAL_ACCEPTANCE_CHECKLIST.md](MANUAL_ACCEPTANCE_CHECKLIST.md)에 계속 기록한다.

## 다음 세션 첫 순서

0. 테스트 학급에서 **덱마스터 12문항을 실제로 치러 보고** 위 백업으로 진도를 되돌린다.
1. **재도전 제한** — ROADMAP의 "부족한 낱말 보충 수련 뒤 열기". 지금은 자격만 유지되면 바로 다시 친다.
   덱마스터·정상 관문 둘 다 해당한다.
2. `git log`·브랜치·작업 트리를 확인한다.
3. 학생 태블릿에서 과제 임시 저장을 눌러 키보드가 닫히지 않고 `저장했어요 ✓`가 뜨는지, 사진이 있는 보고서
   초안과 다시쓰기 저장이 정상인지 확인한다(임시저장이 RPC로 바뀌어 경로가 통째로 달라졌다).
4. 학생 실계정 PC·태블릿·모바일에서 어휘의 탑 탐험 지도의 첫 진입 위치 이동, 지그재그 경로 밀도,
   네 가지 낱말 상태 칸과 포인트 완료 카드 구분을 확인한다.
5. 교사 실계정에서 어휘의 탑 관리 화면에 V1 선택이 없고 학년 변경 시 현재 덱 안내가 함께 바뀌는지,
   일반 PC 해상도에서 설정 저장 버튼까지 스크롤 없이 보이는지 확인한다.
6. 운영 Google Cloud의 Drive API 활성화를 확인하고, 교사 실계정으로 WebP 보고서를 Excel·Google Docs로
   내보내 사진 위치와 Google Drive 임시 파일 삭제를 확인한다.
7. 학생 태블릿 보고서 전체 동선, 교사 PDF 두 양식, 맞춤법 500개 검색·랜덤 5문제를 확인한다.
8. 다음 04:00 백업 뒤 새 `아지트Storage.tar.gz`가 내장·암호화·외장 사본에 포함됐는지, 백업 로그에 `✓ 알림정리`가 남는지 확인한다.
9. ⚠️ **배포 전에 반드시 `npm run preflight:disk` 를 돌린다**(CI 워크플로에도 첫 단계로 들어 있다).
   여유 10GB 미만이면 배포가 막힌다 — 2026-08-18에 디스크가 100% 찬 채로 빌드하다 도커 데몬이
   물려 앱이 내려갔다(DB는 무사). 가장 위험한 순간은 **옛 컨테이너를 지운 뒤 새 것을 못 띄우는 사이**다.
   맥에서 `https://끄적끄적아지트.site/` 는 공유기 헤어핀 미지원으로 **항상 타임아웃**이다 — 장애가 아니다.
   확인은 `curl -k --resolve "xn--vz0ba242ncqcba79xhwx.site:443:127.0.0.1" https://.../` 로 한다.
10. `docker system df`로 빌드 캐시를 확인한다. 도커 VM 디스크는 31GB뿐이라 캐시가 차면 Kong 건강검진 실패와
   Storage 업로드 실패로 번진다. 차오르면 `docker builder prune -a -f`로 비운다(2026-08-16에 16GB 회수).

## 작업 종료

- [WORKLOG.md](WORKLOG.md) 맨 위에 작업 배경·변경·검증·남은 일을 기록한다.
- 완료 상태나 방향이 바뀌었으면 [ROADMAP.md](ROADMAP.md)의 체크박스와 결정 기록을 갱신한다.
- 이 파일에는 다음 세션이 즉시 알아야 할 **현재 상태만** 남기고 상세 이력을 누적하지 않는다.
