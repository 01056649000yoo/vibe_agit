# 세션 활성 컨텍스트

> 이 파일은 Codex `SessionStart` 훅이 자동 주입하는 **짧은 핫 컨텍스트**다.
> 상세 규칙과 이력은 복제하지 말고 [LLM 위키 인덱스](docs/wiki/README.md)를 따라 필요한 부분만 읽는다.
> 작업을 마칠 때 현재 상태가 달라졌다면 이 파일도 짧게 갱신한다.

## 시작 순서

0. **[docs/wiki/PITFALLS.md](docs/wiki/PITFALLS.md)** 를 훑는다 — 되풀이하지 말 것 한 장(36줄).
   사고를 겪거나 잘못 판단했으면 작업 끝에 여기 한 줄을 더한다.
1. `git log --oneline -20`과 `git branch -a`로 다른 모델의 최근 작업과 미병합 브랜치를 확인한다.
2. [ROADMAP.md](ROADMAP.md)의 `현재 위치`와 최신 `결정 기록`을 읽고 실제 코드와 대조한다.
3. [WORKLOG.md](WORKLOG.md)의 최신 항목 몇 개를 읽는다. 이 파일의 요약만으로 작업 완료 여부를 단정하지 않는다.
   **WORKLOG 를 통째로 읽지 않는다** — 최근치만 두고(2,500줄 한도) 지난 달치는 [docs/worklog/](docs/worklog/) 에 있다.
   옛일은 `grep -n "찾을 말" docs/worklog/*.md` 로 찾는다. 세션 시작 훅이 최근 5건 제목은 이미 넣어 준다.

## 절대 규칙 요약

- 말은 우리 말로 맞춘다 — `git push` 하면 도는 것은 **자동 배포**라고 쓴다(CI 라고 쓰지 않는다).
  손으로 하는 것은 **로컬 배포**(`npm run deploy:local`)다. 옛 기록의 표현은 그대로 둔다.
- **한 곳만 고치고 끝내지 않는다** — 같은 값을 여러 곳에서 쓰면 원본을 하나로 모으고,
  못 모으면 그 곳들을 **한꺼번에 보는 검사 하나**를 만든다. 커밋 전 `npm run checklist`.
- 비밀 값은 코드·문서·로그에 쓰지 않는다. 위치만 기록한다.
- 글쓰기 코어 셸은 직접 확장하지 않고 모듈·슬롯으로 연결한다. 연구소 코드는 이식하지 않고 RPC 데이터만 연동한다.
- 기능 삭제보다 모듈화와 기본 OFF를 우선한다.
- 학급 글 조회, 학생 홈/RPC, 포인트, DB·인증·외부 API 변경은 각각의 성능·보안 계약을 먼저 확인한다.
- 설명 정보 아이콘은 `GuideInfoButton`, 교사 메뉴 안내는 `TeacherGuideButton`과 `teacherGuides.js`를 사용한다.
- **UI를 다듬을 땐 값을 화면에서 직접 적지 않는다** — 색·글자·간격·모서리는 `src/styles/design-system.css`의
  `--ui-*` 토큰을 쓴다. 글자는 `--ui-text-xs~3xl` 일곱 단계뿐이고 **바닥은 0.8rem**이다(그 아래는 아이가
  태블릿에서 못 읽는다). 버튼·카드·창은 `components/common`의 공용 부품을 먼저 쓴다.
  옮긴 화면은 `tests/teacherTypeScale.test.mjs`가 지키므로, 새로 옮겼으면 그 `MIGRATED` 목록에 한 줄 추가한다.
  상세는 [디자인 가이드](docs/wiki/DESIGN_GUIDE.md).
- 기존 사용자 변경을 보존하고, 관련 검증을 통과한 뒤 `WORKLOG.md`와 `ROADMAP.md`를 갱신한다.
- **푸시 전 검사를 건다** — `npm run hooks:install` 한 번. 배포 실패의 상당수가 *올리지 않은 새 파일*
  탓인데 로컬에는 파일이 있어 검사가 통과하므로 로컬로는 잡을 수 없다. 셸·시스템 도구에 기대는 검사를
  새로 넣을 때는 배포 관문과 같은 환경에서 먼저 돌린다:
  `docker run --rm -v "$PWD":/app -w /app node:20-alpine npm run test:all`
- Docker 재시작 후 통합 스택은 `cd ~/agit-supabase && docker compose up -d`로 올린다.
  기본은 필수 9개만 올라오고 `admin`·`observability`·`pooler` profile은 필요할 때만 쓴다.
  예전 `supabase` 스택은 2026-08-28에 제거했다. 앱 컨테이너는 배포 compose/런너로 다시 올린다.

## 현재 위치

- **2026-09-05 (우리반 아지트 외부 공유)** — `61248`까지 운영 적용했고 **외부 공유 전역 스위치가 켜져 있다**
  (`class_agit_rollout`: `pilot`·`external_enabled=TRUE`). 시범 학급은 **진남초 4학년 1반 하나뿐**이고
  범위를 넓히지 않는다. 4단계 공유는 교사가 주제별 표에서 제목·지은이·주제를 확인·수정해 발행하고
  주소는 샘링크 단축주소 + QR로 나간다. 되돌릴 때는 `external_enabled=FALSE`로 바꾸고 발행된 주소는 따로 해지한다.
- **연구소(`writing-helper`)를 만지기 전에 반드시 `git fetch` 로 원격과 대조한다.** 2026-08-27에
  로컬 사본이 **74커밋 뒤처져** 있어 낡은 코드 위에 고쳤다가 푸시가 거부됐다. 그사이 질문 만들기가
  통째로 개편돼 고치려던 파일 6개가 전부 바뀌어 있었다.
- **2026-08-27 (연구소 질문 활동)** — 질문 수는 이제 **목표·최소를 따로** 정한다(목표 3·최소 2 가능).
  상한은 `question-generator/config.ts` 의 `QUESTION_SELECTION_MAX`(현재 5) 하나가 원본이고 교사 화면
  선택지까지 거기서 나온다. 교사가 질문을 읽으면서 `☆ 담기` 로 고르기 후보를 미리 담으면
  (`submission.selections[].pickedForVoting`) 고르기 활동에 이미 골라진 채로 올라온다 —
  칠판·전체화면·학생별 보기·질문만 모아보기 네 곳 모두에서 담을 수 있다.
  **AI 맞춤법 자동 교정은 제거했다**(아지트 AI 가 교사 요청에 출력 1000토큰만 주어 응답이 잘렸다).
  상세는 [WORKLOG.md](WORKLOG.md) 최신 항목.
- Stage 0~1은 완료했고 Stage 1.5와 Stage 3은 대부분 완료했다.
- 2026-08-16에 어휘의 탑 V2를 문항 검수부터 운영 전면 적용까지 마쳤다. 전체 활성 학급이 V2이고 교사 화면의
  V1 선택은 제거했으며(V1 데이터·RPC는 비상 롤백용으로만 서버 보존), 학생 화면은 낱말별 익힘 상태·적응 복습,
  덱별 최초 12/12 포인트, 지그재그 탐험 지도까지 반영했다. 상세는 [WORKLOG.md](WORKLOG.md) 최신 항목을 읽는다.
- **2026-08-19~20 (연구소·맞춤법 집중 작업)** — 상세는 [WORKLOG.md](WORKLOG.md) 최신 항목들을 읽는다.
  - 질문 만들기: 방식 3가지(직접/카드/선생님 카드)를 시작 조건까지 실제로 갈랐고, 학생 화면은
    **단계 없는 한 화면**(`question_build`)이다. 판정의 원본은 `question-generator/config.ts`,
    카테고리 기준은 `areas.ts` 하나다. **역할(탐정 모드 등)은 걷어냈다.**
    질문 카드는 완성된 질문이 아니라 **관점 문장**이라 입력창에 그대로 채우지 않는다.
  - 개요 짜기: 교사가 `학생이 고쳐 쓰기`(기본) / `선생님 틀 그대로` 중 고른다(`studentEditable`).
    학생이 불러오는 질문은 **좋은 질문 고르기의 득표순 결과**다(투표 전이면 후보).
  - 연구소 도움말은 `features/activities/guide.ts` 하나가 원본이고, **상단 메뉴 `도움말`**(`/dashboard/guide`)과
    활동 만들기 화면의 `❓ 활동 도움말` 창이 같이 읽는다(학급 목록 탭에 있던 것을 2026-08-20에 옮겼다).
    화면은 `공통 흐름 ① / 활동별 탭 ② / 공통 주의 ③` 세 덩어리이고 꼬리표로 공통·활동별을 밝힌다.
    동그란 번호 배지는 `components/badge-circle.tsx` 하나를 쓴다(display 유틸리티 충돌 검사 있음).
  - 아지트: 대시보드 이름 `글쓰기 연구소 (beta)` · 학생 홈 카드 **NEW**(`home.has_new_lab_activity`) ·
    연구소 결과 불러오기 **기본 켜짐**이고 자리는 **참고함 하나**(끄면 참고함에서도 사라진다) ·
    연구소 결과에 활동을 가려 주는 한 줄(`lab_result_hint_v1`, 한줄모아는 핵심 낱말).
  - **AI 맞춤법 검사**(새 기능, 학급 스위치 기본 꺼짐): **다시 쓰기 요청을 받은 글**에만 열리고
    **글 한 편에 한 번**이다. 횟수는 서버가 쥐고(`spell_check_used_at` + 서버 소유 열 가드),
    본문은 Edge Function 이 DB 에서 읽는다. 무의미 철자는 먼저 걸러 기회를 쓰지 않는다.
    Edge Function(`vibe-ai`)은 앱 이미지 밖(맥미니 폴더)에서 돈다. **자동 배포도 `npm run deploy:local` 도
    이제 자동으로 맞춘다**(2026-08-20에 로컬 배포에도 같은 단계를 넣었다) — 손으로 복사할 일은 없다.
  - 맞춤법 데이터: 화면을 `우리 반 배움 현황 / 항목 만들기 / 등록 데이터` 세 갈래로 정리했고,
    학생 검색(`spelling_search_corpus`)과 AI가 찾은 표현(`spelling_ai_findings`)을 **학급·학생 식별자 없이**
    누적한다. 관리자 `🔤 맞춤법 승격 검토` 탭에서 **여러 학급에서 되풀이되는 것만**(기본 2학급·3회)
    골라 확정하면 카탈로그에 붙여 넣을 코드 조각을 만들어 준다(코드는 사람이 반영·배포한다).
  - 개인정보 처리방침 제5조를 실제와 맞췄다 — Supabase 행 삭제(자체 호스팅), **OpenAI**와
    **암호화 백업(Google Drive)** 추가. AI 피드백이 보내던 **학생 이름도 제거**했다.
- 2026-08-11의 맞춤법 500개·보고서 장르·전용 PDF·Excel/Google Docs 내보내기는 구현을 마쳤고 실계정 확인만 남았다.
- 2026-08-17에 내 글 소식(친구 반응·댓글)을 활동 알림과 같은 `student_notification_events` 원장으로
  통합했다. `module_id='feedback'`이 헤더 🔔, 나머지가 홈 활동 알림이다. 학생은 알림별 `✓ 확인`과
  `모두 확인`으로 정리한다. 과거 소식은 소급 생성하지 않고 전환 시점에 선을 그었다(방학이라 손실 없음).
  읽음 처리 뒤에는 반드시 `studentHomeApi.invalidate()`로 홈 캐시를 비워야 배지가 되살아나지 않는다.
- 2026-08-17 기본 로직 점검에서 **학생이 자기 글의 포인트 지급액을 고칠 수 있는 취약점**을 찾아 막았다
  (`20261117_guard_student_post_server_columns.sql`). 포인트로 이어지는 값은 서버만 쓴다. 가드는
  SECURITY INVOKER여야 하고(DEFINER면 무력화), 과제 임시저장도 `save_my_assignment_draft_v1` RPC로
  옮겨 학생 화면이 보상·상태 값을 아예 보내지 않는다. 학생 쓰기는 전부 전용 RPC를 쓴다.
- 2026-08-17에 임시 저장 성공 알림을 대화상자에서 화면 안 표시로 바꿨다(과제·독서록·일기). 태블릿에서
  `alert`은 키보드를 닫아 화면이 튀고, 반복되면 브라우저가 조용히 막는다. **실패 알림만 대화상자로 남긴다.**
- 운영 `agit-db`의 마이그레이션 적용 상태는 **`npm run migrate:status`로 직접 확인한다**(맥미니에서만 된다 —
  다른 기기에는 컨테이너가 없다). 여기에 개수를 적어 두지 않는다 — 하루에도 여러 번 늘어 곧 틀린 말이 된다.
  보고서 사진은 `agit-storage-data` named volume에 저장된다. DB와 Storage를 건드릴 때는 최신 운영 상태를 다시 조회한다.
- 러너 4개(`vibe_agit`·`writing-helper`·`classroom-tools`·`URL`) plist에 `KeepAlive`를 넣어 죽으면 스스로
  되살아난다(2026-08-17. 그전까지 3개가 조용히 죽어 있었다. GitHub 기본 템플릿에 KeepAlive가 없다).
  일부러 멈출 때는 `launchctl bootout`을 쓴다 — `stop`만으로는 다시 뜬다.
- 2026-08-17에 **공통 학습 엔진을 분리**했다(`learning_item_progress`·`learning_engine_*`). 어휘의 탑이
  첫 콘텐츠이고 읽기·쓰기 모두 엔진을 쓴다. 묶음은 `collection_key`(어휘 `g3:d1`)로 두고 엔진은 해석하지 않는다.
  그 위에 **덱마스터**(공식 도전)를 콘텐츠 중립으로 구현했다 — 자격 익힘 80%, 12문항(선택 7+직접입력 5),
  합격 10/12 그리고 직접입력 3/5, 문항당 45초. 전부 학급 설정이라 마이그레이션 없이 조정한다.
  2026-08-18에 **휘장 표시**를 세 화면(나의 아지트·친구 아지트·교사 학생 아지트)에 붙였고, 같은 날
  **도전 조건 6개를 교사 대시보드에서 정하도록** 화면을 붙였다(RPC가 학급 열을 읽으므로 재배포 없이 적용).
  공개 범위는 화면이 아니라 **보는 사람마다 다른 RPC**로 가른다 — 친구용 응답에는 진행도가 아예 없다.
  ⚠️ 자격은 비율만이 아니다 — **그 층 낱말을 모두 한 번씩 만나야**(`v_seen >= v_items`) 열린다.
  같은 날 **어휘 마스터 정상 관문**도 만들었다(`20261124`·`20261125`). 10개 덱마스터를 채우면
  휘장이 아니라 **지도 꼭대기의 관문이 열리고**, **1·2·3단계를 순서대로** 통과해야 별이 하나씩 늘어난다
  (⭐ → ⭐⭐ → ⭐⭐⭐). 난이도 축은 **직접입력 비중 하나**다(8 → 14 → 20개). 문항 수·합격선·시간은
  세 단계가 같다 — 이 저장소는 시간으로 난이도를 올리지 않는다.
  자격은 **현재 학년 10개 층**으로 좁혀서 본다(엔진 판정은 학년을 모른다). 도전 버튼은 자격이
  없어도 늘 보이고, 누르면 시험 대신 **무엇이 모자란지**가 열린다.
  ⚠️ **자격 계산을 화면에서 하지 않는다** — 개요 RPC의 `master_eligible`·`summit` 을 받아 쓴다.
  휘장 카드는 **정상이 주인공**이고 관문은 아래 진행 띠다. 잠긴 정상도 늘 보인다(친구 아지트 포함) —
  목표는 보여야 향해 간다. 진행 숫자만 친구용 응답에서 빠진다.
  **다음은 재도전 제한(보충 수련 뒤 열기)**이고, 그 뒤 속담 85개를 두 번째 콘텐츠로 붙여 추상화를 검증한다.
- 테스트 진도는 **2026-08-18에 모두 되돌렸다**. 운영에 남은 테스트 데이터는 없다
  (익힘 보유 학생 0 · 덱마스터 통과 0 · 휘장 0). 정상 관문 **1단계는 실계정으로 통과 확인**했다
  (19/20 · 229초). 실기기 점검 항목은 `MANUAL_ACCEPTANCE_CHECKLIST.md` 의 **9-2 절**에 있다.
  
- 그 밖의 후보: 자리배치 → 수업 도구(ROADMAP 2e-2), `나의 아지트 디자인 3차`.
  서바이벌 13종 전체 흡수 계획은 2026-08-17에 폐기했다(전제가 실제와 달랐다 — ROADMAP 2e 머리말).
- Caddy `/lab` 분기와 `helper.` 301 리다이렉트는 **디스크(`/etc/caddy/Caddyfile`)에 반영 완료**다
  (2026-08-18 00:04). 어댑트 결과가 실행 중 설정과 일치하는 것을 확인했으므로 재시작에도 살아남는다.
- 옛 연구소 독립 실행판은 컨테이너·자동 배포 단계까지 제거했다(2026-08-17). `helper.` 주소는 `/lab` 301.
  공용 `supabase-kong`은 샘링크·자비스가 쓰므로 **유지**하되 `127.0.0.1`로 조였고, 샘링크는
  `kong:8000`(도커 네트워크)으로 직접 붙는다. 두 kong 모두 이제 로컬 바인딩이다.
- 덱마스터는 DB·화면 모두 **배포 완료**(2026-08-18). GitHub 429가 이어져 **자동 배포를 거치지 않고 컨테이너에 직접 배포**했다
  (워크플로 단계를 그대로 실행). Actions에 `ba0a284`가 실패로 남아 있지만 실제 운영은 최신이다.
  **하루에 배포를 열 번 넘게 돌리면 429에 걸린다 — 변경을 묶어 한 번에 배포한다.**
- ⚠️ **화면이 걸린 변경은 브라우저로 한 번 열어 보고 배포한다.** 2026-08-18에 덱마스터가
  `게임을 여는 중 문제가 생겼어요`로 죽었다 — 선언보다 먼저 쓴 변수(TDZ)라 빌드도 린트도 통과했다.
  Playwright로 해당 화면 단계까지 도달시키는 검사가 유효했다.
- 디스크는 세 겹으로 지킨다(2026-08-18): ①`scripts/preflight-disk.sh` 가 여유 10GB 미만이면 **배포를 막고**
  ②04:00 백업 스크립트가 Node 컴파일 캐시(>2GB)·도커 빌드 캐시(>3GB)·Electron 앱 캐시(>8GB)를
  **기준을 넘을 때만** 비우며 ③여유 10GB 미만이거나 inode 80% 이상이면 **화면 알림**이 뜬다.
  기준을 넘을 때만 비우는 이유는 매일 지우면 캐시 이득이 사라져 늘 느려지기 때문이다.
- **칭호 기준의 원본은 `src/constants/writerLevels.js` 하나다**(2026-08-18). DB 함수는 거기서
  생성한다 — `npm run sync:title-levels` → `npm run migrate`. `npm run check:title-levels` 가
  ①화면·DB 기준 일치 ②기준 숫자를 따로 베껴 쓴 함수가 없는지를 보고, 배포 워크플로가 먼저 실행한다.
  DB 쪽을 지울 수 없는 이유는 학기 마감 스냅샷이 **그때의 칭호를 얼려 두기** 때문이다.
- **Realtime 구독은 이제 소스에 하나도 없다**(2026-08-18 확인 — `postgres_changes`·`.channel(` 0건).
  갱신은 화면 복귀·포커스 때 다시 부르는 방식이다. 새 기능에 구독을 되살리지 않는다.
- 실기기 인수 검사는 [MANUAL_ACCEPTANCE_CHECKLIST.md](MANUAL_ACCEPTANCE_CHECKLIST.md)에 계속 기록한다.

## 다음 세션 첫 순서

0. **이어서 할 것 — 속담·사자성어 검수** (2026-08-24 기준, 185개 중 20개만 검수 완료).
   초안은 **165개 전부 작성됐다**. 이제 필요한 것은 **사람의 검수**다.
   ① 교사가 **3·4학년 20개를 최종 확인**한다(이미 `editorial_review` 상태).
   ② 이어 **나머지 시범 20개부터** 표현·뜻·한자·예문·난이도·오답을 항목별로 검수한다.
   ⚠️ **확인된 묶음만 순차 승격한다 — 185개를 한꺼번에 공개하지 않는다.**
   새 165개는 `source_imported + editorial_review_required + teacher_confirmation_required` 라
   검수 완료로 오인하거나 학생에게 바로 출제할 수 없다(검사가 막는다).
   운영 DB 시드·학생 화면·출제 RPC 는 **아직 연결하지 않았다**.
   상세는 [WORKLOG.md](WORKLOG.md) 의 `2026-08-24 — 나머지 속담·사자성어 165개 편집 초안 완성` 항목.
1. **연구소·맞춤법 실계정 확인 (밀린 것 한 묶음)** — 이번 작업은 대부분 로그인 뒤 화면이라 브라우저로
   끝까지 못 본 것이 있다. 교사 계정으로 ①질문 만들기 세 방식 ②개요 짜기 두 방식(`틀 그대로`는 항목을
   못 빼고 모두 열려 있어야 한다) ③`＋ 질문 카드 만들기` ④`📖 도움말` 탭과 `❓ 활동 도움말`
   ⑤`설정 → 맞춤법 배움 데이터` 세 탭 ⑥`글쓰기창 관리`에서 `AI 맞춤법 검사` 켜기.
   학생 계정으로 ⑦다시 쓰기 요청 뒤 참고함 아래 안내 줄 → 확인 물음 → 결과(두 번째는 같은 결과)
   ⑧개요에서 득표 질문 불러오기(`친구들 N표`) ⑨한줄모아 두 개가 핵심 낱말로 갈리는지
   ⑩홈 카드 **NEW** 가 새 활동에만 뜨는지.
1-2. **작가 칭호 급간 조정(보류 중)** — 실제 분포가 Lv2~5에 85%, Lv7 이상 6.6%, Lv10은 0.3%다.
   10개 칭호 중 6개를 첫 5.5편(약 3주)에 받고 나머지 4개에 38편이 든다. **Lv7→8 간격(5,460)이
   Lv8→9(4,680)보다 큰** 구조적 결함도 있다. 일기·독서록이 이미 작가 글자 수에 들어가므로
   (`writing_context='self'`) 한 학기 사용량을 보고 정하기로 했다. 칭호는 학기마다 0부터 다시 세니
   **방학 중에 바꾸면 강등되는 학생이 없다.** 기준은 `src/constants/writerLevels.js` 한 곳만 고치고
   `npm run sync:title-levels && npm run migrate` 를 돌리면 된다(잊으면 배포가 막힌다).
1-3. **밀린 콘텐츠 작업** — 속담·사자성어는 **0번에서 진행 중**이다(공통 학습 자료 토대까지 완료).
   맞춤법 기본 자료 500개 확장은 이제 **관리자 승격 검토 화면**을 통해 근거가 쌓인 뒤에 한다.
   ⚠️ 옛 방의 `expires_at`: 기한 개념은 없앴지만 학생 목록 RPC 는 만료된 옛 방을 아직 거른다.
   NEW 판정도 같은 조건으로 맞췄다 — 옛 방을 다시 보일지는 교사 결정이 필요하다.
2. `git log`·브랜치·작업 트리를 확인한다.
3. 학생 태블릿에서 과제 임시 저장을 눌러 키보드가 닫히지 않고 `저장했어요 ✓`가 뜨는지, 사진이 있는 보고서
   초안과 다시쓰기 저장이 정상인지 확인한다(임시저장이 RPC로 바뀌어 경로가 통째로 달라졌다).
4. 학생 실계정 PC·태블릿·모바일에서 어휘의 탑 탐험 지도의 첫 진입 위치 이동, 지그재그 경로 밀도,
   네 가지 낱말 상태 칸과 포인트 완료 카드 구분을 확인한다.
5. 교사 실계정에서 어휘의 탑 관리 화면에 V1 선택이 없고 학년 변경 시 현재 덱 안내가 함께 바뀌는지,
   일반 PC 해상도에서 설정 저장 버튼까지 스크롤 없이 보이는지 확인한다.
6. 운영 Google Cloud의 Drive API 활성화를 확인하고, 교사 실계정으로 WebP 보고서를 Excel·Google Docs로
   내보내 사진 위치와 Google Drive 임시 파일 삭제를 확인한다.
7. 학생 태블릿 보고서 전체 동선, 교사 PDF 두 양식, 맞춤법 500개 검색·랜덤 5문제를 확인한다.
8. 다음 04:00 백업 뒤 새 `아지트Storage.tar.gz`가 내장·암호화·외장 사본에 포함됐는지, 백업 로그에 `✓ 알림정리`가 남는지 확인한다.
9. ⚠️ **아지트 배포는 `npm run deploy:local` 로 한다**(2026-08-19 신규). 손으로 `docker build ... | tail`
   처럼 파이프를 걸면 **빌드 실패를 삼켜** 옛 이미지로 컨테이너만 재시작되고 200이 떠서 성공처럼 보인다.
   이미지 빌드 안에서 아키텍처·보안·배포 검사가 도므로 **빌드 실패는 곧 검사 실패**다.
   이 스크립트는 배포 뒤 서빙 번들이 로컬 빌드와 같은지까지 확인하고 날짜 태그를 남긴다.
   ⚠️ **배포 전에 반드시 `npm run preflight:disk` 를 돌린다**(자동 배포에도 첫 단계로 들어 있다).
   여유 10GB 미만이면 배포가 막힌다 — 2026-08-18에 디스크가 100% 찬 채로 빌드하다 도커 데몬이
   물려 앱이 내려갔다(DB는 무사). 가장 위험한 순간은 **옛 컨테이너를 지운 뒤 새 것을 못 띄우는 사이**다.
   맥에서 `https://끄적끄적아지트.site/` 는 공유기 헤어핀 미지원으로 **항상 타임아웃**이다 — 장애가 아니다.
   확인은 `curl -k --resolve "xn--vz0ba242ncqcba79xhwx.site:443:127.0.0.1" https://.../` 로 한다.
10. `docker system df`로 빌드 캐시를 확인한다. 도커 VM 디스크는 31GB뿐이라 캐시가 차면 Kong 건강검진 실패와
   Storage 업로드 실패로 번진다. 차오르면 `docker builder prune -a -f`로 비운다(2026-08-16에 16GB 회수).

## 작업 종료

- [WORKLOG.md](WORKLOG.md) 맨 위에 작업 배경·변경·검증·남은 일을 기록한다.
- 완료 상태나 방향이 바뀌었으면 [ROADMAP.md](ROADMAP.md)의 체크박스와 결정 기록을 갱신한다.
- 이 파일에는 다음 세션이 즉시 알아야 할 **현재 상태만** 남기고 상세 이력을 누적하지 않는다.
